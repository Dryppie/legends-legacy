# Deep challenger implementation and zero-combat preparation

**Closed preparation failure - 15 September 2026:** the [readiness check failed](Tower-Deep-Challenger-Preparation-Review.md). This package is not ready for binding or combat and must not be retried. The original frozen protocol copy remains unchanged in the sealed evidence package.

15 September 2026. The user approved proceeding with the bounded challenger-search package described in the conversation. The [study protocol](Tower-Deep-Challenger-Study-Protocol.md) fixes the proposed combat workload; this preparation does not allocate values or run combat.

Use the sealed focused execution package as the producing-source/gameplay/control boundary. Copy its captured harness source, replace only the two generation-contract files and add the new deep policy/controller. Compile against the retained captured-v19 gameplay assemblies into an isolated directory. Preserve unrelated checkout work and before/after hashes.

Development verification uses `build/run-tests.ps1` with the exact filter `FullyQualifiedName~BalanceHarnessTowerDeepChallengerTests|FullyQualifiedName~BalanceHarnessTowerSearchPortfolioTests|FullyQualifiedName~BalanceHarnessTowerCompleteFamilyTests`. These classes contain synthetic selection, arithmetic, generation and durability fixtures, with no combat engine calls. Preserve every build/test invocation and any initial failure; ordinary implementation fixes may be verified before the runtime diagnostic freeze. No broad integration filter is permitted.

After the implementation builds and these tests pass, freeze producing sources, executable, scripts and diagnostic inputs. Execute exactly one native saved-evidence check, bounded to 900 seconds, and one independent analysis of its saved outputs, bounded to 300 seconds. The native check:

1. Reproduces all three archived v19 deep arms using their recorded generation inputs, mechanics and feedback. Compare all 4,608 evaluation identities and every proposal, parent, operator, recipe, measurement and final arm hash. This is generation replay from saved measurements, not combat.
2. Verifies the complete focused package and exact candidate content/gameplay identities. Prepare both fixed controls once and require the saved participant hashes.
3. Checks the complete existing reservation registry, expected union 481,891, without deriving candidates or creating a ledger. Preserve unused512 and unused32.
4. Saves all 257 possible 256-trial rate intervals at family capacity 256, plus two synthetic paired cases with 100 versus 50 wins at comparison counts 1 and 255. These use outcome arrays without combat. Saves a seed-free template, exact controls, input pins and measured results. A seed-free template is explicitly non-runnable.

Independent analysis checks the method-choice evidence, exact control selection, replay hashes/counts, template and producing hashes, and confidence arithmetic at the maximum family boundary. Diagnostic total <=1,800 seconds, zero engine fights, fresh values, retries or resumes. New preparation output <=1 GiB within the study's total 4-GiB envelope. Failed diagnostics remain closed; never edit frozen diagnostic inputs and rerun.

The preparation review must identify the concrete executable command, exact fresh-value exception and remaining verification limitations. Ask for the fresh-value exception only after all authorized preparation and verification has completed. No deployment, migration, gameplay edit, old-study restart or seed release belongs to this work.
