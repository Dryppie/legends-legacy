# Core portfolio: frozen saved-selection diagnosis

16 September 2026. Offline BalanceHarness evidence only. **Zero fights, runtime preparations, fresh values, retries, replays or search calls.** Read the completed core-portfolio comparison and its captured construction inputs without changing any sealed package. This diagnostic is authorized by the request to proceed; it needs no seed/time extension.

## Exact questions and calculations

Compare the saved 16 team-coverage and 16 core-portfolio proposals. Independently reconstruct nominations and finalists to determine whether selection follows the frozen rule. Measure which recipes/profiles/core combinations reached the evaluated teams and which available control subsets lost at the observed allocation prefixes. Missing control combinations are descriptive, not targets to inject into generation or proof of combat strength.

Inputs are the sealed `tower-core-portfolio-20260916` inputs, mechanics, controls, `saved-pool.json`, team/portfolio proposal fixtures and producing source; the prior `tower-team-coverage-20260915/pool.json`; and the new `tower-core-portfolio-comparison-{preparation,execution,study}-20260916` packages. Preserve **482,821 reservations**, including the original unused 512. The input freeze pins these files, this protocol, scripts and checkout snapshots. Verify all **67 predecessor packages** unchanged before and after; the latest preparation seal is `ed38634f6589f0aeb9c2b3d93851f4a2c17aa4b5df3d5e33d8950d66968aa778` and execution seal is `0b2ccc79409d844b12592c775fc8e925f518db4ce2c9c8ad1d56766bc916ee2c`.

1. Validate all 560 baseline and 768 candidate recipes: canonical ordering, five distinct compatible families, known Essences, unrestricted inventory, recipe/pool hashes and captured proposal/trace parity. Derive all five roles and 48 authored core containments. A profile is the SHA-256 of the ordinal list of all contained core IDs, including the empty list.
2. Reconstruct the exact comparator at all 320 observed character-placement prefixes, checking **212,480 option scores**. Do not construct or visit counterfactual teams. Baseline keys: descending new roles, prior recipe exposure, signed current-team recipe use, descending new Essences, recipe hash. Core-portfolio keys: descending urgent new roles (only if remaining slots ≤ missing roles), prior profile exposure, signed current profile use, profile hash, prior recipe exposure, signed current recipe use, descending new roles, recipe hash. Variety/reuse alternate by the saved zero-based team index. Exposure updates only after each complete team. Preserve terminal role coverage and verify the recorded 176 states, 16 parties and 89,600/122,880 checks; do not expand caps.
3. Save every pool recipe's selected-owner count and rank aggregates, all decisions' selected key/top ten/tie survivors, complete provider/core/role counts, profiles used, candidate repetitions and role/core ownership. Count profile-hash decisions only when more than one distinct profile remains tied after its first three keys. Count final recipe-hash ties separately. These are ordering explanations, not causal effects.
4. Enumerate all **191** unique control subsets of sizes one to five and all ten exact control recipes. Count pool availability, generated/nominee/finalist/control owners. For every subset and saved prefix, find the highest-ranked containing recipe, record its best/worst rank and first differing key against the selected recipe, retaining the first three unselected witnesses. Report examples chosen by control frequency (descending), then ordinal IDs, separately for sizes two, three and four. This rule is fixed before analysis and does not select on combat outcomes.
5. Reconstruct all 32 discovery ranks and both top-two nominations from saved fitness, and each screening winner from victories, then nomination rank and ID. Verify both control scenarios and merged origins. Report all candidates, finalists and the two predefined variety/reuse schedules with their archived health/win measurements. Confirmation outcomes and adjusted intervals remain unchanged. Do not reselect any team or infer strength for untested recipes.

The analysis uses sets; a separate reader derives masks and comparator keys independently and checks all scores, rankings, tie groups, subsets, witnesses, candidate/summary rows, nominations/finalists and reservation union. Neither reader imports the other's calculation functions. Both use only shared serialization/hash/path helpers. The completed native/archive and independent execution checks remain the source of combat-record validity. No new live registry scan is claimed.

## Limits, failure handling and publication

Incoming diagnostic workload **2,809.958 seconds**, remaining **190.042** under the approved cumulative **3,000-second** ceiling. This scope has a **60-second / 16 MiB** maximum, subject to the unchanged cumulative **4 GiB** cap. Carry forward previous input bytes plus actual sizes of the latest sealed preparation, execution and study, including the shared-test allowance. Charge all freeze/preservation, analysis, audit and publication work, plus one closure second. Source editing is excluded. No deletion, output reuse or budget reset.

Non-publication work stops by 45 aggregate seconds, reserving 15 for closure. Each freeze/analyze/audit phase has a 20-second maximum, reduced by remaining aggregate/cumulative time; child deadlines are enforced. First failure stops dependent analysis. Publication records partial evidence and the unresolved boundary; no retry or replacement run. Do not interpret a failed audit as a verified result.

Run once, after source review and protocol freeze:

```powershell
$python = 'C:/Users/HrHoe/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe'
$work = 'TestResults/balance/tower-core-portfolio-selection-diagnosis-20260916'
& $python -B "$work/diagnose.py" freeze
& $python -B "$work/diagnose.py" analyze
& $python -B "$work/diagnose.py" audit
& $python -B "$work/diagnose.py" publish
```

Publish the measured explanation, explicit limits and a bounded next recommendation, then update six active Markdown handoffs. Do not promote a policy or change C#/gameplay/configuration. No new backend build/tests are required for this archived-data-only scope: retain the prior eight comparison tests through `build/run-tests.ps1`, 83 implementation tests and exact policy parity at their original scope. No combat engine, constructor, allocator or evaluator is called.

V19 remains sealed with 253 required recipes and no confirmation. Reliability Fail 1/3, deep recovery 0/3, v19 Unresolved, adoption Hold. No Kharad tuning, ability-order optimization, migration, deployment or 129,536-fight confirmation.
