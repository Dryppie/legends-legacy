# Coverage diagnostics: completed reporting increment

Status: **complete** — see the [implementation review](Tower-Coverage-Diagnostics-Review.md) and [verified report](../TestResults/balance/tower-coverage-diagnostics-20260913/report-final/report.json). The requirements below describe the completed reporting increment. The [saved v9 diagnosis](Tower-Coverage-Realization-Diagnosis-Review.md) shows that equipped coverage counts omit recipient reach, activation timing and stagger thresholds. It also demonstrates why effect logs need operation-specific interpretation. The implemented scope is reusable diagnostic reporting in the offline BalanceHarness, with **zero new campaign fights, no constructor calls and no search-policy change**.

## Implemented output

Add a separate, versioned diagnostic report that consumes a verified saved discovery/validation archive and its frozen content. Use the existing archive validation and typed mechanic inventory rather than trusting arbitrary event names or duplicating battle execution. Cover every saved arm, finalist and control; accept the already fixed detailed replays when available and explicitly report missing detailed evidence. Do not generate replays to fill gaps.

For each category/provider, expose authored evidence keys, recipient selector, selected/default trigger, delay/cooldown, predicates, use limits and operation/value fields. Keep unknown or conditional routes explicit. Distinguish equipped providers from actual observed recipients. Include all eligible content, not an allowlist learned from winning recipes.

For existing detailed replays, distinguish effect-ID logs, condition identity plus owner/ability attribution, and ability-named stagger logs. Check ambiguity; unmatched observations remain visible rather than becoming zero activity. Separate applied stagger, a completed break, recovery immunity and denied action. Show the target's threshold/growth/break limits and recorded event sequence. Show authored and runtime chance gates without inventing aggregate success probabilities.

Report actual restored healing separately from regeneration, along with first deaths and fixed elapsed-time windows. Reconcile totals with saved summaries. Do not assign marginal damage prevention, uptime or causal contribution to providers from these logs. Retain the distinction between missing evidence and no observed application.

## Boundaries and verification

- Keep generator policy/version defaults, category eligibility, sampling, ranking, refinement, reference boundaries and old serialization unchanged. The report is explanatory and must not become fitness input.
- Preserve all sealed packages/reviews. Write a new report directory with input/content/execution/source hashes, schema version, explicit limits and no overwrite. Carry the latest all-array ledger unchanged; no new seed allocation.
- Support current frozen archives only when the producing execution/content can be verified. Reject a mismatch instead of silently replaying with a different build. Historical support must be explicit.
- Verify deterministic ordering, source/owner/recipient matching, ambiguous names, missing detail, implicit and explicit triggers, chance/condition caveats, ordinary conditions versus boss stagger, threshold changes, and healing/regeneration reconciliation. Use focused fixture tests without a new combat campaign; run backend tests through `build/run-tests.ps1`.
- Preserve the current v9 package and verify the report reproduces its saved diagnostic observations without combat. Do not retest its confidence gate or reinterpret any previous ceiling breach.

The reporting increment ended with a reviewable report, 20 passing fixture tests, byte-identical repeat output and three negative CLI checks. All 9,216 saved trial metrics, 594 equipped-coverage rows, category counts and four replay health/stagger observations reproduce the preceding diagnosis. The reader additionally handles the runtime `Vulnerable` → `condition.vulnerability` identity. Direct routes are attributed conservatively; nested status/summon/equipment applications remain visible as unmatched when attribution is unavailable. No v10 policy was selected by the reporting increment. The later [hypothesis assessment](Tower-Search-Hypothesis-Assessment-Review.md) separately selects a [count-only stagger-reservation proposal](Tower-Stagger-Reservation-Plan.md) for implementation planning. Its [implementation and parity checks](Tower-Stagger-Reservation-Implementation-Review.md) are now complete; the subsequent [v10 pilot](Tower-Stagger-Reservation-Review.md) and its [zero-combat diagnosis](Tower-Stagger-Reservation-Diagnosis-Review.md) are now complete. The new report covers every saved v10 case and four fixed replays; a separate overlay reads v10 structured reservations while comparator nomination identities remain unavailable. The later [elite-loadout assessment](Tower-Loadout-Diversity-Assessment-Review.md) selects one [separate proposal](Tower-Loadout-Diversity-Plan.md) for implementation planning; the report itself makes no policy recommendation. The [v11 implementation](Tower-Loadout-Diversity-Implementation-Review.md), its [separately frozen pilot](Tower-Loadout-Diversity-Review.md) and its [completed zero-combat v11 diagnosis](Tower-Loadout-Diversity-Diagnosis-Review.md) are complete; no further combat campaign is allocated. Kharad remains **Health 3.04881408 / Power 3.85370128**, with fixed gear and untrained Essences. No catalogs/defaults, gameplay, migrations, configuration, deployment, acquisition claims or floor expansion are included. The [current handoff](Tower-Coverage-Replication-Plan.md) contains the latest evidence and **472,194-seed** exclusion union.
