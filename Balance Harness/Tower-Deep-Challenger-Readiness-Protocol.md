# Deep challenger control preparation and readiness closure

15 September 2026. The user authorized the next control-adapter repair and remaining zero-combat checks after the [generation repair](Tower-Deep-Challenger-Repair-Review.md). Target: offline BalanceHarness. Both prior failed preparations stay sealed. Do not repeat their generation runs.

Use a temporary scenario declaring one already reserved historical seed for `CreateInput` and `PrepareAsync`. Keep exported controls and the study template seed-free. Retain production input validation. Add three focused tests: actual preparation without recipe mutation or combat, rejection of an already scheduled control recipe, and preservation of uncleared-floor validation. Run these through `build/run-tests.ps1`, filtering only `BalanceHarnessTowerDeepPreparationTests`. Compile the focused test assembly against the isolated captured-gameplay harness, using `-NoBuild -ArtifactsPath` for the wrapper. The preceding 91 passing cases remain evidence; do not rerun generation for this adapter change. Preserve each development build/test invocation; ordinary fixes may be verified before freezing diagnostics.

Create `TestResults/balance/tower-deep-challenger-readiness-20260915`, referencing sealed sources, gameplay and proven generation outputs instead of duplicating old campaigns. Only the control helper and preparation adapter change. Freeze executable, source, scripts and exact inputs before diagnostics. Execute once:

1. One native check, at most 900 seconds: verify the focused predecessor, reuse the exact three proven arms/4,608 evaluations/4,750 proposals by hash and equality, prepare both fixed controls once with their declared historical seed and require saved participant hashes, compare content-derived +10% mechanics against the old policy, validate the non-runnable template, check the full 481,891-value registry, and save all 257 rate intervals plus paired cases at comparison counts 1 and 255.
2. One independent saved-output audit, at most 300 seconds: verify producing/input pins, reused generation evidence, exact controls and temporary-seed declaration, target mechanics, registry union and interval arithmetic. No engine execution or generation rerun.

Zero fights, fresh seed candidates, retries or resumes. Total diagnostics including the previous 13.203 seconds must remain below 1,800 seconds. All three preparation directories count toward the combined 1-GiB setup ceiling within the unchanged proposed 4-GiB total. Carry forward 3,702.892007 seconds of setup and add elapsed work from this package's start; exclude the intervening authorization wait. Do not reset the proposed study's 10,800-second total, 5,400-second native ceiling or 108,544-fight maximum. Native run time may be reduced by the remaining outer allowance.

On success, seal and report the actual readiness results and exact future commands. The original zero-fresh-seed restriction still requires an explicit exception for 331 values before allocation. Prepare the complete review before asking for that exception. No control or historical outcome may enter independent generation; no old reservation is released or reused for new combat. On failure, preserve partial evidence and explain remaining limitations without retry. No gameplay edit, default optimizer change, migration, configuration change or deployment.
