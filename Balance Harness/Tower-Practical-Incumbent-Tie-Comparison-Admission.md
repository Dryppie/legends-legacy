# Incumbent tie comparison admission

22 September 2026. The offline BalanceHarness package passed the public `tower-incumbent-tie-comparison-check` with **ReadyNoReservation**. It retains the captured gameplay, content and settings, combines them with the tested comparison harness, and binds the complete live historical exclusion union. **The scientific experiment has not launched: zero new values and zero fights.** Search and selector defaults remain unchanged.

This completes the preparation requested by the [runner implementation](Tower-Practical-Incumbent-Tie-Comparison-Implementation.md). The [prospective protocol](Tower-Practical-Incumbent-Tie-Comparison-Plan.md) and its original planning package remain the scientific contract. Admission establishes compatibility and input readiness; it supplies no evidence that the candidate selector is stronger.

## Prepared inputs

The sealed package is `TestResults/incumbent-tie-comparison-admission-20260922-02`, containing 260 inventoried files plus its [manifest](../TestResults/incumbent-tie-comparison-admission-20260922-02/files.json). Its [concrete request](../TestResults/incumbent-tie-comparison-admission-20260922-02/request.json) targets the still-absent output directory `TestResults/balance/tower-incumbent-tie-comparison-20260922`.

| Check | Result |
| --- | --- |
| Historical union | 505,562 permanently excluded values from 222 pinned ledger files. |
| Runtime | 27 files; original gameplay and other dependencies retained byte for byte. |
| Content and storage | 17 captured content/settings files; complete sealed package 37,724,464 bytes. |
| Harness | Tested DLL, producing PDB, dependency description and runtime configuration copied from the verified build. |
| Source provenance | All 189 portable-PDB source documents match their producing checksums and are copied into the package. |
| Compatibility | 407 methods resolved through JIT preparation without invoking their bodies. |
| Public admission | `ReadyNoReservation`; 24 restarts, 11,904 search fights, 59,904 maximum fights declared, none executed. |
| Process ownership | Both preparation processes exited successfully under hidden Windows Jobs with no active descendants. |

The public check independently reconstructs the historical union and validates the existing Pending recovery receipt, including its source journal. The Python preparation also scans the complete registry and preserves every previously required file pin. Duplicate copies of ledgers do not create additional historical values. The recorded abandoned reservation remains permanently excluded; its receipt is reused unchanged.

The source snapshot is authenticated against the portable symbols belonging to the tested DLL. The compatibility pass resolves the comparison, shared search, selection, battle-input and archive entry paths, including nongeneric nested closures and async state machines. This complements the public check's fixed-recipe/input validation. Neither JIT preparation nor successful admission guarantees that a later full experiment will finish within its allowance.

Only the four harness runtime files were replaced. Gameplay DLLs, other runtime dependencies and captured content/settings remain pinned to `TestResults/anchored-comparison-20260917`. The template changes only the declared incumbent generator, execution identity, empty schedules, complete exclusion union, comparison identifier, absent anchored-primary designation and ordinary validation ceiling. It preserves the two supplied teams, canonical order and frozen gameplay scope.

## Input pins

| Artifact | SHA-256 |
| --- | --- |
| Prepared package manifest | `ac6486e38aec867c5df5c1d2c7931edde13736d288de7d6a00dd87a408719776` |
| Original capture manifest | `1a2da312f1cb7af7867f0d173c9fcdf8f2213afc36873240ed2b0da505eec314` |
| Preceding engineering verification manifest | `4148aca044646514dc8cbcea04c2518e3f47f1a12881154700bb75b198df498e` |
| Tested harness DLL | `88e73bcd3d313249da4ef366377a775261fe7c8cc78785617de7d42b14159412` |
| Prepared execution identity | `60851c7c1635fe04ac26153f11678604e89b019d2cc048b7200fb457538fc042` |
| Effective settings | `f9587e8941a021443aecef37dccf0d5dad250a28df32673cacf7f2df50b12b74` |
| Prepared template file | `ea9459ab29ecd8d385227d762a41253bfa3c78a1faaa2e2ecc18d0d404cbe1de` |
| Concrete request file | `76be13baf174373093c2ba60a41554dca37049d8f2e489ba60cc5df3577f7426` |

## Verification and changed files

[prepare-incumbent-tie-comparison.py](analysis/prepare-incumbent-tie-comparison.py) authenticates the original packages, builds a new captured package, scans history, derives the exact request/template, invokes only the public `-check` command and verifies the resulting package. Its `verify` command also rechecks live history and requires the scientific output to remain absent. This is a prelaunch admission check, not a retrospective experiment auditor.

[incumbent-tie-preparation-context.ps1](analysis/incumbent-tie-preparation-context.ps1) captures effective execution/settings identity, verifies the DLL/PDB and producing-source relationship, and resolves compatibility paths. No comparison C# source changed in this preparation step. The previous 108 regression tests and final 25 comparison tests remain bound to the same tested DLL; they were not rerun against a different development build.

**Five preparation tests passed**, covering historical union semantics, invalid or unresolved historical values, exact template derivation, changed settings and premature trial assignments. PowerShell syntax parsing passed. Evidence: [test log](../TestResults/incumbent-tie-preparation-tests-final-20260922.log), [runtime context](../TestResults/incumbent-tie-comparison-admission-20260922-02/context.json), [native admission receipt](../TestResults/incumbent-tie-comparison-admission-20260922-02/native-check.json) and [owned native process receipt](../TestResults/incumbent-tie-comparison-admission-20260922-02/native-check-process.json).

The standalone copied verifier subsequently returned [PreparedPackageVerifiedNoReservation](../TestResults/incumbent-tie-preparation-recheck-20260922.json), authenticating all 260 manifest entries against the separately recorded pin and rechecking the complete live registry. The Python AST, scoped whitespace and local Markdown link checks passed; the current helpers exactly match their sealed copies and the machine-readable protocol is unchanged. No required verification command remains blocked. Public native admission took 25.875 seconds; the preparation receipt records 214.375 seconds through the post-admission history rescan, before sealing and the final verification scans.

```powershell
python -B 'Balance Harness/analysis/prepare-incumbent-tie-comparison.py' self-test
python -B 'Balance Harness/analysis/prepare-incumbent-tie-comparison.py' prepare
```

The bundled Python runtime was used. `prepare` is create-only and is not a command to repeat against the completed package. The first engineering attempt stopped before admission on PowerShell reflection argument conversion. Its [failure receipt](../TestResults/incumbent-tie-comparison-admission-20260922/failure.json) and [context log](../TestResults/incumbent-tie-comparison-admission-20260922/context.log) are preserved; the corrected helper created the separate `-02` package. Neither attempt allocated values, drew production entropy or executed combat. This engineering correction is not a scientific retry.

## Next execution step

Recheck the pinned package and complete live history immediately before the single prospective launch. A changed registry invalidates this admission snapshot and requires a newly authenticated request before any allocation. Keep the original captured gameplay and the tested harness; rebuilding against current development gameplay would change the experiment.

From the repository root, the read-only admission recheck is:

```powershell
python -B 'TestResults/incumbent-tie-comparison-admission-20260922-02/prepare.py' verify --manifest-sha256 ac6486e38aec867c5df5c1d2c7931edde13736d288de7d6a00dd87a408719776
```

The separate future scientific command is recorded here for the handoff and **was not executed** during preparation:

```powershell
python -B 'TestResults/incumbent-tie-comparison-admission-20260922-02/launcher.py' --request 'TestResults/incumbent-tie-comparison-admission-20260922-02/request.json' --harness 'TestResults/incumbent-tie-comparison-admission-20260922-02/runtime/BalanceHarness.dll'
```

The prepared `launcher.py` uses the tested owned Windows Job launcher. One run is bounded by **4,500 seconds /4 GiB**, including native execution and closeout. Native work receives 4,440 seconds /3,968 MiB; the owner retains 60 seconds /128 MiB. It runs 24 common searches, freezes all 48 selector outputs before confirmation, and measures only differing physical recipes. There is no retry, refill, replacement root, sample extension or automatic default change.

After a complete run, authenticate its final inventory and run the separate saved-row Python auditor. Report both audits, the actual differing-root count, paired gains/losses, fixed-denominator endpoint and frozen decision. Preserve an incomplete run and any unresolved reservation rather than launching a replacement.

The preparation allowance is separate engineering work (600 seconds /512 MiB for the corrected build/check package); it does not spend or enlarge the scientific allowance. Changes affect two offline preparation helpers and the status/handoff Markdown files only. The concrete request is offline configuration. There are no application configuration changes, migrations, database operations or service deployments.
