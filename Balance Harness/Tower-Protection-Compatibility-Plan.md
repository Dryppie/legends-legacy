# Encounter-compatible protection: completed bounded experiment

Status: **complete** — see the [implementation and results](Tower-Protection-Compatibility-Review.md). The [precombat plan](../TestResults/balance/tower-protection-compatibility-20260913/implementation-plan.md) and [executable protocol](../TestResults/balance/tower-protection-compatibility-20260913/protocol.json) remain sealed. Requirements below describe the completed isolated change and its frozen allocation; they are not pending work.

## Implemented isolated change

V9 added the opt-in policy `independent-compatible-defense-v9`, with exactly `["defense-joint", "compatible-defense-joint"]`. Preserve v8's `defense-joint` as the comparator and as the new arm's construction/refinement base. Do not combine v7's unconditional core insertion, new quotas, different filler weights or additional operators.

Only the new arm's protection eligibility applies this conservative rule: exclude a provider from the protection collection only when **every protection evidence route is an explicitly typed negative `ModifyDamageTaken` effect and every such type is absent from the complete authored incoming threat set**. Retain any matching, generic, attribute-based, condition-based, barrier, cover, unknown or mixed route. Keep the Essence in the allowed pool, uniform/ordinary construction, other categories and normal mutation. This uses no hardcoded Essence IDs or damage-type preferences.

Derive the threat set from the prepared target encounter and frozen authored native abilities, statuses, summons, basic attacks and relevant damage-producing routes. Keep potential types regardless of frequency or observed damage. Account for nested, periodic, delegated, reflected, converted and condition-produced damage where supported. Incomplete or ambiguous resolution must preserve current eligibility, rather than assert that a type cannot occur. Do not infer absence from the four replays. Verify engine semantics before finalizing the extraction contract; if completeness cannot be justified within this scope, stop before a pilot rather than invent a type or broaden the behavioral change.

For the audited Kharad closure, the expected diagnostic result is **Physical and Magical**, removing two protection nominations from 16 to 14 while leaving the remaining categories and all 80 eligible Essences intact. These counts are an audit expectation, not a production allowlist. Armor remains eligible because Physical damage is present; Resistance remains eligible because Magical damage is present. No damage share, win rate, control recipe, copy count, ancestry or held-out measurement supplies a feature or weight.

Use the filtered collection consistently wherever the new arm already consumes protection coverage, including coverage-aware substitutions. Preserve category order, provider sampling among eligible entries, count sampling, uniform route, v8's 50% insertion skip, legal-core choices, ownership/family checks, filler, final ordering, refinement schedule, ranking and beam/exploration. Keep original v1–v8 serialization, RNG streams and behavior reproducible. Method names imply separate deterministic streams; this is a policy comparison, not an identical-parent causal ablation.

## Precombat verification — completed

1. Preserve all ten preceding packages, sealed reviews, current source/content/execution and both catalogs. Use a separate output directory. Exclude every array of the [latest diagnostic ledger](../TestResults/balance/tower-defense-diagnosis-20260913/seed-ledger.json): **471,387 distinct seeds**, including unused reservations and constructor-only seeds.
2. Verify the threat-set audit against frozen engine semantics, including Physical/Magical coexistence, genuinely absent typed reductions, mixed/general defenses, nested damage, conditions, summons with and without basic attacks, unknown routes, empty threat sets, casing and deterministic ordering. Unknown or incomplete information must preserve existing eligibility. Do not test implementation by asserting that selected parties must win.
3. Reconstruct all six saved v8 arms without combat and verify the three defense comparator arms remain exact under the new policy. A compatibility lookup must remain separate from fresh generation. Verify missing optional features and serialization for every prior policy. Any synthetic scores used for compatibility are explicitly zero-combat probes, not search evidence.
4. Verify deterministic legal parties, uniform reachability, scarce copies, family conflicts, all established floor/slot budgets, immutable inputs, reference removal/reordering invariance and restricted fresh provenance. Keep all six controls outside generation and preserve the original fixed anchor.
5. Run relevant backend checks through `build/run-tests.ps1`, including ordinary/compact reconstruction and interrupted/resumed parity. No constructor-tracing seeds are allocated by this plan. Freeze a complete executable protocol and producing hashes only after verification; any additional diagnostic allocation needs its own explicit reservation before use.

## Frozen comparison and gate

Keep Kharad floor 5 at **Health 3.04881408 / Power 3.85370128**. Use ten level-40, tier-1, rank-2 Standard characters, five level-1 unascended/unevolved Essences each, exact existing fixed gear, no styles or contributions. The full 80-Essence pool including Rare retains hypothetical ownership; practical acquisition is unverified. Improve combinations within this budget, without stronger equipment or training.

Frozen allocation: two methods, three fresh generation restarts, 96 evaluated parties per arm, eight shared discovery seeds, at most 2,048 proposals per arm. Freeze rank one as primary and rank two as exploratory before validation. Validate all distinct finalists and all six saved controls on exactly 256 disjoint fresh seeds; exact duplicates retain every source association.

| Phase | Maximum fights |
| --- | ---: |
| Discovery: 2 × 3 × 96 × 8 | 4,608 |
| Validation: at most 18 × 256 | 4,608 |
| Fixed diagnostics: four old parity checks, three new primaries and original anchor on first validation seed | 8 |
| **Total, zero retry reserve** | **9,224** |

The frozen caps were 600 seconds for execution, 1 GiB per package and 512 MiB per campaign, with no automatic resume, optional sample extension or second feature change. The experiment completed within these limits; this document allocates no repeat battles.

At least two of three preselected new primaries must satisfy all unchanged gate components: adjusted clear-rate lower ≥10%; paired lower improvement over the same-restart **v8 defense comparator** >0; paired lower difference from fixed anchor `team-1abe76ca1891d97a91d484f0a3662048` ≥−10 percentage points. No secondary or different control may replace either comparison after validation.

Joint nominal alpha .05 splits .025 across the complete final rate family and .025 across six paired comparisons, with component alpha .025/12 for discordance bounds. The ordinary evaluator separately uses .05 divided by the complete family. Approximate Wilson coverage is not a lifetime repeated-study guarantee. Preserve every observed >50% as a ceiling breach even when its interval crosses 50%; each intended-budget cohort needs supported ≥10% viability. Keep the earlier 131/256, 136/256 and 134/256 breaches separate from the 479/1,000 Inconclusive confirmation. No sample pooling or held-out reranking.

## Decision and boundaries

Save all proposals, selected recipes, ancestry, rejected nominations, fresh outcomes, resource accounting, reconstruction and fixed replay evidence whether the hypothesis passes or fails. A success would support only this bounded policy comparison; a failure must remain visible without extending its sample. No small pilot establishes exhaustive coverage, near-optimality or complete Tower-family acceptance.

Retuning, default/catalog promotion, gameplay changes, migrations, configuration changes, deployment and floors 6–11 remain outside this experiment. The current implementation resolves direct/periodic damage, Poison/Burn/Bleed, nested owner effects and summons. Unsupported executable routes preserve eligibility. Event/condition identity metadata does not execute effects; applied conditions are checked at their effect sites. Use the [current handoff](Tower-Coverage-Replication-Plan.md) for active status and evidence preservation.

## Completed result and current boundary

**Current World Tower status — 13 September 2026:** the [completed elite-loadout diversity pilot](Tower-Loadout-Diversity-Review.md), opt-in `independent-loadout-diversity-v11`, used **9,224 fights** with zero retries. Independent reliability is **Fail (0/3; 2 required)**; **0 of 6 new-arm finalists** recorded a held-out win. The strongest saved control measured **131/256 (51.17%)**, joint-adjusted interval **41.33–60.92%**. Ordinary and joint-adjusted family assessments are **Fail / Fail**. Every observed >50% remains a ceiling breach; earlier studies remain separate.

The [current handoff](Tower-Coverage-Replication-Plan.md) records all closed studies, recipes and the authoritative **472,194-seed** exclusion union; exclude every array, including unused and constructor-only reservations. Kharad remains **Health 3.04881408 / Power 3.85370128**, with fixed gear and untrained/unevolved Essences. The [v11 implementation](Tower-Loadout-Diversity-Implementation-Review.md) passed 264 tests and exact archived/comparator checks; the [fresh pilot](Tower-Loadout-Diversity-Review.md) completes its separate combat comparison. All six controls stayed outside generation, rank-one primaries froze before validation, both campaigns reconstructed and eight fixed replay checks passed. The [completed zero-combat v11 diagnosis](Tower-Loadout-Diversity-Diagnosis-Review.md) verifies all 9,216 saved records, 333 parent decisions, 220 control nominations and four fixed replays. All 167 new-method decisions used four distinct elite loadouts; all six new finalists retain nominal control capacity. Realized recovery differs by trigger and recipient, and no causal remedy is established. Next is one source-grounded assessment of death-dependent recovery coverage, permitting rejection; no new policy or campaign is selected. Earlier v10 and other failures retain their original scopes. No retuning, default/catalog promotion or floor expansion is allocated.
