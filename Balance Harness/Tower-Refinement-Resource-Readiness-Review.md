# Refinement comparison: resource readiness

16 September 2026. **OutputEnvelopeInsufficient**. The current archive format needs at least **165,458,836 bytes (157.79 MiB)** to complete, before combat reports and other omitted files. Only **162,675,081 bytes (155.14 MiB)** remained before this package. The storage allowance cannot contain a complete run.

This is a pre-execution storage finding, not a failed combat run. No production request, seed schedule or combat authorization was created. The policy comparison remains team coverage versus discovery refinement, with identical captured gameplay, fixed ability order and the existing controls. Search strength remains unmeasured.

| Counted requirement | Bytes |
| --- | ---: |
| One historical integer array, conservative minimum | 6,410,528 |
| Fourteen required historical arrays | 89,747,392 |
| Four executable copies | 73,496,536 |
| Four content-data copies | 2,214,908 |
| Minimum completed study | 165,458,836 |
| Maximum space available before this package | 162,675,081 |
| Minimum shortfall | 2,783,755 |

The fourteen historical-array copies are mapped in [history-copies.json](../TestResults/balance/tower-refinement-resource-readiness-20260916/history-copies.json). Each copy contains all 482,821 prior values. The estimate counts only integers, commas, one line break and four spaces per value. Independent property-array serialization agrees exactly. Many actual copies have deeper indentation. Property names, brackets, other metadata, all new schedule values, recipes, combat reports, journals, manifests and temporary files are omitted. Executable/content retention is counted once per four required stages using exact sealed asset lengths and hashes. This is a lower bound, not a complete run-size estimate. The comparison uses headroom before this new package, deliberately overstating available space.

The unchanged launcher also performs a binding refresh, binding recheck and prelaunch refresh. The previous native Check took 56.8624 seconds; these operations differ, so multiplying that time is only a planning estimate. Incoming remaining time was 109.898530544 seconds, with no proven allowance for all traversals, runtime preparation, combat and reconstruction. No time or output cap is increased.

Reduce new archive duplication under a separately versioned and verified storage contract before preparing the real comparison request. Preserve all historical archives and the cumulative 4 GiB cap; do not allocate seeds or launch the existing format.

Verification reused the immediate sealed registry package, exact launcher/source identities, authoritative ledger hash, content and executable dependencies. No old full historical audit, live registry traversal, backend test rerun, build or runtime preparation. Existing launcher test evidence through `build/run-tests.ps1` remains prior evidence, not a new test result. The [frozen protocol](Tower-Refinement-Resource-Readiness-Protocol.md) caps this once-only check at ten seconds and 2 MiB; the [completion receipt](../TestResults/balance/tower-refinement-resource-readiness-20260916/completion.json) carries exact cumulative time/output. The script and all measured arithmetic are retained. Reproduce only in a new separately frozen scope; never rerun this package:

```powershell
& 'C:/Users/HrHoe/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe' -B 'TestResults/balance/tower-refinement-resource-readiness-20260916/check.py'
```

The changed files are this readiness package, its protocol/review and six active Markdown handoffs. No implementation, gameplay, configuration, migration or deployment changes. **Zero fights, runtime preparations, new seeds and retries.** All 482,821 reservations remain; preserve v19's unused 512 values and 253 recipes. V19 Unresolved, later reliability Fail 1/3, deep recovery 0/3 and adoption Hold remain unchanged. Sealed experiments, protocols and evidence were not edited.
