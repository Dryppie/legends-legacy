# Group/count comparison: frozen bounded pilot

15 September 2026. Target: offline `LL/tools/BalanceHarness`. Authorized preparation after the group/count implementation. The earlier 85-value pilot and 45-value comparison approvals are exhausted. This protocol authorizes no fresh allocation or combat by itself; execution requires a specific new exception for **45 fresh values and at most 512 fights**.

## Exact inputs and comparison

Compare baseline `independent-joined-mechanics-v1` / `joined-mechanics-joint` with candidate `independent-group-count-v1` / `group-count-joint`. Compile the frozen group/count implementation source into one driver so both policies use the same executable and gameplay dependencies. Use the unchanged captured guardian Health/Power +10% candidate and captured content from the deep-challenger source used by the previous comparison. Preserve content/settings/gameplay DLL hashes, scenario identity, UTC timestamp, equipment, attributes, neutral identities, floor 5, all 80 eligible Essences, unrestricted owned copies, level 40/tier 1/rank 2 and five Essence slots per character. Ten characters form two five-player parties. Ability order stays ordinal by Essence ID, with no order search.

Each policy receives **44 distinct evaluated teams**, at most **704 proposals**, FreshEvery 4, initial 11 fresh teams and one shared generation-root value. Both use the same four discovery seeds. Run baseline before group/count; separate policy random streams remain as implemented. This compares two policies on one shallow restart, not an isolated causal effect or a controlled timing benchmark. No control recipe/outcome enters generation. Do not retune the group/count schedule based on preparation outcomes.

| Phase | Teams | Fights per team | Maximum fights |
| --- | ---: | ---: | ---: |
| Joined baseline discovery | 44 | 4 shared | 176 |
| Group/count discovery | 44 | 4 shared | 176 |
| Screen | Top 2 per policy, up to 4 recipes | 8 shared | 32 |
| Confirmation | Top 1 per policy plus 2 controls, up to 4 recipes | 32 shared | 128 |
| Total | | | **512** |

Freeze each policy's top two using existing discovery fitness and ordinal identity ties; freeze both lists before screening. Within each policy select the screen winner by wins descending, discovery rank, then ordinal identity. Freeze winners before confirmation; never reselect afterward. Deduplicate exact full-context recipes only across screen/confirmation, retain all origins, and never refill freed seats. Discovery overlaps are independently charged. Incomplete discovery stops all dependent stages.

The fixed controls are `team-040e60d3dbc5c127321653c47ed3a9d3` and `team-49f6979895354870c89362d4abf214bb`, both fixed order. They remain outside discovery and screening. No old combat evidence is pooled.

## Seeds, interpretation and limits

Proposed allocation: **1 generation + 4 discovery + 8 screen + 32 confirmation = 45 new values**, disjoint from every retained reservation and from each other. The current union is **482,416**; successful allocation would retain **482,461**. Refresh registry membership and hashes at binding/execution; reject drift. Allocation namespace is `tower-group-count-comparison-v1`, master label 2026091505. Journal every candidate attempt durably before use and preserve pending reservations on failure. Fixture labels are never allocated or used in combat.

Primary contrast: group/count finalist minus joined baseline finalist on 32 paired confirmations. Retain four secondary contrasts, each finalist versus each control. Wilson rate family factor 8 and paired gained/lost family factor 20 are unchanged from the previous comparison; draws are non-wins. Identical deduplicated recipes have exact zero difference. Keep zero/negative results, broad uncertainty and the exploratory scope. This small study cannot establish reliability, optimality, general search improvement or adoption.

Also prespecify descriptive boss health remaining, duration, per-team complete catalogue-group owner counts and fresh proposal requested/placed/final counts, rejections and uniform routes. Save paired finalist health differences and all complete group counts for both nominees and controls; these are diagnostic summaries without extra significance claims or selection changes. Optional telemetry must report missingness separately from measured zero. A full catalogue cycle is not required or funded: 44 evaluated teams examine only a short trajectory.

Hard total caps: **512 charged fight attempts including every repetition/replay, 1,800 seconds diagnostic workload including preparation/verification, 4 GiB new output, zero retries/resumes/replays**. Preparation diagnostics at most 300 seconds; preparation plus execution evidence at most 1 GiB, study at most 3 GiB. Native combat at most 900 seconds within the remaining outer allowance; retain verification time. Compilation is separately recorded engineering work, each command at most 300 seconds. Use existing compact campaigns, incremental storage accounting, cancellation and durable attempt journal; verify native archives and independently recount results. Never weaken caps or historical verification.

## Frozen preparation sequence

Build from the sealed `tower-group-count-implementation-20260915` source and captured gameplay DLLs, preserving the dirty checkout. Copy the eight comparison controller tests, adapt their exact policy identities and add the 48 existing composition/joined/group-count cases: **56 backend cases through `build/run-tests.ps1`**. Combat-entry guards and synthetic evaluators only. Retain schedule/context/cost, nominations, deduplication, interval, cancellation/attempt and old-policy regression tests. Run exactly four Python metric fixtures: missing-versus-zero health/duration, rejection of invalid numbers, group counts including absent groups, and paired seed alignment/differences.

One native preflight refreshes the reservation registry, validates definitions and identical inputs except policy/method, prepares the two fixed controls without combat, and saves 66 interval fixtures (0..32 wins, factors 8 and 20). Use the first 45 already-reserved labels only in explicitly non-runnable fixtures, leaving the complete real history intact. No content search or candidate scoring during preflight. One independent audit verifies all definitions, controls, ledger union, interval fixtures and passing tests. Preserve the nine prior indexed packages plus the group/count implementation before/after. Freeze scripts, source, fixtures, protocol, projects and executable hashes before their respective once-only diagnostics. Preserve failures; no retry or hidden amended diagnostic.

Finish and seal the executable bind/run/verify/audit controller, descriptive metrics, readiness report and active Markdown before requesting the specific seed exception. No bound study exists during preparation. Execution must check the protocol-hash authorization. Do not modify v19, its 253 retained recipes or unused confirmation values, run the 129,536-fight confirmation, tune Kharad/content, deploy or increase old caps. Historical reliability Fail 1/3, deep recovery 0/3, sealed v19 Unresolved and adoption Hold remain unchanged.
