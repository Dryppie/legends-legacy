# Bounded alternatives for competing group-owner categories

15 September 2026. Target: offline BalanceHarness. Implement explicit opt-in `independent-group-allocation-v1` / `group-allocation-joint`, following the [saved-slot audit](Tower-Control-Slot-Audit-Review.md). No gameplay tuning or ability execution-order search.

## Frozen implementation contract

Retain the metadata-derived diversity group ordering. Determine each group's missing authored coverage categories with at least one compatible family/provider and positive owned copies, if ownership is limited. When the number of such categories exceeds the group's free slots, create at most five adjacent allocation variants, each prioritizing a different missing category. Otherwise retain one variant. Empty/full groups also receive one variant. Each variant consumes one ordinary proposal; existing attempt/evaluation caps and duplicate charges are unchanged. Keep every eighth fresh request uniform.

Sibling variants share the original diversity schedule's owner count, placement seed and filler seed. Process their prioritized category first, then the original completion category order, retaining the existing provider reach/tie rules, exact slot/family/copy checks and group-count-drift rejection. After a complete allocation sweep, advance the original diversity count/filler sweep. Persist allocation variant index/count, eligible categories, priority and exact completion steps. Optional new fields are omitted for old policies. No Essence names, controls, combat outcomes or new strength rankings enter construction. Reduced group breadth within a short prefix is an explicit tradeoff, not a promised strength gain.

## Exact verification

Freeze source, fixtures, protocol, scripts, input/reference hashes and dirty-checkout preservation before execution. Build isolated candidate/test assemblies against the previously captured gameplay DLLs and cached restore metadata. Run **110 backend cases once through `build/run-tests.ps1`**: 95 existing composition/joined/count/variation/diversity/completion cases plus 15 allocation cases covering single-slot alternatives, shared seeds, full groups, metadata/family/copy exclusions, global copy limits, metadata order, uniform routing, rejected reservations, opt-in enforcement, trace compatibility, synthetic search budgets/determinism/provenance, cancellation, registration, group/count sweeps and multi-category providers. Synthetic test measurements are not fights or balance evidence.

One captured-content command constructs exactly **19 requests for each of five policies**, 95 requests total: count, variation, diversity, completion and allocation. Match all 76 old-policy choices exactly to the sealed completion verification output. Use its captured construction inputs and existing label 17. Reconstruct the latest saved completion policy's **44-evaluation search once**, using only its archived measurement callbacks, and require an exact whole-generation match. No combat preparation or scoring. Also produce one metadata-only allocation sweep over the same 214-group catalogue, at most 1,070 guided positions, without constructing or evaluating those teams.

One independent Python audit reconstructs the catalogue order, category eligibility, complete first-sweep allocation schedule and each recorded completion insertion from metadata and stable hashes. Check all constructed recipes, fixed ordinal order, ownership/family/slot limits, retained reservations, count-drift rejection, old parity and the test receipt. Persist existing performance trace measurements for construction and archived reconstruction; do not infer combat improvement from timings.

## Limits and closure

Zero fresh seed values, fights, preparations, combat replays and retries. Carry **1,189.992 diagnostic seconds** and **1,594,856,311 bytes** from the sealed slot audit under the unchanged cumulative **1,800-second / 4-GiB caps**. New diagnostics, including evidence setup, tests, construction, audit and publication, at most **180 seconds**; each diagnostic process at most 60 seconds. Build commands at most 300 seconds each, separately recorded engineering time. New retained output at most **256 MiB**. Preserve all **35 preceding sealed packages** before/after. A failed frozen check or cap stops dependent diagnostics; retain evidence without retry.

Publish measurements, exact commands, test results, changed files and limitations; update six active Markdown handoffs. All 482,596 reservations, v19's unused 512 and 253 recipes remain preserved. Historical reliability Fail 1/3, deep recovery 0/3, sealed v19 Unresolved, adoption Hold. No seed allocation, larger study, 129,536-fight confirmation, Kharad tuning, deployment, configuration changes or migrations.
