# Complete retained-family parity v3 — frozen reference-host execution

Frozen **14 September 2026** before this scope's setup/probe/native execution. Target: offline `LL/tools/BalanceHarness`. This is the authorized continuation of the [v2 stopped reference review](Tower-Complete-Family-Parity-V2-Review.md), whose package remains immutable under seal `eed44db5eda7a893a2fd792541ec914703d229887a820020a1f30ffa9d0277ca`. New output: `TestResults/balance/tower-complete-family-parity-v3-20260914`. No old package is rerun, repaired or resumed.

## Exact scope

Reuse the v2 sealed candidate executable and corrected reference host byte for byte. The host explicitly calls `BalanceHarness.Program.Main` in the unchanged midpoint harness (SHA-256 `16a2d316afcf227fae0904d5c4bd2144e29f4bf4e2798df381eb918c3f191a8b`); that assembly's own startup object remains `BalanceHarness.MidpointEntry`. Copy its runtime dependencies and verify every copied hash before execution. Reuse the identical v2 reference definition, two scenarios and literal schedules. The sole request change is `referenceRun`, pointing to the new output root. Do not rebuild or alter gameplay, controller sources, recipes, ordering, content, execution mode or limits.

The first cell is non-anchor `0000b8a0159100cfee696335c83a6b6908665d193492bd665a5df56cbdeaab68`. The second is midpoint anchor `02aba209053fea61609745b10bf1e7c367e9633cd7b0a264faa5ee78beb8d2a0`, origin `team-eb63c3547f4c75de71105c01ecd9a989`. First-stage values are the same first **32** ceiling-screen shared entries; second-stage values are the same **256** midpoint shared entries. All **288** are distinct, disjoint between stages and previously registered. Preserve all **481,603 reservations**, including the original **512 unused v19 confirmation values**. No authoritative seed ledger changes. This remains diagnostic replay, not fresh balance evidence.

1. Capture the dirty checkout baseline and verify the complete v2 evidence inventory and 792 external bindings. Copy the sealed inputs, executables, producing sources and exact 215-name fixture-test evidence into the new package. Compare every previously tested harness/test source with the producing baseline. The **215 tests already passed through `build/run-tests.ps1`**; unchanged sources reuse that result rather than repeating fixture tests. No compilation or new tests are scheduled here. A source mismatch stops execution.
2. Freeze every producing file, input binding and exact command in `engineering-freeze.json`. Run the reference host's **single `--probe`** with a **30-second** watchdog. Verify its selected general dispatcher, midpoint assembly entry point, full execution identity against the saved midpoint scope, and all captured gameplay hashes; require zero preparations/fights. Run the unchanged candidate's **single typed `check`**, with a **90-second** watchdog, against the copied request and reference definition. It natively verifies the entire selected saved-anchor archive and exact scenario/schedule/content/settings equality without combat. Freeze the successful receipts and final producing inventory before reference combat.
3. Reference: invoke the corrected host once with exactly the copied reference definition, new `native/reference` output and unchanged midpoint content root. Its fixed internal arguments enforce **32 maximum attempts**, chunk size **32**, retry reserve **0**, `prepared-v1`, `owned-storage-v1`, **120 seconds / 256 MiB**. A scientific exit code of 1 is accepted only with a complete verified 32-trial archive. The frozen feasibility gate is **at most 9 wins of 32** (one-cell first-stage upper bound at most 50%). Otherwise stop without replacement or candidate execution.
4. Candidate: invoke the unchanged diagnostic entry point once with **288 maximum attempts**, one second-stage place, **1,080 seconds / 2 GiB**. Verify the full source and prepare all **43,879** retained rosters before any candidate fight, comparing every saved participant hash. Execute the actual shared production batch adapters: **32** first-stage fights compared with the new reference, then **256** anchor fights compared with the sealed midpoint reports. Verify exact full report digests and gameplay fields, ignoring only physical archive index/case labels. Preserve durable attempts, storage accounting, two-stage selection, production reconstruction before final publication, and one completed reconstruction without combat. Do not invoke the full-study public run contract.
5. Run the sealed independent verifier once, with **120 seconds**. It verifies new archives/inventories, exact schedules, all 288 comparisons, 43,879 preparation rows, stage decisions, global/child charges and trace counts. Preserve the checkout/old packages/history within **120 seconds**, then seal within **120 seconds**. Freeze the closure script before those operations. Charge **60 seconds** for static metadata/short reads. On any failure or cap, preserve evidence and stop dependent execution; no retry, substitution, additional replay or extension.

## Resource envelope and evidence

Maximum new combat is **32 reference + 32 candidate first stage + 256 candidate second stage = 320 fights**, below 512 including every repetition. Expected preparations: **43,879 full-family + 1 reference reusable + 2 candidate reusable = 43,882**. No fresh balance seeds, retries or resumes. Setup/copy/hash checks, both zero-combat checks and producing freezes share **120 seconds**; abort before reference if exceeded. Together with reference 120, candidate 1,080, independent 120, preservation 120, seal 120 and static 60, declared charges total **1,740 seconds**, below the **1,800-second** diagnostic bound. Existing native limits are unchanged. New retained output, including all copied evidence, metadata, changed Markdown and any external new test output, must remain below **4 GiB**. The watchdog counts actual new package bytes; native child caps remain enforced independently. Each command writes a start marker and timed success/failure receipt and cannot repeat.

Persist the existing detailed `TowerPerformanceTrace` output, CPU/allocation/peak-memory measurements, exact attempt journals, preparation hashes and final archive inventories. Report inclusive/exclusive timings correctly; do not add nested inclusive phases. This single small parity run does not provide a new reference/candidate throughput comparison or historical timing percentages. Retain the earlier measured discovery-accounting improvement without rerunning its fixtures.

Preserve all sealed studies and prior reviews, all 253 original v19 recipes/nominations and all unrelated dirty work. No content tuning, Kharad adjustment, confirmation launch, migrations, deployment, public cap increase or seed allocator is in scope. Successful parity closes only the actual small production-adapter path and full-roster preparation gate. Full-family second-stage capacity, combat across the 256-cell outer batch boundary and complete-study throughput remain unmeasured. Reliability **Fail 1/3**, adoption **Hold**; sealed v19's original Unresolved/no-confirmation result remains unchanged.
