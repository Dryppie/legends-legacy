# Focused challenger execution: authorized fixed study

Frozen **15 September 2026** before compilation, tests, binding or combat. The user approved preparing, running and verifying the [989-team design](Tower-Focused-Challenger-Design-Review.md). Target: offline `LL/tools/BalanceHarness` using captured-v19 gameplay. All original studies and unrelated checkout changes remain untouched.

## Fixed inputs and scope

- Exactly the ordered 989 selected cells from the sealed design package, SHA256 `d3438b9fd63d8ce489d77a475231e3f905a86b7fb5b3abc11acaf2aec68333d2`.
- Existing isolated guardian Health/Power +10% content, original verified scenario/context/participant identities, captured-v19 gameplay assemblies.
- Exactly 256 fights per team, all selected teams forced to the full sample: **253,184 attempts maximum**, no first-stage combat, no fresh allocation, **zero retries or resumes**.
- Explicitly transfer the existing unused 256-value second schedule to this separate focused study. Preserve all 481,891 reservations, the original unused512 and unused32. Its registered ledger causes the unchanged original launcher's live-history membership check to reject that old request. Never silently edit the original request or call consumed values fresh.
- Only focused-set support can result; never complete retained-family Pass. Search reliability remains Fail1/3 and adoption Hold.

## Execution and measurement

The new experiment-specific `producing/FocusedProgram.cs` adapter compiles alongside an unchanged snapshot of the verified harness, with references to the captured gameplay DLLs. It reuses `TowerCompleteFamilyRun.Execute`, preparation, batch publication/verification and reconstruction. It does **not** invoke, filter or weaken the public exhaustive-study launcher. Existing confidence arithmetic matches the selected design: alpha .025 divided over989, 94/256 maximum to clear the ceiling, 46/256 minimum to support viability.

This uses four existing compact batches of 256,256,256,221 teams. Durable attempt charging, nested incremental storage accounting, cancellation handling and full archive verification remain in the existing executor. Native performance traces retain timings, CPU, allocations and memory. The outer supervisor records elapsed time, exits and file hashes; progress reads the attempt-ledger length without scanning the growing archive.

The approved envelope is **10,800 charged seconds and 8,589,934,592 new bytes**, including setup, execution, verification and reporting. A durable clock is recorded in `control/scope-started.json`; 900 seconds are conservatively charged for earlier inspection/design. All wall time after that marker is additionally charged. Native execution must finish within9,900 charged seconds, reserving900 for independent verification/reporting. Its archive allowance subtracts actual new producing/control setup bytes and a64-MiB reserve for later external records/documents. No time, attempt or storage extension is permitted. Preserve partial evidence and stop on failure or a limit.

## Frozen verification sequence

1. One repository-wrapper invocation selecting exactly `BalanceHarnessTowerCompleteFamilyTests` (the52 existing zero-engine fixture cases), with explicit test-name verification and preserved TRX. Tests exercise the reused executor, not combat. No integration filter, synthetic engine output or broad test rerun is authorized.
2. One captured build. Verify all four gameplay DLL hashes against the sealed v19 identity; retain the complete producing snapshot and compiler output.
3. One native binding, bounded to900seconds and no combat. Check the complete source binding, exact selected identities, history registry and original ten reservation files. Prepare each of989 teams once and compare all participant hashes. Check four zero-engine assessment cases (70,95,129,0 wins for one selected cell; the other cells zero) and all257 interval rows. Fail closed on any mismatch. The new owned ledger retains every prior reservation.
4. One independent binding check before combat: exact selection/scenarios/participants, seed ownership/union, declared resource accounting and confidence bounds. No replay fights.
5. One native run, including preparation checks, durable charging, per-batch archive verification, complete reconstruction and final inventory verification.
6. One native standalone verification with an engine-entry guard, then one independent saved-evidence/count/interval/seed-order/roster/accounting check. Neither can fight. Preserve source and reservation hashes, record changed/new output bytes and update current Markdown.

All operations are create-only. Build/test/bind/run/verification failures retain their logs and close this scope without retry. A different future scope would need a new explicit decision. No deployment, migrations, live boss/content edits, fresh seeds or search tuning are included.

The concrete commands, exact test names, producing hashes and phase limits are recorded in the new `TestResults/balance/tower-focused-challenger-run-20260915` package before execution. Preparation and diagnostics must fit within the original30-minute diagnostic ceiling in addition to the approved global study envelope; ordinary build/test phases are reported separately. Study execution has the separately approved fight budget above.
