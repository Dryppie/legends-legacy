# Group/count construction: implementation and zero-combat verification

15 September 2026. Authorized follow-up to the completed saved combat diagnosis. Target only offline BalanceHarness. Implement `independent-group-count-v1` / `group-count-joint`. Keep fixed ordinal Essence order; change no gameplay content, caps, seed ledger, old policy, experiment or deployment. No fresh balance seeds, fights, combat replays or preparations. Preserve all 482,416 reservations.

## Fixed implementation

Reuse the bounded 214-group captured catalogue (general caps remain 128 base recipes, 256 joined groups, at most five members). A per-arm fresh-proposal ordinal, consumed even for rejected/duplicate proposals, defines scheduling. Every eighth fresh request is uniform; an empty catalogue is always uniform. Other requests visit each catalogue group once per sweep in ordinal group-ID order with a seed-derived rotation. For guided index k, group count G and owner count N, visit = k mod G and requested owners = 1 + ((floor(k/G) + visit) mod N). Each group receives every 1..N count once in G*N guided requests. Reset the ordinal for every generation arm. Scheduling consumes no fitness or control recipe.

Reserve the entire selected group on distinct shuffled owners before coverage/filler. Check slot/family/ownership constraints with existing insertion rules, roll back the whole reservation on failure and retain tentative insertion evidence. Fill remaining slots through existing category coverage/filler with no additional sampled core insertion. Count the group in the completed recipe; reject count drift instead of evaluating a mislabeled count. Record catalogue identity/truncation, request index, group/count, placements, final count and rejection. Null trace fields must be omitted from old-policy serialization. Reuse unchanged proposal/candidate budgets, ranking, mutation schedule, nominations and cancellation/charging paths.

## Exact diagnostics, frozen before execution

1. Verify nine sealed packages: the eight packages pinned by the corrected saved-data diagnosis, plus that completed diagnosis (files.json SHA-256 005752f069d347cbf0de633dd1a4940079284f367b0defcc9769582b3e8680dd). Preserve the dirty source inventory and prior shared test TRX. Freeze this protocol, scripts, source/tests, current repository fixture and captured dependency hashes.
2. One old-executable reference call: 128 synthetic joined-policy evaluations on the unchanged 12-Essence/two-owner/four-slot fixture, generation label 17 and synthetic labels 101/102. Save the full result hash for candidate parity. No engine or allocator. Retain the older composition reference hashes used by existing tests.
3. One isolated backend test invocation through build/run-tests.ps1. Run the existing 17 composition and 15 joined cases plus the frozen GroupCountTests cases. Cover complete group/count cycles, periodic uniform/empty catalogues, order invariance/large indexes, exact placements, atomic ownership/family/slot failure, preserved prefixes, accidental count drift, deterministic complete search, per-arm reset, cancellation/checkpoint charging, exhaustion, policy/order boundaries and exact previous joined output. All classes install combat-entry guards and use synthetic evaluation only; labels 17/18/101/102 are not seed allocations.
4. One captured-content diagnostic with an engine-entry guard: construct the catalogue once; enumerate exactly its complete G*10 group/count schedule, including interleaved uniform requests; reserve all G*10 prefixes with synthetic Random(17), without filling/evaluating them. For the captured untruncated G=214 this is 2,140 prefixes over 2,445 request positions (305 uniform). Independently verify every unique group/count pair, prefix legality, exact owner multiplicity, complete pair coverage and uniform positions. Also generate exactly the first 32 complete fresh requests using the same input-only content and synthetic random label 17; retain every recipe/trace/rejection, validate canonical order/constraints/counts independently, and report accepted/rejected counts. No fitness, combat result or control recipe enters generation. Count-drift/ownership rejection is a valid measured result, not a reason to retry. Report construction time only, not strength or performance speedup.
5. Final preservation, scoped source diff checks, report/active Markdown updates and separate output seal. Do not run another search-quality experiment in this scope.

Maximum 300 additional diagnostic seconds, included with the comparison/diagnosis total of 254.030 / 1,800 seconds. Each executable diagnostic/test command is capped at 120 seconds and by remaining diagnostic time; preservation and independent audit together fit the remainder. Maximum new output 512 MiB inside the existing aggregate 4-GiB envelope. Zero diagnostic retries/resumes. Preserve failures and stop dependent diagnostics. Builds are separately measured engineering work with 300-second limits. These fixtures run zero fights regardless of synthetic evaluation count.

Compile copied current harness source against captured gameplay dependency DLLs from the joined-comparison preparation. Compile only the three relevant zero-combat test classes and their two fixtures. Run the resulting isolated assembly through the repository test wrapper, preserving compiler projects, logs, source/binary hashes and TRX. Do not build unrelated dirty gameplay. This is targeted verification, not a full backend suite or a combat reliability claim.

Complete a measured implementation report with exact commands, changed files, rejection/coverage limitations and the next step. Historical reliability Fail 1/3, deep recovery 0/3, sealed v19 Unresolved and adoption Hold remain unchanged. No gameplay tuning, migration, configuration change or deployment.
