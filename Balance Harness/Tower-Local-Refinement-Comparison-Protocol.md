# Local V4 versus baseline: bounded comparison

16 September 2026. Target: offline BalanceHarness. Preparation is authorized. This new study requires the specific seed/time/output approval below before any allocation or combat. The [V4 audit closure](Tower-Local-Refinement-Audit-Closure-Review.md) verified the 69 passing backend facts; do not rebuild the harness or rerun those tests.

## Frozen scientific design

Compare `independent-team-coverage-v1` with `independent-discovery-refinement-local-v4`, selected through `tower-discovery-refinement-comparison-v3`. Both arms use identical captured gameplay assemblies, content, settings, equipment, timestamps and seed schedules. V4 retains seven fresh and nine refinement proposal positions, choosing its best completed discovery parent and changing one Essence in one character slot. The baseline spends all sixteen proposals on fresh construction. Fixed ordinal ability order, ranking, nominations, selection and all native caps remain unchanged. No Kharad or gameplay tuning.

| Stage | Maximum teams | Fights per team | Maximum attempts |
| --- | ---: | ---: | ---: |
| Baseline discovery | 16 | 4 | 64 |
| Local V4 discovery | 16 | 4 | 64 |
| Two nominees per arm: selection | 4 | 8 | 32 |
| One finalist per arm and two controls | 4 | 32 | 128 |

At most **288 charged fight attempts**, zero retries, resumes or combat replays. Incomplete discovery stops the whole comparison before nomination; no refill. Duplicate later-stage recipes retain every origin and can reduce the actual count. A finalist can play 44 fights across the three stages. Selection uses wins, then original discovery rank and ID. Controls remain `team-040e60d3dbc5c127321653c47ed3a9d3` and `team-49f6979895354870c89362d4abf214bb`.

Primary endpoint: V4-minus-baseline confirmation win rate with the existing adjusted paired interval. Boss health, duration and refinement-parent improvements are descriptive. This is one exploratory comparison; it cannot establish reliability or global optimality. Confirmation and selection outcomes never feed discovery.

Request exactly **45 fresh values**: one generation label, four discovery seeds, eight selection seeds and 32 confirmation seeds. Use new master **2026091605**, the unchanged durable allocator and V1 derivation namespace. Do not derive any candidate value during preparation. Exclude all **482,956 existing reservations**, using the sealed completed recovered-V3 study's ledger. Retain the old Pending V3 input and its verified external recovery receipt as exclusions only. Neither failed study is resumed or rebound.

## Exact preparation and input binding

New directories under `TestResults/balance`: `tower-local-refinement-comparison-readiness-20260916`, `tower-local-refinement-comparison-study-20260916` and `tower-local-refinement-comparison-execution-20260916`.

Current cumulative ceilings remain **4,080 diagnostic seconds / 4,496,293,888 bytes**. Start from the closure receipt's **3,756.5312723556026 seconds**, and reconcile its final file size with prior output. Preparation permits **60 seconds / 2 MiB** within existing authority, including setup, input/hash verification, a small host-only compilation, native inspection, independent audit and publication. Charge twenty seconds for initial inspection; source/script drafting is excluded. No full registry scan, seed derivation, generated party, preparation service, combat or replay. Stop dependent phases on the first failure and preserve evidence; zero retries.

Snapshot the dirty checkout and freeze the protocol, scripts, source/input manifests and commands before execution. Record unrelated concurrent changes without overwriting them. Phase ceilings, sharing the 60-second total: setup/freeze 10 s, host build 10 s, native inspect 10 s, independent audit 10 s, publication 5 s. Reuse the exact tested V4 harness DLL and all captured gameplay/dependency bytes. The small comparison host uses a pinned per-file resolver so inspection can reference existing assemblies without copying a 20-MiB runtime into the remaining preparation allowance. Its execution path uses a complete verified runtime copied only after approval; no gameplay/harness build occurs. Retain the existing host's production API calls and explicit authorization checks.

Verify the closure and V4 verification seals, the successful recovered-V3 execution and study inventory, original source/dependency pins and 69-test TRX. Build the reference-only host with cached restore assets and `Private=false` references. Native `inspect` must validate the complete request, comparison version, current 482,956-value ledger, recovered Pending source, controls, content/settings/gameplay identity and driver receipt, then emit an unsigned authorization template. It must not create the study or refresh the live registry.

The independent preparation audit checks the exact request/template, identical gameplay identity, the 45-value historical increment since the previous request, version/policy bindings, recovery pins and resource envelope. Reuse the existing pure reader against 128 saved confirmation records and four rates. Freeze the adapted execution reader for V4; it will verify transcript/schedules, durable charges, archives, nominations, selection, paired statistics, fixed order and one-slot/one-Essence edits. No new backend tests are needed for request/host preparation; the native inspection verifies host loading of the already tested harness.

The saved registry snapshot is a baseline, not a cache that replaces discovery. Production binding and launch keep their complete live scans, input checks, cancellation, durable attempt charging and native archive reconstruction.

## Exact prospective approval

Request **45 fresh values**, at most **288 attempts**, **zero retries**, plus **180 additional cumulative diagnostic seconds** and **112 MiB additional cumulative output**. Only explicit approval changes cumulative ceilings to **4,260 seconds / 4,613,734,400 bytes**. Earlier seed approvals are exhausted; no old experiment cap changes.

- Study: **360 seconds / 84 MiB**. Binding has 150 seconds, conservatively charged in full; launch, its live refresh, combat and native reconstruction share the remaining 210 seconds.
- Complete execution: **400 seconds**, including staging the producing runtime, process cleanup, independent audit and publication.
- Producing runtime staged in the execution directory: **24 MiB maximum**, verified against the pinned source manifest. It is a separate copy of the exact tested harness plus captured dependencies and runtime metadata; the host remains required to call the production APIs directly.
- Execution evidence outside that runtime: **4 MiB maximum**. Study, runtime and evidence together reserve **112 MiB**. Count all bytes, including partial output on failure.

Before seeking approval, prove that actual readiness output plus the full 400-second/112-MiB execution reservation fit those prospective cumulative ceilings. Recalculate the study's component minimum from fourteen compact historical arrays, one runtime and four content copies. Reports and transient writes remain outside that minimum; success is not guaranteed. No allocation/authorization receipt may be signed during readiness.

After the exact approval, bind it to the readiness seal and request hash and invoke the frozen execution wrapper once. It must validate the approval before staging or launching, own the process tree, preserve the first failure and every derived reservation, and publish an honest completion receipt. Recheck frozen assets immediately before execution. Do not add a fourth standalone full registry scan.

All 482,956 reservations remain excluded, including the earlier failed V3 allocation's 45 unused values, the earlier failure's 40 and V19's separate 512 unused values and 253 recipes. V19 reliability Unresolved; later Fail 1/3, deep recovery 0/3; adoption Hold. No deployment, migrations, configuration changes or ability-order tuning.
