# Automatic Tower team discovery: contracts, generation and acceptance

**Whole-party coverage pilot — 12 September 2026:** [opt-in v4 coverage and placement search](Tower-Party-Coverage-Review.md) completed **8,712 fights in 219.65 seconds** across measured execution/reconstruction phases, retaining about **339 MiB**, with **144 passing tests**. It found two independent builds at **34.77% and 38.67%** on fresh validation seeds; saved controls scored **14.45–32.03%**. Both new builds came from one restart, so reliability remains **Fail (1/3 passing restarts; 2 required)**. The stronger secondary does not replace the preselected primary in the gate. All recipes, ancestry and evidence are saved. Boss settings, catalogs and defaults remain unchanged. Next: separately freeze replication of unchanged v4 on new restarts/seeds, adding these two builds as reference controls without using them as independent parents or fitness. The [replication handoff](Tower-Coverage-Replication-Plan.md) lists the six saved controls, reuse boundaries and required preflight decisions; its budget example is not a frozen run.

**Saved-team diagnosis — 12 September 2026:** the [controlled substitution experiment](Tower-Saved-Team-Ablation-Review.md) completed **2,332 fights in 38.49 seconds** across measured execution/reconstruction phases, with **85 passing tests**. The saved Kharad team won **29.30%**; all eight declared recipe changes show a supported paired loss after adjustment. Replacing half its Fairies produced **0 wins**, while redistributing the exact same inventory produced **7.81%** wins. All nine recipes and evidence are saved (about **330 MiB**, plus **39 MiB** for a preserved zero-fight setup failure). This establishes recipe-level sensitivity in one team; independent search quality remains **Fail**. Boss settings, runtime, catalogs and defaults are unchanged. The subsequent v4 implementation and bounded comparison are now complete; see the current status above. Keep saved recipes out of independent parents and fitness; do not hardcode this team or its Essence counts.

**Mechanics-core pilot — 12 September 2026:** the [source-informed core search](Tower-Mechanics-Core-Review.md) completed **8,712 fights in 206.52 seconds** of timed execution/reconstruction, retaining about **331 MiB**, with **160 passing tests**. The experimental `independent-mechanics-v3` policy derives 48 condition/basic-attack core hypotheses and preserves v2 as its comparison arm. All 12 generated finalists again won **0/256**; saved controls won **17.58–32.03%**. Search reliability remains **Fail (0/3 passing restarts; 2 required)**. Detailed same-seed replays show earlier character losses in the new teams, without establishing a causal explanation. Recipes, ancestry and evidence are saved; boss settings, catalogs and defaults are unchanged. The subsequent saved-team substitution diagnosis is now complete; see the current status above.

**Independent-search reliability pilot — 12 September 2026:** the [frozen coordinated-generation comparison](Tower-Independent-Reliability-Review.md) completed **8,708 fights in 190.20 seconds** of timed execution/reconstruction, retaining about **302 MiB**, with **150 passing regression tests**. The opt-in `independent-coordinated-v2` policy explores shared combinations across characters while preserving the unchanged constructive comparator and default v1 behavior. All **12 generated finalists won 0/256** on fresh held-out seeds; the four saved controls won **20.31–28.13%**. The predeclared reliability gate is **Fail (0/3 passing restarts; 2 required)**. Better discovery damage in two restarts does not establish competitive builds. All 576 evaluated recipes, ancestry and frozen validation builds are saved; catalogs, boss content and the 10–50% policy are unchanged. The separately frozen mechanics-core follow-up above now completes that comparison with another Fail. Both policies remain experimental. The saved-team diagnosis and v4 coverage comparison are now complete, as reported above; independent reliability and workload-level performance remain open before a large campaign.

**Archive bookkeeping follow-up — 12 September 2026:** the [bounded matched profile](Tower-Bookkeeping-Performance-Review.md) completed **520 diagnostic fights in 49.35 seconds** of timed runs, with identical compact outcomes, eight matching old detailed replays and **289 passing regression tests**. Authoritative scans of the existing 18,769-file archive became **77.02% faster**; strict 256-team validation became **78.43% faster** with **96.16% less allocation**. Full scans, seed rejection and durable attempt flushes remain enforced. The small 128-fight campaigns were **5.01% slower overall** in these samples, so no whole-campaign speedup or historical-hours reduction is established. Boss settings, saved builds and acceptance policy are unchanged. The separately frozen independent-reliability pilot above now completes that experiment with a Fail; mechanics-guided search reliability and workload-level performance remain open before another large campaign.

**Checked local application — 12 September 2026:** Kharad now uses **Health 3.04881408 / Power 3.85370128**, a linked **+4%** increase from the preceding values. The [application review](Tower-Kharad-Checked-Application-Review.md) verifies exactly two content-value changes and six matching detailed reports using **8 diagnostic fights in 16.42 seconds**; **80 Tower tests passed**. The saved **2,918-party** confirmation supports a scoped **Pass** at these settings, with the strongest recorded team at **84/256 (32.81%)**. These repeated seeds check application parity, not new win-rate evidence. Exact full-family and top-build recipes remain saved; normal retained catalogs were not promoted. Competitive search quality remains **Fail**, and near-optimality is unestablished. The bounded bookkeeping profile above now completes that follow-up; independent search reliability and broader-floor coverage remain open. Earlier dated entries retain their historical scopes.

**Staged confirmation — 12 September 2026:** the [complete 2,918-party +4% Kharad family](Tower-Staged-Confirmation-Review.md) returns **Pass** under the new, separately tested `tower-staged-bonferroni-wilson-95-v1` policy. The highest observed second-stage result was **84/256 (32.81%)**, with an adjusted interval of **22.63–44.91%**. All prior recipes and all newly evaluated challengers remain included. The workload used **152,168 fights** across bounded search, confirmation and replays; timed execution/reconstruction took **68.70 minutes**, retaining about **2.72 GiB**. **286 relevant tests passed**. Exact recipes and evidence are saved. This confirmation preceded the checked local application reported above; its sealed review preserves that historical state. Catalogs remain unchanged, and search-quality/near-optimality work remains open.

**Linked calibration candidate — 12 September 2026:** the [five-setting screen](Tower-Kharad-Linked-Screen-Review.md) selected **+4% to both Kharad Health and Power**, resolving to **3.04881408 / 3.85370128** in isolated copies. The four confirmed teams won **29%, 29%, 30% and 28%** on 100 fresh paired seeds; the largest 20-cell-adjusted upper bound at that setting is **45.04%**. All **2,004 fights** completed in **38.64 seconds**, retaining about **190 MiB**, with matching replays and verification. At the screen stage the candidate was **not applied**, and the then-current Kharad setting retained its expanded-benchmark Fail. A next-run inventory includes **2,678 recorded recipes**, covering all evaluated pilot teams as well as the historical portfolio. That preflight, staged implementation and bounded confirmation are now complete, as reported above; the screen remains historical selection evidence.

**Challenger confirmation — 12 September 2026:** [fresh 1,000-seed confirmation](Tower-Challenger-Confirmation-Review.md) establishes **two Kharad ceiling breaches and paired improvements**: **56.4% / 56.5%**, versus the historical leader's **47.1%**. Their joint-adjusted lower rate bounds are **52.08% / 52.18%**. The third challenger observed **51.2%** and remains included despite uncertainty. The expanded benchmark at the previous setting was **Fail**; the historical 2,438-recipe Pass retains its original scope. All **4,004 fights** completed in **86.98 seconds**, with about **162 MiB** retained after independent checks. Confirmed recipes are saved separately; catalogs and boss settings are unchanged. The linked Health/Power screen and fresh complete-family confirmation are now complete, as reported above; checked local application is now complete for that family. Search reliability and broader-floor coverage remain open.

**Bounded challenger pilot — 12 September 2026:** the [floor-5 pilot](Tower-Bounded-Challenger-Pilot-Review.md) completed **6,212 fights** across independent discovery, explicit saved-team improvement, all **65** frozen validation teams and four matching replays. Its timed phases totaled **154.40 seconds**, retaining about **276 MiB**. Three new improvements observed **60.94%, 59.38% and 53.13%** on 64 fresh seeds, versus the saved leader's 50%; uncertainty prevents a confirmed breach or improvement claim. Independent finalists had no validation clears. Exact candidates are saved separately; catalogs and boss settings are unchanged. The separately frozen confirmation is now complete, as reported above; the pilot's observations remain unchanged. This does not grant a new portfolio Pass; search quality and broader-floor coverage remain open.

**Resumable bulk integration — 12 September 2026:** [chunk-wise verification and resumable campaigns](Tower-Resumable-Bulk-Integration-Review.md) are implemented for opt-in compact discovery, explicit supplied-build improvement and frozen balance-family execution. They share content once per campaign, preserve prepared execution, verify saved batches before reuse and charge interrupted attempts against a fixed retry reservation. **270 regression tests passed**. Full reports are retained only for the current chunk during bulk verification/scoring; lightweight schedules, outcomes and indexes remain in memory. Saved catalogs, boss settings, fixed gear/untrained-Essence budgets and the 10–50% policy are unchanged. The [bounded challenger pilot](Tower-Bounded-Challenger-Pilot-Review.md) now records campaign resource use and saves promising candidates for fresh confirmation; full Tower Lab/study integration and a new statistical stopping policy remain separate work.

The earlier [prepared execution comparison](Tower-Prepared-Execution-Review.md) measured **26.96–27.09% lower overall time** and **58.41–58.58% lower allocation** in its frozen benchmark. Those historical percentages do not measure the new durable attempt journal or complete campaign path.

**Competitive search update — 12 September 2026:** [stronger Kharad searches and calibration](Tower-Competitive-Build-Search-Review.md) supported the historical **2,438-recipe portfolio** at Health **2.931552** / Power **3.705482**, then applied locally after exact parity checks. The checked +4% application above supersedes those values. The strongest team confirmed at **23,835/50,000 (47.67%)**, adjusted upper **48.23%**. There are **49 distinct floor-5 controls combined (44 local, 32 published, 27 shared)** for future fresh searches. Search quality still fails the predeclared reliability/plateau rule; near-optimality and broader-floor competitive balance remain unestablished.

The [versioned retained catalog](../LL/tools/BalanceHarness/Fixtures/tower-retained-builds.json) now also carries strong verified competitive discoveries for fresh checkouts. See the [complete recipe exports](Competitive-Kharad-Recipes-20260912/README.md) and current review for their original settings and measurements. Compatible recipes require fresh trials on subsequent content.

**Progression audit and calibration — 11 September 2026:** [floors 2–5 review](Tower-Progression-Floors-2-to-5-Review.md) records independent four-Essence searches on floors 2–4, their separately confirmed linked Health/Power calibration against every known breach, and Kharad's fresh **20.9%** strongest-control check. Compatible controls and calibrated top builds persist for future searches. Floors 6–11 and practical acquisition coverage remain open.

**Fresh search and Kharad follow-up — 11 September 2026:** [new independent searches](Post-Calibration-Tower-Team-Search-Review.md) confirmed Garran's strongest saved team at **34.8%** and found a stronger Kharad team at **59%**, triggering a separate calibration. The [expanded-portfolio follow-up](Kharad-Expanded-Portfolio-Calibration-Review.md) applied another **8% to both Kharad Health and Power**; its strongest of 122 parties confirmed at **25.25%**, and the full family passes. At that stage, main-dashboard searches retained **4 floor-1 / 6 floor-5 controls**. Broader progression, practical Essence access and further independent ceiling searches remain open.

**First retained-build calibration — 11 September 2026:** compatible saved builds now enter new Tower Lab searches automatically as fresh benchmark controls; completed future studies retain their generated finalists. The [retained-build calibration](Retained-Tower-Builds-Calibration-Review.md) applied linked Health/Power factors of **1.06 to Garran** and **1.56 to Kharad** relative to their pre-campaign inputs. The strongest of 57/106 retained parties confirmed at **34% / 24.25%**, respectively, and both frozen families pass the 10–50% policy. This uses the declared full Essence pool including Rare Essences; this historical result is followed by the fresh searches and expanded calibration above.

**Original independent-team pilot results — 11 September 2026:** increments 1–5 of [Automatic Tower team discovery](Automatic-Tower-Team-Discovery-Plan.md) are complete. The [fixed pilots](Automatic-Tower-Team-Pilot-Review.md) found a floor-1 generated primary at **859/1,000 wins (85.9%)** and five floor-5 generated finalists at **972/972 each**. Independent search found viable builds; both declared cohorts fail the 50% balance ceiling. These pilots use the full 80-Essence pool with hypothetical ownership, including Rare Essences. The retained-build follow-up above now calibrates these declared budgets; practical Essence access and the wider progression curve remain separate coverage. Wider progression balance remains unestablished; no bosses were tuned by these pilots.

The original implementation used Garran at Health **1.6764** / offense **1.6368** and changed no gameplay content. The later [retained-build calibration](Retained-Tower-Builds-Calibration-Review.md) applied the current Health **1.776984** / offense **1.735008**. His earlier **291/1,000** user-party result remains historical evidence for its original setting and budget.

## Contract and isolation

[`TowerBossDiscoveryContract.cs`](../LL/tools/BalanceHarness/TowerBossDiscoveryContract.cs) introduces a separate `TowerBossDiscoveryDefinition`, schema 3. Legacy boss-search schema-1/2 readers, execution, cost formulas and report reconstruction retain their existing semantics. Other harness workflows also use a schema number 3; their definitions are different contracts and are not interchangeable.

The new definition freezes:

- Target floor, actual production `RequiredSlots`, character/gear budget, starting time and budget purpose.
- One to four distinct equipment contexts, each with every character explicitly represented. Templates have empty Essence vectors and stable neutral character IDs; the factory does not load historical recipes or repeat the user's example across floors.
- Legal Essence IDs and source families, with optional owned-copy limits. Within-character family restrictions apply; repeated Essences across different characters remain legal. An omitted owned-copy entry means zero copies when an owned inventory is supplied; `null` means hypothetical ownership.
- Generation methods, restart seeds, bounded attempts, target-strength objective, stage allocations, all explicit combat schedules, exclusions and a hard cap.
- Separate benchmark references and supplied starts, reference evidence hashes, complete content hashes, settings hash and execution hash.

`TowerBossDiscovery.Create(...)` builds an independent definition from a content root, explicit budget/equipment contexts, master seed and caller-supplied seed exclusions. It reads current production Essence families and floor size, validates actual equipment through production materialization, and creates schedules before inspecting references. It performs no combat. Custom definitions can then restrict the pool or ownership and must be validated again.

`GenerationInputs(...)` returns a detached input object containing the budget, allowed pool, versioned generation policy, discovery schedules, shortlist capacity, equipment selections and source hashes. Reference recipes, source labels, reference fitness, actor identity vectors and selection/confirmation schedules are absent. Increment 2 verifies unchanged inputs, proposals, scores and discovery shortlists when references are added, removed or reordered, including three matching production-combat discovery runs.

Every generated character position can receive any legal ordered Essence selection. Preparation holds actor identities fixed behind the generation boundary. Equipment labels impose no Essence roles. Reordering a candidate's Essences remains a distinct tested loadout.

Independent mode requires zero supplied starts. `improve-supplied` requires explicitly named starts matching registered references. The provenance validator checks parent existence, operator arity, generation seed/method and propagation of reference ancestry through mutation and recombination; missing, cyclic or relabeled ancestry is rejected. The explicitly versioned `retained-teams-v1` generator now implements this separate workflow; legacy reserved definitions require explicit conversion. See the [competitive search review](Tower-Competitive-Build-Search-Review.md) for operators, CLI commands and verification.

Exact reference recipes are deduplicated after normalizing equipment order and equivalent explicit/default identity vectors. Distinct actual actor identities remain distinct recipes. References must match the target floor, full party and context's fixed equipment budget; the user's floor-1 party is accepted only through explicit registration.

## Opt-in coordinated construction

`independent-coordinated-v2` accepts exactly the methods `constructive-joint` and `coordinated-joint` in that order. The first arm preserves v1 construction, mutation and random streams. The second constructs a legal character, selects a random subset of its Essences, and shares that ordered core across a randomly selected group of actual party positions. A `broadcast-core` mutation copies a subset from a generated donor to several recipients, replacing conflicting source families before filling the remaining slots. It clones parents and applies full family, slot and optional owned-copy checks. Every rejected/duplicate proposal and every generated parent relationship remains recorded.

The detached generator still receives no reference recipes, reference fitness, saved IDs, actor identity vectors or held-out schedules. The architecture hypothesis was informed by historical repetition patterns; that is not a claim of blind algorithm design. `fresh-coordinated` has zero parents and `broadcast-core` one generated parent. Both operators are restricted to the appropriate independent coordinated/mechanics arms of the versioned policies. Retained improvement remains separately versioned. The policy is available to explicit schema-3 definitions in ordinary and compact discovery; it is not selected by default or promoted through Tower Lab. Its first bounded reliability pilot failed, as reported above.

## Opt-in mechanics-core generation

`independent-mechanics-v3` requires exactly `coordinated-joint` and `mechanics-joint`, in that order. The comparator preserves v2's mechanics, random stream and operators. `TowerMechanicCores.cs` derives positive enemy-condition pairs, source-scoped basic-attack supplier/listener pairs and connected three-Essence chains from the frozen inventory. It preserves evidence node keys and timing/target uncertainties. Same-owner placement avoids assuming that a condition predicate or attack event broadcasts to every character. This does not prove that the combination works in combat.

The new arm samples kinds and deduplicated recipes, constructs two distributed core groups, fills remaining slots with existing constructive proposals, and adds a one-parent `mechanic-core` mutation. Families and optional owned copies remain enforced; exhausted copies permit uncored filling, and an empty compatible-core pool records an explicit coordinated-construction fallback. All proposals remain subject to combat fitness. `fresh-mechanics` requires zero parents and `mechanic-core` one generated parent; v4 also preserves these operators for its mechanics comparator and permits core replacement in the coverage arm. Saved references and held-out outcomes remain excluded from generation.

Mechanics archives gain an optional `cores` field for v3 and v4. It is omitted when null, preserving v1/v2 serialization and streams. The generic inventory and its existing interaction list are unchanged. The first v3 pilot failed its search-quality gate; neither factory nor Tower Lab defaults were promoted.

## Opt-in whole-party coverage and placement

`independent-coverage-v4` requires exactly `mechanics-joint` and `coverage-joint`, in that order. V3's mechanics arm is preserved as the comparator. `TowerPartyCoverage.cs` extracts direct authored effect evidence for recurring enemy control, enemy pressure, attack enabling, protection and recovery. It excludes zero-chance effects, ambiguous targets and one-shot control triggers; delegated status/summon effects remain outside this first coverage extractor. These are proposal categories, not measured effectiveness or uptime.

Fresh construction samples category providers and reservation counts from zero through the full party size, shuffles positions, enforces families/ownership and attempts compatible cores before filling and reordering remaining slots. Reservation traces report requested/satisfied placements, not final coverage totals. A one-in-eight uniform route keeps uncategorized legal teams reachable. `coverage-count` changes one provider across a sampled character group; `placement` exchanges existing Essences without changing the global inventory. Half of placement proposals prefer core completion in at most 32 sampled swaps. Combat fitness still decides ranking.

`fresh-coverage` has zero parents; `coverage-count` and `placement` have one generated parent and are legal only in the independent v4 coverage arm. The optional `coverage` archive field is omitted for v1–v3; old serialization and arm streams remain reproducible. No saved recipe, Essence ID or count is hardcoded. Ordinary/compact discovery and resume/reconstruction support the policy; factory and Tower Lab defaults remain v1. The [first pilot](Tower-Party-Coverage-Review.md) saved independent 34.77%/38.67% teams but passed only one of three restart gates. Replicate unchanged v4 separately before promotion or retuning.

## Progression and costs

`budgetPurpose` defaults to `intended-progression`. Known checkpoints require four slots at floor 1, five at floor 5, six at floor 10 and at least seven at floor 11. A legal alternative budget requires `diagnostic` at those checkpoints. This is a reporting/experiment guard, not a gameplay slot gate: lower-budget parties remain legal combat probes and their successes remain visible. Intermediate transitions and gear are explicit modeling assumptions, not newly approved progression targets.

Default target-only cost with one equipment context:

| Stage | Allocation | Maximum combats |
| --- | --- | ---: |
| Discovery | 2 methods × 3 generation seeds × 128 candidates × 8 paired combat seeds | 6,144 |
| Selection | 16 shortlisted generated parties × 64 seeds | 1,024 |
| Generated confirmation | Up to 5 frozen parties × 1,000 seeds | 5,000 |
| Reference confirmation | Each deduplicated reference × 1,000 seeds in its declared context | 1,000 × C |
| Diagnostics | Up to 8 candidates × 32 seeds | 256 |
| Replay reserve | Explicit allowance | 200 |
| Total | C is the actual reference count | **12,624 + 1,000 × C** |

Each extra equipment context adds its full discovery, selection, generated-confirmation and diagnostic costs. Reference cost follows its declared context; the replay reserve is charged once. Every plan must fit its explicit cap, at most 100,000 combats. Definitions support 128 candidates and 1,000 confirmation samples without inheriting legacy 100-candidate/100-confirmation or all-floor multiplication limits.

The contract supports up to **96 references** after the [fixed-pilot preflight correction](Automatic-Tower-Team-Pilot-Review.md). The combat cap still applies: a large family may require a smaller, explicitly predeclared confirmation schedule. The floor-5 pilot retains all 90 compatible historical references and uses 972 samples per confirmation cell, reserving 99,964 combats. This decision precedes all outcomes. Complete definitions exceeding Tower Lab's 2 MB import limit can be prepared and executed through the CLI.

Combat schedules are disjoint between stages/contexts and exclude the declared historical union. Methods and restarts share the same context/stage schedule, so restarts do not multiply independent confirmation samples. Direct factory/CLI callers must supply the complete exclusions independently of reference selection, including original user-party, offense-only and linked Garran tuning ledgers. Tower Lab now imports built-in history and recognized ledgers beneath its configured results root, records their sources/hashes in the preview, and rechecks history before starting. Its bounded scan does not cover every external or deeply nested campaign: review the [scanner limits](Automatic-Tower-Team-Lab-Review.md) and add missing exclusions explicitly. Neither workflow reserves seeds against concurrent external jobs.

## Available commands

Build the harness, then validate a previously authored schema-3 definition without running a search:

```powershell
dotnet build LL/tools/BalanceHarness/BalanceHarness.csproj --configuration Release --no-restore
dotnet LL/tools/BalanceHarness/bin/Release/net10.0/BalanceHarness.dll tower-boss-discovery-prepare --definition discovery.json --content-root LL/src/API/API.LL --output TestResults/balance/discovery-preparation-new
```

The new output contains `definition.json`, `cost.json` and, in independent mode, `generation-inputs.json`. Existing outputs are rejected. This command requires a complete definition and performs preflight only. To execute independent discovery, use `tower-boss-discover --definition <schema-3-json> --output <new-directory> [--content-root <API.LL-directory>]`; reconstruct it with `tower-boss-discovery-verify --run <directory>`. See the [generation guide](Automatic-Tower-Team-Generation-Review.md) for costs, artifacts and limits. A definition's execution hash must match the producing build.

For standalone acceptance, author a `TowerBalanceDefinition` before confirmation. It has schema 1, an ID, `intervalPolicy: "bonferroni-wilson-95-v1"`, frozen content/settings/execution hashes, required cohorts, cells, exclusions and a maximum battle count. Each cohort identifies its floor/budget, party size, context, `equipmentBudgetHash` and `purpose`. Each cell declares a unique ID, cohort ID, `generated`/`reference` role, exact normal `TowerScenario` including the complete seed schedule, and minimum samples. Cohort equipment hashes use `TowerBossDiscovery.EquipmentBudgetHash(...)`.

Run each declared scenario through the existing `tower --scenario` command and create a source mapping, for example:

```json
[
  { "cellId": "generated-primary", "runDirectory": "runs/generated-primary" },
  { "cellId": "user-reference", "runDirectory": "runs/user-reference" }
]
```

Relative run paths resolve against the mapping file's directory. Cell IDs must match the frozen definition exactly. Then:

```powershell
dotnet LL/tools/BalanceHarness/bin/Release/net10.0/BalanceHarness.dll tower-balance-evaluate --definition confirmation.json --sources sources.json --output TestResults/balance/tower-assessment-new
```

[`TowerBalanceRuns.cs`](../LL/tools/BalanceHarness/TowerBalanceRuns.cs) reads saved normal-Tower archives, verifies their recorded files/reports and reconstructs preparation against their retained content. It does not accept summary win counts or execute more battles. Output includes the frozen definition, source mapping, verified trial evidence, `assessment.json` and `assessment.md`. Exit codes are **0 Pass, 1 Fail, 2 Invalid, 3 Inconclusive**.

The standalone evaluator's source archives must remain available; its assessment output is not a self-contained combat archive. Integrity checks establish recipe/content/seed consistency, not that a human-authored protocol predates its results. Retrospective evaluation remains retrospective. For integrated execution, `tower-boss-study --definition <schema-3-json> --output <new-directory> [--content-root <API.LL-directory>]` freezes the shortlist before selection and the generated finalists/reference family before confirmation, then runs fresh confirmation and acceptance once. `tower-boss-study-verify --run <directory>` reconstructs all stages without new combat. This study archive retains producing assemblies/dependencies, content, settings and detailed replay audits. See the [confirmation review](Automatic-Tower-Team-Confirmation-Review.md) for the explicit selection policy, source deduplication, accounting and limits.

## Acceptance behavior

[`TowerBalanceEvaluator.cs`](../LL/tools/BalanceHarness/TowerBalanceEvaluator.cs) assesses each declared floor/budget/equipment-context cohort separately. Every included party must support an upper win-rate bound of at most 50%; at least one must support a lower bound of at least 10%. A zero-clear control can coexist with a viable party. A high-clear outlier cannot be hidden by averaging weaker parties.

The report shows pointwise 95% Wilson intervals and approximate Bonferroni-adjusted Wilson intervals using the fixed number of declared recipe/context cells. Missing cells do not shrink that family. Draws count as non-wins. Any observed rate above 50%, including 10/10, blocks acceptance; 1/10 and 5/10 are inconclusive. If every party's adjusted upper bound is below 10%, the cohort fails; if viability remains uncertain it is inconclusive. Missing, duplicate, replaced, corrupt or incomplete evidence is invalid, while any raw above-ceiling observation remains visible.

An aggregate pass covers only the declared cohort set. Every declared context must meet its own viability requirement. All requested progression floors must be declared to support a progression-wide conclusion; the evaluator does not invent omitted floor coverage. Diagnostic findings remain explicitly labeled and cannot establish intended progression. Keep diagnostic experiments separate from the intended-progression acceptance family. Bounded search provides no guarantee about unsearched parties.

Search ranking remains a strength objective, without a 50% cap or a target of 30%. Balance assessment, search quality, completed execution and archive integrity remain separate conclusions. Independent Tower Lab studies invoke this evaluator after fresh confirmation; legacy searches retain their prior semantics; the separate starter 50–90% evaluator is unchanged.

## Verification and next increment

Backend verification uses `build/run-tests.ps1`. Increment 1 passed 261 relevant regression tests, followed by all 45 contract/evaluator tests after its final source-path fix; those logs remain in `TestResults/balance/tower-discovery-contracts-20260911/`. Increment 2 passed 273 relevant tests in `TestResults/balance/tower-independent-generation-20260911/`, and increment 3 passed 286 in `TestResults/balance/tower-staged-confirmation-20260911/`. These are separate historical verification runs.

**Historical increment-4 verification: 295 relevant tests passed**, plus desktop/mobile browser checks for preview, execution, cancellation, results, exports and replay. Its build completed with zero errors and five existing warnings in unrelated tests. Evidence is retained in `TestResults/balance/tower-team-lab-20260911/`; the [Tower Lab review](Automatic-Tower-Team-Lab-Review.md) records the commands and scope. The bounded browser studies verify implementation behavior and do not establish floor balance.

```powershell
dotnet build LL/tests/EssenceSystem.Tests/EssenceSystem.Tests.csproj --configuration Release --no-restore
./build/run-tests.ps1 -NoBuild -Configuration Release -Filter 'FullyQualifiedName~BalanceHarnessTowerBoss|FullyQualifiedName~BalanceHarnessTowerBalanceEvaluatorTests|FullyQualifiedName~BalanceHarnessTowerTests|FullyQualifiedName~BalanceHarnessTowerBenchmarkTests|FullyQualifiedName~BalanceHarnessTowerDashboard|FullyQualifiedName~BalanceHarnessGoal'
```

Tests cover progression checkpoints and diagnostic budgets, full-party production preparation through floor 15, reference isolation, provenance, owned-copy/family legality, stage costs, strict serialization, uncertainty boundaries, independent interval fixtures, verified archive evaluation and legacy boss-search reconstruction. Two older test assumptions were corrected during regression: hexadecimal prefixes now use ordinal comparison, and historical-reference content checks account for later local tuning instead of assuming the old content hashes still match.

Independent generation, staged confirmation, Tower Lab integration and the [fixed pilots](Automatic-Tower-Team-Pilot-Review.md) are complete. The pilots establish viable generated builds but fail both tested balance cohorts. The reference-cap correction passed 296 relevant tests; see the pilot review for exact evidence and limits. The retained-build campaign above now calibrates the full-pool budgets requested by the user; practical acquisition limits remain separate coverage. No migration, production configuration change, deployment or new dependency is introduced.

The latest [progression batch](Tower-Progression-Floors-2-to-5-Review.md) passed **25 relevant retention, Tower Lab and World Tower tests both before and after local application**, plus 140 exact current-content reports, 60 unaffected-floor pairs and 32 detailed replays. These targeted checks used the captured compiled engine and do not claim a rebuild or full-suite check of concurrent source changes. Both evidence packages are sealed.

The [competitive search increment](Tower-Competitive-Build-Search-Review.md) now allows **1,000,000 cumulative historical exclusions** and **32 MiB imports** in both the browser and server, while retaining the separate 100,000-combat study/confirmation cap. It adds retained-build improvement, equal-budget search comparisons, fresh quality audits and complete-portfolio calibration across explicitly bounded verification partitions. Floor-6–11 coverage remains open; stronger search evidence takes priority before competitive acceptance.

## Next increment: bulk performance — planned

Checked Kharad application and the [bounded bookkeeping follow-up](Tower-Bookkeeping-Performance-Review.md) are complete. Campaign scans now reuse metadata only within each full scan; strict balance validation builds its exclusion set once per call. New timing scopes expose storage, attempt journal/flush and shared-file costs. Integrity, cancellation/resume and result parity pass, but the small campaign timing did not improve overall; retain the workload-level gate before scaling up. The [independent coordinated pilot](Tower-Independent-Reliability-Review.md) is complete and fails its reliability gate. The new opt-in policy is reproducible and legal, but its generated finalists still do not win. The mechanics-core v3 follow-up is also complete and fails. The [saved-team diagnosis](Tower-Saved-Team-Ablation-Review.md) now finds supported losses for all eight declared changes, including exact-inventory redistribution. The [v4 comparison](Tower-Party-Coverage-Review.md) now finds two competitive independent builds but fails the repeatability gate at 1/3. The next bounded objective is a separate replication of unchanged v4, with fresh restarts and the new builds retained as reference controls. V1 remains the default.

Follow the [active plan](Automatic-Tower-Team-Discovery-Plan.md#immediate-next-increment-bulk-performance). The [bounded profiling command](Tower-Performance-Benchmark-Review.md), [compact format](Tower-Compact-Archive-Review.md) and [per-case prepared execution](Tower-Prepared-Execution-Review.md) are implemented and validated. The heading anchor is retained for existing links. [Resumable bulk integration](Tower-Resumable-Bulk-Integration-Review.md) adds bounded full-report verification and opt-in CLI discovery/confirmation campaigns; the prepared worker lives for one team's seed batch. The separate [two-stage statistical policy](Tower-Staged-Confirmation-Review.md) is now implemented and tested. Migrating the full Tower Lab/study driver remains separate work.

| Deliverable | Required exit checks |
| --- | --- |
| Bounded profiling command — implemented; see [measured coverage and limits](Tower-Performance-Benchmark-Review.md) | Freeze representative weak/strong, summon/status-heavy and long-duration recipes, seeds, producing build and total diagnostic budget. Separate load/validation, preparation, combat, checkpoints/report construction, serialization/write, hashing and verification. Record cold/warm timings, worker count, allocations/memory where available and logical bytes. Include all warmups/repetitions/replays in actual cost. |
| Compact versioned archive — implemented, opt-in; see [review](Tower-Compact-Archive-Review.md) | Shared content/recipes/input/preparation, compressed atomic chunks, exact non-event report reconstruction, strict schedule/integrity checks and selected detailed replay are verified. The evaluator verifies a shared bundle once per invocation. Legacy readers remain unchanged. The 1,200-combat comparison measured 88.69% smaller ordinary archives and 10.31–15.17% lower overall runtime; campaign writers are not migrated. |
| Reusable preparation — implemented per case, opt-in; see [review](Tower-Prepared-Execution-Review.md) | `prepared-v1` validates/owns one recipe, reuses its fixed preparation and compiled definitions, and copies actors before fresh simulation. Mutable engine/health/status/cooldown/RNG state is isolated, and bulk playback checkpoints are skipped. Full report/ranking/replay, seed-order/concurrency, mutation, cancellation and 6,000-tick parity checks passed. Overall benchmark time fell 26.96–27.09% and allocation 58.41–58.58%. Cross-bundle worker caching remains separate; new CLI campaigns use this bounded per-case worker. |
| Bounded full-report verification and resumable execution — implemented | Recheck the complete committed prefix against frozen identities and chunk/report digests. Retain one chunk of full reports and lightweight indexes/outcomes. New resume receipts and durable charged-attempt journals preserve fixed budgets; no persistent verification trust cache is used. Torn/pending writes fail closed. |
| Coordinated execution — sequential CLI integration implemented | Preflight the full contract before combat; enforce overall time/storage/combat caps and meaningful completion progress. Benchmark worker counts rather than assuming more is faster. Avoid duplicate verifier passes and concurrent maintenance; preserve completed chunks on cancellation or failure. |
| Equivalence and performance gate | Compare legacy and optimized outcomes, duration/final-state fields and every consumed search-fitness value on the same seeds. Check seed-order/worker-count independence, export/replay, tampering and interrupted resume. Demonstrate repeated end-to-end speed and storage improvements before scaling up; timing noise is not a gameplay balance failure. Run relevant backend checks through `build/run-tests.ps1`. |

New commands and recovery limits are documented in the [harness README](../LL/tools/BalanceHarness/README.md#resumable-compact-discovery-and-calibration). Verification rechecks the completed prefix; it never treats a previous verification result as a durable trust cache. A campaign counts both logical scheduled trials and charged attempts, including lost/uncommitted attempts. Discovery shortlists remain discovery evidence until separately confirmed.

Keep storage/execution optimization separate from changes to statistical acceptance. The new `tower-staged-bonferroni-wilson-95-v1` contract predeclares complete family coverage, anchors, two disjoint schedules, stage-specific uncertainty allocation, capacity and stopping rules. The existing evaluator and historical schedules remain fixed. Use `tower-staged-balance` and its dedicated verifier for this policy; it is not a change to ordinary fixed-sample intervals. Preserve the 10–50% target and the user's fixed gear/untrained-Essence budget. Stronger combination discovery and challenger checks remain necessary before broader-floor competitive acceptance.

## Increment 1 changed files

| Area | Files and purpose |
| --- | --- |
| Offline tool | `TowerBossDiscoveryContract.cs`, `TowerBalanceEvaluator.cs`, `TowerBalanceRuns.cs` and `Program.cs`: new contract, standalone verified assessment and two commands. |
| New verification | `BalanceHarnessTowerBossDiscoveryContractTests.cs` and `BalanceHarnessTowerBalanceEvaluatorTests.cs`: 45 contract, numerical, production-preparation and CLI/archive cases. |
| Existing verification | `BalanceHarnessTowerBossDiagnosticsTests.cs` and `BalanceHarnessTowerBossValidationReferenceTests.cs`: ordinal hash-prefix assertions and accurate historical-content expectations after tuning. |
| Documentation | This guide, `Automatic-Tower-Team-Discovery-Plan.md`, `Tower-Balance-Acceptance-Policy.md`, `Boss-Specific-Essence-Loadout-Plan.md`, `Balance-Harness-Plan-With-Benchmarking.md`, `Essence-Loadout-Search-Plan.md` and the harness `README.md`: current completion status, commands, limits and next increment. |

Other existing working-tree changes, sealed experiment packages and the user-authored party fixture were preserved.
