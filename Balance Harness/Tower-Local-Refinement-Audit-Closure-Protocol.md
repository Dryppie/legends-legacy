# Local V4 saved-evidence audit closure

16 September 2026. Target: offline BalanceHarness verification evidence and Markdown only. The previous [implementation review](Tower-Local-Refinement-Review.md) reports 69 passing backend facts and an interrupted reporting audit. This scope finishes that audit from saved evidence; it does not rerun the implementation experiment.

## Frozen scope

Write only `TestResults/balance/tower-local-refinement-audit-closure-20260916`, this protocol, the new closure review, the existing local-refinement review and six active Markdown handoffs. Read the sealed implementation and verification packages in place. Never patch their scripts, receipts, manifests or binaries. No harness, test or gameplay code changes; no new builds, backend tests, generation proposals, seed allocations, preparations, fights, replays or retries.

Start from **3,749.6242723556106 diagnostic seconds**. Reconcile prior output as the previous receipt's `priorOutputBytes` plus the verification package's final size, its retained temporary fixture bytes and its already charged 1-MiB shared-runner allowance: expected **4,492,396,291 bytes**. Existing ceilings remain **4,080 seconds / 4,496,293,888 bytes**. Allow at most **45 additional diagnostic seconds / 2 MiB new output**, including setup, reader fixtures, hashing, checks, failure evidence and publication. Charge five seconds for initial inspection plus measured phases. No extra temporary output allowance. Source/Markdown drafting is excluded; executing publication is charged.

Freeze this protocol, the new reader/driver, previous manifests and all relevant inputs before execution. Snapshot the current dirty checkout. Record unrelated concurrent differences without writing those files; harness inputs must still match their original pinned hashes. Phases run once: setup (5 s), bounded audit worker (25 s), publication (10 s), under the shared 45-second ceiling. Preserve the first failure, skip dependent checks and publish an incomplete result if necessary. Stop owned worker processes on deadline. Never rerun the failed historical audit command.

## Exact checks

1. Verify complete file membership and SHA-256 contents of the previous verification package and its sealed implementation predecessor. Verify every original input/source/dependency pin and the captured final harness/test binary map. Keep both historical failed receipts unchanged.
2. Read the saved TRX, exact `build/run-tests.ps1` invocation, build/test exits and count receipt. Require 69 distinct passing test results, zero skipped/failed tests, matching counters, zero process timeouts and no surviving child processes. These are existing results; execute no backend code.
3. Select exactly one timing row whose path is `local-refinement.construct`, regardless of row order. Require one call and finite nonnegative inclusive/exclusive milliseconds. Exercise four pure-reader cases: unrelated leading row, reversed row order, missing path and duplicate path. Missing/duplicate paths must fail closed. Do not assume list position or silently choose one duplicate.
4. Verify the saved 6,400-check exhausted neighbourhood, all 6,400 skip counts (6,150 measured neighbours, 200 family collisions, 50 unchanged entries), null replacement fields and zero new fights/seeds. Verify all three V1/V2/V3 reference/candidate hashes match, with 16 charged proposals apiece. The historical timing is **108.4539 ms**; no new performance measurement or speedup claim is made.
5. Complete the previously unrun `git -c core.safecrlf=false diff --check` over the eleven implementation source files and relevant test files. Also check those files directly for trailing whitespace, including untracked additions that Git's tracked diff omits. Verify the current dirty checkout against its snapshot and record any unrelated concurrent differences separately.
6. Publish a new success/failure receipt and measured resource ledger, preserving the earlier failed audit as historical evidence. Update the active handoffs and existing review to distinguish completed engineering verification from untested combat strength. Record exact commands and seals; capture the final Markdown without duplicating the large historical documents unnecessarily.

All **482,956 reservations** stay excluded, including the failed V3 allocation's 45 unused values, earlier failure's 40 and V19's separate 512 unused values and 253 recipes. Ability order remains fixed. V19 reliability Unresolved; later Fail 1/3, deep recovery 0/3; adoption Hold. No configuration, migrations or deployment changes. A real-input V4 comparison remains separate and needs its own frozen preparation and explicit seed/combat/resource authorization.
