# Separate captured-v19 portfolio confirmation — frozen study plan

Frozen **14 September 2026**, before seed allocation, for the offline BalanceHarness. The user authorized preparation and verification of this separate study. **Execution is not authorized by this preparation task.** The prepared package must remain unstarted until separately authorized. The sealed v19 experiment and earlier engineering evidence remain read-only.

## Scientific scope and complete family

Complete the original captured-v19 comparison using the reviewed `execution-v2` runtime in `TestResults/balance/tower-portfolio-confirmation-implementation-20260914`. Its harness SHA-256 is `0e0f72505454303c72d0bf253fa5f6665d0504505e5c4cfd5e94e438f9ef501f`. Application, Common, Domain and Services.LL must match the retained v19 executable exactly. Bind the original runtime/OS/architecture, all 16 bound content files and combat settings. Current checkout gameplay is outside this study.

Import `tower-search-portfolio-20260914` and `tower-search-portfolio-work-20260914` through their complete sealed manifests and receipts. Retain every ordered recipe and origin in the **253-cell family**: 112 controls and 141 new recipes, including all 119 recipes required solely by the ceiling rule. Preserve all six original and screened nomination groups, their primary/secondary choices and three original roots. No discovery, screening, recipe edits, pooling or replacement of nominees is allowed.

The fixed anchor remains `team-1abe76ca1891d97a91d484f0a3662048`; the stronger control remains `team-a7e5de669c4a17287d84060e8ab6359b`. Preserve the floor-5 fixed cohort: ten level-40, tier-1, rank-2 Standard characters, five level-1 unascended/unevolved Essences each, existing fixed gear, no styles/contributions and hypothetical ownership. Retain captured Kharad Health **3.5366243328** and Power **4.4702934848**.

## Fresh schedule and reservations

Use policy `tower-portfolio-confirmation-v1`, master seed **2026091420**, and its existing SHA-256/32-bit deterministic allocation. Survey top-level study seed ledgers before allocation and require the latest v19 ledger to include their complete array-value union. Exclude all **480,707** historical reservations, including all **512 unused v19 confirmation values**. If the survey finds additional reservations, stop before preparation and preserve the finding; do not silently change this frozen schedule contract.

Reserve exactly **512 fresh unique values**, shared in their saved order by all 253 cells. The complete definition contains **129,536 fights**. The expected cumulative ledger is **481,219 reservations**; every unused new value remains excluded from future studies even if this study never starts or stops early. The master seed is an allocator input, not a combat trial. Freeze the actual array, full exclusion array, recipes, source bindings, plan, content, executable and limits in the prepared protocol before any combat.

## Execution limits and failure behavior

The separately authorized future execution is limited to **129,536 starts**, **14,400 seconds (four hours)** and **8,589,934,592 logical file bytes (8 GiB)** in the study package. Use chunk size 32, `owned-storage-v1`, a durable outer start/completion journal, mandatory lifecycle audits and exact archive verification. There are **zero retries, replays, diagnostic fights, resumes or automatic cap extensions**. These are new study limits; all historical evaluator/discovery/v19 caps remain unchanged.

These limits are bounds, not a runtime or storage prediction. Earlier 94-recipe confirmation completed 48,128 fights in 903.44 seconds and retained about 610 MB; v19 screening took 8.92 minutes for 12,288 fights. These workloads differ from this complete family. Neither those timings nor the earlier bookkeeping speedup establishes whole-study throughput. Four hours permits substantial variation; reaching any cap leaves incomplete evidence and an unresolved decision, with no retry.

The execution timer starts after prepared verification and the durable start marker; its cancellation token covers execution and final publication. The final performance snapshot precedes inventory publication, so separately measure the full command. Completed reconstruction is a separate read-only command, bounded to 900 seconds by its future invocation wrapper, and starts zero fights. Preserve partial evidence on cancellation, failed checks or resource limits. Do not compute an adoption result from an incomplete family.

## Frozen inference rules

Use the existing ordinary 10–50% evaluator on the entire family, and the unchanged joint split of alpha .05: .025 over all 253 rates and .025 over nine paired differences / 18 discordance intervals. For at least two of three screened portfolio primaries require every component: adjusted rate lower bound at least 10%, paired lower improvement over the matching screened deep primary greater than zero, and paired lower difference against the fixed anchor at least minus ten percentage points. Keep stronger-control comparisons separate.

Report all observed rates above 50% even when intervals cross the ceiling, ordinary/joint family acceptance, supported viability and the original reliability gate separately. No automatic gameplay, catalog, optimizer or adoption promotion follows a result. Reliability remains **Unresolved**, adoption **Hold** and ordinary/joint **NotRun / NotRun** until complete valid confirmation evidence is reconstructed. A supported ceiling breach requires a separately scoped recalibration and full relevant-family confirmation. No Kharad tuning, deployment or floor 6–11 work is part of this study preparation.

## Exact preparation verification, before execution

Use new study directory `TestResults/balance/tower-portfolio-confirmation-20260914` and work directory `TestResults/balance/tower-portfolio-confirmation-preparation-20260914`. Freeze input/script hashes and command arguments before invoking preparation. Perform one preparation, one prepared check using its own retained executable, one independent reconstruction of seed allocation/definition/inventory, and one complete preservation pass over the five sealed source/engineering packages. No combat commands are invoked. If an operation fails, retain evidence and stop dependent work; do not rerun preparation or allocate a replacement schedule.

Allow at most **900 seconds of measured preparation/verification workload**, excluding builds/correctness tests, and **1 GiB of new retained work plus prepared-study bytes** at this stage. All real combat counts remain zero. Run the existing relevant 201-test filter through `build/run-tests.ps1` in a new artifacts directory; synthetic test allocations are not study reservations. Freeze exact commands in `preparation-definition.json`, retain logs/timers and publish an independent verification receipt, reservation handoff and measured preparation review. Preserve the complete prepared inventory separately from the work-package seal; add no review files inside the frozen study.
