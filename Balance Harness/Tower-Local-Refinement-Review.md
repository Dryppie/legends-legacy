# One-Essence, one-slot refinement implementation

**Audit closure update — 16 September 2026:** Local V4 engineering verification is complete: 69 existing backend facts verified, V1–V3 parity retained, and the saved-output audit closed. See the [closure review](Tower-Local-Refinement-Audit-Closure-Review.md) and its cumulative resource receipt. The earlier failed receipt below remains historical evidence.

16 September 2026. **VerificationIncomplete**. Local V4 refinement implemented; 69/69 frozen backend tests passed. The first failed phase is retained without retry; dependent verification was not run.

The maximum synthetic neighbourhood completed **6,400 checks in 108.4539 ms**: 6,150 previously measured neighbours, 200 family conflicts and 50 unchanged entries. See the [native trace](../TestResults/balance/tower-local-refinement-verification-20260916/maximum-neighbourhood.json). This is one in-memory construction observation, not a whole-run speedup or evidence of stronger builds. The [legacy comparison](../TestResults/balance/tower-local-refinement-verification-20260916/legacy-parity.json) records identical complete serialized V1, V2 and V3 fixture results against the pinned pre-change assembly.

The reporting audit incorrectly required `timings[0]` to be `local-refinement.construct`. The trace is sorted by path, so `hash.canonical-json` precedes the construction row. The construction row is present with one call; no backend fact failed. The audit stopped before its whitespace check, final predecessor-seal check and success receipt. These remaining checks were **not rerun**. Frozen source/dependency pins were checked during publication, and the [publication preservation receipt](../TestResults/balance/tower-local-refinement-verification-20260916/publication-preservation.json) records 737 unrelated files unchanged relative to the continuation snapshot. The initial failed snapshot separately retains four concurrent frontend differences at closure; this task did not write them.

Both scopes used **31.389 additional diagnostic seconds** and **15.47 MiB** of retained output, temporary fixtures and the shared-runner allowance. The latest cumulative receipt leaves **330.376 seconds** and **3,897,597 bytes** under the existing limits, after counting the final seal. Zero fresh values, preparations, fights, replays or diagnostic reruns. Harness build: no warnings; the isolated test subset has one pre-existing unused fixture-field warning.

The opt-in `independent-discovery-refinement-local-v4` policy changes one Essence in one character slot per emitted local proposal. One sampled offset determines a finite traversal; existing legality checks protect team roles, family uniqueness and shared inventory. The exact replacement and skipped-check counts are archived. Exhaustion preserves ordinary duplicate charging. Cancellation is checked on every entry. Fixed ordinal ability order, fresh-generation schedule/prefix, ranking, nominations and selection remain unchanged.

Comparison version `tower-discovery-refinement-comparison-v3` selects V4 and requires its matching driver receipt; gate version `tower-discovery-comparison-gate-v3` retains the complete-allocation requirement. The default and previous versions remain unchanged, and the optional trace is omitted from legacy serialization. Native archive reconstruction regenerates the proposal trace.

Changed files: new `TowerLocalRefinement.cs`; refinement policy, generator/controller, composition/structural/coverage and provenance registration; comparison model, gate and preflight; two new test classes; this protocol/review and six active handoffs. No gameplay content, configuration, migrations or deployment changes.

The 69 facts cover single replacements, constraints, metadata determinism, identical fresh teams, best completed parents, finite exhaustion, cancellation/checkpoint charging, opt-in boundaries, synthetic comparison binding/reconstruction and stopped discovery. The evidence-only compatibility test loads the pinned pre-change assembly and compares complete V1/V2/V3 generation results. Synthetic journal callbacks are not executed fights; no engine, preparation service or production allocator runs.

[Completion and resource receipt](../TestResults/balance/tower-local-refinement-verification-20260916/completion.json), [test command and environment](../TestResults/balance/tower-local-refinement-verification-20260916/control/tests-command.json), [TRX](../TestResults/balance/tower-local-refinement-verification-20260916/control/tests.trx), [source/input freeze](../TestResults/balance/tower-local-refinement-verification-20260916/freeze.json), [failed audit](../TestResults/balance/tower-local-refinement-verification-20260916/control/audit-failure.json). Commands and phase limits are in the [protocol](Tower-Local-Refinement-Verification-Protocol.md); run `python -B TestResults/balance/tower-local-refinement-verification-20260916/workflow.py <phase>` in its frozen phase order. The completed directory rejects repeats. Exact backend command: `pwsh -NoProfile -File build/run-tests.ps1 -NoBuild -ArtifactsPath TestResults/balance/tower-local-refinement-verification-20260916/tests -Filter <frozen eight-class filter>`; the linked command receipt supplies every argument and pinned environment. Reproduction requires a new output scope and its own resource accounting.

The initial implementation scope stopped before building when three frontend files changed concurrently; its failed freeze is sealed in the preceding package. This continuation runs the frozen diagnostics for the first time and records concurrent checkout changes separately. Both scopes share the original 180-second/18-MiB allowance.

Limitations: synthetic fixtures do not establish that V4 finds stronger teams, preserves a mechanic core after an edit, or completes 16 unique candidates on real content. Family/inventory/role legality is guaranteed, not combat synergy. The earlier V3 and baseline finalists both won 0/32; all nine V3 refinements worsened mean boss health against their parents. The local-edit hypothesis remains untested in combat.

Failed phase evidence: audit-failure.json. No failed command was rerun.

Next: correct the saved-output audit to find the timing by path, then complete its remaining independent checks in a separately frozen scope using these existing results. No build or test rerun is needed for that reader correction. This package remains sealed with its failed audit; no new combat or allocation is authorized.

All 482,956 reservations remain excluded, including the old failed V3 study's 45 unused values, the earlier failure's 40 and V19's separate 512 unused values and 253 recipes. V19 reliability Unresolved; later Fail 1/3, deep recovery 0/3; adoption Hold.
