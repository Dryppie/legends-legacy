# Completion versus allocation: replacement comparison preparation

15 September 2026. Target: offline BalanceHarness. This separately frozen package replaces preparation only; both failed packages remain sealed and are never retried. Preparation is authorized. No fresh seeds or combat are authorized until a specific exception is granted against this protocol.

## Comparison held fixed

Baseline `independent-group-completion-v1` / `group-completion-joint`; candidate `independent-group-allocation-v1` / `group-allocation-joint`. Keep the original comparison model, namespace `tower-group-allocation-comparison-v1` and master label **2026091509**. No values were allocated by the failed preparation, so there is no seed allocation to repeat or reuse.

Use the verified source and captured gameplay DLLs with the deep-challenger captured guardian Health/Power +10% content, settings, UTC timestamp, equipment, attributes, neutral identities, floor 5, 80 eligible Essences, unrestricted owned copies, level 40/tier 1/rank 2 and five Essence slots per character. Ten characters are two five-player parties. Ability execution order remains fixed ordinal Essence ID. No gameplay changes or Kharad tuning.

Each policy gets 44 distinct evaluated teams, at most 704 proposals, FreshEvery 4, 11 initial fresh teams and one shared generation-root value. Baseline runs first with the existing separate policy streams. Controls and reference outcomes never enter search or screening. Allocation's adjacent alternatives and duplicate proposals do not earn extra budget.

| Phase | Teams | Shared seeds per team | Maximum fights |
| --- | --- | ---: | ---: |
| Completion discovery | 44 | 4 | 176 |
| Allocation discovery | 44 | 4 | 176 |
| Screen | Top 2 per policy | 8 | 32 |
| Confirmation | One finalist per policy plus two controls | 32 | 128 |
| Total | | | **512** |

Nominate by existing discovery fitness and identity ranking. Screen winner: wins descending, discovery rank, ordinal identity. Freeze finalists before confirmation; never reselect. Merge identical full-context recipes only at screen/confirmation, retaining all origins and never refilling seats. Discovery overlaps stay charged; incomplete discovery stops dependent stages.

Controls remain `team-040e60d3dbc5c127321653c47ed3a9d3` and `team-49f6979895354870c89362d4abf214bb`. Retain complete generation, group, completion and allocation traces, including rejections and duplicates. No historical combat pooling or full-catalogue coverage claim.

Proposed **45 fresh values**: 1 generation + 4 discovery + 8 screen + 32 confirmation. Preserve **482,596** reservations; approved binding would retain **482,641**. Primary outcome: allocation finalist minus completion finalist on 32 paired confirmation seeds, plus four finalist/control contrasts. Wilson family factor 8 and paired gained/lost factor 20; draws are non-wins and identical merged recipes have exact zero difference. Boss health, duration and group counts are descriptive with missingness retained. No reliability or speedup claim follows from one shallow restart.

## Preparation-only registry snapshot

Use the exact 164-path/hash registry snapshot from `tower-history-registry-performance-20260915`, independently verified by `tower-history-registry-independent-closure-20260915`. Pin both seals, the snapshot and the completed independent receipt. The new preparation adapter verifies the snapshot's own pinned hash, every recorded ledger hash and the reservation union before materializing explicitly **non-runnable fixtures using already reserved labels**.

This avoids another complete live traversal during preparation. It does **not** certify that live membership cannot change afterward. The existing **complete live registry scans before binding, execution and native verification remain unchanged**. Binding compares exact live membership and hashes with the prepared map **before creating the study or allocating any value**. Any new, missing or changed ledger stops binding. This boundary is valid only because preparation cannot allocate seeds or invoke combat.

Keep the binding and execution method bodies byte-for-byte identical to the failed adapter. Only the request's verified-snapshot field, preparation helper and preparation call change. Add seven backend fixtures for valid snapshot loading, unpinned/tampered snapshots, changed/missing ledgers, cancellation and a new live ledger invalidating the same equality check used by binding. Run those seven once through `build/run-tests.ps1` in isolation.

Reuse the unchanged **131 passing backend cases**, four passing metric fixtures and six passing independent-reader fixtures by source/binary/TRX hashes. Reuse the verified iterative integer-union helper in the independent audit. Do not rerun native registry measurements, old tests, candidate construction or combat. Build the isolated adapter and seven new tests once with cached restore metadata and captured gameplay DLLs; no dirty gameplay rebuild or restore.

One native preflight validates matched definitions and budgets, prepares two controls without combat and saves 66 interval fixtures. One independent audit verifies the snapshot-backed definitions, all reservation values, controls and intervals; checks all 1,956 saved diversity schedule positions, 564 saved allocation positions and 19 saved completion plus 19 allocation traces. Preserve all **40 predecessor packages** before/after. Record dirty checkout/source hashes and leave unrelated work in place.

## Limits, gates and evidence

New preparation: `TestResults/balance/tower-group-allocation-comparison-v2-preparation-20260915`; future study/execution use the corresponding `v2-study` and `v2-execution` names. Carry **1,512.567 diagnostic seconds** and **1,818,924,029 bytes**. Add a conservative **1 MiB** charge for the seven tests' temporary files. Preparation diagnostics, including setup/freeze/tests/preflight/audit/publication, are at most **60 seconds**, each phase at most 30 seconds, within the unchanged cumulative **1,800 seconds**. Builds are separately measured engineering work, at most 300 seconds each. Reserve ten seconds for preparation closure.

Future execution remains **512 charged fights including all repetitions; zero retries/resumes/replays; 4 GiB cumulative output**. Prior output stays below 2 GiB; preparation plus execution evidence below 512 MiB; new study below 1.5 GiB. No old cap increases. Binding, run, native verification, independent audit and publication share the smaller remaining global time allowance; cooperative deadlines reserve five seconds for output/termination inside each outer phase cap.

Before requesting a seed exception, seal successful preparation and report actual remaining time. Require **at least 210 seconds remaining** as an execution-planning gate: prior analogous binding/run/verification/audit used 47.000/71.063/54.375/19.875 seconds, with additional closure margin. This is a planning threshold, not a runtime guarantee. If preparation passes but less time remains, mark execution blocked by the current budget and do not allocate seeds or silently enlarge the limit. Every execution phase still stops at the cumulative cap even after approval.

Seed authorization must name 45 fresh values and maximum 512 fights and bind to this protocol's hash. Require unchanged source, captured gameplay and registry hashes at their stated boundaries. Retain durable seed/attempt charges, cancellation, storage accounting and exact archive verification. Save detailed native run traces, clocks, CPU/allocation/peak-memory metrics, command receipts and independent findings. Stop dependent work on any failure or limit and preserve evidence without retries.

V19 remains sealed with all 253 recipes and unused 512 confirmation values. No 129,536-fight confirmation, gameplay/content tuning, ability-order optimization, deployment, configuration, migrations or default search-policy promotion. Historical reliability Fail 1/3, deep recovery 0/3, v19 Unresolved and adoption Hold remain unchanged.
