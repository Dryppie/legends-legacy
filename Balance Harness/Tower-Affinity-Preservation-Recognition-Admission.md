# Paired-pool recognition: captured-runtime and resource admission

24 September 2026. Target: the offline `LL/tools/BalanceHarness`.

**Admission passed for the frozen 145-cell paired-pool diagnostic.** The [sealed admission](../TestResults/affinity-preservation-recognition-admission-20260924/admission.json) contains a runnable request, captured gameplay dependencies and content, the tested harness, independent auditor and owned launcher. It prepared all 145 exact teams and reconciled **760,272 excluded combat values across 258 history files**. No battle, production entropy draw, new combat value or scientific launch occurred.

The next step is one owned **37,120-fight diagnostic using 3,072 fresh panel values**. Admission does not change the preservation pilot's `NoObservedOutputDifferentiation` decision, qualify a team or promote a policy. Reporting remains development evidence for the twelve frozen roots.

## Exact bindings

| Artifact | SHA-256 |
| --- | --- |
| [Admission manifest](../TestResults/affinity-preservation-recognition-admission-20260924/files.json) | `f96631cc46058b8730246304106beb067957f7480cc5bc6bb4270e339579f622` |
| [Native request](../TestResults/affinity-preservation-recognition-admission-20260924/request.json) | `bbb5b6f89d210b256d2038d0aa2ecd37766dbaa0d33a27d3688c1fdbc0fb8642` |
| [Native definition](../TestResults/affinity-preservation-recognition-admission-20260924/definition.json) | `044a85b4a45d24f33963e06ade9b99c925301d16e707a4c54e4ab6af878bd862` |
| [Unchanged frozen plan](Tower-Affinity-Preservation-Recognition-Plan.json) | `8456bb84f4f061eba78d35371602ec8a49ac81e5e60a550552c02bda3d3fddf6` |
| Tested harness DLL | `2577c25c5cdba2da955e1ddac2bb62e1755dab682ec24a05ea204d091e2d3f29` |
| Admitted execution identity | `9d7d48fb7e5847578764c6816f7a2a05341260567b5d22a6ad41981f3e1fa705` |
| [Prospective admission declaration](Tower-Affinity-Preservation-Recognition-Admission-Declaration.json) | `727c63230c8a684b4c84831921ed57e7d64ef6113bf63760eeb6c8feecdc387f` |

The native profile is `tower-affinity-preservation-recognition-v1`. The canonical plan's `FrozenDesignNativeIntegrationRequired` and `runnableRequest: false` fields remain unchanged as historical planning data. A separate admitted request supplies the verified execution route. Neither membership nor population sampling was redrawn.

## Compatibility and provenance

The new [admission adapter](analysis/prepare-affinity-preservation-recognition.py) binds the preservation pilot's captured admission, scientific publication and stage review; the completed affinity-creation recognition archive; and the preceding native verification and complete owned fixture. It reproduces the entire frozen plan from the retained catalogue and single population-sampling draw. The sealed prior builder helper is retained at its expected relative path, so deterministic reproduction needs no live-checkout imports or new entropy.

All captured gameplay dependencies and content remain byte-identical to the producing preservation runtime. Only the four tested harness files are substituted: DLL, portable symbols, dependencies file and runtime configuration. The [compatibility helper](analysis/affinity-preservation-recognition-context.ps1) authenticated **228 producing source documents**, resolved **609 native methods**, reconstructed the stored paired diagnostic and literal reservation classification, and checked **145 transports /290 input projections**. It retained all 321 provenance cells, 145 rates, 327 contrasts, 48 stratum summaries and 176 explicit unknowns. Execution identity is preserved except for the tested harness assembly.

The native check prepared every exact team under a combat trace guard. Independent Python and native history readers agreed on the complete exclusion union. Existing pending-reservation recovery receipts are carried forward. History was checked again before publication and during the owned read-only verification. A future launcher must refresh it under exclusive leases before freezing or allocating values.

## Resource qualification

The forecast was frozen before admission, with **margin 2**, no timing resampling and no downward correction. Native and audit time each use the larger completed per-fight observation from the preservation pilot and affinity-creation recognition run, scaled to 37,120 fights, plus current compatibility/check process time before applying the margin.

Every compressed report from both sources was authenticated: **15,744 +27,648 reports**. The largest is **21,004 bytes**. Native storage uses that maximum for every prospective fight, the larger observed recognition/fixture non-report overhead, a 64-MiB reserve, and margin 2. Audit storage has a separate 16-MiB reserve and the same margin. Search-only transport duplication is not scaled into the recognition archive.

| Stage | Forecast | Enforced cap |
| --- | ---: | ---: |
| Native time | 5,185.625 seconds | 9,000 seconds |
| Native storage | 1,943,177,934 bytes | 5.5 GiB less 4 MiB closeout reserve |
| Both audits and publication time | 1,156.190 seconds | 1,800 seconds |
| Audit/publication storage | 53,217,170 bytes | 512 MiB |

All estimates fit their separate limits. They are planning estimates, not guaranteed completion bounds. Unobserved recipes and machine load can exceed historical behavior. The owned launcher enforces the combined **10,800-second/6-GiB** execution allowance, with no transfer between stages, refill, resume or automatic retry.

The admission completed once in **302.703 seconds**. The subsequent read-only package/live-history verification passed **357.172 seconds after the owned admission began**, within the same **600-second/512-MiB** allowance. Combined observed admission/verification storage before the receipt was **103,834,314 bytes**. Both owned processes exited successfully, without timeout or surviving descendants. The full allowance was charged at the start; no refund or additional preparation was used.

Cumulative recorded charges are **36,191.656 seconds /29,827,579,398 bytes**. Cumulative declared maxima are **101,040 seconds /63,166,218,240 bytes**, including this admission and the newly admitted future execution allowance. Future combat is not included in recorded charges. Development tests and documentation checks remain separate engineering work.

## Verification and changed files

**40 tests passed:** 21 new admission tests and 19 legacy regressions. Coverage includes source publication/review binding, captured execution identity, exact definitions, recovery receipts, four-file substitution, independent resource caps, invalid/nonfinite forecasts, required backend evidence, deterministic plan reproduction, tampered sampling/helpers, process ownership and failure/no-retry accounting. PowerShell syntax validation also passed.

The preceding **142 passing backend tests, 64 Python cases and complete 37,120-report owned fixture** are authenticated inputs. No C# source changed in this step; another backend rebuild or test run was unnecessary. No required command remains blocked.

Principal commands (`python` denotes the bundled interpreter):

```powershell
python -B -X utf8 'Balance Harness/analysis/test-affinity-preservation-recognition-admission.py'
python -B -X utf8 'Balance Harness/analysis/test-affinity-creation-recognition-admission.py'
python -B -X utf8 'TestResults/affinity-preservation-recognition-admission-verification-20260924/run-admission.py'
```

The last command is a historical single-use invocation. Its existing package/output identities cannot be reused. Read-only verification uses the retained adapter and external pin:

```powershell
python -B -X utf8 'TestResults/affinity-preservation-recognition-admission-20260924/prepare.py' verify --package 'TestResults/affinity-preservation-recognition-admission-20260924' --manifest-sha256 f96631cc46058b8730246304106beb067957f7480cc5bc6bb4270e339579f622 --live
```

Changed repository files are the new adapter, compatibility helper, tests, declaration and this report, plus line-three status banners in nine harness/assessment/admission documents. Historical bodies and sealed evidence are preserved. The [verification package](../TestResults/affinity-preservation-recognition-admission-verification-20260924/closeout.json) retains the tests, process receipts, hashes and documentation checks.

There are no migrations, application configuration changes, deployments, gameplay edits or policy-default changes. Unrelated working-tree changes are preserved.

## Next execution

The admitted output is `TestResults/balance/tower-affinity-preservation-recognition-20260924`; it remains absent. Launch once using the retained launcher, captured runtime and admission manifest pin. The launcher owns native execution, both audits and publication.

After a fresh freeze, the study permits one 24,576-byte combat entropy batch. All exposed fresh words, including the unused tail, are permanently reserved; 3,072 values populate twelve disjoint panels. The full 145-cell design, 327 contrasts, paired weighted pool summaries, separate uncertainty components, both-arm provenance and 176 explicit nulls remain required. Its only completed decision is `CompleteDiagnosticOnly`; policy promotion requires separate evidence.
