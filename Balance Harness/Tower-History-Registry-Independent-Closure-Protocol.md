# Independent registry verification: bounded closure

15 September 2026. Target: offline BalanceHarness evidence verification. The user authorized proceeding after the registry diagnostic stopped. This is a separately frozen independent reader of completed native results. It neither retries nor modifies either sealed failed package, changes production source, reruns a native scanner, nor authorizes combat or seed allocation.

## Inputs and unchanged conclusions

Use `tower-history-registry-performance-20260915` sealed by `177a69882cf49fc62890a556b2f6d6f4cced6fa9ec076650178fc8ed057e97da`. Retain its 131 passing backend tests, matched 9,216-archive membership/counters, 57.467-second full native enumeration, 164 ledger paths/hashes and 482,596-value union. Pin the tested source, candidate/test binaries, source protocol and results. Preserve 39 predecessor packages before/after. Inspect the dirty checkout and leave unrelated work in place.

The scanner's 71.12% reduction in managed allocated bytes came with a 13.29% slower archive scan in one reference-first pair. This closure cannot establish a latency improvement, reduce a scan cap or certify comparison readiness. No production edit is planned: do not silently tune the buffer or change enumeration semantics while auditing the tested version.

## Exact new diagnostic work

Evidence root: `TestResults/balance/tower-history-registry-independent-closure-20260915`. Freeze all scripts and inputs before execution. Run exactly six in-memory Python fixtures for union recursion/type handling, pending-reservation rejection, empty structures, exact ledger filenames, reparse rejection and exact subtree exclusion. They contain synthetic literals only, not allocated balance seeds. The unchanged backend binaries/TRX are verified by hash; backend tests are not repeated.

Run one complete independent Python reader using `os.scandir`, checking all file/directory attributes, inaccessible-path errors, all reparse entries, cancellation/deadlines and the unchanged 2,000,000-directory bound. Use the same excluded subtree as the completed native scan: the prior registry-performance package. Do not additionally exclude the new closure package; it must contain no file named `seed-ledger.json`, `prior-seed-ledger.json` or `history-input.json`.

Save a durable progress journal at start, every 65,536 entries, each phase boundary and success/failure, with elapsed/CPU time, visited directories/files/entries, ledgers discovered and checked counts. Iteratively rebuild the integer union without creating one temporary set per value; preserve the previous reader's handling of integer list members, nested objects/lists, booleans and pending reservations. The fixtures cover these semantics.

Require exact case-insensitive path membership against all 164 native paths, every ledger content hash, exact sorted union parity and 482,596 values. Require the union to match the latest completed comparison ledger and every preceding successful-preflight registry entry to retain its hash. Record newer entries explicitly. Compare the sealed archive inventory/count evidence and the 131-case TRX. Directory/ordinary-file totals may differ from the saved scan as new non-ledger evidence is added; report the difference rather than hiding new paths or trusting a prior allowlist.

The reader writes discrepancy evidence before failing. It cannot produce an atomic snapshot against unrelated concurrent external writes; a changed ledger invalidates this attempt. No skipped directories, cached membership, sampled hashes or weaker reservation checks are permitted.

## Frozen limits and completion

Carry **1,410.255 diagnostic seconds** and **1,817,480,644 output bytes**, including prior failures, publication charges and the previous 1-MiB temporary-fixture allowance. Retain the cumulative **1,800-second / 4-GiB** ceilings. This package uses at most **200 new diagnostic seconds** and **64 MiB**. Freeze/fixtures/closure each get at most 20 seconds; the independent reader gets at most **180 seconds**, with five seconds reserved inside that cap for cooperative failure output. Every phase also obeys the smaller remaining cumulative/package allowance, retaining ten seconds for closure. No builds, native measurements, control preparations, candidate generation, fights, fresh seeds, replays or retries.

After one successful independent check, publish the exact measurements, remaining budget, source-retention decision and limitations, then update six active Markdown handoffs and seal evidence. If a check fails or a limit is reached, preserve progress, discrepancies and errors, stop dependent work and publish an incomplete result without retry. These closure actions are part of the frozen allowance.

Preserve all 482,596 reservations, v19's 253 recipes and unused 512 confirmation values. No Kharad/gameplay tuning, ability-order search, default policy promotion, configuration, migrations, deployment or 129,536-fight confirmation. Historical reliability Fail 1/3, deep recovery 0/3, v19 Unresolved and adoption Hold remain unchanged. Completing this registry check alone does not authorize the proposed 45 fresh values or maximum-512-fight comparison.
