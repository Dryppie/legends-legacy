# Joint structural search adapter: frozen zero-combat scope

15 September 2026. Target: offline `LL/tools/BalanceHarness`. Implement an explicit `independent-joint-structural-v1` / `joint-structural` policy in the existing generation kernel. Preserve all previous policies and gameplay. This scope authorizes no combat, preparation, replay, seed allocation, tuning, deployment or default-policy promotion.

## Implementation boundary

Construct from authored cores and the five authored coverage categories, without outcomes or supplied rosters. At most 64 cores, 128 providers, five Essence slots and ten characters. Each core uses 256 states / 16 recipes. Deduplicate full recipes; exclude shorter recipes without padding. Allocate with 256 states / 250,000 candidate checks / 16 parties, one distinct recipe per character and the existing shared inventory. Exactly one generation seed label, at most 16 candidate evaluations and 16 proposal attempts. The adapter caches construction, submits each roster once, records unavailable proposals, and uses the existing evaluation/checkpoint/cancellation path. Fixed ordinal ability order. Optional trace fields must preserve old serialization.

## Exact checks, frozen before execution

1. Capture dirty checkout/source before edits. Before/after verify the complete existing preservation chain and the immediately preceding `tower-joint-party-allocation-20260915` seal `4cfcbe96a428a382fc24b1fdac2f4238cc8e427e685bacf6d1d08d2a01b0a864`. Preserve 482,641 reservations, all 253 v19 recipes and unused 512 confirmation values.
2. Build reference harness from captured pre-edit source, candidate harness from post-edit source, and isolated test assembly, using identical captured gameplay DLLs and cached restore metadata from `tower-group-allocation-comparison-v2-preparation-20260915`. No gameplay rebuild/restore. Each build: at most 300 engineering seconds, measured separately.
3. Run exactly 12 zero-combat backend Facts through `build/run-tests.ps1`: registration/bounds; deterministic complete search; exhausted pool attempt charging; single-roster exhaustion; evaluator cancellation; evaluator failure; cancellation before construction; input immutability; shared-copy shortage; metadata permutation; optional trace serialization; pre-evaluation checkpoints and provenance. Synthetic scores only; an engine-entry guard fails immediately.
4. Reference and candidate each run exactly seven synthetic old-policy fixtures: composition, joined, group-count, variation, diversity, completion, allocation. Each uses 32 candidates, at most 512 attempts, five owners, 12 synthetic Essences, generation label 17 and existing fixture labels 101/102. Compare full serialized generation results, including proposals, measurements and shortlist, by canonical hash. These labels allocate no reservations or combat.
5. Candidate runs exactly one captured-content structural generation: previous preparation's frozen independent inputs/mechanics; change only policy/method, retain its first generation label, set candidates/attempts/shortlist to 16. Synthetic evaluator reports all losses, 100% guardian health and zero survival using the existing discovery matrix labels. Compare all resulting roster builds to the preceding unrestricted allocator's 16 saved rosters. Independently audit pool hash, core/state/check caps, party identities, copy/family/diversity/role constraints and proposal accounting. No reference/confirmation outcomes enter generation.
6. Persist stage timings, wall/CPU/allocation/peak-memory metrics, input/source/binary hashes, test commands/TRX, independent audit and failure receipts. Update six active handoffs and a completion review, check scoped whitespace and unrelated dirty-file hashes, seal all new evidence.

## Limits and stop rules

New directory: `TestResults/balance/tower-joint-search-adapter-20260915`. Zero fights, fresh values and retries. Prior diagnostics: **1780.3349999999648 seconds**; remaining **19.665000000035207 seconds** of the unchanged 1,800-second cumulative limit. Carry prior output as the preceding completion's prior bytes plus its actual sealed directory size. New output at most 192 MiB within cumulative 4 GiB. Setup, freeze, tests, native checks, audit and publication count as diagnostics. Reserve five seconds for publication/preservation, including one conservatively charged closure second. Each diagnostic phase has a deadline within the remaining allowance; external processes have cooperative cancellation and enforced timeouts.

Stop dependent checks on any failure or limit; preserve and charge the failure without rerun. Do not change this scope after execution starts. Build time is separately measured engineering work, consistent with the existing chain. No wall-time or fight allowance is reset. Existing reliability **Fail 1/3**, deep recovery **0/3**, v19 **Unresolved**, adoption **Hold**.
