# Complete retained-family durable binding — zero-combat verification protocol

Frozen **15 September 2026**, following [verified setup preservation](Tower-Complete-Family-Setup-Preservation-Review.md). Target: offline `LL/tools/BalanceHarness`. Implement durable external reservation and final controller protocol publication; verify with synthetic fixtures and a read-only real-input check. **Do not invoke the real reservation command, derive any prospective balance value, prepare combat or launch a study.** The 288 future values remain unallocated.

New package: `TestResults/balance/tower-complete-family-binding-20260915`. Limits: **zero fresh balance values, preparations and fights; zero diagnostic retries/resumes; 1,800 seconds diagnostic workload; 4 GiB total new retained output**. Correctness tests and compilation are separately timed, with their output included. Charge 60 seconds for static work and the measured baseline/capture work; preserve evidence on failure. No previous cap or sealed artifact may change.

1. Persist allocation intent and registered Pending history before derivation. Flush each candidate start before computing it; flush candidate/value/acceptance and update reservations before observing cancellation. Preserve a Pending marker on interruption and make history readers reject unresolved reservations. Bound candidate counts and incremental storage, including atomic replacement temporary bytes. Publish a complete history only after the schedules and transcript are durable. Never retry, resume or overwrite a reservation.
2. Final binding must verify captured executable/runtime identity, sealed source, complete live reservation history, prior sealed setup artifacts and charges. Reverify allocation derivation and rejection order before writing the fixed controller protocol last. Require its durable reservation proof before controller execution or verification. Keep gameplay, recipes, search/selection rules, durable fight charging, archive verification, 256-cell batching and all study caps unchanged.
3. Freeze exactly **104 test cases** before `build/run-tests.ps1`: 32 new `BalanceHarnessTowerCompleteBindingTests`, the existing 19 setup and 52 complete-family cases, and exactly `BalanceHarnessTowerSearchBenchmarkTests.Ledger_includes_unused_and_constructor_arrays_but_not_scalar_metadata`. Filter and expanded case allowlist are recorded in `test-selection.json`. All allocation fixtures use literals or `binding-fixture-only` with master 7 and synthetic history; never the future balance allocator domain. Run through PowerShell 7, saving command, TRX, exact passed-name verification and time. Correctness compilation fixes may receive a separately recorded build/test pass; native diagnostics cannot retry.
4. Build one full public CLI host from the prior setup package's captured source, overlaying only Program, history/input gates and the two new binding/reservation files. Reference the sealed captured-v19 gameplay DLLs; do not rebuild them for the native diagnostic. Pin captured source, compiler, executable, runtime, request and all required setup/input hashes before native execution.
5. Run exactly one `tower-complete-family-binding-check <request.json> <new-result.json>` with Action `CheckOnly`, at most **600 seconds / 64 MiB native output**. Use the sealed UTC source, completed setup preservation and authoritative 481,603-value history. The prospective study path must remain absent. Record detailed existing performance trace phases for setup, source and history. This is a real-input check of the full CLI, not execution of `reserve-bind`.
6. Run one independent read-only check, at most **120 seconds**, of receipt identity, exact family capacity, setup byte/hash bindings, unchanged gameplay and historical union including the original unused 512 values. Run one preservation audit, at most **300 seconds**, of all nine predecessor sealed packages, protected inputs, current producing files and checkout baseline. The native check and preservation audit each traverse live history once; the independent check reuses the frozen membership and verifies values/hashes. No growing-tree scan occurs per fixture candidate or fight. Concurrent UI work under `LL/src/Presentation/ll/`, `docs/ui-rework-verification/` and exactly `UI_REWORK_IMPLEMENTATION_PLAN.md` is observed and left untouched; fixed protected inputs take precedence over this checkout-only exception.
7. Update the seven active handoff documents and add a measured review. Seal within **120 seconds**, checking scoped whitespace/local links, retaining copies of final code/documents, counting package bytes plus whole changed files/shared TRX and a 2 MiB metadata allowance. Write the evidence inventory last. Include all separately measured setup/build/test work in the future study setup time basis. This diagnostic's draft request is not the eventual complete sealed study request.

Reproducible phase commands and immutable hashes are in `workflow.py`, `scope.json`, `native-freeze.json` and command receipts. The eventual real reservation and any full study require a separate explicit scope. This verification cannot establish cross-process power-loss recovery, full-family second-stage capacity, combat across the 256-cell batch boundary or full-study throughput. Reliability remains **Fail 1/3**, adoption **Hold**. Original v19 remains sealed **Unresolved**, with all 253 recipes and no confirmation inside that experiment. Preserve all 481,603 current reservations, including the original unused 512. The future full-study envelope remains **2,434,784 attempts / 24 hours / 64 GiB / zero retries**.
