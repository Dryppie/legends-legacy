# Saved control-triple slot and authored-coverage audit

15 September 2026. Target: the offline BalanceHarness's saved evidence. Continue the [completion trajectory review](Tower-Group-Completion-Trajectory-Review.md) by explaining which recorded construction boundaries kept Enchanted Fairy away from Pack Howler / Royal Venom / Venomous Spiderling. This is descriptive inspection, not evidence of a stronger replacement build.

## Frozen checks

Read the existing completion comparison's 92 proposals, 88 evaluated recipes, two controls, saved reservations and completion traces, plus its captured ability definitions, coverage metadata and 214-group catalogue. Identify Essences by the previously sealed shared-control set and names; do not derive a new selection target from combat scores. Record every evaluated character carrying the triple, its five-slot loadout, coverage providers, free slots, family compatibility, saved construction prefix and completion additions. Keep rejected/duplicate proposals distinct from evaluations. Count characters, teams and distinct loadouts separately.

Reconstruct each saved completion prefix at the entry and exit of each recorded coverage category, including categories with no insertions. Validate before/after additions. Record Fairy's missing-category, capacity, family and owned-copy gates on each triple owner. Preserve all applicable blockers rather than choosing one arbitrarily. Retain final additions beyond completion and aggregate coverage-reservation choices; the saved generic filler does not identify each insertion's recipient/order, so do not invent a filler sequence or a counterfactual score. Examine all catalogue groups containing the triple, their remaining capacity, saved visits and the previously sealed schedule positions.

Reconstruct direct authored coverage for all 80 allowed Essences from captured ability effects and compare exact categories/evidence keys against both policies' metadata. Retain full authored abilities for the shared set and every accompanying Essence on a recorded triple owner: targets, effects, trigger conditions, chances and cooldowns are evidence, not measured uptime. A separate verifier must recount saved carriers, slot gates, category frontiers and feature evidence without importing the analysis reader.

Freeze scripts, protocol, input hashes and checkout hashes before execution. Run exactly nine small Python fixtures once: empty capacity; full capacity; case-insensitive family collision; already-covered category; exhausted shared copies; simultaneous blockers; active control classification with default chance; passive trigger exclusions/zero chance; and valid/malformed multi-step trace reconstruction without input mutation. Then one analysis and one independent verification. No backend changes are planned; preserve the preceding 103 backend tests through `build/run-tests.ps1` and four metric fixtures without rerunning combat-bearing tests.

## Limits and publication

Zero fights, fresh values, preparations, replays, recipe construction, scoring, RNG calls and retries. Each phase is capped at 45 seconds, publication at 60 seconds, all new diagnostics at 120 seconds and new output at 32 MiB. Carry 1,181.991 seconds and 1,592,999,474 bytes from the sealed trajectory package under the unchanged cumulative 1,800-second / 4-GiB caps. Charge one conservative second for the preceding schema-only inspection error (a local Python variable shadowed a path; no diagnostic results were produced). Retain that error; no diagnostic retry is authorized. Stop dependent checks on failure or a cap and preserve evidence. Verify all 34 predecessor packages before/after; preserve unrelated dirty checkout files.

Publish actual findings, remaining uncertainty, measured commands and six active Markdown handoffs. No search policy implementation or default promotion in this audit. Preserve 482,596 reservations, v19's unused 512 and all 253 recipes, fixed ability order and unchanged gameplay. Historical reliability Fail 1/3, deep recovery 0/3, v19 Unresolved, adoption Hold. No Kharad tuning, deployment or 129,536-fight confirmation.
