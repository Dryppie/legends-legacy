# Affinity-creation frozen-pool recognition builder

24 September 2026. Target: the offline `LL/tools/BalanceHarness`.

**The versioned builder is implemented, tested and used to freeze a concrete diagnostic plan.** The [plan](Tower-Affinity-Creation-Recognition-Plan.json) includes 108 team/root cells from the completed benchmark-validation pilot, with a separately recorded probability sample of lower-ranked candidates. Deterministic verification reproduces the complete plan from authenticated source evidence and sampling bytes.

This completes the builder step. **Next: add the separately versioned native execution and independent-audit profile for this exact plan.** The existing recognition profile pins the earlier adaptive-cohort plan and must continue rejecting this one. The new plan records `FrozenDesignNativeIntegrationRequired` and `runnableRequest: false`. No combat, combat entropy, reservations, native preparation, policy promotion or gameplay change occurred. One 128-byte population-sampling draw was made; those bytes are not combat values.

## Question and frozen membership

The [saved-stage diagnosis](Tower-Benchmark-Validation-Stage-Review.md) could not determine whether the affinity-creation pool was weak or whether racing discarded useful recipes. Seven novel validation challengers produced no gate pass, while only three of 204 generated candidate/root occurrences had same-root held-out observations. The completed pilot remains **Inconclusive**; this diagnostic neither changes its decision nor satisfies its unmet criterion for a larger comparison.

The new [builder](analysis/affinity-creation-recognition-plan.py) authenticates the closed v5 scientific archive, its publication verification and the sealed stage review. It reads the candidate arm, whose generation and first four racing panels were verified identical to control. Every accepted recipe enters the catalogue; membership uses the frozen nomination and beam lists. Selected outputs, validation decisions and held-out win counts do not choose catalogue membership or sampling probabilities.

| Stratum | Catalogue per root | Future measurements per root | Across twelve roots |
| --- | ---: | ---: | ---: |
| Retained references | 3 | 3 | 36 |
| Final novel nominees | 2 | 2 | 24 |
| Other final-beam candidates, or near misses | 2 | 2 | 24 |
| Remaining generated candidates | 13 | 2 sampled | 24 |
| Total | **20** | **9** | **108** |

The complete catalogue contains **240 team/root cells**, comprising 36 reference occurrences and 204 generated occurrences. Generated occurrences cover 57 distinct recipes across roots. Repeated identities in different roots remain separate cells with separate future panels; no global recipe deduplication is performed. The 132 unsampled lower-stratum cells retain explicit `independentOutcome: null`. No newly planned cell has a diagnostic outcome yet.

The three references retain their original order and roles: primary `399bc776…`, other reference `8287f779…`, and fixed benchmark `96b94357…`. Nominees retain frozen nominee order, near misses retain final beam order, and the thirteen lower candidates are sorted by full party identity. Every prior frozen v5 challenger is included through the reference or nominee stratum, regardless of whether it passed validation.

Every scenario preserves actor identity, equipment, progression, quality, fixed time, assumptions and owner order from its producing root. Only composition is substituted and the old seed list removed. Validation checks ten ordered owners, five canonical Essences per owner, allowed-pool membership, case-insensitive family uniqueness, party identity and scenario hashes. The captured cohort has no owned-copy limit; an altered inventory scope is rejected.

## One recorded probability sample

The [catalogue package](../TestResults/affinity-creation-recognition-catalogue-20260924/catalogue.json) was published before the sampling draw. The sampler then durably wrote its intent and source snapshot before requesting **one 128-byte OS CSPRNG batch**. All bytes, accepted and rejected offsets, unused tail, full party identities and package bindings are retained in the [sampling receipt](../TestResults/affinity-creation-recognition-sampling-20260924/choices.json).

The preserved protocol has `C(13,2) = 78` unordered pairs per root. It rejects bytes 234–255 and maps the next accepted byte to `byte % 78`, indexing lexicographically ordered pairs. Each pair has three representations in 0–233. Under the uniform-byte assumption, every pair has probability **1/78** and each candidate has inclusion probability **2/13**. Batch completion does not favor particular pairs. A hash authenticates recorded bytes but cannot prove their entropy origin independently.

The actual draw consumed **14 bytes**, including **two rejected bytes**, and retained **114 unused bytes**. There was no refill or retry. Sampling entropy must never be recycled for combat or entered as if it were a combat reservation.

| Root | First sampled lower candidate | Second sampled lower candidate | Pair index |
| --- | --- | --- | ---: |
| 1 | `a4270a2d0c70` | `d6562c29ebae` | 59 |
| 2 | `6dfd76361616` | `e6aa7fe8d732` | 45 |
| 3 | `91465eec5cb7` | `fc95321de33f` | 41 |
| 4 | `1ffcb4a47284` | `482ae6d57e51` | 2 |
| 5 | `869592655f8d` | `ce35baa2316b` | 55 |
| 6 | `1ffcb4a47284` | `d6562c29ebae` | 9 |
| 7 | `bcb64e9658e0` | `d2a7df84002b` | 69 |
| 8 | `869592655f8d` | `f7932940f285` | 67 |
| 9 | `f7932940f285` | `fc0f301f7e61` | 77 |
| 10 | `482ae6d57e51` | `f45d1d60b687` | 30 |
| 11 | `3e9a59e0722d` | `42593b0275fe` | 33 |
| 12 | `4f25db598277` | `f65f24d378b5` | 31 |

Existing output directories are refused. An exhausted or failed draw retains its intent and available bytes and cannot produce a partial plan. Literal engineering entropy is marked and rejected by the real plan-publication path. Verification rejects resealed choice changes, changed sampler code, failed receipts and mismatched catalogue producers.

## Prospective measurements and interpretation

Each root's nine teams will share **256 fresh combat values**. Panels must be disjoint across roots and excluded from complete live history. The frozen design requires **12 ×256 = 3,072 fresh combat values** and **12 ×9 ×256 = 27,648 attempted fights**. These are prospective counts, not executed work, resource qualification or a power claim. There are no new search or selection fights in this design.

Reporting includes all **108 recipe rates** and **216 candidate/reference paired contrasts**, retaining every root and stratum. The specified approximate Wilson family is **540**: 108 recipe rates plus 432 gain/loss rates. Its native and independent-audit implementation must be verified for the new profile before execution. The strongest fixed reference remains the benchmark; the diagnostic must not substitute whichever reference happens to win a panel.

Each sampled lower candidate receives weight **13/2** when estimating its finite stratum total. Its stratum mean is the mean of the two sampled gains. For each root, the prespecified seventeen-candidate mean estimator is:

```text
(sum of four measured nominee/near-miss gains
 + (13/2) × sum of two sampled lower-candidate gains) / 17
```

An unweighted average of the six measured novel candidates would overrepresent nominees and near misses. Exhaustive tests over all 78 pairs verify the weighted identity for arbitrary literal finite populations. Sampling uncertainty and combat uncertainty remain separate; individual combat intervals are not population-mean intervals. With only two sampled lower candidates per root, population estimates may be imprecise. Unsampled outcomes remain null.

This study is **development diagnosis only**, with `CompleteDiagnosticOnly` as its reporting decision. It cannot qualify teams, promote a policy, establish reliability on future roots, or retrospectively score a replacement selector. The v5 nomination and validation policy is retained as source provenance and is not retuned here. Any later policy comparison or team confirmation requires separately frozen fresh evidence.

## Versioning and remaining native work

The plan version is **`tower-affinity-creation-recognition-plan-v1`**. Its source contract fixes `tower-benchmark-validation-comparison-v1`, racing v5, the affinity-creation generator policy and `tower-racing-benchmark-validation-v1`. The captured source execution hash is `6aab62f71d865cdd96c4e28b50426de4a374c51cfd3be22d58d03ebe3c0c49cd`; runtime/content compatibility must be checked during a new admission.

The legacy builder and its earlier plan are unchanged. Native recognition currently pins the old plan hash `f8e8b206cd6b0cf46f420ab4d6d4d4a9568e5e46186ae8a8ae18a552fa0957b8`. The new plan hash is `170065b593a49609e72142d766443c3a47f7a271b937cc88d52436de2b4792a2`. The existing command is not a runnable route for this new cohort.

The next implementation must add a separate native/audit profile for these exact root-specific families, preserve legacy rejection behavior, and verify reconstruction of all plan identities and reporting arithmetic. Before any execution, a fresh admission must freeze complete live-history bindings, captured-runtime compatibility, a cumulative time/storage envelope covering owned execution and both audits, and a single post-freeze combat entropy batch. Every exposed fresh combat value must be permanently reserved, with no replacement roots or retries. None of those execution steps was attempted by this builder.

## Verification and accounting

**40 tests passed**: 23 new [affinity-recognition builder tests](analysis/test-affinity-creation-recognition-plan.py) and 17 legacy recognition-builder regressions. Coverage includes new/legacy version separation, exact source identity, all 78 pairs, inclusion probabilities, weighted estimates, scenario legality, changed strata, outcome-independent membership, missing cells, entropy exhaustion, no redraw, literal-fixture rejection, resealed tampering and full package round-trip verification.

Commands used the bundled Python runtime with `-B -X utf8`:

```text
Balance Harness/analysis/test-affinity-creation-recognition-plan.py
Balance Harness/analysis/test-frozen-pool-recognition-plan.py
TestResults/affinity-creation-recognition-builder-20260924/publish.py
```

The bounded publisher calls `catalogue`, `sample`, `build` and `verify` in order. The [declaration](../TestResults/affinity-creation-recognition-builder-20260924/declaration.json) preceded publication and sampling, fixing **180 seconds /64 MiB**, charged in full on success or failure. Publication and verification completed once in **11.750 seconds**, retaining **13,049,017 bytes**, including the canonical plan copy. The [completion receipt](../TestResults/affinity-creation-recognition-builder-20260924/completion.json) records zero new fights and combat values. Development tests and documentation checks are separate engineering work.

Cumulative recorded charges are **30,405.312 seconds /25,525,204,135 bytes**; cumulative declared maxima are **67,680 seconds /43,486,543,872 bytes**. No future combat budget is admitted by those figures. The last complete live-history verification remains **737,749 excluded values across 254 files**, inherited from the preceding publication and not rescanned here.

External package pins:

- Catalogue manifest: `ccd05f08fda2c86ab014c12ca056d061053683d7b47c69cafee6902a08831814`.
- Sampling manifest: `4062d335ff704b6d523dff8c2d950e1ba185a57246ec4cf6635db0decf936b63`.
- Plan manifest: `4c7e048591ce426ea2301c2b1596ac8c61fcc2aced076b675be17027373cf7d8`.
- Aggregate builder publication manifest: `d55a67bb45bf649399ce066fb3b4be71c9354248cdd69528841ec1a908af0c83`.
- Producing stage-review manifest: `bde705235252a4afd04cad3ce5256060e5453422656e209da7ccb8d91ed452f9`.

Changed repository files are the new builder, tests, canonical plan, this report, exact-byte line-ending rules in `.gitattributes`, and current-status banners in nine harness/assessment/implementation/admission documents. The LF rules keep the plan and reproducing Python sources stable across checkouts. The [final verification record](../TestResults/affinity-creation-recognition-builder-verification-20260924/verification.json) checks arithmetic, sample reproduction, local links, preserved pins and historical document bodies. No required verification command was blocked. No C# source or admitted runtime changed, so no backend rebuild or backend test rerun was needed. There are **no migrations, application configuration changes, deployments or gameplay-default changes**.
