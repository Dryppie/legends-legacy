# Completion versus category allocation: frozen bounded comparison

15 September 2026. Target: offline BalanceHarness. Preparation is authorized. No fresh seeds or combat are authorized yet. Prepare, verify and seal the executable protocol before requesting **45 fresh values and at most 512 charged fights**. Previous seed exceptions are exhausted.

## Matched inputs and fixed selection

Baseline: `independent-group-completion-v1` / `group-completion-joint`. Candidate: `independent-group-allocation-v1` / `group-allocation-joint`. Use the verified allocation source and fixtures in one isolated executable against the same captured gameplay DLLs. Retain the deep-challenger captured guardian Health/Power +10% content, settings, UTC timestamp, equipment, attributes, neutral identities, floor 5, 80 eligible Essences, unrestricted owned copies, level 40/tier 1/rank 2 and five Essence slots per character. Ten characters are two five-player parties. Both policies use the new comparison identity and fixed ordinal Essence-ID ability execution order. No gameplay or Kharad changes.

Each policy receives 44 distinct evaluated teams, at most 704 proposals, FreshEvery 4, 11 initial fresh teams and one shared generation-root value. Baseline runs first. Retain the separate policy-specific random streams; this compares whole policies, not an isolated causal mechanism. No controls or reference results enter generation or screening. Allocation's adjacent category alternatives can reduce early group breadth and create duplicate proposals; do not refill or increase budgets to compensate.

| Phase | Teams | Shared fights per team | Maximum fights |
| --- | ---: | ---: | ---: |
| Completion discovery | 44 | 4 | 176 |
| Allocation discovery | 44 | 4 | 176 |
| Screen | Top 2 per policy | 8 | 32 |
| Confirmation | Top 1 per policy plus 2 controls | 32 | 128 |
| Total | | | **512** |

Freeze both top-two nominations using existing discovery fitness and identity ranking. Screen winner per policy: wins descending, discovery rank, ordinal identity. Freeze finalists before confirmation; never reselect. Merge identical full-context recipes only at screen/confirmation, retain all origins and never refill seats. Charge discovery overlaps separately. Incomplete discovery stops dependent stages.

Controls: `team-040e60d3dbc5c127321653c47ed3a9d3` and `team-49f6979895354870c89362d4abf214bb`. Do not pool historical combat observations. Preserve both policies' uniform route, owner/filler sweeps, complete group/completion/allocation traces, rejections and duplicate charges. Full catalogue coverage is not funded or required.

## Seeds, measures and hard limits

Proposed allocation: **1 generation + 4 discovery + 8 screen + 32 confirmation = 45 fresh values**. Namespace `tower-group-allocation-comparison-v1`; master label **2026091509**. Preserve all **482,596** reservations; approved binding would retain **482,641**. Recheck exact registry membership and hashes before binding/execution. Durably charge allocation candidates before deriving values and retain pending reservations on failure. Existing reserved labels are used only for explicitly non-runnable preparation fixtures.

Primary comparison: allocation finalist minus completion finalist on 32 paired confirmation seeds; retain four finalist/control contrasts. Wilson rate family factor 8, paired gained/lost family factor 20, draws as non-wins, exact zero for identical merged recipes. Retain positive, zero and negative results with uncertainty. Boss health, duration and paired health differences are descriptive, preserving missingness. Record requested/placed/final group counts, category priorities, variant widths and full completion traces without altering selection. Save existing performance traces, stopwatch/CPU/allocation/peak-memory measurements and native archive verification. No reliability or speedup claim follows from this one shallow restart.

Limits: **512 charged fights including all repetitions; zero retries, resumes or combat replays; 1,800 seconds cumulative diagnostics and 4 GiB cumulative new output**. Carry **1,213.927 seconds** and **1,664,956,540 bytes** from allocation verification, including prior failures and closures. New preparation diagnostics, including setup/tests/preflight/audit/publication, at most **180 seconds**; each preparation diagnostic process at most 60 seconds. Builds are separate engineering work, at most 300 seconds each. Eventual native combat at most 900 seconds within the smaller remaining outer allowance; binding and both verification passes share that remaining cumulative allowance.

New output partitions: prior evidence below **2 GiB**, preparation plus execution evidence below **512 MiB**, this new study below **1.5 GiB**. The combined ceiling stays **4 GiB**. This reduces the new study's allowance from the previous pilot's 2 GiB; no sealed package, old cap or global cap is increased.

## Exact preparation and execution verification

Freeze protocol, adapted model/controller/eight tests, independent audit, metric fixtures, workflow and publisher before diagnostics. Copy verified source/fixtures and captured DLLs, asserting checkout hashes. Pin project inputs and binaries before diagnostics. Preserve all **37 predecessor packages** before/after, including both allocation packages and the earlier slot/trajectory audits; preserve unrelated dirty work.

Build the isolated controller and tests once, without gameplay rebuild or restore. Run exactly **118 backend cases through `build/run-tests.ps1`**: the 110 verified cases plus eight adapted comparison-controller cases, using synthetic evaluators and engine guards. Run four Python metric fixtures. One native preflight checks matched definitions/budgets, refreshes the reservation union, prepares two controls without combat and saves 66 Wilson interval fixtures. One independent audit checks those outputs and fixtures; validates the existing 1,956-position diversity schedule; independently reconstructs all 564 saved allocation-sweep positions; and verifies both sets of 19 saved completion/allocation construction traces. No fresh candidate construction or scoring outside the declared synthetic backend tests.

For eventual execution, independently reconstruct both policy schedules from the bound root and every completion/allocation insertion. Verify archived records, nominations, screen/finalist selection, paired contrasts, durable attempts and exact inventory hashes. Seal preparation and publish a measured readiness report before asking for the specific seed exception. Bind/run/native-verification/independent-audit are once-only operations gated by authorization tied to the protocol hash and preceding success. On failure or cap, retain evidence and stop dependent diagnostics; closure may document/preserve the stop without repeating diagnostics.

Preserve sealed v19, its 253 recipes and unused 512 confirmation values. No 129,536-fight confirmation, content tuning, deployment, configuration, migration or default promotion. Historical reliability Fail 1/3, deep recovery 0/3, sealed v19 Unresolved, adoption Hold. Preparing this comparison does not authorize seed allocation or combat.
