# Benchmark tie preference: runtime and resource admission

Current status (2026-09-25): Original affinity creation with benchmark validation is the supported baseline. The one nomination experiment completed 15,744 actual fights and all audits; both arms retained the benchmark on all 12 roots. This tuning cycle is closed. See [completed result and next work](<Tower-Affinity-Nomination-Pilot-01-Execution.md>).

23 September 2026. Target: the offline LL/tools/BalanceHarness.

**The selector comparison is admitted for one future execution.** The [sealed admission](../TestResults/benchmark-tie-admission-20260923/admission.json) is ProposalStudyAdmittedNoReservation. It completed in **195.188 seconds**, retaining **123,753,882 bytes**, with zero combat, production entropy draws, reservations or scientific launches. This qualifies an executable and resource plan; it provides no evidence that the selector improves search performance.

The external admission manifest pin is **80c8b6023642b75c7802cf0cc8b8f56d2becb4e14d1e09f847a43d5d073c189c**. The [declaration](Tower-Benchmark-Tie-Admission-Declaration.json), fixed before measurement, binds the new admission version, separate output, tested harness, source manifests, implementation files and forecast rule. Its full **900-second /1-GiB allowance** is charged. No admission retry or timing resample occurred.

## Qualified runtime and unchanged design

Only the four tested harness files replace files in the captured runtime: DLL, portable symbols, dependency manifest and runtime configuration. All gameplay dependencies and captured content retain their original hashes. The harness DLL remains f5583644f02239313621b20757a0debea5b8102712a8f8153ca457db8acf8ab8; the qualified execution hash is 2b7867879524a4a8f040b5862a8363cb0ce9f0f821e4fcd8fb9aaba2ef24d2f3.

- All 48 saved recipe/reference instances across four export arms were materialized and prepared with the same two previously declared values: **96 preparations per runtime, 192 total**. Input and prepared-participant hashes matched exactly, with combat guards active.
- The candidate reproduced all four saved export arms and the complete typed inventory. Its producing symbols authenticated **223 source documents**. **878 method bodies** resolved, including the new selector-plan and trajectory-equality entry points.
- The production retention routine preserved all **27 runtime files**, including symbols, within the existing 64-MiB retention limit.
- Two complete independent history scans agreed on **704,982 permanently excluded values across 250 files**. The native request check independently reconstructed the history and returned InputsVerifiedNoReservation. Existing abandoned-reservation recovery pins remain bound.
- The independent auditor passed again on the externally pinned, published selector fixture. Its literal reports remain engineering evidence.

The [qualified plan](../TestResults/benchmark-tie-admission-20260923/plan.json) changes only the scope hash from the [original prospective plan](Tower-Benchmark-Tie-Comparison-Plan.json). Its canonical hash is ebb760a4405f018d5d418077ada0e7c3fae96d4f7e1e01c6e32b99aa2dafc75b. Context changes are limited to the execution identity, episode ID and refreshed exclusions. Both arms retain the same affinity-creation generator; only the candidate's final benchmark preference differs. Roots, panels, decision thresholds and the global held-out barrier are unchanged. The original prospective plan remains byte-for-byte intact.

## Resource decision

The forecast takes the maximum of the preceding creation admission, the frozen native audit-cost probe, current preparation and selector-fixture costs, and twice the completed creation study costs scaled from 19,840 to the conservative 21,888-fight ceiling. Selector/creation fixture cost ratios can only increase estimates. Storage floors are applied before scaling the audit hashing estimate; the factor-of-two margin and 120-second publication reserve remain.

| Partition | Forecast seconds | Hard seconds | Forecast bytes | Hard bytes |
| --- | ---: | ---: | ---: | ---: |
| Native work | 4,239.253 | 9,000 | 4,860,555,295.20 | 5,905,580,032 |
| Audit/publication | 1,692.865 | 1,800 | 34,989,702 | 536,870,912 |
| Total | 5,932.118 | 10,800 | 4,895,544,997.20 | 6,442,450,944 |

Both partitions fit. The preceding forecast still determines the final estimates; the faster current measurements did not reduce them. Audit time retains approximately **107 seconds of forecast headroom**. These are estimates, not guaranteed bounds. Exhausting either partition invalidates an incomplete run, without borrowing time or retrying. The selector's tighter structural maximum of 18,816 observations was not used to relax admission.

The [forecast](../TestResults/benchmark-tie-admission-20260923/resource-forecast.json) retains the component floors and ratios; [current measured preparation estimates](../TestResults/benchmark-tie-admission-20260923/current-formula-forecast.json) are saved separately. The latter are intermediate calculations using the legacy sizing helper; the final forecast and request enforce the unchanged v2 partitions above.

Cumulative recorded charges are **24,994.359 seconds /20,269,562,774 bytes**, including previous failures and reviews. Cumulative declared maxima are **43,440 seconds /29,192,355,840 bytes**. The new scientific allowance remains unconsumed: **10,800 seconds /6,442,450,944 bytes**. Adding it to recorded charges gives **35,794.359 seconds /26,712,013,718 bytes**. Development tests and the read-only check after sealing remain separately identified engineering work.

## Verification and changed files

**41 Python tests passed:** 23 selector admission cases, 12 shared admission cases and six resource cases. They cover exact selector identity, unchanged policies and limits, native input parity, required method resolution, complete recipe coverage, manifest tampering, increasing costs, partition exhaustion and preserving prior charges. The native PowerShell helper also passed parsing.

    python -B -X utf8 'Balance Harness/analysis/test-benchmark-tie-admission.py'
    python -B -X utf8 'Balance Harness/analysis/test-proposal-affinity-admission.py'
    python -B -X utf8 'Balance Harness/analysis/test-proposal-affinity-resource-admission.py'

The existing **68 passing backend cases**, ten passing follow-up cases and exact producing source/binary bindings were authenticated. No C# source changed, so a backend rebuild was unnecessary. All six owned admission processes exited successfully with empty jobs.

The [admission log](../TestResults/benchmark-tie-admission-run-20260923.log), [independent verification after sealing](../TestResults/benchmark-tie-admission-verification-20260923/verification.json) and [verification script](../TestResults/benchmark-tie-admission-verification-20260923/verify.py) retain the checks. The verifier reauthenticated every admission-package byte and consumed historical input, recomputed the forecast from saved measurements, checked runtime/content/source parity and prior charges, and confirmed no scientific output exists. It did not rescan history or repeat timing measurements. No verification command was blocked.

New implementation files are [prepare-benchmark-tie-admission.py](analysis/prepare-benchmark-tie-admission.py), [benchmark-tie-admission-context.ps1](analysis/benchmark-tie-admission-context.ps1) and [test-benchmark-tie-admission.py](analysis/test-benchmark-tie-admission.py), plus the declaration and this report. Current-status links were updated in the harness guides and preceding assessments. Historical admission scripts, scientific archives and prior manifest pins remain unchanged. There are no migrations, application configuration changes, deployments or gameplay-default changes.

## Next execution

The [execution handoff](../TestResults/benchmark-tie-admission-verification-20260923/execution-handoff.json) identifies the exact qualified harness, request, admission pin and separate output. The next step is the single admitted twelve-root comparison through the owned launcher. It must revalidate the complete registry under its lease before drawing values, retain every exposed value and failed attempt, freeze all outputs before held-out evaluation, and pass both audits before publication. A stale admission or exceeded limit stops the run without refill or retry.

No study was launched by this admission. The earlier creation pilot remains Inconclusive; the selector hypothesis has no fresh scientific result yet.
