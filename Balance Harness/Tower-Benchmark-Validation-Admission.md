# Benchmark validation: runtime and resource admission

Current status (2026-09-25): Original affinity creation with benchmark validation is the supported baseline. The one nomination experiment completed 15,744 actual fights and all audits; both arms retained the benchmark on all 12 roots. This tuning cycle is closed. See [completed result and next work](<Tower-Affinity-Nomination-Pilot-01-Execution.md>).

24 September 2026. Target: the offline `LL/tools/BalanceHarness`.

**The v4-versus-v5 comparison is admitted for one future execution.** The [sealed receipt](../TestResults/benchmark-validation-admission-20260924/admission.json) is `ProposalStudyAdmittedNoReservation`. The admission completed in **158.094 seconds**, retaining **124,271,166 bytes**, with zero combat, new seed values, reservations or scientific launches. This qualifies the executable and budget; it does not establish that v5 improves search results.

The [declaration](Tower-Benchmark-Validation-Admission-Declaration.json) fixed a separate **900-second /1-GiB** allowance before timing. Its full allowance is charged on success or failure, with no retry or timing resample. The completed v4 pilot remains `AbandonThisConfiguration`; its output and all prior evidence pins are preserved.

## Qualified inputs and runtime

The [prospective comparison plan](Tower-Benchmark-Validation-Comparison-Plan.json) remains byte-for-byte unchanged. The [comparison implementation](Tower-Benchmark-Validation-Comparison-Implementation.md) fixes twelve paired roots, the same affinity-creation generator in both arms, 528 search fights per arm/root, the shared nomination values and isolated sixty-value validation panels. Admission changes only the plan's scope binding to the refreshed history and qualified runtime. The new allocation remains **4,668 fresh values**; the launcher will retain every exposed value from the single declared entropy batch if execution proceeds.

All 48 saved recipe instances were materialized and prepared with two declared historical values in each runtime: **192 native preparations** in total. Inputs and prepared-participant hashes agreed exactly. A combat callback prohibited battle execution throughout these preparations.

The final producing assembly is `24e20a6f46d17ed14f53f72d1ff9cccbc3db1782f38d8e12ca67b91ac1c76cb6`. Admission authenticated its matching portable symbols and **225 source documents**, resolved **905 methods**, replayed all four saved generation arms, checked the typed inventory and exercised the production runtime-retention path. All **27 runtime files** are pinned; captured gameplay dependencies and content are unchanged. Only the four harness build files replace their captured counterparts.

The authoritative full-history scan and native reconstruction agreed on **721,365 permanently excluded values across 252 files**. Pending-history recovery receipts remain pinned. The registry was rescanned before sealing, without modifying any historical ledger or creating a scientific output directory.

## Resource decision

The frozen forecast takes upward-only maxima of the preceding admission, the native audit-cost probe, current preparations and the v5 owned fixture, and twice the completed v4 pilot's phase costs scaled from 17,792 to the 21,888-fight ceiling. The v5/v4 fixture ratios can only increase costs. In particular, the **2.312 native-time ratio** raises the planning estimate; faster storage measurements do not lower prior floors. Storage floors are applied before audit hashing is scaled. The factor-of-two margin and 120-second publication reserve remain.

| Partition | Forecast seconds | Hard seconds | Forecast bytes | Hard bytes |
| --- | ---: | ---: | ---: | ---: |
| Native work | 6,693.288 | 9,000 | 4,860,555,295.20 | 5,905,580,032 |
| Audit/publication | 1,692.865 | 1,800 | 34,989,702 | 536,870,912 |
| Total | 8,386.152 | 10,800 | 4,895,544,997.20 | 6,442,450,944 |

Both partitions fit. Audit forecast headroom is **107.135 seconds**. These are empirical planning estimates, not guaranteed upper bounds. Either exhausted partition invalidates an incomplete execution without borrowing, refill or retry. The [saved forecast](../TestResults/benchmark-validation-admission-20260924/resource-forecast.json) includes the floors and ratios; [current preparation estimates](../TestResults/benchmark-validation-admission-20260924/current-formula-forecast.json) are retained separately.

Cumulative recorded charges are **28,091.671 seconds /23,423,155,716 bytes**, including this full admission allowance and all preceding failures and reviews. Cumulative declared maxima are **55,920 seconds /36,842,766,336 bytes**. The separate scientific allowance remains unconsumed: **10,800 seconds /6,442,450,944 bytes**. Adding that allowance to recorded charges gives **38,891.671 seconds /29,865,606,660 bytes**. Implementation tests and read-only verification after sealing are engineering work, separately identified from scientific charges.

## Verification and files

**41 Python tests passed**: 23 new admission cases, twelve shared admission cases and six resource cases. They cover changed build/source evidence, scientific identity and limits, native input parity, required validation methods, complete recipe coverage, tampered manifests, increasing costs and partition exhaustion. The PowerShell helper passed parsing. The previously passing **132 backend cases** and their exact producing sources, assembly and TRX were authenticated; no C# source changed and no rebuild or backend rerun was needed.

```powershell
python -B -X utf8 "Balance Harness/analysis/test-benchmark-validation-admission.py"
python -B -X utf8 "Balance Harness/analysis/test-proposal-affinity-admission.py"
python -B -X utf8 "Balance Harness/analysis/test-proposal-affinity-resource-admission.py"
```

All six owned admission processes exited successfully with empty jobs. The retained independent Python auditor reconstructed the complete v5 fixture. The [read-only verification](../TestResults/benchmark-validation-admission-verification-20260924/verification.json) reauthenticated the sealed package and consumed inputs, recomputed the forecast from retained measurements, checked runtime/source/content parity and cumulative accounting, and confirmed no scientific output exists. It did not repeat timing or history scanning. No verification command was blocked.

New implementation files are [prepare-benchmark-validation-admission.py](analysis/prepare-benchmark-validation-admission.py), [benchmark-validation-admission-context.ps1](analysis/benchmark-validation-admission-context.ps1) and [test-benchmark-validation-admission.py](analysis/test-benchmark-validation-admission.py), plus the declaration and this report. Only seven current-status banners changed in earlier documents. Historical report bodies, admission scripts, plans and scientific archives remain unchanged. There are no migrations, application configuration changes, deployments or gameplay-default changes.

## Next execution

The [execution handoff](../TestResults/benchmark-validation-admission-verification-20260924/execution-handoff.json) identifies the exact harness, request, external admission pin and fresh output. Next is the single admitted twelve-root comparison through the owned launcher. It must revalidate the complete registry under its lease before drawing values, preserve every exposed value and failed attempt, freeze all 24 outputs before held-out evaluation, and pass both audits before publication. Stale inputs or an exceeded limit stop execution without replacement or retry.

Admission manifest SHA-256: `e6fdb1369df6585fde905523e3190af221ef0dc9323e4b3ae3b1bf37659b5576`.

No scientific study was launched by this admission. There is no fresh efficacy result for v5.
