# Group-completion comparison: frozen bounded pilot

15 September 2026. Target: offline BalanceHarness. Preparation is authorized; fresh seeds and combat are not yet authorized. Finish preparation and seal the package before requesting **45 fresh values and at most 512 fights**. Earlier exceptions are exhausted.

## Identical inputs and allocation

Baseline: `independent-group-diversity-v1` / `group-diversity-joint`. Candidate: `independent-group-completion-v1` / `group-completion-joint`. Use the corrected completion-verification source in one isolated executable against the same captured gameplay DLLs. Preserve the deep-challenger captured guardian Health/Power +10% content, settings, UTC timestamp, equipment, attributes, neutral identities, floor 5, all 80 eligible Essences, unrestricted owned copies, level 40/tier 1/rank 2 and five Essence slots per character. Ten characters represent two five-player parties. Both policies share the new comparison scenario identity and fixed ordinal Essence-ID ability order. No live gameplay or Kharad edits.

Each policy receives 44 distinct evaluated teams, at most 704 proposals, FreshEvery 4, 11 initial fresh teams and one shared generation-root value. Baseline executes first. Keep the implemented policy-specific random streams. This is one shallow restart comparing whole policies; it cannot isolate a causal mechanism or establish general strength. No reference recipe or result enters generation or screening. Do not adjust choices from preparation or combat outcomes.

| Phase | Teams | Shared fights per team | Maximum fights |
| --- | ---: | ---: | ---: |
| Diversity discovery | 44 | 4 | 176 |
| Completion discovery | 44 | 4 | 176 |
| Screen | Top 2 per policy, up to 4 recipes | 8 | 32 |
| Confirmation | Top 1 per policy plus 2 controls, up to 4 recipes | 32 | 128 |
| Total | | | **512** |

Freeze both top-two nominations by existing discovery fitness and ordinal identity rules before screening. Select each policy's screen winner by wins descending, discovery rank, then ordinal identity. Freeze finalists before confirmation; never reselect. Deduplicate identical full-context recipes at screen/confirmation only, retain every origin, never refill seats. Discovery overlaps are charged separately. Incomplete discovery stops all dependent stages.

Fixed confirmation controls: `team-040e60d3dbc5c127321653c47ed3a9d3` and `team-49f6979895354870c89362d4abf214bb`. No old combat evidence is pooled. Both policies retain the diversity group order, five/one/ten-owner and alternate-filler sweeps, and every eighth fresh request as uniform. Completion additionally fills missing authored coverage on group owners before the existing filler. Record all traces, including rejected and duplicate requests. Actual exploration depends on the frozen root and proposal outcomes; full catalogue coverage is neither required nor funded. Coverage presence does not establish targeting, uptime or combat strength.

## Seeds, measurements and limits

Proposed allocation: **1 generation + 4 discovery + 8 screen + 32 confirmation = 45 values**. Namespace `tower-group-completion-comparison-v1`, master label **2026091508**. Preserve **482,551** reservations; approved successful binding would retain **482,596**. Refresh exact registry membership and hashes before binding/execution; reject drift. Durably journal allocation attempts and preserve pending reservations on failure. Preflight uses already-reserved labels only in explicitly non-runnable fixtures.

Primary contrast: completion finalist minus diversity finalist on 32 paired confirmation seeds. Four secondary finalist/control contrasts remain. Wilson rate family factor 8, paired gained/lost family factor 20, draws as non-wins, exact zero for identical merged recipes. Save positive, zero and negative results with uncertainty. Boss health, duration and paired health differences remain descriptive, separating missingness from measured zero. Save catalogue group owner counts, requested/placed/final counts, filler metadata and complete completion traces. None alters selection. Persist existing performance traces, stopwatch, CPU, allocations, peak memory and native archive verification; this is not a controlled speed benchmark.

Hard limits: **512 charged fight attempts including all repetitions/replays; zero retries/resumes/replays; 1,800 seconds cumulative diagnostics; 4 GiB cumulative new output**. Carry forward **863.931 seconds** and **1,286,901,467 bytes** from completion verification and preceding output, including both fixture/setup failures and corrections. New preparation diagnostics including publication are capped at 300 seconds. Native combat is capped at 900 seconds within the remaining outer allowance. Compilation is separate engineering work, each command capped at 300 seconds. New partition: prior output below **1.5 GiB**, preparation plus execution evidence below **512 MiB**, study below **2 GiB**; enforce the combined 4 GiB ceiling. No old package cap changes.

## Exact preparation and execution checks

Before diagnostics freeze this protocol, adapted controller/model/tests, independent audit, metrics, workflow and publisher. Copy the corrected verified source/fixtures and captured DLLs; assert owned checkout source and fixtures match. Freeze projects and binaries before their diagnostics. Preserve all **30 preceding sealed packages** before/after, including failed and corrected completion verification, and capture unrelated dirty file hashes.

Build the isolated driver and tests once each without gameplay rebuild or restore. Run exactly **103 backend cases through `build/run-tests.ps1`**: the 95 verified composition/joined/group-count/variation/diversity/completion cases plus eight adapted comparison-controller cases. Engine guards and synthetic evaluators only. Run exactly four Python metric fixtures for missing/zero telemetry, invalid numbers, absent group counts and paired seed alignment. One native preflight validates equal inputs/budgets, refreshes the reservation union, prepares two controls without combat, and saves 66 Wilson interval fixtures. One independent audit checks those outputs and passing fixtures; checks the diversity schedule against all 1,956 archived positions; and reconstructs completion traces for all 19 saved completion-verification requests from frozen metadata. No new candidate scoring or combat beyond synthetic tests.

During eventual execution, independently reconstruct the seeded diversity schedule for both policies and every completion insertion from recorded metadata. Verify archive records, selection, paired contrasts and durable attempts. Publish a measured readiness review and update six active handoffs; seal preparation before requesting the specific fresh-seed exception. Bind/run/native verification/independent audit are once-only commands gated by protocol-hash authorization and prior success. On failure or cap preserve evidence and stop dependent diagnostics; closure may preserve/document the stop without rerunning diagnostics. Generic preparation approval does not allocate seeds or launch combat.

Preserve sealed v19, all 253 recipes and its 512 unused confirmation values. No 129,536-fight confirmation, Kharad/content tuning, deployment or default promotion. Historical reliability Fail 1/3, deep recovery 0/3, sealed v19 Unresolved and adoption Hold remain unchanged. Comparative combat strength remains unmeasured.
