# Refinement comparison: storage and time readiness

16 September 2026. Target: offline BalanceHarness. The next comparison is team coverage versus discovery refinement on identical captured gameplay inputs, fixed ordinal ability order and the existing two controls. Its existing contract uses 45 fresh values and at most 288 fights; neither is authorized here.

Before preparing a production request, establish whether a completed archive can fit the remaining cumulative output allowance. The [last sealed receipt](../TestResults/balance/tower-refinement-live-registry-20260916/completion.json) records 3,130.101469456 / 3,240 diagnostic seconds, leaving 109.898530544 seconds. This once-only, zero-combat readiness check is capped at **10 diagnostic seconds and 2 MiB of new output**, inside the unchanged cumulative **4 GiB** cap. Editing is excluded; verification and publication are charged. Reserve two of the ten seconds for publication/cleanup. No build, backend test rerun, real registry traversal, runtime preparation, seed derivation, allocation, fight, replay or retry.

Package: `TestResults/balance/tower-refinement-resource-readiness-20260916`. Freeze this protocol, the script, consumed source hashes and the last completed native check before execution. Reuse the unchanged sealed launcher binary/source and its earlier passing backend tests through `build/run-tests.ps1`; this check does not alter implementation.

Run one read-only calculation with these fixed steps:

1. Verify the immediate predecessor's inventory, the producing harness/source and the consumed ledger, content and dependency identities. Do not repeat the old full historical audit. Carry forward the predecessor's recorded prior bytes, its complete sealed package bytes and any charged temporary bytes.
2. Read the authoritative 482,821-value reservation union. Match its canonical sorted-array hash to the completed native preflight. Independently count decimal digits and the minimum pretty-printed array bytes. All counted arrays are object-property arrays written with `HarnessJson.Options.WriteIndented = true`: each value has at least four spaces and one line break, with a comma between adjacent values. Exclude property names, brackets, closing indentation and every other byte. Check the arithmetic using an independently serialized property-array fixture containing the same saved integers; keep that fixture in memory only.
3. Count fourteen unavoidable copies of that historical integer sequence in a completed current-format study: one in `seeds.json`, one in `seed-ledger.json`, two in binding `definitions.json`, two in `run/definitions.json`, and one each in the definition and campaign contract of the two discovery, one selection and one confirmation stages. Deduplication of nominees does not remove any of these four stages. New schedule values, scenarios, reports, journals, manifests, temporary writes and all other metadata are omitted, making this a conservative lower bound.
4. Reconstruct the executable file list using the captured `TowerBossStudy.RetainExecutable` rules: three harness entry files, flattened runtime assets from the dependency manifest, present localized resources and all runtime-directory assets. Verify their hashes and lengths. Count four copies, one per completed stage. Count four copies of the pinned content data, excluding settings and unrelated files. Do not create any of these copies and do not modify archive contracts or weaken verification.
5. Compare the sum with the maximum remaining bytes **before this new package**, which deliberately overstates usable space. If even that conservative floor does not fit, record `OutputEnvelopeInsufficient` and stop production-request preparation before any fresh-value/combat authorization. If it fits, record only `MinimumFitsNotAnUpperBound`; fitting a lower bound is not proof that a full run fits.
6. Record the time limitation separately: binding refresh, binding recheck and prelaunch refresh require three full traversals in the current launcher. The last 56.8624-second end-to-end Check is a planning reference, not a lower bound or a guarantee for those different operations. The remaining 109.899 seconds is not a proven full-run allowance. No automatic cap increase.
7. Publish measured time, exact byte arithmetic, input/source identities, limitations and the status. Update the six active Markdown handoffs; check links, whitespace and preservation of unrelated dirty files. Seal success or first failure, with no repeat. On failure retain partial evidence and do not allocate or execute.

```powershell
$python = 'C:/Users/HrHoe/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe'
& $python -B 'TestResults/balance/tower-refinement-resource-readiness-20260916/check.py'
```

This check may establish that storage changes are needed before a runnable request can be prepared. It does not authorize deleting retained evidence, increasing the 4 GiB cap, changing gameplay, adopting the search policy or allocating fresh values. Preserve all 482,821 reservations, v19's unused 512 confirmation values and all 253 v19 recipes. V19 remains Unresolved; later reliability Fail 1/3, deep recovery 0/3 and adoption Hold remain. No migration, configuration or deployment changes.
