# Refinement novelty corrected probe: bounded zero-combat protocol

16 September 2026. Target: offline `LL/tools/BalanceHarness`. Preserve the sealed v1 comparison, the v2 role-validity package and all 482,866 reservations. No fight, preparation, combat replay, fresh seed, retry, ability-order tuning, gameplay change, deployment or old-cap increase is authorized here.

The current synthetic v2 search has 15 evaluated teams and one duplicate in 16 proposals. Diagnose that duplicate, implement an explicitly versioned construction change and verify it without combat. Keep both v1 and v2 deterministic behavior intact. Do not bypass duplicate charging, validity checks, cancellation or the fixed 16-proposal bound. Novelty may constrain a bounded construction search; it must not trigger an unrecorded random retry or combat evaluation.

## Frozen diagnostics

Evidence directory: `TestResults/balance/tower-refinement-novelty-v2-20260916`. Maximum **90 additional diagnostic seconds / 9 MiB new output**, including compiler intermediates, tests, diagnosis, audits and publication. Previous cumulative usage: 3,329.1304694556065 seconds, from the sealed failed-probe completion. Preserve the existing cumulative 3,600-second / 4-GiB ceilings. Reuse pinned gameplay/runtime dependencies and suppress redundant compiler reference/output copies. Code inspection and implementation are preparation; every executed diagnostic/build/test/publication phase is charged.

1. **Diagnosis, once, 15 seconds:** freeze the probe source, compiler inputs, runtime assets and captured discovery input/mechanics before execution. Compile a thin reader against the sealed v2 harness. Perform one synthetic v2 search with the same generation input/seed and hash-derived synthetic scores as the previous diagnostic. Require result hash `885d726a9264832ff4711668c97c2bfe6fd25398538b28ebdd3bb9f8d45e6934`; persist the full proposal trace and identify the duplicate and its previous identity. No combat engine entry is permitted.
2. Implement a generic bounded fix for the identified operator as opt-in `independent-discovery-refinement-novel-v3`. Before any candidate execution, freeze the exact candidate sources, diagnostic host and **28 test cases** (the existing 12 refinement tests, 8 role tests and 8 new novelty tests) in a candidate manifest. Keep the comparison launcher on v1 in this scope.
3. One harness build (20 seconds), one isolated test build (12 seconds), and one backend test invocation (20 seconds) through `build/run-tests.ps1`. The new tests cover novelty selection, deterministic construction, unchanged original policies, validity/inventory constraints, finite exhaustion, proposal charging, cancellation and provenance/order restrictions. No test reruns.
4. **Captured verification, once, 8 seconds:** one v1 reconstruction using its 14 saved measurements must match the saved generation hash; one v2 synthetic search must match the hash above; two v3 synthetic searches, one with reversed metadata, must match each other. Persist every proposal/result and the actual distinct evaluation counts. Require ordinary validity and completed same-arm ancestry, no order mutation, and at most 16 proposals. A result below 16 valid distinct teams remains an explicit limitation, not permission for further runs.
5. Audit pins, unchanged gameplay assets, previous sealed evidence, scoped whitespace and unrelated dirty-file preservation (5 seconds). Publish the measured result and update six active Markdown handoffs (5 seconds), even if a prior phase failed. Reserve the remaining 5 seconds for freeze/accounting overhead.

On the first failure or cap, preserve evidence and skip dependent phases. No retries, refill runs, seed allocation or combat follow-up. Do not modify any sealed evidence. V19's 253 recipes and 512 unused confirmation values, and the failed comparison's separate 40 unused values, remain preserved. Reliability conclusions and adoption Hold remain unchanged.

The previous probe failed at compilation before any search. This separately frozen scope uses the documented friend-assembly identity `EssenceSystem.Tests` and matching launch filename. The failed package remains sealed; all prior costs remain charged.
