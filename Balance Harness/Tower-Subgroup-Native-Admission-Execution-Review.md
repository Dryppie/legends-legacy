# Four-party native preparation admission

**This admission attempt closed at its first failed prerequisite or check.** No party is admitted by this attempt and no stronger team is established.

The retained [failure record](../TestResults/balance/tower-subgroup-native-admission-20260916/failure.json) identifies phase `input`. The input log shows an inventory assertion in `prepare.py:28`. This was an implementation error in this attempt: the check compared top-level directory entries with a manifest of relative file paths that includes a nested DLL. The [read-only diagnosis](../TestResults/balance/tower-subgroup-native-admission-20260916/inventory-diagnosis.json) compares relative file paths and hashes and confirms all 26 files match, with zero missing, extra or changed files. It does not reopen native admission. The [prepared correction](../TestResults/balance/tower-subgroup-native-admission-20260916/inventory-correction.patch) is retained without modifying or rerunning the failed script. Compilation, native preparation and the native-record audit were not run.

## Implementation and trust boundary

The new isolated [helper](../TestResults/balance/tower-subgroup-native-admission-20260916/Admission.cs) is written to reference the retained historical runtime through [Resolver.cs](../TestResults/balance/tower-subgroup-native-admission-20260916/Resolver.cs). It has not been compiled. The [input stage](../TestResults/balance/tower-subgroup-native-admission-20260916/prepare.py) is intended to check the executable inventory, historical source/build binding, 16 captured content hashes, exact settings and full four-party recipes. The later stages would pin installed .NET compiler/reference/runtime files and use a direct compiler invocation without restore, dependency installation or build servers. Those later checks were not reached in this attempt.

The helper is written to call `TowerBattleRunner.CreateInput` and `PrepareAsync`. The reviewed preparation chain rehydrates snapshots, resolves gear and Essences, applies guardian scaling and creates encounter metadata. It does not invoke a simulation executor. The helper would activate the historical `TowerPerformanceTrace` callback to reject battle starts and record preparation activity. No callback or native preparation ran here. The proposed single previously reserved preparation value was not selected because the input stage stopped earlier.

Native typed-input and prepared-actor hashes are separate from the adapter's canonical party fingerprints. The independent audit compares records and computes its own file hashes; it does not claim an independent reimplementation of every native serialization rule. Prepared Essence order and native ability IDs are retained, but engine ability compilation is not performed. No outcome archive is authenticated here. The captured full-pool legality assumption remains `OwnedCopies=null`; real account inventory is outside this admission.

## Result and remaining work

The [decision](../TestResults/balance/tower-subgroup-native-admission-20260916/decision.json) reports all four parties explicitly. The [input log](../TestResults/balance/tower-subgroup-native-admission-20260916/input.log) and process failure receipt show the first stopped phase. This attempt stopped before native command publication, complete source/content admission, preparation-value selection, or the helper build. The correction can be applied in a new isolated attempt using the remaining 60 engineering seconds and 6,603,512 bytes; the closed attempt must remain unchanged. Proposed ceilings for that recovery are input 15 seconds /1 MiB, compilation 15 seconds /2 MiB, preparation 15 seconds /1 MiB, independent audit 10 seconds /1 MiB, and publication 5 seconds /1 MiB, with cleanup reserves included and no further transfer. A subsequent explicit proceed can authorize that single recovery attempt. It permits one helper compilation and four preparations, with no fresh seeds or combat and no automatic retry after failure.

Successful preparation would still leave independent-panel authorization and exclusion reconciliation, a frozen native 1,024-fight run envelope, and completed native archive/attempt-ledger authentication outstanding. No fresh panel or combat is authorized by a preparation pass.

The four frozen recipes, simultaneous statistical gates, 256 shared independent outcomes per party, zero-retry limit, and stopping rule remain unchanged. All 483,732 exclusions and V19's 253 required recipes/512 unused confirmation values remain preserved. V19 remains Unresolved and adoption remains Hold. Closed experiments remain closed.

## Verification, preservation and resources

Only the new isolated helper/workflow/evidence package, this review, and a current notice in the original search review (when its before-image was captured) are changed. Existing runtime, source, content, sealed packages and unrelated dirty files are preserved. Publication checks verify available input pins, dirty baseline hashes, document preservation and resource arithmetic.

The approved once-only transfer moves 90 seconds /8 MiB from run to engineering without changing overall caps. Because admission stopped in the input phase, the [completion receipt](../TestResults/balance/tower-subgroup-native-admission-20260916/completion.json) charges the full input and publication ceilings: **30 seconds /2 MiB**, including inspection/setup, the failed check, read-only diagnosis, prepared correction and preservation. The unexecuted compilation, preparation and audit allocations remain unused. This leaves **60 engineering seconds /6,603,512 bytes** and **1,717.222728 overall diagnostic seconds /89,525,518 output bytes**. No prior charge is reversed; run/audit carried consumption is unchanged. Each executed phase uses an owned Windows job with cleanup and storage reserves.

No production build, backend test suite, dependency installation, gameplay-content change, migration, persistent-configuration change or deployment occurred. Backend tests through `build/run-tests.ps1` were not invoked: this scope uses an isolated admission executable and an independent Python record audit. No new seeds, combat, replay or benchmark ran.
