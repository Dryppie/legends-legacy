# Five-candidate, three-reference confirmation: failed admission and repair

**Verification and accounting follow-up:** The [complete verifier improvement and resource amendment](Tower-Practical-Three-Reference-Confirmation-Resource-Amendment.md) are ready for explicit v2 implementation. Complete input authentication measured 55.94 to 18.41 seconds in one comparison, and 30 relevant tests pass. The proposed cumulative total is 8,400 seconds /4.5 GiB, retaining this failed charge and one proposed new admission. This failed package has not been retried.

23 September 2026. Target: offline `LL/tools/BalanceHarness` admission tooling. **The single captured-runtime admission failed before native preparation.** The helper could not resolve `Microsoft.Extensions.Configuration, Version=10.0.0.0` when constructing `OfflineContent`. No scientific request, output, reservation or combat was created. The failed package is preserved; it has not been retried or resumed.

The [frozen plan](Tower-Practical-Three-Reference-Confirmation-Plan.md) and [tested implementation](Tower-Practical-Three-Reference-Confirmation-Implementation.md) remain unchanged. The JSON protocol pin remains `c6544a63a569de5197c4e2a35749c01a14388478e6a9986509d991c64bd6a74f`. Five challengers, three retained references, 6,500 common values per team, 52,000 fights and every-reference qualification remain prospective. No recommendation, primary, search default or balance conclusion changed.

## What happened

The [failed package](../TestResults/three-reference-confirmation-admission-20260923) contains the original helper, charge receipt, copied runtime/content, authenticated source snapshots and both process receipts. Its [worker log](../TestResults/three-reference-confirmation-admission-20260923/worker.log) records successful authentication of all three input archives: the retained capture, the tested implementation and the closed selector comparison.

The copied runtime substituted only the four tested harness artifacts. Gameplay dependencies and content stayed bound to the retained capture. The helper authenticated **205 producing PDB source documents** before reaching the loader failure. The [context log](../TestResults/three-reference-confirmation-admission-20260923/context.log) records the actual exception. The public `tower-three-reference-confirmation-check` command was never reached; the eight native preparations and full admission history refresh did not run.

The owner finished after **499.765 seconds**, below its 600-second ceiling, retaining **55,649,229 bytes**. The context child failed after **3.016 seconds**. Both Windows Job receipts report exit code 1, no timeout and **zero active processes** after cleanup. This was a framework-resolution failure, not a timeout or a failed combat result.

The [separate read-only closeout](../TestResults/three-reference-confirmation-admission-tooling-20260923/verification.json) took **113.546 seconds**. It rechecked the copied provenance, runtime/content, PDB sources and literal fixtures, verified that the failed package stayed unchanged, and independently refreshed the complete live history: **633,313 excluded values across 240 ledgers, unchanged**. That later engineering check did not complete the failed admission or prepare native parties.

The external failed-package inventory SHA-256 is `b238a72eb240fef91cf18a6ed5876cee0ad70880f623142059724c979595c5be`. The tooling/closeout manifest SHA-256 is `4bf14189a9d728e5aa2db10944ea60757b295bd80153477d88368087e73cc292`. [Inventory](../TestResults/three-reference-confirmation-admission-tooling-20260923/failed-admission-files.json), [tooling manifest](../TestResults/three-reference-confirmation-admission-tooling-20260923/files.json) and [history inventory](../TestResults/three-reference-confirmation-admission-tooling-20260923/live-history-files.json) are retained outside the failed package.

## Repair and verification

The harness runtime configuration requests both `Microsoft.NETCore.App` and `Microsoft.AspNetCore.App`. Launching `pwsh` directly uses PowerShell's runtime configuration, which omitted the ASP.NET shared framework required by `OfflineContent`'s empty `ConfigurationBuilder`.

The [working preparation helper](analysis/prepare-three-reference-confirmation-admission.py) now starts the managed PowerShell host through `dotnet exec`, using the harness's runtime configuration and PowerShell's dependency file. It discovers the installed host without introducing an installation-specific source path. The original helper inside the failed package remains unchanged.

**12 Python contract tests pass.** They cover the exact native fixture family, altered roles/order/scenarios/seeds, invalid history, all four tested harness files and retained dependencies, prior-charge bindings, process cleanup, terminal receipt bytes, no-resume guards and the corrected host command. The [test source](analysis/test-three-reference-confirmation-admission.py) and [final log](../TestResults/three-reference-confirmation-admission-tooling-20260923/python-tests-framework-fix.log) are retained. The [PowerShell context helper](analysis/three-reference-confirmation-context.ps1) passes the parser check.

A [pure framework probe](../TestResults/three-reference-confirmation-admission-tooling-20260923/framework-probe-exact-host.log) using the corrected host resolved the exact missing assembly under .NET 10.0.12 and constructed an empty configuration with zero providers. It loaded **zero gameplay assemblies** and performed **zero native preparations, allocations or fights**. This verifies the host repair; it does not establish captured-runtime compatibility or constitute another admission. The first probe needed an explicit assembly load before PowerShell could resolve the otherwise unreferenced type.

The initial contract-test log retains a fixture error that referenced a nonexistent `build.level` field. The test was corrected to alter an existing equipment definition before the admission started. The preflight also corrected a one-byte terminal-accounting error caused by the pinned writer's trailing newline; the byte test checks the actual written file.

No backend source changed in this step. The implementation's prior 81 distinct passing backend tests and 18 Python tests remain the relevant implementation evidence; backend tests were not rerun. An optional `Get-CimInstance` process-inspection query returned Access denied. The owned Job receipts supplied the needed completion evidence without that query.

## Accounting and next step

The failed admission remains charged the **full 600 seconds /512 MiB**, with no refund or transfer. Its charge receipt is inside the preserved package. There is no successfully sealed admission or runnable scientific request.

Source authentication and copying consumed nearly all of the window before the three-second context check. Fixing the loader alone therefore does not establish enough room for two complete history scans, the native check and sealing. A 100-file read-only timing sample measured path resolution and hashing only; it did not explain the complete archive cost and is not a feasibility bound.

The verification improvement and prospective accounting revision are now linked above. **Next: implement the explicit v2 resource profile and its tests before another bounded admission.** Preserve every tested-file, source, runtime, content and complete-history binding. The amendment explicitly retains this failed 600-second /512-MiB charge; the original 7,800-second /4-GiB v1 scope remains unchanged. The 7,200-second /3.5-GiB scientific execution envelope has not been used and is not permission to spend it on another admission.

Framework probes, helper tests and the separate read-only closeout are engineering work. The accepted **18,180-second /13,584-MiB prior engineering ledger remains separately disclosed**, and complete historical engineering totals remain unknown. The closeout inventory is external to the failed package, so documenting and checking the failure does not modify that attempt or claim a late successful admission seal.

There are no migrations, application configuration changes, database operations, deployments or infrastructure changes. The existing references and captured gameplay remain unchanged.
