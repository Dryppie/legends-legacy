# Parity v4 — completed-reference exit handling amendment

Frozen **14 September 2026**, after the reference completed and **before any candidate execution**. The [v4 protocol](Tower-Complete-Family-Parity-V4-Protocol.md), its producing hashes, reference command and failed wrapper receipt remain unchanged. This amendment corrects a diagnostic launcher error; it does not repeat reference combat or change any scientific decision rule.

The captured `TowerBalanceEvaluator` maps **Inconclusive to exit code 3**. The reference completed all **32** scheduled trials, durably charged exactly 32 attempts with zero retries, published its final archive, and returned that scientific status. The wrapper incorrectly accepted only 0/1. This failed classification is retained. The reference's **0 wins / 32 defeats** meets the already frozen feasibility condition of at most 9 wins; neither case nor schedule is replaced. Its scientific assessment remains Inconclusive and is not balance evidence from this diagnostic.

This amendment supersedes the dependent-execution stop **only for this completed reference's misclassified scientific exit code 3**. Freeze the amendment script, this Markdown, original failed receipt and complete reference file inventory. Run one read-only acceptance check, bounded together with amendment freezing to **30 seconds**: verify every reference hash, exact final and compact inventories, completed statuses, definition/content/settings/execution identity, complete ordered 32-value schedule, participant hashes, all 32 durable journal starts and exact no-retry accounting. Require the original Inconclusive assessment with no validation issues and the original feasibility gate. Write a separate acceptance receipt; never rewrite the failed command receipt.

Only after that check succeeds, invoke the **exact already-frozen candidate command once**, with unchanged inputs, **288 attempts / 1,080 seconds / 2 GiB**, through a launcher that reads the separate acceptance receipt. The unchanged native candidate again verifies the reference archive before preparing all 43,879 rosters or starting combat. Its same 32/256 stages, saved midpoint reference, full report comparisons and reconstruction remain required. The existing independent verifier follows once. Any further failure stops dependent execution and is retained.

No reference rerun, seed allocation, retry, resume, replacement, new case, cap extension or additional combat is authorized. Total actual fights remain bounded by **32 already completed + 288 pending = 320**. Charge the extra read-only acceptance/freezing allowance from the original 60-second headroom: total declared diagnostic allowances become **1,770 seconds**, still below the unchanged **1,800-second / 4 GiB** global limits. Original native limits and all 481,603 reservations remain unchanged. This is routine wrapper correction within the authorized engineering diagnostic; no new confirmation or gameplay scope follows.
