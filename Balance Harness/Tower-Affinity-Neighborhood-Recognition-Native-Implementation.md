# Complete affinity neighborhood: native implementation

24 September 2026. Target: the offline `LL/tools/BalanceHarness`.

**The frozen complete-neighborhood profile is implemented and verified. Runtime qualification and admission remain the next step.** This implementation performs no real combat, OS entropy draw or production seed reservation. The preceding allied-action pilot remains `Inconclusive`; no proposer or gameplay policy changes.

The immutable [plan](Tower-Affinity-Neighborhood-Recognition-Plan.json) retains SHA-256 `a0b4a5dbc8fe7c65d4df80bfcb17b8f5623343cef7e0c1a1f2506648b3e2fc2b`. Its historical status stays `FrozenDesignNativeIntegrationRequired`; this separate handoff records completed implementation without rewriting the plan.

## Exact native protocol

`tower-affinity-neighborhood-recognition-v1` admits exactly 44 physical recipes: three ordered references and 41 generated recipes, with the protected 26-recipe subset. All use one common ordered panel of 2,048 fresh values, producing 90,112 fights. Transport slices are exactly 1,000, 1,000 and 48 values for each team: 132 bound recipes. Physical party identities are retained without invented root prefixes.

The native implementation pins the plan and canonical family, captured content and settings. Runtime identity and complete sorted reservation history are supplied by later admission. One 65,536-byte entropy batch exposes 16,384 signed words; the first 2,048 distinct fresh values form the panel and every other exposed fresh value is permanently reserved. Interrupted reservation or shortfall cannot refill, resume or redraw.

All 46 simultaneous endpoints use the frozen Hoeffding calculation. Native arithmetic uses integer win-count numerators and exact integer denominators; the independent Python auditor reconstructs rational coefficients from membership. The 41-recipe and 26-recipe means use equal physical-recipe weights, irrespective of the number of derivations. The shared benchmark cancels from their difference; its range is `[-15/41,+15/41]`. No Wilson interval or root-population estimator is used for this profile.

The result includes a separately typed `neighborhood` outcome. Existing Wilson/stratum arrays remain empty and `approximateWilsonFamily` is zero. The optional new property is omitted from older profile JSON, preserving their format. All 44 descriptive win counts and rates appear in Markdown, alongside all 46 simultaneous gain intervals. Counts and the fixed denominator are retained in machine-readable output.

Generated recipes alone can trigger `FreshConfirmationWarranted`; every qualifying identity is reported in sorted order. Otherwise the result is `RetireBelowPracticalThreshold` or `RetireUnresolvedAtBudget` under the exact frozen inequalities. The 236/237 net-win boundary, reference-only gains and all three decisions are tested. Every terminal outcome has zero additional samples and no promotion. Missing, reordered, mispaired or invalid evidence fails instead of producing a scientific stop decision.

## Owned execution and independent audit

The versioned [launcher](../build/run-affinity-neighborhood-recognition.py) reuses the established exclusive leases and hidden Windows Job owner. Children are assigned before resuming; ownership continues through native measurement, native reconstruction, independent Python reconstruction and the final native publication barrier. Both complete audits must agree before publication. Native postpublication verification checks final files, accounting and evidence again.

The [independent auditor](analysis/audit-affinity-neighborhood-recognition.py) reads every compressed terminal report and rebuilds the common panel, direct-row order, recipe bindings, complete reservation classification, journals, win counts, coefficients, bounds and decision. It rejects duplicate JSON keys and nonfinite numbers. Its receipt preserves the original native JSON number tokens only after all fields independently agree, avoiding a change to canonical hashes from Python float formatting.

The frozen future limits are enforced, but have not been admitted or charged:

| Phase | Seconds | Bytes |
| --- | ---: | ---: |
| Admission | 1,800 | 536,870,912 |
| Native execution | 14,400 | 17,179,869,184 |
| Both audits and publication | 3,600 | 1,073,741,824 |
| Separate postpublication verification | 3,600 | 268,435,456 |
| Prospective total | 23,400 | 19,058,917,376 |

The owned execution request contains exactly the admission charge plus native/audit allowance: 19,800 seconds and 18,790,481,920 bytes. Postpublication verification remains separate. No phase borrows unused allowance. The launcher requires an externally pinned admission, exact package membership, its retained harness, matching owner/auditor code and a new output. Engineering throughput does not establish real-combat feasibility or captured-runtime compatibility.

Public command prefix is `tower-affinity-neighborhood-recognition`, with actions `context`, `check`, `audit` and `verify`. `run` and `publication-check` belong to the owned workflow. There is no new gameplay endpoint or runtime default.

## Changed files

- [TowerAffinityNeighborhoodRecognition.cs](../LL/tools/BalanceHarness/TowerAffinityNeighborhoodRecognition.cs) implements the native endpoints, decisions, export and report. Existing recognition dispatch, family protocol, common-panel identity and request-limit files select the new version; `Program.cs` dispatches its commands.
- [audit-affinity-neighborhood-recognition.py](analysis/audit-affinity-neighborhood-recognition.py) and [run-affinity-neighborhood-recognition.py](../build/run-affinity-neighborhood-recognition.py) provide independent reconstruction and process ownership.
- [Native tests](../LL/tests/EssenceSystem.Tests/BalanceHarnessAffinityNeighborhoodRecognitionTests.cs), [Python tests](../build/test-affinity-neighborhood-recognition.py), and [full owned fixture](../build/test-affinity-neighborhood-recognition-owned.py) verify calculations, failures and publication. Three existing process-fixture files route this version only within the separate test executable.
- `.gitattributes` fixes the four new Python files to LF. This report and only line three of nine current-status guides record progress; every other byte of those guides is preserved.

There are no migrations, application configuration changes, deployments, gameplay edits or default-policy changes. Unrelated working-tree changes and the frozen scientific evidence are preserved.

## Verification

The required `build/run-tests.ps1` wrapper passed **144 backend cases**, including the new protocol and the existing fixed-family and recognition regressions. The final report addition passed **26 native cases** again. The [retained TRX](../TestResults/affinity-neighborhood-native-implementation-verification-20260924/backend-regression-tests.trx) and [final native TRX](../TestResults/affinity-neighborhood-native-implementation-verification-20260924/neighborhood-verified-tests.trx) distinguish these runs; repeated cases are not additional coverage.

**26 Python tests passed** against the final native outcomes and full archive. Coverage includes independent agreement with both native arithmetic and the frozen reference evaluator, complete-family membership, exact threshold decisions, shared-subset cancellation, entropy collisions and unused tails, incomplete evidence, duplicate keys, legacy-version rejection, external admission pins, mutually exclusive leases, surviving-descendant termination, native/audit/storage/publication/seal failures, and tampered direct report metadata. Fault injection changes readers, leaving the sealed archive bytes intact.

The final [owned fixture](../TestResults/tower-neighborhood-recognition-owned-fixture-verified-20260924/verification.json) passed all 90,112 literal reports through production ownership, native reconstruction, independent audit, publication and native postpublication verification. Its complete sealed output is **151,311,422 bytes**, with **733.782 recorded seconds**. All owned children exited successfully with no timeout and zero active descendants. Its archive manifest is `f79c5ce7c8362f03be0322238467274564962093e9fe32e49660744a3ae863e7`.

Two earlier synthetic fixtures were stopped after detecting a request-binding mismatch: their Python input omitted `pendingHistoryRecoveries` and `recoveryReceiptHashes`, while native serialization includes both nullable fields. They could not pass the independent audit. Their owners recorded failure, exited with zero active descendants and retained partial reports and literal reservation ledgers; neither published a result. The corrected fixture explicitly carries the fields. The production launcher now rejects their omission before creating output or drawing entropy, and a cross-language regression binds the native serialized request and canonical hash. No frozen archive was rewritten, and the successful fixture uses a fresh synthetic directory.

All three attempts retain their full declared engineering ceilings of 1,200 seconds/2 GiB each, totaling **3,600 seconds /6,442,450,944 bytes**. Recorded engineering fixture time totals **2013.579 seconds** and retained fixture storage totals **292,396,504 bytes**, including failed attempts. These engineering fixtures and ordinary build/test documentation are separate from scientific campaign accounting. Literal seed ledgers reside outside the production balance registry. Production entropy, combat and history changes are all zero.

The first sandboxed build could not read the existing user NuGet configuration. The same authorized wrapper then succeeded with filesystem access; the final build also passed. Builds emitted existing warnings and zero errors. No required verification command remains blocked. The initial failure log remains in the verification package.

The preceding 35-member verification package and all 143 inherited historical pins were authenticated again. Report links, LF files, scoped whitespace, source/runtime hashes and the nine line-three-only document edits are retained in the [verification package](../TestResults/affinity-neighborhood-native-implementation-verification-20260924/verification.json).

Principal commands (`python` is the bundled interpreter, with `-B -X utf8`):

```powershell
./build/run-tests.ps1 -Configuration Release -ArtifactsPath TestResults/affinity-neighborhood-native-build-20260924 -Filter 'FullyQualifiedName~BalanceHarnessAffinityNeighborhoodRecognitionTests|((FullyQualifiedName~BalanceHarnessAffinityPreservationRecognitionTests|FullyQualifiedName~BalanceHarnessAffinityCreationRecognitionTests|FullyQualifiedName~BalanceHarnessFrozenPoolRecognitionTests|FullyQualifiedName~BalanceHarnessFixedFamilyConfirmationTests)&FullyQualifiedName!~Full_)'
./build/run-tests.ps1 -Configuration Release -ArtifactsPath TestResults/affinity-neighborhood-native-verified-build-20260924 -Filter 'FullyQualifiedName~BalanceHarnessAffinityNeighborhoodRecognitionTests'
python -B -X utf8 build/test-affinity-neighborhood-recognition-owned.py --fixture-host TestResults/affinity-neighborhood-native-verified-build-20260924/bin/BalanceHarness.ProcessFixture/release/BalanceHarness.ProcessFixture.dll --output TestResults/tower-neighborhood-recognition-owned-fixture-verified-20260924
python -B -X utf8 build/test-affinity-neighborhood-recognition.py --native-outcomes TestResults/affinity-neighborhood-native-implementation-verification-20260924/native-outcomes-verified --fixture TestResults/tower-neighborhood-recognition-owned-fixture-verified-20260924
```

## Next step

Implement bounded runtime qualification and admission for the exact frozen complete neighborhood. Authenticate source publication, stage review and plan; retain captured gameplay dependencies and content, substitute only tested harness artifacts, prepare all 44 recipes and 132 transport bindings without combat, refresh complete live history, and demonstrate separate native/audit/postpublication time and storage forecasts within the fixed ceilings. Preserve all prior charges and failures; seal an externally pinned admission before any OS entropy or real combat. Reject infeasible admission without reducing samples, borrowing allowance or changing policies.

Scientific accounting is unchanged: recorded **41262.098 seconds / 33,519,903,348 bytes**, declared maxima **116,580 seconds / 72,360,132,608 bytes**. The last complete history verification remains 782,796 values across 262 files; this implementation did not rescan or allocate production history. See the [implementation handoff](../TestResults/affinity-neighborhood-native-implementation-handoff-20260924.json).
