# Deep challenger policy repair and separate zero-combat verification

**Closed preparation - 15 September 2026:** [generation parity passed, then control preparation failed](Tower-Deep-Challenger-Repair-Review.md). This package is sealed and cannot be retried. The frozen protocol copy remains unchanged in its evidence directory.

15 September 2026. The user authorized proceeding with the repair and a separate zero-combat verification package after the [closed readiness failure](Tower-Deep-Challenger-Preparation-Review.md). Target: offline BalanceHarness. The failed preparation remains sealed and is never retried or edited.

Register `independent-deep-challenger-v1` in the existing coordinated-loadout, core creation/validation and coverage creation/validation gates. Preserve all historical branches, operators, random streams, budgets and content. Add tests that actually generate with the new policy and compare the complete deep arm with the old portfolio component, exercise all four loadout operations, reject missing cores/coverage, and cancel at fixture evaluations 384 and 400.

Run development tests through `build/run-tests.ps1` with exactly `FullyQualifiedName~BalanceHarnessTowerDeepChallengerTests|FullyQualifiedName~BalanceHarnessTowerSearchPortfolioTests|FullyQualifiedName~BalanceHarnessTowerCompleteFamilyTests`. These are synthetic tests without combat. Keep every invocation receipt; ordinary development fixes may be tested before freezing runtime diagnostics. Do not run the loadout-composition class's combat integration test.

Create `TestResults/balance/tower-deep-challenger-repair-20260915` using the failed package's pinned captured source and gameplay, updating only the seven owned harness files. Compile an isolated adapter. Before executing diagnostics, freeze source, executable, scripts, protocols and every input. Perform exactly one native check (900 seconds maximum) and one independent saved-output audit (300 seconds maximum), with zero retries, engine fights or fresh balance seed candidates. Total diagnostic allowance remains 1,800 seconds including the prior 3.766-second failed check.

The native check must:

1. Verify the focused predecessor inventory and captured gameplay/content identities.
2. Recreate mechanics/coverage from captured content for the new policy and compare with the existing deep policy's mechanics, then replay all three archived v19 deep arms using saved measurements. Require exact complete arm hashes, all 4,608 evaluations, recipes, proposals, parents, operators, ordering and stop reasons.
3. Prepare both fixed controls once against the captured +10% content and require their saved participant hashes. Check the seed-free definition without making it runnable.
4. Verify the complete live history union of 481,891 reserved values, including unused original512 and unused32. Derive no candidate values and create no seed ledger.
5. Save all 257 possible maximum-family rate intervals and the two synthetic paired cases (100 versus 50 wins, comparison counts 1 and 255).

The independent audit checks frozen identities, exact saved replay, control selection, reservation union, non-runnable template and confidence arithmetic. Its purpose is readiness verification; there is no new balance estimate or speedup claim.

Both old and new preparation output count toward a combined 1-GiB producing/control ceiling within the proposed 4-GiB total. Carry forward the old 3,144.243291-second setup charge and add this preparation's elapsed work. Exclude the intervening wait for user authorization. Any later study must still fit the original proposed 10,800-second total, its 5,400-second native run ceiling and 108,544-fight maximum; do not reset charges by creating this package. No combat is authorized by this repair.

On success, seal the new package, report measured checks and remaining execution limitations, and update active Markdown. The earlier zero-fresh-seed restriction still requires the specific 331-value exception before any independent-study allocation. Ask for that exception only when preparation has passed. On failure, preserve the evidence, close this package without retry, and explain the unresolved issue. No predecessor change, reservation release, gameplay edit, default optimizer change, migration or deployment.
