# Allocation versus joint structural search: bounded pilot

15 September 2026. Target: offline BalanceHarness. Preparation is authorized; allocating seeds or running this pilot requires the specific seed and diagnostic-time exceptions below. No gameplay, boss tuning, ability-order optimization, deployment or default-policy promotion.

## Frozen comparison

Baseline: `independent-group-allocation-v1` / `group-allocation-joint`. Candidate: `independent-joint-structural-v1` / `joint-structural`. Version/seed namespace `tower-joint-structural-comparison-v1`, master label 2026091510. This label alone does not allocate any value.

Use the same captured deep-challenger gameplay assemblies/content/settings, existing guardian Health/Power +10% configuration, floor 5, timestamp, equipment, neutral identities, level 40/tier 1/rank 2, 80 eligible Essences, unrestricted copies and five Essence slots. Ten characters represent two five-player parties. Keep ordinal Essence order fixed. Do not rebuild or change dirty gameplay content.

Each policy gets 16 distinct evaluated teams on the same four discovery seeds. Baseline permits at most 256 proposals; the finite candidate permits at most 16. Both retain their verified operators and policy behavior. Different proposal limits accommodate rejection in the adaptive baseline; neither earns extra evaluated teams or fights. Baseline runs first. Candidate construction remains independent of its generation label; one label identifies this pilot, not an independent structural restart. Controls and held-out outcomes never enter search.

| Phase | Teams | Fights per team | Maximum fights |
| --- | ---: | ---: | ---: |
| Baseline discovery | 16 | 4 | 64 |
| Structural discovery | 16 | 4 | 64 |
| Screen | Top two per policy | 8 | 32 |
| Confirmation | One winner per policy plus two controls | 32 | 128 |
| Total | | | **288** |

A discovered team therefore plays 4 fights, a screened team 12, and a confirmed finalist 44. Each external control plays 32. Exact duplicate full-context recipes may merge only in screen/confirmation, retaining every origin and never refilling seats; actual totals may fall below 288. Discovery overlaps remain charged. Incomplete discovery stops dependent stages.

Use existing discovery fitness/rank and identity ties. Screen winner: wins descending, discovery rank, ordinal identity. Freeze winners before confirmation; never reselect afterward. Controls: `team-040e60d3dbc5c127321653c47ed3a9d3` and `team-49f6979895354870c89362d4abf214bb`.

Propose exactly **45 fresh values**: one generation label, four discovery, eight screen, 32 confirmation. Preserve all **482,641 reservations**; successful binding would retain **482,686**. Primary contrast: structural finalist minus allocation finalist on paired confirmation seeds. Also retain four finalist/control contrasts. Existing Wilson family factors remain 8 for rates and 20 for paired discordance bounds. Draws are non-wins; identical merged recipes have exact zero difference. Boss health/duration are descriptive with missingness preserved. One small pilot cannot establish reliability, optimality or adoption.

## Preparation and verification

New preparation directory `TestResults/balance/tower-joint-structural-comparison-preparation-20260915`; execution/study use matching `execution` / `study` names. Preserve the immediately preceding adapter seal `1fb9856fd5b952f6751fc5db6996378cd3a46452b5b9f25203582a8823e58234` and all old sealed packages.

Freeze the adapted existing comparison model/runner, source snapshots, exact commands and independent audit before tests. Build an isolated executable and test assembly from the already verified harness source and identical captured gameplay DLLs, using cached restore metadata; at most 300 engineering seconds per build, separately measured. Run exactly eight backend Facts once through `build/run-tests.ps1`: native/global costs and policy isolation; context drift; both-policy nominations and incomplete discovery; screen ties/partial evidence; duplicate winner/control merging; paired interval extremes/draws/misalignment; incomplete confirmation; durable interrupted attempt charging. The nomination test uses synthetic scores and captured mechanics, with an engine-entry guard.

Independently check exported fixture definitions, exact budgets and shared inputs, the changed runner constants, source/binary hashes and test count. Reuse the sealed 12 adapter tests and seven exact old-policy parity fixtures by hashes. No fresh construction experiment, battle preparation, full registry traversal or full preservation-chain traversal during this small preparation. Relevant sources, predecessor seal and unrelated dirty files are checked now. Full chain/registry verification is deferred explicitly to the approved preflight, before allocation; no preservation claim is inferred from an unchecked live registry.

Before approval, diagnostic preparation (freeze/tests/audit/publication) shares only the existing **6.838000000052261 seconds** remaining after **1793.1619999999477 seconds**. Reserve one closure second. New preparation output at most 128 MiB plus a conservative 1 MiB temporary-test charge, within cumulative 4 GiB. Stop dependent checks on failure; zero retries.

## Required execution exception and stop rules

Request **600 additional diagnostic seconds**, increasing the cumulative ceiling from 1,800 to **2,400 seconds**, solely for this pilot's preflight, binding, fights, verification, independent audit and publication. This is a proposed exception, not an automatic reset. Request **45 fresh values and at most 288 fights**, including every repetition/replay. Keep **zero retries/resumes/replays** and the unchanged cumulative **4 GiB** cap. New study at most 1 GiB, execution evidence at most 128 MiB. No old experiment cap changes.

After explicit approval, record its wording, protocol/preparation hashes and limits. First reverify the full preservation chain and live registry, require the exact 482,641-value union, validate matched definitions and prepare the two unchanged controls without fighting. An independent preflight audit checks registry hashes/union, captured controls, budgets and interval fixtures. Only then may binding allocate 45 values through the existing durable intent/journal. Binding rechecks live membership; execution and native verification retain their complete live registry checks. Any discrepancy stops before dependent work.

The native runner retains storage accounting, archive verification, durable attempt caps and cancellation. The wrapper imposes phase deadlines inside the remaining cumulative allowance, including reserved closure time; all commands, failures and measurements persist. On completion independently reconstruct all archived fights, nomination/screen/finalist choices, seed and attempt charges, rate/paired intervals, recipe constraints and descriptive boss-health metrics. On failure preserve all partial reservations/attempts and artifacts without retry or widening limits. Full gameplay build/backend suite are outside this scope.

V19 keeps all 253 recipes, no confirmation and its 512 unused confirmation values. No 129,536-fight confirmation. Reliability Fail 1/3, deep recovery 0/3, v19 Unresolved and adoption Hold remain unchanged.
