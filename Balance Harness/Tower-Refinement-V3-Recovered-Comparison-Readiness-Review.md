# V3 comparison readiness after history recovery

16 September 2026. **ReadyAwaitingSpecificApproval**. The new V3-versus-baseline comparison is prepared with the verified scanner and explicitly pinned recovery receipt. Native preflight and independent checks passed. Zero new values or fights.

The [frozen protocol](Tower-Refinement-V3-Recovered-Comparison-Protocol.md) and [exact request](../TestResults/balance/tower-refinement-v3-recovered-comparison-readiness-20260916/request.json) compare team coverage V1 with discovery refinement novel V3, explicitly using comparison-v2. Both arms retain identical captured gameplay, content, settings, context and schedules. Ability order stays fixed.

| Stage | Maximum teams | Fights each | Maximum fights |
| --- | ---: | ---: | ---: |
| Baseline discovery | 16 | 4 | 64 |
| V3 discovery | 16 | 4 | 64 |
| Selection | 4 | 8 | 32 |
| Finalists and two controls | 4 | 32 | 128 |

Maximum **288 charged attempts**, zero retries/resumes/replays. Request exactly **45 fresh values** using new master **2026091604**: one generation label, four discovery, eight selection and 32 confirmation. A finalist can play 44 fights. Incomplete discovery stops the comparison; no refill. The paired confirmation win-rate difference is primary. Boss health is descriptive; adoption remains Hold.

The original failed V3 study remains sealed and Pending. Its **45 unused values** are included in the **482,911-value** authoritative ledger. The new request explicitly pins its Pending file, recovery receipt, exact source inventory and the saved 178-file registry snapshot. Recovery supplies exclusion values only; no old study is rebound or resumed.

The producing harness is the exact DLL from the [history recovery verification](Tower-History-Recovery-Review.md), which passed **45 tests through build/run-tests.ps1**. These test results and the existing comparison host are reused; no source changes, rebuilds or test repetitions occurred. Native inspection successfully loaded that host with the new harness, validated the request and wrote an unsigned authorization template. The DLL also contains the diagnostic entry point: reproduction uses the sealed comparison host to invoke production APIs, not the DLL's Main method.

Readiness performed no fresh seed derivation, generated team, actor preparation, full registry scan or combat. The independent reader checked 128 existing confirmation records and four rates, with no replay. Source and unrelated dirty files passed preservation checks.

The prior measurement found **41.5581 seconds** for the sequential scan, **17.5043 / 16.3488 seconds** for the candidate scans, and **38.2906 seconds** for complete history validation. This is a single sequential-first observation with uncontrolled cache/load, not a guarantee of complete binding or future execution time. Production retains all three live registry traversals across binding and launch; no extra standalone scan is scheduled.

The **84 MiB study cap** exceeds the counted **73.03 MiB component minimum** by **10.97 MiB**. The minimum includes fourteen full compact historical arrays, one captured runtime and four content copies, and excludes reports and temporary writes. Completion is not guaranteed. The full 400-second / 88 MiB execution reservation fits after the requested extensions.

The study remains capped at **360 seconds / 84 MiB**: 150 binding seconds are conservatively charged in full, leaving 210 for launch, its live refresh, combat and native reconstruction. The complete execution task is capped at **400 seconds / 4 MiB evidence**, including independent audit and publication.

Await specific approval for 45 fresh values, 240 additional cumulative diagnostic seconds and 64 MiB additional cumulative output. Then execute this new sealed request once: at most 288 attempts, 400 task seconds, zero retries. Proposed cumulative ceilings become **4,080 seconds / (4 GiB + 192 MiB)**. Current ceilings remain **3,840 seconds / (4 GiB + 128 MiB)** until approved. All earlier seed approvals are exhausted. No execution authorization has been created.

| Completed phase before publication | Charged seconds |
| --- | ---: |
| audit | 0.9850 |
| freeze | 1.4070 |
| inspect | 1.1100 |

Setup and publication are also charged in the [completion receipt](../TestResults/balance/tower-refinement-v3-recovered-comparison-readiness-20260916/completion.json). Readiness is bounded to 40 seconds / 24 MiB within existing authority. First failure: none. Exact commands, source/runtime provenance and native metrics are retained with the package.

```powershell
$python = 'C:/Users/HrHoe/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe'
$work = 'TestResults/balance/tower-refinement-v3-recovered-comparison-readiness-20260916'
# Completed readiness phases must not be repeated.
# After the specific seed/time/output approval and bound receipts exist:
& $python -B "$work/execution.py"
```

Changed files: this new protocol/review, isolated request/runtime/host/scripts and six active Markdown handoffs. No harness/search/gameplay source, configuration, migration or deployment changes. Preserve all 482,911 reservations, including V3's 45, the earlier failure's 40 and V19's separate 512 unused values and 253 recipes. V19 reliability Unresolved; later Fail 1/3, deep recovery 0/3; adoption Hold.
