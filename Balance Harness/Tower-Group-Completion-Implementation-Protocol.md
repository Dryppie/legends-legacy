# Group-owner coverage completion: implementation and zero-combat proof

15 September 2026. Target: offline BalanceHarness. The user authorized the next compatible-completion engineering step. Implement an explicit opt-in policy `independent-group-completion-v1` / `group-completion-joint`. No fresh seeds, fights, combat preparations, replays, content changes or default promotion.

The existing filler already samples authored coverage providers and team-wide counts before intent-weighted completion. The new policy first completes missing coverage categories on the characters holding the selected group. It reuses the exact diversity catalogue order, owner-count/filler schedule and placement seeds, including every eighth uniform request. For each seeded category, choose a legally compatible provider reaching the most still-uncovered group owners, with stable seeded ties. Respect shared copy budgets, case-insensitive families and slots; never remove the reserved group. Each iteration adds at least one Essence, bounding work by available slots. Finish through the existing coverage filler and preserve count-drift rejection.

Categories, evidence and eligible providers come exclusively from the existing authored coverage metadata. No control recipes, Essence names, reference outcomes or new strength estimates enter selection. Category presence does not prove self-targeting, uptime or combat benefit. Save category order, provider choice, compatible-provider count, evidence and per-character before/after insertions. The optional completion trace is omitted for old policies, preserving serialized golden outputs.

Freeze source, fixtures, driver, independent audit, workflow and publication scripts before diagnostics. Build one isolated driver and one test assembly against captured gameplay DLLs using cached restore metadata. Run **95 backend cases through `build/run-tests.ps1`**: the existing 79 cases plus 16 completion cases covering categories, repeated owner completion, slot/family/copy constraints, missing features, trace integrity, metadata order, explicit policy registration, uniform/diversity schedule compatibility, deterministic search and cancellation. Synthetic evaluator repetitions in these cases are zero-combat tests.

One captured-content command constructs the same first **19 requests for each of four policies** (group-count, variation, diversity and completion): 76 total requests, retaining rejections and duplicates. Match all 57 old choices exactly against the sealed diversity verification output. Reconstruct the saved diversity comparison's **44-evaluation search once**, returning only its archived measurements to callbacks, and require an exact complete generation hash match. This is a deterministic search reconstruction, not a combat replay; no new scoring or measurement is performed. One independent Python audit reconstructs all completion choices/insertions, legality, unchanged schedules and construction measurements from frozen metadata and saved outputs. It does not import or invoke the production generator.

Caps: **zero fights/seeds/preparations/replays, zero retries; 300 seconds new diagnostics and 1,800 seconds cumulative; 512 MiB new output and 4 GiB cumulative**. Carry forward **842.082 seconds** and the complete retained output chain from the diversity trajectory completion, including failures and corrections. Compilation is separate engineering work, each command capped at 300 seconds. Preserve all **28** preceding sealed packages before/after. On failure or caps, retain evidence and stop dependent diagnostics; report the unresolved boundary.

Keep candidate/attempt budgets, deterministic ranking, nominations, fixed ordinal ability order, ownership, cancellation, durable attempt charging and archive verification unchanged. Preserve **482,551 reservations**, v19's unused 512 and all 253 recipes. No Kharad tuning, 129,536-fight confirmation, deployment, configuration or migrations. Historical reliability Fail 1/3, deep recovery 0/3, sealed v19 Unresolved and adoption Hold remain unchanged. Construction success is not a combat-strength result.
