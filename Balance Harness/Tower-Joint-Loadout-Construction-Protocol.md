# Joint role construction: zero-combat feasibility verification

15 September 2026. Target: offline BalanceHarness. The user authorized proceeding from the saved trajectory diagnosis to generic construction fixtures. Implement a separate structural constructor; do not integrate or promote another search policy.

The constructor accepts allowed Essence families, authored coverage/evidence, an authored core, explicit required categories, at most five slots and remaining available copies. It jointly explores compatible providers for the most constrained missing category, preserves the core, canonicalizes Essence IDs, deduplicates states and records category witnesses. Every step adds one provider. No reference recipes, outcomes, RNG seeds, names, ability-order optimization or combat APIs are inputs. Category presence is not a targeting/uptime or strength guarantee. Coverage-complete recipes may leave unused slots; filler and distribution across a party remain outside this standalone API.

Hard bounds: 128 providers, 4,096 visited states and 256 retained solutions per invocation at most. The captured diagnostic uses **256 states / 16 recipes per core**. A state or recipe limit reports incomplete search explicitly; zero retained solutions under a limit never proves infeasibility. Exhaustion establishes structural feasibility/infeasibility only under the supplied core, role, family, slot and copy constraints. It does not enumerate every redundant or padded loadout.

Freeze `TestResults/balance/tower-joint-loadout-construction-20260915` before checks. Run one isolated adapter build and one isolated test build, using cached restore metadata and captured gameplay/harness DLLs. Builds are engineering work, separately measured, each at most 300 seconds. No dirty gameplay rebuild or restore. Run exactly **11 new backend tests once through `build/run-tests.ps1`**. The exhaustive fixture compares all **448 role/copy/slot cases** against all 16 subsets of a four-provider synthetic catalogue. Other tests cover a greedy-choice trap, input-order determinism, family/copy constraints, state/result limits, complete/invalid anchors, missing roles, cancellation, metadata/bounds and witnesses/input immutability.

Run one captured metadata diagnostic over every distinct legal two/three-member base core, expected **48**, derived from the sealed 80-Essence inputs. Require all five existing authored categories in at most five slots. Persist each core, bounded result, recipes and evidence witnesses, elapsed time, CPU, allocations and peak memory. Zero parties/fights/seeds are generated. Do not inspect controls to choose cores, targets, ordering or limits. Independently verify all emitted recipes/witnesses, limits and catalogue membership. For each exhausted core, brute-force the at-most-three additional slots from the captured catalogue until a structural witness is found or all combinations are checked, verifying the feasibility claim; bounded negative cases remain unresolved. This is metadata-only verification, never battle simulation or preparation.

Freeze, backend tests, captured diagnostic, independent audit and publication together may consume **at most 40 additional diagnostic seconds**, within the unchanged cumulative **1,800 seconds / 4 GiB**. Carry **1,749.694 seconds** and the previous package's actual final bytes. Each diagnostic phase is at most 15 seconds, with ten seconds reserved for publication. New package at most 192 MiB. Stop dependent checks on the first failure or limit; zero retries. Preserve failures and measure costs. All 44 predecessor packages and all existing harness source files are hash-checked before/after; preserve unrelated dirty work.

Reuse sealed previous test and parity results by source/binary hashes. Historical source/policies are unchanged; no old-policy construction or combat replay is necessary. Update the review and six active handoffs with measured results and remaining limits. Preserve all **482,641 reservations**, v19's 253 recipes and unused 512 confirmation values, fixed ability order, reliability Fail 1/3, deep recovery 0/3, v19 Unresolved and adoption Hold. No fresh allocation, tuning, cap increases, 129,536-fight confirmation, configuration, migrations or deployment.
