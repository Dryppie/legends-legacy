# Captured-v19 retained-family UTC context — frozen zero-combat protocol

Frozen **14 September 2026** following the [completed audit and failed binding](Tower-Retained-Family-Audit-Review.md). Target: offline `LL/tools/BalanceHarness`. The user authorized proceeding with the timestamp-equivalence correction and revised confirmation binding. No new combat or seed allocation is authorized.

Preserve the entire preceding audit, its producing source, literal context identities, all original scenarios, all 43,879 entries / 47,834 origins, all 253 midpoint recipes and every earlier review. The audit engineering seal is `d346801a04691964966f3ac780dc52f39dac432a89f5d5c6693602ff187b5884`. The authoritative midpoint ledger remains **481,603 reservations**, including all 512 unused original v19 values. The old failed design is never retried or overwritten.

Use new output `TestResults/balance/tower-retained-context-20260914`. Limits: **zero fights, zero fresh seeds, zero retries/resumes, 1,800 seconds diagnostic workload and 4 GiB total new output**. Native checks additionally allow **900 seconds / 1 GiB**. Count captured/build output and whole changed files. Record engineering compilation/correctness tests separately. Freeze exact source, producing executable, scripts and source input hashes before diagnostic execution; failures preserve evidence and stop the dependent phase.

1. Add a separate `tower-confirmation-context-utc-v1` identity policy. Normalize only the context's starting instant to UTC. Keep legacy scenario, input, participant, recipe and context hashes visible. Test equivalent positive/negative offsets, dates crossing midnight and a genuinely different instant, plus context/actor/Essence distinctions, complete mapping, anchor coverage, collisions and cancellation. Run through `build/run-tests.ps1` with the exact new context-test filter plus the preceding retained-audit tests; use isolated artifacts.
2. Build a captured diagnostic entry from the preceding captured source plus the new identity helper and diagnostic entry, using the four unchanged captured gameplay DLLs. Read the sealed inventory and native rows once to map all entries using the new helper, verifying legacy identities and retaining all origin/participant hashes. Do not re-materialize the entire family.
3. Prepare the **90 affected scenarios twice each**, once with the original +01:00 notation and once with UTC. Compare normalized input, full prepared combatant state, encounter plan and saved participant digests. Additionally prepare the first midpoint recipe at **UTC, +01:00, −07:00, +14:00, −12:00, and UTC plus one tick**: **186 preparations total**, no engine entry. The last case must remain a different context. Record exact original timestamps; do not rewrite saved scenarios. A combat guard rejects engine entry.
4. Persist a disassembly of the captured executor, including generated state machines, its start-time consumers and the World Tower preparation factory. Check whether the start time reaches combat decisions or only result metadata. Review that captured evidence before permitting the revised binding phase. This is zero-combat runtime/input equivalence evidence, not a new combat replay or new balance measurement.
5. After proof succeeds, freeze and run one new binding under the explicit UTC policy. Retain all 327 historical anchor origins and all 253 midpoint origins; expect their 560-cell union, no collisions and coverage of the unified context. Independently recompute every new context/cell identity from original saved metadata. Produce seed-free complete cell/anchor mappings and all 33 first-stage / 257 maximum-second-stage interval rows for the existing conditional design. Verify intervals independently. Do not allocate the proposed 288 values, implement a combat launch or increase old experiment caps.
6. Record per-phase wall time, CPU, allocations, memory, output, complete commands and failures. Recheck frozen bindings and prior reviews, classify unrelated concurrent checkout changes without altering them, update active handoffs and seal the new package. Stop on any unresolved proof, mapping, history, capacity or resource check.

The independent confirmation design remains a separate future execution contract. Reliability **Fail 1/3**, adoption **Hold** and all earlier scoped balance results remain unchanged. UTC context grouping does not establish win rates, acquisition feasibility, equivalence to current gameplay or exhaustive search coverage.
