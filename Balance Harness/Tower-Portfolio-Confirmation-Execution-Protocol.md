# Captured-v19 confirmation execution protocol

Frozen **14 September 2026**, before the first confirmation fight. The user explicitly authorized execution after the prepared-study handoff. This new authorization permits the single frozen confirmation run and its reconstruction; it does not change the historical preparation receipt or authorize gameplay changes.

Execute the existing `TestResults/balance/tower-portfolio-confirmation-20260914` package exactly once with its retained `executable/BalanceHarness.dll`. Require protocol SHA-256 `18613ccee673347b619a8e742c770f87bf7d224351a33b9d9665e03e6b43e6e1`, seed ledger `8033fe0a8169e05e236866451ac6b87a4445230998bf2114e1be67004c98e7bd`, complete definition `099edf3287963cb265d30d1bf3170312a18a6b729d40e43653937e37ed985d3e` and the exact 65-file prepared inventory before starting.

The [frozen study plan](Tower-Portfolio-Confirmation-Plan.md) governs all scientific inputs, ordinary/joint acceptance and the numerical 2/3 reliability gate. Preserve all 253 recipes, both original/screened nomination sets, fixed anchor, stronger control and captured v19 gameplay. Use the already reserved 512 shared values. Allocate **zero additional seeds**, run **zero discovery, screening, diagnostics or replays**, and preserve all **481,219 reservations**, including the 512 unused v19 values.

Limits remain **129,536 starts, 14,400 execution seconds, 8,589,934,592 logical study bytes and zero retries**. Owned accounting, durable attempt charging, exact archives and mandatory audits remain enabled. No resume, second start, cap extension, recipe omission, nomination replacement, tuning, content change, deployment or promotion is authorized. If a limit or failure stops the run, preserve its evidence and leave inference unresolved for an incomplete family.

Use separate work directory `TestResults/balance/tower-portfolio-confirmation-execution-20260914`. Before combat, freeze the execution wrapper, these instructions, commands, prepared inventory and preceding package indices in `execution-definition.json`. Run one native prepared check (300-second command bound), one native confirmation invocation, and, only after a complete valid run, one native reconstruction (900-second command bound, zero fights). An exit code of 1 from a **complete** run means the adoption gate did not pass; it still requires reconstruction. A failure marker, incomplete count or execution error stops the dependent success path.

The native four-hour timer starts after input verification. A separate **14,700-second command watchdog** includes up to 300 seconds for that pre-execution work; it does not raise the native 14,400-second execution cap. Preserve command start/end times, exit codes and full logs. A hard watchdog stop records partial evidence without retry. Run reconstruction in its own bounded process. Keep all command logs outside the study inventory.

After successful reconstruction, independently audit the complete seed/cell coverage, wins and paired discordances, unchanged frozen inputs and complete final inventory; retain per-recipe results and the three original reliability comparisons. Use a 900-second bound for this read-only analysis. Check all six sealed preceding source/engineering/preparation packages once by exact inventory and hash. Keep new execution-work artifacts below 1 GiB; the study retains its separate 8-GiB cap. Freeze analysis commands before their invocation and do not launch any further combat based on intermediate outcomes.

The relevant 201-test wrapper run and producing source were sealed during preparation. Reuse that verified result if source remains unchanged; run relevant tests through `build/run-tests.ps1` if a source change or unresolved concern requires it. Record checkout hashes before execution and preserve unrelated/concurrent work. Update active handoffs and add a measured completion/failure review; never rewrite historical reviews, frozen plans, seed ledgers or sealed receipts. Report elapsed time, retained bytes, attempt counts, verification results, ordinary/joint assessments and reliability separately. Performance measurements from this single workload are not a paired before/after speedup.
