# Complete retained-family final request — zero-seed verification protocol

Frozen **15 September 2026** after the [durable binding verification](Tower-Complete-Family-Binding-Review.md). Target: offline `LL/tools/BalanceHarness`. Close external request-file accounting, verify it, then prepare a concrete final reservation request. **The original zero-fresh-seed limit remains in force: do not invoke ReserveAndBind or any combat command.** This protocol does not authorize 288 new values or a full study.

New package: `TestResults/balance/tower-complete-family-request-20260915`. Limits: **zero fresh balance values, preparations and fights; zero diagnostic retries/resumes; 1,800 diagnostic seconds; 4 GiB new output**. Compilation/correctness tests are separately timed and their output is counted. Preserve original failed evidence and all old sealed packages; no old cap changes.

1. Read the binding request from one file handle that excludes concurrent writers, deserialize and hash those same bytes, and automatically add the external request to the effective setup map. Reject an in-study original, linked file or self-reference, including path aliases. Retain its exact hash in the completion receipt and durable binding start. The original is charged as external setup; the in-study copy is charged by the existing owned storage. This removes the need for a file to contain its own hash. Keep reservation derivation, deterministic gameplay/search, caps, cancellation, attempt charging and archive verification unchanged.
2. Run exactly **108 synthetic cases** through PowerShell 7 and `build/run-tests.ps1`: the preceding 104-case allowlist plus one exact-byte/mutation fixture and three self/alias/in-study rejection cases. Extend the existing synthetic publication fixture to assert that the original request's bytes/hash reach the final protocol. Freeze the full expanded selection and source hashes before tests. Use only literal or `binding-fixture-only` synthetic values. Correctness fixes may receive a separately recorded pass; native diagnostics cannot retry.
3. Build one full CLI from the preceding sealed captured source with only `TowerCompleteFamilyBinding.cs` overlaid. Reference the same sealed captured-v19 gameplay DLLs. Pin executable, compiler, request, runtime, gameplay/source/history hashes before native execution. Reuse the prior source/roster/combat evidence; do not repeat combat diagnostics.
4. Run exactly one new CheckOnly request through `tower-complete-family-binding-check`, at most **600 seconds / 64 MiB native output**. Bind the complete setup, preservation and prior binding packages, current executable and fixed producing inputs. Verify all 43,879 recipes and the authoritative 481,603-value history. The effective map must include the CheckOnly request itself, with its exact additional length and hash. The prospective actual study is `TestResults/balance/tower-complete-family-reserved-20260915` and must remain absent.
5. Run one independent read-only receipt/setup/history check within **120 seconds**, then one preservation audit within **300 seconds**. Preserve ten predecessor packages (**12,606 indexed files**), protected source inputs and unrelated checkout work. Each native/preservation operation traverses live history once; the independent check verifies the frozen paths, hashes and union. Preserve all 145 registered history paths and the original unused 512 values. Concurrent UI exceptions are limited to checkout comparison under `LL/src/Presentation/ll/`, `docs/ui-rework-verification/` and exactly `UI_REWORK_IMPLEMENTATION_PLAN.md`; fixed protected inputs take precedence.
6. Update the seven active handoff documents and add a measured request review. Charge baseline/capture and measured diagnostics, **60 seconds static work**, **120 seconds sealing**, and **60 seconds final request creation/verification**. Include every separately measured correctness/build pass in the cumulative future setup basis. Seal final code/document copies and all new package files; reserve a **2 MiB metadata allowance** and count whole changed checkout files/shared TRX toward 4 GiB. A failed diagnostic stops dependent work without retry.
7. After sealing, create exactly one external `TestResults/balance/tower-complete-family-reservation-request-20260915.json` using CreateNew. Its Action is ReserveAndBind, but **only write and read the file; never invoke the allocator**. Bind complete sealed setup/preservation/binding/current-package inventories, manifests, captured identity, allocator metadata, unchanged history, prospective study path and the final cumulative setup time basis. The wrapper will automatically bind the request's own bytes at execution. Independently validate every listed hash, identity/cap field, the exact package inventory, request bytes and absent study; print the final receipt without creating additional unbound receipt files. This phase changes only action, completed setup inventory and accumulated setup time relative to the native CheckOnly input; it does not claim the final expanded request was itself executed natively.

Only the actual creation of the 288 future values conflicts with the user's original zero-fresh-seed limit. Present the completed request and the precise reservation-only action for confirmation after all permitted work is complete. Keep full-study execution excluded. Reliability **Fail 1/3**, adoption **Hold**; original v19 remains Unresolved with no confirmation inside that experiment. Whole-study throughput and full-family second-stage capacity remain unverified. No gameplay/content/deployment changes.
