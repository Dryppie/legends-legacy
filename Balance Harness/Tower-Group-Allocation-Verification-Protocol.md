# Category-allocation verification after setup correction

15 September 2026. Target: offline BalanceHarness. The user authorized completing the [stopped implementation verification](Tower-Group-Allocation-Implementation-Review.md). Preserve that sealed package unchanged. This separate scope corrects its preflight to count 85 Fact methods plus 25 InlineData cases, retaining the intended **110 backend cases**. Static review also found an unrun serialization assertion matching the word `allocation` inside the route value; change only that assertion to check whether the JSON property exists. Production source remains identical to the failed package's captured source. No test or build was executed in that package.

## Frozen workload

Retain the [implementation contract](Tower-Group-Allocation-Implementation-Protocol.md), its 15 new test cases and the 95 existing cases. Before execution, freeze protocol, source, corrected fixture, scripts, reference inputs, captured gameplay DLLs and unrelated dirty-checkout hashes. Verify all 36 predecessor packages, including the setup failure. Build one isolated candidate and test assembly against captured gameplay DLLs and cached restore metadata. Run the seven named test classes once through `build/run-tests.ps1`, with exactly 110 runtime cases required to pass. No engine callback is permitted; synthetic test scores are zero-combat fixtures.

One content command constructs exactly 19 requests for each of count, variation, diversity, completion and allocation: **95 requests total**. Match all 76 old choices exactly against the sealed completion-verification output. Use the same captured construction inputs and existing diagnostic label 17. Reconstruct the latest saved completion search's **44 evaluations once** using only archived measurement callbacks, requiring the exact complete generation hash. Also produce one metadata-only allocation sweep across the same 214 groups, at most 1,070 guided positions, without constructing those teams. One independent Python audit reconstructs group order, category eligibility, schedule, completion steps, legal recipes and fixed order; verify unchanged old outputs and the TRX. Persist existing performance trace measurements.

## Bounds and completion

Zero fresh seed values, fights, combat preparations, combat replays and retries. Carry **1,199.726 diagnostic seconds** and **1,600,627,342 bytes** from the failed implementation package, including its closure. New diagnostics, including setup/tests/construction/audit/publication, at most **180 seconds**, each phase at most 60 seconds. Builds are separately measured engineering work, at most 300 seconds each. New retained output at most **256 MiB**; cumulative bounds remain **1,800 seconds and 4 GiB**. A failed check stops dependent work; preserve evidence without retry. Prior failures stay charged and sealed.

Complete a measured report and six active Markdown handoffs. Keep the new policy explicit opt-in; changing which Essence gets a slot does not change ability execution order. No control-specific Essence preferences, new strength claim, gameplay tuning, defaults, configuration, migrations or deployment. Preserve all **482,596 reservations**, v19's unused 512 and 253 recipes. Historical reliability Fail 1/3, deep recovery 0/3, sealed v19 Unresolved, adoption Hold. No further seeds, combat comparison or 129,536-fight confirmation is authorized by this verification.
