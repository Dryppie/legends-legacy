# Four-party native admission: proposed scope

The literal comparison adapter is complete. The next useful step is a zero-combat admission of the retained runtime and the four frozen party inputs. This document is a proposal; it does not authorize compilation, native preparation, seed allocation or combat.

## Concrete implementation boundary

Target the isolated Balance Harness tooling, with no production or gameplay changes. The historical pilot's `resolver.cs` points to `TestResults/balance/tower-incumbent-selection-20260916/runtime`. Its `study/executable-files.json` records 26 files and BalanceHarness.dll SHA-256 `4cbb6bd5dd03205ea136533ea6ee8ad054a48cef4fe26322ea4eb4d8830c0202`. The resolver establishes a candidate runtime location, not its present integrity. Before loading it, verify the complete inventory, captured source binding, pinned content and dependency compatibility. Stop if any required identity or file is unavailable; do not rebuild production to repair admission.

Use an isolated small C# helper referencing that admitted runtime. Source inspection identifies `TowerBattleRunner.CreateInput` (line 37) and `PrepareAsync` (line 65) as the concrete input/materialization route. `PrepareAsync` rebuilds and hashes the input, then calls `WorldTowerCombatRuntimeFactory.CreateAsync`; `RunCoreAsync` (line 96) separately marks a battle and invokes simulation. Review the preparation call chain before invoking it. No generic four-party prepare-only CLI has been established by this inspection.

The helper must admit AA, BB, AB and BA from the frozen matrix and adapter inputs without modifying their full party contexts, recipes, gear, threat options, timestamps or fixed ability order. If a declared random value is necessary for preparation, use one explicitly identified, already reserved historical value solely as a preparation input. Record its provenance and preparation-only status. Do not allocate an independent comparison panel, run a battle or interpret preparation as an outcome.

Capture canonical native inputs and prepared participant identities/ability order. Compare them with the frozen recipe and captured-context contract, including all ten character slots, five Essence assignments per character, legal duplicate/capacity rules, neutral identity treatment and the absence of contributions. The adapter's Python fingerprints are not native prepared-actor hashes. Record both identities without conflating them. Preparation success alone cannot authenticate a future outcome archive.

## Proposed once-only authorization

Transfer **90 seconds and 8 MiB from run to engineering**, leaving overall cumulative caps unchanged. Proposed component caps become engineering **1,695 seconds /680,525,824 bytes** and run **1,605 seconds /359,661,568 bytes**; audit is unchanged. This transfer is not applied by this document.

Within that ceiling, permit source/helper implementation, one isolated helper compilation using installed dependencies, focused zero-combat checks and native preparation of the four fixed parties. Run any backend tests through `build/run-tests.ps1`. Use the existing owned-process guard, storage accounting and cleanup reserves. Count failures and temporary/compiler outputs. Stop on the first unavailable prerequisite, mismatch or exhausted cap; no automatic retry, production rebuild, dependency installation, scope extension or combat fallback.

Before execution, freeze the helper, runtime/source/content inventory, selected already-reserved preparation input, expected four-party identities, command list and phase ceilings. Finish with four explicit admission results, an independent comparison against frozen inputs, hashes, an attempt statement showing zero combat and zero newly allocated values, a resource receipt and preservation evidence. A failure ends this admission attempt and identifies the unmet prerequisite.

Successful admission still leaves independent-panel authorization, the frozen 1,024-fight native run envelope, and completed native archive/attempt-ledger authentication outstanding. The future four-party comparison and all statistical thresholds remain unchanged. No stronger party is established here.

## Inspection and accounting

This scope only read repository instructions, the original request, prior receipts/preflight, selected harness source and selected historical binding/runtime references. A source-search command named the nonexistent `TowerCompactRun.cs` and returned exit 1; that failed read was retained in the conversation and is included in the charge. No build, test, benchmark, native preparation, seed allocation or combat ran. There are no migrations, configuration or deployment changes.

Only this new review and its new companion evidence package were written. Existing source, reviews and sealed packages were not edited. This scope conservatively charges the remaining **1.5258801385061815 engineering seconds** and **128 KiB** for bounded inspection/publication. It leaves **zero engineering seconds /312,056 engineering bytes**, and **1,747.2227276443576 overall diagnostic seconds /91,622,670 output bytes**. Carried run/audit consumption and every prior charge remain intact. The proposed transfer requires authorization because the preceding adapter allocation is exhausted and the original analysis request excludes builds/native execution.

All 483,732 exclusions, V19's 253 required recipes and 512 unused confirmation values, V19 Unresolved, adoption Hold, and closed experiment decisions remain preserved.
