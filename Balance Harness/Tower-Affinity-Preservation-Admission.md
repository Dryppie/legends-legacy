# Affinity preservation: runtime and resource admission

24 September 2026. Target: the offline `LL/tools/BalanceHarness`.

**The v3-versus-preserving-v4 proposer comparison is admitted for one future execution.** The [sealed admission](../TestResults/affinity-preservation-admission-20260924/admission.json) is `ProposalStudyAdmittedNoReservation`. Qualification completed in **197.969 seconds**, retaining **144,897,249 bytes**. It ran no battles, drew no fresh values and made no production reservations. This admission qualifies inputs and resource planning; it does not establish that preservation improves search.

The [declaration](Tower-Affinity-Preservation-Admission-Declaration.json) was pinned before qualification and fixed a separate **900-second /1-GiB** allowance, charged in full on success or failure, with no retry or timing resample. Its SHA-256 is `7ac038625d68f4e4c1a75e03eefbef3138f2299ed2520127bb9191296cd8692b`. The [comparison implementation](Tower-Affinity-Preservation-Comparison-Implementation.md) and prospective plan remain unchanged.

## Qualified runtime and inputs

The candidate is the previously tested harness assembly `d8a24014c3c12030b65d658635e2fc0dde0991e9046b5c26826bc13c680f981d`. Its matching portable symbols authenticate **227 producing source documents**, including generated sources. Qualification resolved **933 methods**, including the preservation plan and paired-outcome checks, both proposal policies, the racing controller and benchmark-validation gate.

The package retains the preceding benchmark-validation admission's captured content and gameplay dependencies. Only the four tested harness build files replace their captured counterparts. Platform and non-harness assembly identities match. The production runtime-retention path also passed, and all **27 runtime files** are pinned.

Qualification covers **88 saved recipe instances**: the four legacy export arms contribute 48; both complete two-wave original/preserving arms contribute another 40 from the frozen development root. Each instance uses the first and last declared historical schedule values in both runtimes, producing **352 native preparations**. Every materialized-input and prepared-participant hash agrees. A combat guard remains active throughout preparation. Duplicate physical recipes retain their separate saved arm instances; 88 is an instance count, not a diversity claim.

The current harness reproduced all four legacy export arms and both preserving-preview arms exactly, including both proposal waves. It rebuilt the typed inventory from the captured content and matched the recorded inventory. No prospective proposal roots were drawn, previewed or screened.

The complete authoritative history scan and native input check agree on **743,892 permanently excluded values across 256 files**. Historical files and pending recovery receipts were authenticated, then the registry was rescanned before sealing. No ledger was altered and the scientific output directory does not exist.

## Frozen design and resource decision

The [repository plan](Tower-Affinity-Preservation-Comparison-Plan.json) remains byte-for-byte unchanged. The [qualified package plan](../TestResults/affinity-preservation-admission-20260924/plan.json) changes only its scope binding for the refreshed history and current qualified runtime. Its native plan hash is `65940aab720746e5f4a5005c43978f240b10abe65efae40a3995be1cc8dd0dff`; the original native hash is `8a961c57bdefb329222bf238ffdf6880d30cb44059e9633a5cf41ce059ce5c9d`. Candidate/control policies, benchmark parent, affinity IDs, inventory, panels, budgets and decision rules are identical to the prospective design.

The comparison retains twelve paired roots, 528 search fights per arm/root, shared racing/nomination/validation panels and independent 256-value held-out panels. It requires **4,380 fresh values** and freezes all 24 complete search outputs before held-out evaluation. If execution proceeds, the owner retains every exposed value from the single declared 16,384-value entropy block. No refill, root replacement or policy adoption is permitted.

The forecast takes upward-only maxima of the preceding admission, the frozen audit-cost probe, current native preparations and the preserving owned fixture. It also includes twice the completed benchmark-validation pilot's phase costs, scaled from 16,768 to the 21,888-fight ceiling, with upward-only preservation/validation fixture ratios. Faster new fixture timings do not reduce prior floors. Storage floors are applied before scaling audit hashing. The factor-of-two margin and 120-second publication reserve remain.

| Partition | Forecast seconds | Hard seconds | Forecast bytes | Hard bytes |
| --- | ---: | ---: | ---: | ---: |
| Native work | 6,693.288 | 9,000 | 4,960,096,519.11 | 5,905,580,032 |
| Audit/publication | 1,715.798 | 1,800 | 34,989,702.00 | 536,870,912 |
| Total | 8,409.085 | 10,800 | 4,995,086,221.11 | 6,442,450,944 |

Both partitions fit; audit forecast headroom is **84.202 seconds**. These remain empirical estimates, not guaranteed upper bounds. Exhausting either hard partition invalidates an incomplete execution without borrowing or retry. The [resource forecast](../TestResults/affinity-preservation-admission-20260924/resource-forecast.json) retains all floors, ratios and limits.

## Changed files and verification

- [prepare-affinity-preservation-admission.py](analysis/prepare-affinity-preservation-admission.py): the new pinned, bounded admission controller, preserving the existing history and resource safeguards.
- [affinity-preservation-admission-context.ps1](analysis/affinity-preservation-admission-context.ps1): guarded native preparation, symbol/source authentication, generation replays, runtime retention and scope-only plan binding.
- [test-affinity-preservation-admission.py](analysis/test-affinity-preservation-admission.py): 26 boundary tests covering input drift, missing waves, replay coverage, source pins, no borrowing and upward-only forecasts.
- The declaration, this report, nine current-status lines, the sealed admission package and [verification handoff](../TestResults/affinity-preservation-admission-verification-20260924/closeout.json). No harness or gameplay source changed in this step.

`python -B -X utf8 "Balance Harness/analysis/test-affinity-preservation-admission.py"` passed all **26 tests**. The PowerShell helper passed syntax parsing, and the single declared admission run completed through the Windows Job process owner. Native input validation, independent Python audit of the retained preserving fixture, production retention and final launcher admission validation all passed. The existing **167 backend tests and 37 archive tests** remain pinned; no backend rebuild or rerun was needed because backend code is unchanged. No requested verification command remains blocked.

The previous 81-member handoff was authenticated before changes. All 41 historical pins and nine historical document bodies were checked afterward. The implementation report, original plan and scientific evidence remain intact. The package manifest SHA-256 is `607b32435968ca9ea96e35234b5246aa0a359ef9b178521d7719b2b57d6b1109`; the request file SHA-256 is `4d0dcc66e31a6e9d96530a07fbe145a28b4bfdf6738a01aaea6305d6a8e65ae6`.

## Accounting and next step

This admission adds its measured **197.969 seconds /144,897,249 bytes** to recorded cumulative work, now **33,732.922 seconds /27,297,992,772 bytes**. Its full 900-second /1-GiB allowance increases declared cumulative maxima to **77,880 seconds /49,543,118,848 bytes**. Unit tests and handoff documentation remain separate engineering verification. The future pilot has not started and is not charged as an executed campaign.

The next step is one bounded execution of this admitted twelve-root comparison, followed by both audits and a review against the unchanged decision rules. Revalidate the pinned package and live history at launch. The limits remain **21,888 fights, 10,800 seconds and 6 GiB**; incomplete work retains its charges and exposed values. The prior benchmark-validation pilot remains `Inconclusive`, and the recognition diagnostic remains `CompleteDiagnosticOnly`.

No migrations, application configuration changes, deployments or default-policy changes are involved. Unrelated working-tree changes were preserved.
