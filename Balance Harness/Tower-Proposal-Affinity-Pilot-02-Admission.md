# Proposal-affinity pilot 02: repaired runtime qualified and admitted

**Subsequent execution — 23 September 2026:** The [single admitted pilot 02 study](Tower-Proposal-Affinity-Pilot-02-Execution.md) completed 17,536 fights and all audits, reaching `AbandonThisConfiguration`. This admission and its launch command are consumed and must not be reused. The remainder records the historical admission state.

23 September 2026. Target: the offline Balance Harness.

**Pilot 02 is admitted for one owned scientific launch. It has not launched.** The [sealed admission](../TestResults/proposal-affinity-pilot-02-admission-repaired-20260923/admission.json) binds the repaired runtime to a separate output and scope, `proposal-affinity-pilot-02`. No production entropy, new values, reservation or combat occurred during qualification or admission. The complete registry remains **672,220 values across 246 files**. This is engineering readiness, with no efficacy or adoption conclusion.

The [pilot 01 failure](Tower-Proposal-Affinity-Pilot-01-Execution.md) remains closed and fully charged. Its missing-PDB repair is now qualified against the captured gameplay dependencies. Only the four tested harness files replace the original capture; all 23 other runtime files remain unchanged. The baseline and repaired runtime produced identical native inputs and prepared participants for **72 historical cases each**. Qualification also reproduced all three saved preview arms, resolved native entry points and authenticated **222 producing source documents** against the matching DLL/PDB. Only `TowerProposalStudyProtocol.cs` and `TowerProposalStudyRun.cs` differ from the consumed resource-v2 admission's production sources.

Admission now exercises the actual `TowerProposalStudy.RetainRuntime` method before it can approve a request. The [native retention certificate](../TestResults/proposal-affinity-pilot-02-admission-repaired-20260923/retention-qualification.json) and independent filesystem inventory agree on **all 27 runtime files**, including `BalanceHarness.pdb`, totaling **20,939,629 bytes** under one 64-MiB copy limit. A permanent combat guard remains active throughout qualification. No scientific worker or allocation method is invoked by this check.

One failed admission is preserved. The [first declaration](Tower-Proposal-Affinity-Pilot-02-Declaration.json) qualified the runtime, then failed in the new PowerShell reflection wrapper because command-produced paths remained wrapped `PSObject` values. It stopped before calling the runtime copier, native input check or scientific launch. Its [failure receipt](../TestResults/proposal-affinity-pilot-02-admission-20260923/failure.json) records **57.906 seconds** and the full **900-second /1-GiB** charge. The wrapper now places typed arguments into a CLR array before invocation. An owned integration check verified the corrected wrapper against the real retained runtime.

The failed admission's [read-only closeout](../TestResults/proposal-pilot-02-admission-failure-closeout-20260923/verification.json) sealed its complete inventory, verified its successful qualification and rescanned the full history. It used **48.125 seconds /99,973 retained bytes** against a separate **120-second /64-MiB** allowance. Its external manifest pin is `642f6333b073ec35c60742ea4714bf635bd9477692a8f24b925041ac604034c0`.

The [repaired declaration](Tower-Proposal-Affinity-Pilot-02-Repaired-Declaration.json) creates a new admission package and charge. It reuses the sealed input/participant projections, producing-source proof and timing samples from that failed admission. **No preparation timing or native audit-cost probe was repeated.** It rejects changed history, runtime or producing sources before reusing qualification. The corrected retention check, native input verification, independent legacy-fixture audit, full history refresh and final package authentication all passed. Every new child process exited successfully with no active descendants. The successful admission took **143.515 seconds** and retained **114,917,110 bytes**.

The scientific design remains frozen: twelve roots, two policies, 528 search trials per arm, 256 held-out values per root, 3,948 assigned values from one 16,384-word entropy batch, all 24 outputs frozen before held-out observation, and at most 21,888 fights. Decision thresholds, the benchmark party and no replacement/refill/resume/retry rules are unchanged. Only the plan's scope binding changes. Resource envelope v2 remains **9,000 native seconds plus 1,800 seconds shared by both audits and publication**, with **5.5 GiB native plus 0.5 GiB audit storage**.

The [resource forecast](../TestResults/proposal-affinity-pilot-02-admission-repaired-20260923/resource-forecast.json) is **4,239.253 native seconds /1,301.008 audit seconds**, with approximately **3.195 GB** projected total storage. Every consumed admission estimate is retained as a floor. The frozen probe's doubled timing margin, 120-second publication reserve and upward-only archive scaling remain in force. These are planning estimates; hard limits still invalidate incomplete work.

| Recorded allowance | Seconds | Bytes |
| --- | ---: | ---: |
| Through pilot 01 failure closeout | 15,360 | 11,140,071,424 |
| Failed pilot 02 admission | 900 | 1,073,741,824 |
| Failed-admission closeout | 120 | 67,108,864 |
| Successful pilot 02 admission | 900 | 1,073,741,824 |
| **Recorded charged total** | **17,280** | **13,354,663,936** |
| Future pilot 02 scientific allowance | 10,800 | 6,442,450,944 |
| **Cumulative maximum including future study** | **28,080** | **19,797,114,880** |

No failed allowance is refunded or transferred. These are declared charges, not measured resource consumption. Development builds, literal fixtures and wrapper/unit tests remain separate engineering work.

**All 26 Python admission tests pass**, covering historical qualification, resource partitions, immutable manifests, pilot identity, prior charges, exact symbol retention and rejection of changed evidence during reuse. The four hyphenated test scripts were invoked directly; an initial discovery command selected zero tests. The [successful test log](../TestResults/proposal-pilot-02-repaired-admission-tests-20260923.log) is retained. PowerShell parsing and the real owned retention integration check passed. The declaration also authenticates the existing **34 repair backend tests**, **184 baseline backend tests** and both updated owned completion/failure fixtures. C# was unchanged during this work, so no additional backend build was needed. No required verification remains blocked.

Changed implementation files are [the versioned admission driver](analysis/prepare-proposal-affinity-resource-admission.py), [the native qualification wrapper](analysis/proposal-affinity-admission-context.ps1) and [seven new admission regression tests](analysis/test-proposal-affinity-pilot-02-admission.py). Two declarations, this report and current-status links record the separate attempts. Historical packages and the original scientific plan remain intact. There are no migrations, application configuration changes, gameplay-default changes or deployments.

The current [admission manifest](../TestResults/proposal-affinity-pilot-02-admission-repaired-20260923/files.json) has external SHA-256 **`e5172f3b6b3db3ddcbe2e4a68cdd96eeb04e6822a3e65d12078676b1da7979fa`**. Request SHA-256 is `f04d9dccf86015be6a4d1b6fc6ed01a7eea219007e56d9b16d8a181ef6fa3616`. Harness SHA-256 is `aeec5f125fdccc0a682a93fb053ba90e19f5ec09627c60101a5983264e1e272f`; execution hash is `71de7999d4af2c9ab3398d3c46a31f8a3d72e92d6c22ee608f4558f352eb8cd5`. The original plan file remains `f7840ded410c34a3283fd3424ec18af98cfc2057cee76181b02dfddf51666188`; the newly bound plan file is `3c3339a7da7c5b7be344cc6df9d6a54b64a1bb0fd9d5c5cc4c5cd2c59c7c9d3c`.

The [handoff](../TestResults/proposal-affinity-pilot-02-handoff-20260923.json), SHA-256 `cd121f37f8dadaf5e28180be66028bdedde6171429088e110b6f1c824454255f`, records the pins, accounting and verification. The next step is the single scientific launch below. The output must remain absent, and the launcher checks live history before allocation. **This command has not been run:**

```powershell
& 'C:/Users/HrHoe/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe' -B -X utf8 TestResults/proposal-affinity-pilot-02-admission-repaired-20260923/run-proposal-affinity-study.py --request TestResults/proposal-affinity-pilot-02-admission-repaired-20260923/request.json --harness TestResults/proposal-affinity-pilot-02-admission-repaired-20260923/runtime/BalanceHarness.dll --admission-pin e5172f3b6b3db3ddcbe2e4a68cdd96eeb04e6822a3e65d12078676b1da7979fa
```
