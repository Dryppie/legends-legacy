# Local V4 audit closure

16 September 2026. **VerifiedSavedEvidenceClosure**. Local V4 engineering verification is complete: 69 existing backend facts verified, V1–V3 parity retained, and the saved-output audit closed.

The corrected reader selects exactly one `local-refinement.construct` row by path. Four pure-reader fixtures passed, including reversed rows and rejection of missing/duplicate paths. The saved construction measurement remains **108.4539 ms for 6,400 checks**; this is historical fixture evidence, not a new benchmark.

Verified 137 verification-package files, 44 predecessor files, 384 original source/dependency pins and 21 final binary files. The saved TRX contains exactly 69 distinct passing backend facts, and all original build/test process receipts completed without timeouts or surviving children. The recorded invocation used `build/run-tests.ps1`. Git's whitespace check and a direct check including untracked implementation files passed. The checkout audit covered 760 unrelated files; any concurrent changes are recorded separately.

The original stopped freeze and failed reporting audit are still sealed and unchanged. This separate receipt completes their outstanding engineering checks. No old command was retried; no build, backend test, generator, allocator, preparation service or combat engine ran in this scope.

Changed files: new closure protocol/review and saved-evidence reader; the local-refinement review and six active Markdown handoffs. No harness/gameplay code, configuration, migrations or deployment changes.

Run the once-only phases as `python -B TestResults/balance/tower-local-refinement-audit-closure-20260916/workflow.py setup`, then `audit`, then `publish`. The [protocol](Tower-Local-Refinement-Audit-Closure-Protocol.md) freezes checks and limits; the [completion receipt](../TestResults/balance/tower-local-refinement-audit-closure-20260916/completion.json) records cumulative resources. The [audit receipt](../TestResults/balance/tower-local-refinement-audit-closure-20260916/audit.json), [reader fixture results](../TestResults/balance/tower-local-refinement-audit-closure-20260916/reader-fixtures.json) and [whitespace command/results](../TestResults/balance/tower-local-refinement-audit-closure-20260916/control/diff-check.json) retain the evidence. Closed directories reject repeats; reproduction needs a new accounted output scope.

Limits of the conclusion: V4 has not been measured in combat. Synthetic tests establish construction legality, determinism and workflow behavior, not stronger teams or guaranteed completion on real content. The old V3/baseline finalists both won 0/32. No strength or whole-run speedup claim is made.

Prepare a separately frozen real-input V4-versus-baseline comparison; seed allocation and combat require explicit approval plus sufficient output allowance. No further builds or tests are needed to close this audit.

All 482,956 reservations remain excluded, including the failed V3 allocation's 45 unused values, earlier failure's 40 and V19's separate 512 unused values and 253 recipes. Ability order remains fixed. V19 reliability Unresolved; later Fail 1/3, deep recovery 0/3; adoption Hold.
