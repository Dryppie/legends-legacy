# Practical retained-composition pilot: readiness stopped

**The authorized pilot stopped at readiness: 21 checks passed and four failed. No fresh production values were derived or reserved, and no combat ran.** The agreed gate worked: an invalid exact-budget execution path was detected before binding or launch. All 483,046 previously reserved values remain preserved. The unchanged comparator has no new combat-quality result; adoption remains Hold and V19 reliability remains Unresolved.

The user authorized the [frozen pilot](Tower-Retained-Practical-Pilot-Protocol.md), including the 100-second /64-MiB transfer from unused run allowance to engineering. That transfer is now applied prospectively: engineering 775 seconds /336 MiB, run 2,525 seconds /656 MiB, audit 300 seconds /32 MiB. The earlier planning overrun remains charged. The [completion receipt](../TestResults/balance/tower-retained-practical-pilot-20260916/completion.json) is the current accounting authority. Overall limits remain 7,980 diagnostic seconds /5,804,916,736 bytes.

The isolated package assembled the existing standalone harness with its pinned gameplay dependencies, reproduced the historical native admission, and checked executable retention from that location. The producing identity did not change. The exact pilot's direct validation correctly reports 512 discovery +128 selection +768 confirmation =1,408 fights. Native admission at the older loose maximum did not establish that this exact-budget study could execute.

| Failed check | Cause | Consequence |
| --- | --- | --- |
| Normal synthetic study dispatch | `TowerBossImprovement.Inputs` replaces the one retained method with both independent methods while projecting inputs, then validates that temporary definition. Discovery doubles from 512 to 1,024, producing a fictitious 1,920 total above the 1,408 cap. | Production-path blocker before study execution. |
| Cancelled synthetic study dispatch | The same projection fails before reaching the cancellation fixture. | Cancellation coverage for this precise definition is incomplete. |
| Complete history reconciliation | Registry paths use Windows separators; the authoritative lookup key retained a forward slash. The dictionary lookup failed after the registry scans. | Full live union reconciliation was not completed; no allocation could proceed. |
| Durable attempt-counter fixture | `File.ReadAllLines` opens with sharing that conflicts with the active writer. | Test-reader defect; durable launch/counter coverage is incomplete. |

The passing checks cover the 12 current retained-standalone parity/version/exhaustion cases, literal synthetic reservation collisions and Pending interruption states, mismatched authorization, the seven-component improvement rule, and seven scientific-exit cases. The above-ceiling winner fixture remains eligible for a search improvement finding. This does not override the separate balance ceiling. Synthetic ledgers are outside the production history registry and retained inside a ZIP; they cannot pollute future reservation scans.

The [source patch proposed for review](../TestResults/balance/tower-retained-practical-pilot-20260916/proposed-input-projection-fix.patch) extracts the existing input-copy operation. Both callers validate the original experiment before copying its generator inputs; the independent public entry still rejects supplied mode. Improvement input creation no longer invents different methods for validation. This preserves the numerical cap, search policy, mutation/retention logic, order, selection rule and stage schedules. The patch has **not been applied, compiled or tested**. It changes the harness executable identity, so the protocol's identity-preservation gate requires review before continuing. Raising `MaximumBattles` to bypass the error would change the approved envelope and is not proposed.

Two corrected launcher-source candidates are also retained: normalize the authoritative path with `Path.GetFullPath` in both readiness and binding; open the counter fixture's reader with `FileShare.ReadWrite`. These are review candidates, not replacements for the failed frozen sources. A future package must additionally admit and pin the deliberately rebuilt harness identity, rerun the complete exact-budget workflow and preserve all current failures. The failed package's `readiness.json` explicitly prevents its binding and launch commands.

**Proposed continuation:** approve the [specific repair and revalidation](../TestResults/balance/tower-retained-practical-pilot-20260916/repair-proposal.json), capped at 75 diagnostic seconds /64 MiB, with a further 75-second /48-MiB transfer from unused run allowance. Engineering would become 850 seconds /384 MiB and run 2,450 seconds /608 MiB; overall and audit limits stay fixed. This authorizes only the described harness identity change and readiness corrections, followed—only if every gate passes—by the original one-shot pilot of at most 297 fresh values /1,408 fights within its unchanged 1,200-second /600-MiB run and 180-second /16-MiB verification ceilings. No search variant, automatic retry or extra sample is proposed. No part of this continuation has been executed.

Verification performed: isolated offline restore and Release build passed; `build/run-tests.ps1 -NoBuild -ArtifactsPath TestResults/balance/tower-retained-practical-pilot-20260916/tests -Filter 'FullyQualifiedName~PilotTests|FullyQualifiedName~BalanceHarnessRetainedStandaloneTests'` ran all 25 cases with the result above. The process tree exited and was verified empty. The patch's raw context check failed on line-ending differences; `git apply --check --ignore-space-change` passed without applying it. Source pins, prior package seals, unrelated dirty files and scoped whitespace checks are recorded in the final preservation receipt. Full history reconciliation, actual reservation, study execution and saved-study reconstruction could not proceed past the failed readiness gate.

Changed files are this review, the README and five handoff notices, plus the isolated launcher, tests, runtime copies, proposed corrections and evidence under `TestResults/balance/tower-retained-practical-pilot-20260916`. Active harness/gameplay sources remain unchanged. No migrations, persistent configuration changes or deployment occurred; copied settings apply only to the isolated admission fixture.
