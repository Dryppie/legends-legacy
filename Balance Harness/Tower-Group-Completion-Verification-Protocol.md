# Corrected group-owner completion verification

15 September 2026. Target: offline BalanceHarness. The user authorized correction of the family-conflict fixture and a separately frozen zero-combat verification. The prior implementation package remains sealed at `4784d42f7596ee00a5c52989d56ba755d69468d99a7159905b7acd654147e6fb`, including its 94/95 result and failure. Derive the conflicting family from the reserved Essence and uppercase it. No production source or policy changes are part of this correction.

Freeze source, corrected fixtures, captured gameplay inputs, native driver, independent audit, workflow and publication scripts before execution. Rebuild the isolated driver and test assembly from captured source and cached restore metadata. Run the same **95 backend cases exactly once through `build/run-tests.ps1`**. They comprise 79 existing cases and 16 completion cases. Synthetic evaluator repetitions are zero-combat tests; no allocator or combat engine is used. Stop dependent diagnostics if any case fails.

After passing tests, execute the previously unrun captured-content driver once: first **19 requests for each of group-count, variation, diversity and completion**, retaining every rejection and duplicate (76 requests total). Require exact equality of all 57 old choices. Reconstruct the archived diversity search's **44 evaluations once**, supplying its recorded measurements to callbacks; require exact equality of the complete generation result. This reconstructs search and does not run combat or score new outcomes. The independent Python audit must reconstruct every new completion decision/trace from metadata and verify legality, preserved group/count schedules and old generation parity.

The implementation retains authored category/provider selection, seeded diversity order, existing filler, shared copy/family/slot limits, fixed ordinal ability order and opt-in activation. Coverage counts do not establish combat benefit, self-targeting, uptime or optimal support selection. No choice may be adjusted in response to diagnostic output.

Caps: **zero fights, fresh seeds, combat preparations/replays and retries; 300 seconds new / 1,800 cumulative diagnostic workload; 512 MiB new / 4 GiB cumulative output**. Carry forward **851.801 seconds**, **1,225,009,465 bytes** of sealed prior output, and all preceding failures. Compilation is separate engineering time, each command limited to 300 seconds. Preserve all **29 preceding sealed packages** before and after. On failure or cap, preserve evidence, stop dependent diagnostics and report incomplete checks. Evidence closure may verify preservation and reconcile receipts; it must not rerun diagnostics.

Sequence: `workflow.py preserve`, `candidate-build`, `tests-build`, `tests`, `content`, `audit`, then `publish.py`. Every phase runs once and requires prior success. The frozen package is `TestResults/balance/tower-group-completion-verification-20260915`. Reproduction requires a new frozen package and carried accounting, never mutation or retry of sealed evidence.

Preserve candidate/attempt caps, durable attempt charging, cancellation, archive verification, deterministic ranking and nominations. Preserve **482,551 reservations**, v19's unused 512 and all 253 recipes. No gameplay/Kharad tuning, configuration, migrations, deployment, default promotion or 129,536-fight confirmation. Historical reliability Fail 1/3, deep recovery 0/3, sealed v19 Unresolved and adoption Hold remain unchanged. Previous fresh-seed exceptions are exhausted; this task authorizes no new combat.
