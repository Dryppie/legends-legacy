# Refinement comparison driver: corrected fixture verification

16 September 2026. Target: offline BalanceHarness diagnostic infrastructure. The user requested proceeding after the sealed process-fixture failure. Incoming [receipt](../TestResults/balance/tower-refinement-comparison-driver-closure-20260916/completion.json): **2,954.448469456 / 3,000 diagnostic seconds used**, **45.551530544 seconds remaining**. This separate scope allows **43 seconds / 32 MiB** within the unchanged cumulative **4 GiB** cap. Carry the exact sealed predecessor size and every earlier temporary-output charge. No reset, old-scope retry, combat, preparation, replay or fresh seed.

## Frozen change

Use the already prepared fixture correction, with complete process receipts saved **before every result assertion**, explicit UTF-8 log decoding and result values included in assertion errors. Require at least the deliberately launched process count, while retaining zero active job processes, exact exit/timeout behavior, captured output and descendant-ready markers. Lifetime process totals remain visible; do not assume or claim a cause for the earlier unsaved failing conjunct. The Windows job wrapper and all C# implementation/test source remain unchanged.

Reuse the previously passed **117 backend assertions** only after exact current source, captured executable/test binary and late-TRX verification against the sealed producing package. The original command was `build/run-tests.ps1`; the old timed-out scope remains FailurePreserved. No rebuild or backend-test repetition is necessary for unchanged source. The earlier eight-test preservation-helper receipt and unchanged helper remain in the sealed verification chain.

Evidence directory: `TestResults/balance/tower-refinement-comparison-driver-verification-20260916`. Freeze all scripts/protocol/input hashes before execution. Run once, sequentially:

1. **Freeze:** verify consumed predecessor manifests/files, root C# source, both captured binary inventories, backend entry point, reused 117-test TRX and ledger. Save dirty-checkout hashes and exact input pins.
2. **Process fixtures:** normal completion with quoted/Unicode arguments and stdout/stderr; nonzero exit 7; one-second timeout of root/child/grandchild; one-second timeout after the root exits with a child still running; expired deadline rejected before launch; missing executable rejected before launch. Six checks, no repeats. Persist each process result before assertions. Timeout cases require zero active processes and descendant-ready output; normal/nonzero cases retain their exit codes. Intentional timeout tests are negative-test inputs, not retries.
3. **Captured controller:** first execution of the unchanged, previously compiled native diagnostic, writing only to this new directory. Three fabricated cases: complete, 14/16 baseline, and 14/16 candidate. Require complete-result reconstruction, stop before the second arm on partial baseline and stop before screening on partial candidate. Preserve receipts, synthetic journals, stage calls, timing, CPU, allocations and peak memory. No generator, preparation or engine invocation.
4. **Independent audit:** unchanged audit of schedules, equal gameplay inputs, ranked nominations, selections/deduplication, paired outcomes, exact journal/manifests and the 482,821 reservation union.
5. **Preserve:** mandatory full verification of **74 predecessor packages**, including both recent failures. Use the unchanged legacy assertions and four-worker helper, with a fresh per-pass cache, inside the owned Windows process wrapper and an internal deadline. No verified publication without this pass.
6. **Publish:** check every required success receipt, source pins, unrelated-file preservation, whitespace and document links. Update six active Markdown handoffs and seal with measured time/output. On failure, stop dependent work and publish only failure evidence; no retry. Partial evidence cannot establish completed verification.

Phases before preservation share **15 seconds**: freeze at most 6, process fixtures 7, captured controller 9 and independent audit 4. Preservation gets at most 35 seconds, also bounded so all normal phases stop by **39 cumulative seconds**. Reserve four seconds for success/failure publication, including one closure second. Subprocess cleanup stays inside each phase envelope. Source editing is excluded; diagnostics, audits and publication are charged. Missing termination proof fails the phase.

```powershell
$python = 'C:/Users/HrHoe/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe'
$work = 'TestResults/balance/tower-refinement-comparison-driver-verification-20260916'
& $python -B "$work/workflow.py" freeze
& $python -B "$work/workflow.py" process-tests
& $python -B "$work/workflow.py" captured
& $python -B "$work/workflow.py" audit
& $python -B "$work/workflow.py" preserve
& $python -B "$work/publish.py"
```

Execute only after the preceding success receipt. On any failure use `publish.py failure`; never repeat against sealed/failed output. This wrapper targets ordinary CreateProcess descendants. It creates suspended, assigns before resuming, prevents breakaway and checks job emptiness. A catastrophic parent exit before assignment can leave a suspended child; normal cleanup terminates it without resuming. The scope does not test WMI launchers or universal process containment.

Preserve all **482,821 reservations**, including the original unused 512 v19 values. V19 retains 253 recipes and Unresolved status. Reliability Fail 1/3, deep recovery 0/3 and adoption Hold remain unchanged. No Kharad tuning, ability-order optimization, configuration change, migration or deployment. Real compact-runtime integration, fresh-seed allocation and combat remain separately gated. No search-strength or campaign-throughput claim follows from these synthetic diagnostics.
