# Fixed-team confirmation: frozen admission package

**Historical admission snapshot:** the user subsequently approved this exact run, capped-failure risk and separate engineering-accounting treatment. The [completed fixed-team confirmation](Tower-Practical-Fixed-Team-Confirmation-Execution-Review.md) is now closed with AdoptFixedTeam. The pending decisions, absent-output statements and proposal accounting below record admission-time state; sealed package files and their hashes remain unchanged.

17 September 2026. Scope: offline BalanceHarness request preparation and documentation. **The frozen request passed the public native check with zero combat and zero reservations.** The package is sealed; a gameplay decision and the explicitly disclosed engineering-accounting treatment remain pending. Adoption is **Hold**, both anchors remain recommended, all **486,374 exclusions** remain, and V19 retains **512 unused values /253 required recipes /Unresolved**.

The [admission receipt](Tower-Practical-Fixed-Team-Confirmation-Admission.json), [proposed request](../TestResults/fixed-team-confirmation-admission-20260917-03/request.proposed.json), [exact definition](../TestResults/fixed-team-confirmation-admission-20260917-03/definition.json), [accounting disclosure](../TestResults/fixed-team-confirmation-admission-20260917-03/accounting.json) and [pending decision](../TestResults/fixed-team-confirmation-admission-20260917-03/decision.json) are the current handoff. The [frozen scientific plan](Tower-Practical-Fixed-Team-Confirmation-Plan.md) and [293-test implementation review](Tower-Practical-Fixed-Team-Confirmation-Implementation-Review.md) retain their original evidence and scope.

## What is frozen

The request binds the exact candidate `399bc776…`, anchor 040e and anchor 49f6, their full seed-free scenarios and execution order, all 16 content hashes, settings, and the tested producing execution. The unchanged design uses **5,500 paired trials per team /16,500 total attempts**, six consecutive slices per team, family seven, and one final strength decision. The native 1,000-seed scenario limit remains unchanged. There is no search or selection stage.

The retained executable has **26 files**. Its .NET 10.0.12 /Windows 10.0.26200 /X64 identity matches the successful implementation tests, including BalanceHarness assembly SHA-256 `d728747ab6a9332d2e24c4d38e880f7272465a5152dc5263c0a7e65b53b23a49`. The new execution hash is `32fc85b3ee4f6dd14a2c0c068599e4ae7345d41d94e01d92491144507e72c36e`. Existing binaries were copied without rebuilding. **183 source pins** bind the reviewed implementation and existing harness sources. Raw application settings were not copied; the package retains the safe effective settings and a source-file hash.

Complete recursive history discovery pins **204 files**, including nested historical copies and Pending sources. One supported older recovery receipt is explicitly bound. Native validation resolves the full **486,374-value** union against the definition; an after-check scan verifies unchanged membership and hashes. No older Pending input is ignored or rewritten. The future output remains absent, and no new reservation artifact or entropy batch exists.

The native request hash is `1c9b66d1f67ba26305c70258275f230bd8468f25d3af21df5b5aefd2e93b91d7`. File-byte hashes, package manifest/receipt identities and all paths are in the machine-readable admission receipt. Native request identity and file-byte identity are separate hashes.

## Checks and preparation costs

The final public `tower-fixed-team-confirmation-check` passed in **17.281 seconds** with status `ContractValidNoReservation`, 18 transport bindings, maximum 16,500 fights and maximum 11,000 new reservations. Its owned process tree exited successfully and was empty before completion. It prepared the three exact native teams, authenticated content/settings/runtime, and checked complete history. It did not run combat or reconstruct an old battle archive.

The final package sealed in **119.063 seconds**, retaining **25,866,409 bytes (24.67 MiB)**, inside its prospectively declared **300-second /64-MiB** preparation cap. The whole cap remains charged. These observations establish admission validity, not native combat or full-publication feasibility.

Two earlier preparation scopes failed and are preserved:

| Scope | Outcome | Full charge |
| --- | --- | --- |
| [Preparation 01](../TestResults/fixed-team-confirmation-admission-20260917/failure.json) | External oversight stopped the inefficient full-report metadata scan at its work deadline, before any native command. | 120 seconds /64 MiB |
| [Preparation 02](../TestResults/fixed-team-confirmation-admission-20260917-02/failure.json) | Native check passed in 17.062 seconds; the closing registry scan then reached the package deadline. This package is not admitted. | 120 seconds /64 MiB |
| [Preparation 03](../TestResults/fixed-team-confirmation-admission-20260917-03/preparation-receipt.json) | Sealed successfully after complete pre/post scans and native validation. | 300 seconds /64 MiB |

The final scanner uses four bounded directory workers and Windows enumeration metadata, preserves the native directory bound, rejects reparse points, checks deadlines during traversal, and authenticates every matching ledger. Two intermediate read-only scanner checks reached their 15/45-second engineering deadlines; neither launched native work. Preparation 03's larger cap was declared before it started. Failed directories and their original scripts, requests and logs remain sealed and are authenticated by the final reader. There were **two public zero-combat native checks total**, zero gameplay attempts, zero entropy calls, and zero new permanent reservations across all three scopes.

## Accounting and proposed run

| Operational scope | Charged or proposed seconds | MiB |
| --- | ---: | ---: |
| Previously closed diagnostic/probe/preparation chain | 1,860 | 1,088 |
| Failed preparation 01 | 120 | 64 |
| Failed preparation 02 | 120 | 64 |
| Successful preparation 03 | 300 | 64 |
| **Carried operational charges** | **2,400** | **1,280** |
| **Fresh run proposal** | **2,400** | **1,024** |
| **Proposed cumulative operational ceiling** | **4,800** | **2,304** |

No earlier allowance is refunded or used to fund the proposed run. The request carries **1,342,177,280 prior bytes** and a proposed cumulative ceiling of **2,415,919,104 bytes**, leaving exactly **1 GiB additional allowance**.

**Engineering is unmetered, not zero.** No numerical cap was declared for implementation builds/tests or this increment's script authoring, read-only checks and documentation. The retained successful test sessions record 321.170 and 477.122 seconds, and the implementation evidence directory contains 1,528,734 bytes. Those observations omit other builds, failed tests, temporary output and engineering/review time; they cannot establish a complete historical cost. The accounting JSON therefore records engineering charges as `null`, with explicit incomplete-cost labels.

The concrete proposed treatment is to keep that unmetered engineering separately disclosed from the capped native-operation ledger. **Accepting this treatment remains a prerequisite for the run.** The numerical request is conditional on it and is not an all-in engineering-cost total. If that treatment is rejected, resolve the accounting disposition and issue a new request; do not invent a historical allowance, silently debit zero or consume phase headroom.

The fresh operational proposal includes enclosing validation/setup, runtime/content copies, reservation, combat, both built-in audits, publication, outer logs/oversight and closeout:

| Nontransferable phase | Seconds | MiB growth |
| --- | ---: | ---: |
| Admission/reservation | 180 | 128 |
| Combat/report retention | 1,440 | 768 |
| Native reconstruction, independent audit, publication | 600 | 64 |
| Closeout reserve | 2 | 4 |
| Remaining enclosing setup/oversight headroom | 178 | 60 |
| **Additional total** | **2,400** | **1,024** |

The frozen plan's 813.556-second /412.61-MiB sizing illustration, its doubled stress assumptions, and the implementation's literal-report fixtures remain supporting observations with their original limitations. The present native check adds an admission observation. None measures the full native 16,500-fight workload, complete journals and publication together. A launch must explicitly accept capped operational failure, enforce combined native/outer output accounting, and leave the phase limits unchanged.

## Decision and next boundary

The [deferred command](../TestResults/fixed-team-confirmation-admission-20260917-03/command-after-approval.json) is recorded but not executed. The next decision is one **40-minute /1-GiB additional run** of this exact 16,500-fight request, accepting the separate engineering-accounting treatment and terminal capped-failure risk. Revalidate package/source/runtime/content/recovery/history pins and absent output immediately before owned execution; include that work and outer oversight within the fresh envelope. No extra probe, standalone public native audit or replay follows from this package.

One 44,000-byte cryptographic batch can permanently expose up to **11,000 values**, giving at most **497,374 exclusions** at this history. The first 5,500 eligible values form the shared panel; eligible unused tail values remain reserved. Entropy interruption can leave blocking Pending history without a supported automatic recovery. No refill, retry, resume, sample extension, budget transfer or automatic follow-up is included.

Only complete agreeing native and independent audits can produce `StrongerFixedTeamConfirmed` /`AdoptFixedTeam` for the exact candidate. A complete negative result keeps `Hold` and both anchors. Incomplete evidence cannot pass. Balance remains `NotAssessed`, and search-method reliability/global optimality are outside this endpoint.

## Reproduction and files

The [admission reader](analysis/practical-fixed-team-confirmation-admission.py) verifies the sealed package, scientific definition, request/cumulative arithmetic, test/runtime/source bindings, historical archives, complete current registry, failed preparation evidence and saved native result without invoking native code:

```powershell
python -B -X utf8 "Balance Harness/analysis/practical-fixed-team-confirmation-admission.py" verify
```

The bundled Python interpreter was used. Do not rerun `prepare`: existing output is refused, and no preparation allowance remains open. The earlier historical planner is not rerun or rebound to the changed implementation source. Backend tests/builds were not repeated because this increment changes no backend source; the 293 passing implementation tests remain the relevant controller evidence.

Added the admission reader, review and JSON receipt; retained all three preparation packages and verification records; updated current Markdown handoffs. Final preservation/link/whitespace results are retained in [documentation verification](../TestResults/fixed-team-confirmation-admission-verification-20260917/final-checks.json). Gameplay/controller source, the plan JSON, previous implementation receipt and old sealed evidence are unchanged. No migrations, application configuration changes or deployments are required.
