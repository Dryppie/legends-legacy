# Affinity-creation recognition: captured-runtime admission

24 September 2026. Target: the offline `LL/tools/BalanceHarness`.

**Admission passed for the frozen 108-cell recognition diagnostic.** The [sealed admission](../TestResults/affinity-creation-recognition-admission-20260924/admission.json) contains a runnable request, captured gameplay dependencies and content, the tested harness, independent auditor and owned launcher. It prepared all 108 exact teams and reconciled **737,749 excluded combat values across 254 history files**. No fight, new combat value, production entropy draw or scientific launch occurred.

The next step is one owned **27,648-fight** diagnostic. Admission does not change the completed validation pilot's **Inconclusive** result, qualify a team or promote a policy. The diagnostic remains development evidence conditional on the twelve frozen roots.

## Exact bindings

| Artifact | SHA-256 |
| --- | --- |
| [Admission manifest](../TestResults/affinity-creation-recognition-admission-20260924/files.json) | `a1bb9417e44fefc9fe596ff0af95b9a6e3418071756498d90b8470e9474270a0` |
| [Native request](../TestResults/affinity-creation-recognition-admission-20260924/request.json) | `3d2dfd52c93264875ad22a82720ce8850e82d56b6d6d083aade3806329821ab1` |
| [Native definition](../TestResults/affinity-creation-recognition-admission-20260924/definition.json) | `bccf1b99e380612c2646ad2b105ffcd375eb6a0af76d9ddc1d5d7b2c2cd3ffb8` |
| [Unchanged frozen plan](Tower-Affinity-Creation-Recognition-Plan.json) | `170065b593a49609e72142d766443c3a47f7a271b937cc88d52436de2b4792a2` |
| Tested harness DLL | `bcfd818a72244dbe3526a075627d2ba4c91d66dce9484fcc64405acdcb771811` |
| Admitted execution identity | `d0adebcd5c517650f3666b7c3a8ba403aa382bd1b709b1b5f9805f1efe3581db` |
| [Prospective admission declaration](Tower-Affinity-Creation-Recognition-Admission-Declaration.json) | `e8e48c30c868ceaece902f49ce7babb34f203fc17ae3899c3cc19a027aeb1d3c` |

The native version is `tower-affinity-creation-recognition-v1`. The plan's original `FrozenDesignNativeIntegrationRequired` and `runnableRequest: false` fields remain unchanged as historical planning data. The separate admitted definition/request supplies the now-verified execution route. Neither membership nor the recorded population sample was redrawn.

## Compatibility and provenance

The new [admission adapter](analysis/prepare-affinity-creation-recognition.py) authenticates the v5 pilot admission, its closed scientific archive, the completed earlier recognition archive, the sealed native implementation verification and its full owned fixture. It retains the catalogue, sampling and plan packages and reproduces the complete plan from the recorded sampling bytes without requesting entropy.

All captured gameplay dependencies and content remain byte-identical to the v5 producing runtime. Only `BalanceHarness.dll`, its portable symbols, dependencies file and runtime configuration are replaced, using the four files pinned by the previous engineering verification. The [compatibility helper](analysis/affinity-creation-recognition-context.ps1) authenticates the DLL/symbol identity and **226 producing source documents**, resolves **557 native methods**, reconstructs the diagnostic result and classification of 6,144 literal words, and checks **108 transports /216 input projections**. The complete captured execution identity is preserved except for the tested harness assembly.

The public native check then prepares all 108 exact teams under a combat trace guard. The independent Python history scan and native scanner agree on the complete reservation union. Existing pending-reservation recovery receipts remain bound to the new request. History is rechecked before admission publication and again during read-only package verification. Execution must refresh it once more under exclusive leases before freezing and allocating any values.

The earlier recognition profile, its plan, auditor, launcher and admitted studies are unchanged. No application or gameplay source was edited in this step.

## Resource qualification

The forecast rule was frozen in the declaration before admission. Its margin is **2**, with no timing resampling or downward correction. Native and audit time use the larger of the prior recognition observation and the v5 observation scaled to 27,648 fights, plus the current context/check process time, before applying the margin.

Storage uses the diagnostic's actual archive format. Every compressed report from the completed v5 pilot and earlier recognition study was authenticated: **16,768 +27,648 reports**. Their largest compressed report is **22,325 bytes**. The native forecast combines that maximum for every future fight, the larger observed non-report overhead, a 64-MiB reserve and the fixed margin. It does not scale the unrelated search archive's duplicated transport overhead into the diagnostic. Audit storage separately includes the larger prior audit observation and a 16-MiB reserve, with the same margin.

| Stage | Forecast | Enforced cap |
| --- | ---: | ---: |
| Native time | 3,739.269 seconds | 6,000 seconds |
| Native storage | 1,604,541,120 bytes | 3 GiB, less the existing 4-MiB closeout reserve |
| Both audits and publication time | 847.055 seconds | 1,200 seconds |
| Audit/publication storage | 46,526,958 bytes | 512 MiB |

All estimates fit their separate caps. They are **planning estimates, not guaranteed completion bounds**. Unobserved recipes and machine load can exceed historical behavior. The owned launcher retains the unchanged combined **7,200-second/3.5-GiB scientific allowance**, with no transfer between stages, refill, resume or automatic retry.

The admission itself completed once in **252.844 seconds**, retaining **81,234,044 bytes**. Its full **600-second/512-MiB** allowance was charged at the start. The subsequent read-only verification is bounded by the remainder of that same allowance; its process and accounting receipts are retained in the [verification package](../TestResults/affinity-creation-recognition-admission-verification-20260924/verification.json).

That owned read-only verification **passed**, completing **325.325 seconds after the admission charge began**. It authenticated the sealed package and complete live history, with zero active descendants and no timeout. No additional admission charge, preparation or reservation was needed.

Cumulative recorded charges are now **31,005.312 seconds /26,062,075,047 bytes**. Cumulative declared maxima are **75,480 seconds /47,781,511,168 bytes**, including this admission and the newly admitted future scientific allowance. No future scientific execution is included in recorded charges. Development tests and documentation checks remain separate engineering work.

## Verification and changed files

**19 new admission tests passed.** They cover profile/source bindings, exact definitions, preservation of recovery receipts, four-file runtime substitution, rejection of incomplete/nonfinite resource inputs, separate stage limits, deterministic population reproduction, tampered sampling, required backend evidence, owned-process cleanup and failure/no-retry accounting. The unchanged native implementation's **122 passing backend tests** and full 27,648-report owned fixture are authenticated inputs to admission. No C# code changed, so another backend build or test rerun was unnecessary.

Commands (`python` denotes the bundled interpreter):

```powershell
python -B -X utf8 'Balance Harness/analysis/test-affinity-creation-recognition-admission.py'
python -B -X utf8 'Balance Harness/analysis/prepare-affinity-creation-recognition.py' prepare --declaration 'Balance Harness/Tower-Affinity-Creation-Recognition-Admission-Declaration.json'
python -B -X utf8 'TestResults/affinity-creation-recognition-admission-verification-20260924/verify-owned.py'
```

The admission command above is a historical single-use invocation: its existing package/output identities cannot be reused. Read-only verification can be run through the retained adapter with the external manifest pin:

```powershell
python -B -X utf8 'TestResults/affinity-creation-recognition-admission-20260924/prepare.py' verify --package 'TestResults/affinity-creation-recognition-admission-20260924' --manifest-sha256 a1bb9417e44fefc9fe596ff0af95b9a6e3418071756498d90b8470e9474270a0 --live
```

Changed repository files are the new admission adapter, compatibility helper, tests, declaration and this report, plus current-status banners in nine harness/assessment/admission documents. Historical document bodies and sealed source pins are preserved. There are **no migrations, application configuration changes, deployments, gameplay edits or policy-default changes**.

No required verification command remains blocked. The [documentation and evidence closeout](../TestResults/affinity-creation-recognition-admission-verification-20260924/closeout.json) checks the manifest pins, unchanged historical bodies, recorded accounting and local links.

## Next execution

The admitted output is `TestResults/balance/tower-affinity-creation-recognition-20260924`; it remains absent. A later launch must use the retained launcher, retained runtime and external admission pin. The launcher owns native execution, both audits and publication, and refuses an existing output or substituted inputs.

After its fresh freeze, the study may draw one 24,576-byte combat entropy batch, use 3,072 fresh values across twelve disjoint panels and permanently reserve every exposed fresh value, including the unused tail. All 108 rates, 216 paired contrasts and 132 explicit null outcomes remain required. The only completed decision is `CompleteDiagnosticOnly`; any team confirmation or policy promotion requires separate fresh evidence.
