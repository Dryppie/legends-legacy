# Group-variation comparison: frozen bounded pilot

15 September 2026. Target: offline `LL/tools/BalanceHarness`. Preparation is authorized; this document does not authorize fresh seeds or combat. All earlier seed exceptions are exhausted. Finish preparation before requesting **45 fresh values and at most 512 fights**.

## Inputs and allocation

Compare baseline `independent-group-count-v1` / `group-count-joint` with candidate `independent-group-variation-v1` / `group-variation-joint`. Use the verified group-variation source in one isolated executable, linked against the same captured gameplay DLLs. Preserve the deep-challenger captured guardian Health/Power +10% content, settings, UTC timestamp, equipment, attributes, scenario identity, neutral identities, floor 5, all 80 eligible Essences, unrestricted owned copies, level 40/tier 1/rank 2 and five Essence slots per character. Ten characters are two five-player parties. Ability order remains ordinal by Essence ID. No live gameplay or Kharad edits.

Each policy receives 44 distinct evaluated teams, at most 704 proposals, FreshEvery 4, 11 initial fresh teams, and one shared generation-root value. Baseline executes first. Separate policy streams remain as implemented. This is one shallow restart comparing whole policies; it cannot isolate a causal mechanism or establish general strength. No reference recipe or result enters generation or screening. Do not adjust schedules from preparation or combat outcomes.

| Phase | Teams | Shared fights per team | Maximum fights |
| --- | ---: | ---: | ---: |
| Group/count discovery | 44 | 4 | 176 |
| Group-variation discovery | 44 | 4 | 176 |
| Screen | Top 2 per policy, up to 4 recipes | 8 | 32 |
| Confirmation | Top 1 per policy plus 2 controls, up to 4 recipes | 32 | 128 |
| Total | | | **512** |

Freeze both top-two nominations by the existing discovery fitness and ordinal identity rules before screening. Choose each policy's screen winner by wins descending, discovery rank, then ordinal identity. Freeze finalists before confirmation and never reselect. Deduplicate identical full-context recipes at screen/confirmation only, retain every origin and never refill seats. Discovery overlaps are charged separately. Incomplete discovery stops all dependent stages.

Fixed confirmation controls: `team-040e60d3dbc5c127321653c47ed3a9d3` and `team-49f6979895354870c89362d4abf214bb`. No old combat evidence is pooled. The verified variation schedule tries owner counts 1, 5, 10, then 5 with another filler draw, while retaining every eighth fresh request as uniform. Actual explored bundles depend on the frozen generation root and rejected/duplicate requests; full catalogue coverage is neither required nor funded.

## Seeds, measurements and limits

Proposed fresh values: **1 generation + 4 discovery + 8 screen + 32 confirmation = 45**. Namespace `tower-group-variation-comparison-v1`, master label 2026091506. Preserve all **482,461** reservations; an approved successful binding would retain **482,506**. Refresh exact registry membership and hashes before binding/execution; reject drift. Durably journal allocation attempts and retain pending reservations on failure. Preflight uses already-reserved labels solely in explicitly non-runnable fixtures.

Primary contrast: group-variation finalist minus group/count finalist on 32 paired confirmation seeds. Keep four secondary finalist/control contrasts. Wilson rate family factor 8 and paired gained/lost family factor 20, draws as non-wins, and exact zero for identical merged recipes remain unchanged. Save all positive, zero and negative results and uncertainty. Boss health, duration and paired health differences are descriptive, with missingness separate from measured zero. Save every catalogue group's owner counts, fresh requested/placed/final counts, variation bundle/filler metadata, rejections and uniform routes. None changes selection. Persist the existing performance trace, stopwatch, CPU, allocations, peak memory and native archive verification; this is not a controlled timing benchmark.

Hard limits: **512 charged fight attempts including repetitions/replays; zero retries, resumes or replays; 1,800 seconds cumulative diagnostic workload; 4 GiB cumulative new output**. Carry forward **271.957 seconds** from the corrected verification accounting receipt, including the prior failed fixture. Carry forward its preceding retained output chain as well. New preparation diagnostics are additionally capped at 300 seconds. Native combat is capped at 900 seconds within the remaining outer allowance. Compilation is separate engineering work, each command capped at 300 seconds. Preparation plus execution evidence are capped at 512 MiB, study at 3 GiB, and prior output must remain below 512 MiB; check their combined total. No old cap is raised.

## Exact preparation and execution checks

Before diagnostics freeze this protocol, the adapted comparison model/driver, independent audit, metrics, workflow and publication scripts. Copy the sealed implementation source, verified corrected fixtures and captured DLLs; assert the owned checkout source/fixtures match. Freeze projects and binaries before their diagnostics. Preserve all 18 preceding sealed packages before/after.

Build the isolated driver and tests once each without gameplay rebuild or restore. Run exactly **71 backend cases through `build/run-tests.ps1`**: 17 composition, 15 joined-mechanics, 16 group/count, 15 group-variation and 8 comparison-controller cases. Engine-entry guards and synthetic evaluators only. Run exactly four Python fixtures for missing/zero telemetry, invalid numbers, absent group counts and paired seed alignment. One native preflight validates equal inputs/budgets, refreshes the reservation union, prepares the two controls without combat and saves 66 Wilson interval fixtures. One independent audit checks the frozen inputs, policies, controls, registry union, intervals and passing fixtures. No fresh candidate scoring or engine fights during preparation.

Publish a measured readiness review and update the six active handoffs; seal the preparation package before requesting the specific seed exception. Bind/run/native verification/independent audit are once-only commands gated on protocol-hash authorization and exact prior-phase success. On any failure or cap stop, preserve evidence and stop dependent diagnostics; publication/preservation may still document that stop. Allocation, combat and verification are not permitted by generic preparation approval alone.

Preserve sealed v19, all 253 recipes and its 512 unused confirmation values. Do not run its 129,536-fight confirmation, tune content, deploy or promote a default policy. Historical reliability remains Fail 1/3, deep recovery 0/3, sealed v19 Unresolved and adoption Hold. Construction coverage is established; comparative combat strength is still unmeasured.
