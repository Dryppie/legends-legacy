# Team-level coverage and legal loadout repetition

15 September 2026. Target: offline BalanceHarness. Implement `independent-team-coverage-v1` / `team-coverage` as a separate opt-in policy. Do not change existing policy semantics, gameplay, content, ability ordering, selection fitness or default adoption. Zero fights, preparations, replays, fresh seed values and retries.

## Exact construction rules

Require every one of the five authored roles somewhere in the complete team, rather than on every character. Permit one to ten uses of a loadout subject to one shared inventory; no minimum beyond one distinct loadout. Authored role coverage remains a heuristic, not proof of targeting compatibility or strength.

Construct full loadouts around the same authored cores. Add a complete-loadout entry point retaining family/copy constraints, with zero or more required roles and all Essence slots filled. Preserve the original constructor's stop-at-role-coverage behavior exactly. For each core run six fixed focuses: no role (one recipe), then the five ordinal roles (three recipes each). All six share 256 visited states and at most 16 recorded recipes; no budget resets or cap increase. Deduplicate the resulting full pool. No control IDs or combat results enter construction.

Extend the diverse allocator with explicit collective role coverage. Prioritize missing-role coverage, then low previous exposure. Alternate within-team preferences between distinct recipes and reusing an assigned recipe, using the retained party index only. All restarts share 256 states, 250,000 candidate checks and 16 retained complete parties. Keep shared copies, cancellation, canonical identities and incomplete-cap reporting. Keep registration at one existing generation label, at most 16 proposals/evaluations and 64 cores. Normal checkpoint-before-evaluation charging and provenance validation remain required.

## Frozen checks and measurements

New output: `TestResults/balance/tower-team-coverage-20260915`. Capture the dirty checkout before editing. Freeze all scripts/source/fixtures/commands before tests. Build in isolation against sealed gameplay assemblies. Run exactly **59 zero-combat backend tests once** through `build/run-tests.ps1`: 11 original constructor, 10 original allocator, 12 original structural, 12 diverse-policy and 14 new team-coverage tests. New tests cover full versus minimal construction; exhaustive full-loadout sets; constructor bounds and immutability; exhaustive collective-role assignments; legal repetition and missing roles; shared inventory; alternating candidate preferences; global caps; input validation/cancellation; registration; complete specialized generation; empty-pool charges; cancellation/failure checkpointing; metadata/provenance parity.

Run the seven prior-policy fixtures once, requiring exact saved parity (224 synthetic evaluations). Run structural v1 and diverse v1 once each, requiring exact saved 16-candidate result parity. Run team coverage twice (16 synthetic evaluations each), reversing metadata on the second run and requiring exact equality. All 288 synthetic evaluations use fixed fabricated results and a combat-entry guard; no live preparation or combat service runs. Construct and persist the new pool once separately for independent examination of every focus, state total, recipe and role witness.

Using saved control recipes only as fixed-slot feasibility fixtures, allocate each control once with exactly sufficient shared copies and once with one reserved copy removed (four allocator calls). Require both exact controls to be feasible under the new rules and both shortages to fail. Do not insert control recipes into the generated pool. Independently audit all captured teams, identities, complete recipes, family/copy constraints, team role union, repeated-loadout counts, canonical order, attempt counts, traces and shortlist membership. Retain all 120 pair distances for each of the three policies. Require 16 new-policy candidates, at least one specialist character and at least one repeated-loadout team; preserve measured coverage even if a gate fails. Exact control-pool membership is descriptive, not a discovery guarantee.

Carry **2,042.5539999999621 seconds** into the unchanged approved cumulative **2,400 seconds**: **357.4460000000379 seconds** remain. This scope permits at most **300 seconds**, including compilation, and **192 MiB** new output within cumulative **4 GiB**, plus a conservative 1 MiB charge for the shared test receipt. Phases and ceilings: setup 15, freeze 20, build 90, test-build 60, tests 30, captured 30, audit 20, publication 15 seconds; reserve 15 seconds for closure. Stop dependent phases at the first failure; do not retry. Publication may close a failed scope without accepting new claims.

Verify all **53 predecessor packages** before/after, including diversity seal `7a2108f20711681f2dcbfc48295d701e097f9f785fc094041365b69c5d2bfdc6`. Preserve unrelated dirty work, all **482,686 reservations**, 253 v19 recipes and unused 512 original confirmation values. Reliability Fail 1/3, deep recovery 0/3, v19 Unresolved and adoption Hold remain. Update active Markdown with measured findings, commands and remaining limitations. No additional fight study, Kharad tuning, ability-order optimization, deployment or 129,536-fight confirmation.
