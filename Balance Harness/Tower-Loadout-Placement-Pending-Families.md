# Loadout placement: bounded native pending families

Current status: optional native worker binding v7 now covers fixed and recipe-dependent pending names with a finite command catalogue. The scope admits at most 72 distinct paths and 73 create attempts, with declared per-file, live and lifetime byte caps. Native lease, runtime/cache and finite observer bounds remain unresolved. Both compressed guards and the 1,806/1,800-second gate stay closed.

The target is the offline Balance Harness. `StudyWorkSupervisor(..., native_pending_families=...)` accepts byte declarations for exactly `native`, `nativeAudit` and `publication`, together with the existing managed retention scopes and bounded persistent sidecars. It copies and validates the declarations, seals them in the existing storage binding, and passes each native phase a v7 worker binding. `RetainedOwner.run_worker(..., pending_families_budget=...)` exposes the same native boundary directly. Explicit v5/v6 declarations remain supported; mixing family and explicit declarations is rejected. The independent Python worker remains on v4 in the supervisor.

`tower-proposal-pending-families-v1` has `maxFileBytes`, `maxLiveBytes` and `maxTotalWrittenBytes`, all finite nonnegative integers. No scientific byte defaults are introduced. Native audit and publication require all three caps to be zero and reject even empty pending creates before their factories run. V7 carries a distinct `pendingFamiliesBudget` field, and emits work receipt v4 with pending-storage observation v2. Older bindings reject that new field, including an explicit null. The existing binding-size and original-sidecar limits remain in force.

The native catalogue contains 36 fixed destinations: six reservation endpoints; provisional result and native receipt; study binding, freeze, summary and evidence-storage index; and twelve pair files plus twelve placement catalogues. It also accepts `study/heldout-01-<64 lowercase hexadecimal characters>.json` through root `12`, with at most three distinct recipe names per root. Sources must be the canonical target-adjacent `.pending` path. Every destination allows one create except `history-input.json`, which allows two. Some catalogue members are alternatives across storage modes; the 72/73 bounds conservatively cover their union.

Dynamic registration occurs under the existing scope lock before the create factory. It reserves a distinct-name slot and records the member. Publishing or deleting releases live bytes but never refunds member slots, create allowances or lifetime writes. The same append-only writer checks cover dynamic and fixed files. Malformed names, additional roots, excess family members, exhausted creates and excessive writes fail closed. The protected-input check covers the whole dynamic study directory, as well as the fixed declarations and original binding, request, producer and receipt paths. Existing link checks still apply. This is cooperative enforcement, not filesystem confinement.

Python independently reconstructs the fixed catalogue and validates the dynamic grammar, per-root count, contract limits, creation counts and byte conservation. The owner checks the exact bound budget, root, phase and binding digest before retention. The supervisor checks the sealed plan before each dispatch and prevents phase reuse. Final observations retain complete, failed, missing and not-invoked phases separately. `pathInventoryComplete=true` refers only to the reviewed proposal command's selected pending writers. `wholeProcessCoverage` and `usableForAdmission` remain false.

The source inventory follows `Reserve`, `RunStudy`, `Execute` and final `Save` calls. `TowerProposalStudy.Save` uses `TowerCompleteReservation.Storage`, including nested endpoints and recipe-dependent held-out names. Search evidence uses the direct writer in `TowerAdaptiveRacingNative.Evidence`; content copies, recipe/battle files, manifests, attempt logs and compressed evidence also use direct retained writes. The compressed study index uses `Save` and is included. Direct retained writes, filesystem metadata, raw APIs, external mutations, serialization memory, leases, runtime caches and observer work remain outside this pending contract. Changing the producer's command paths requires reviewing and, where necessary, versioning the catalogue.

Verification passes **342 backend tests** through `build/run-tests.ps1` and **519 Python tests**. The backend total includes 29 new family cases and all 313 prior cases in disjoint groups. Python includes 21 new family/interoperability cases and all 498 prior cases. No tests are skipped. The final isolated build has 45 existing warnings and zero errors. The native family fixture actually publishes all 72 literal members, including nested `Save` outputs and the one allowed replacement. It also retains exhausted-family and forbidden-writer failures. The four-phase supervisor fixture uses synthetic process observations and no scientific study execution.

A fresh fabricated-outcome study provides current runtime and input identities for production audit checks. The real v7 native audit succeeds through the retained owner with an empty zero-cap pending contract. Its worker completes; the deliberately single-phase diagnostic owner remains incomplete because it did not run a scientific four-phase sequence. Existing v4/v5/v6 audit and tamper checks also pass. No native encounter preparation or combat occurs. Correctness durations are not resource qualification samples.

```text
./build/run-tests.ps1 -ArtifactsPath .artifacts/load-pending-families-canonical-20260925 -Filter <229 pending/receipt cases>
./build/run-tests.ps1 -NoBuild -ArtifactsPath .artifacts/load-pending-families-canonical-20260925 -Filter <90 native regressions>
./build/run-tests.ps1 -NoBuild -ArtifactsPath .artifacts/load-pending-families-canonical-20260925 -Filter FullyQualifiedName~BalanceHarnessContentAccountingTests
python -B -X utf8 TestResults/loadout-placement-pending-families-verification-20260925/run-verification.py
python -B -X utf8 TestResults/loadout-placement-pending-families-verification-20260925/run-supplemental.py
python -B -X utf8 TestResults/loadout-placement-pending-families-verification-20260925/run-interop.py
python -B -X utf8 TestResults/loadout-placement-pending-families-verification-20260925/run-audit.py
git diff --check -- <changed files>
```

The [verification package](../TestResults/loadout-placement-pending-families-verification-20260925) retains exact commands, logs, snapshots, native receipts and runtime identities. The initial build exposed a target-typed constructor compatibility issue. The next test run exposed mixed Windows separators in dynamic destinations. Both were corrected; failed logs, exports and preliminary runtimes are retained. Preliminary Python smoke passes are excluded from final totals. Historical sealed evidence and captured runtimes remain unchanged.

Changed files comprise the new native family catalogue and tests; pending scope and binding/receipt boundary; Python owner, validator and supervisor integration; routed fixture and family tests; LF attributes; this report; [resource specification v9](Tower-Loadout-Placement-Resource-Boundaries-v9.json); the [boundary inventory](Tower-Loadout-Placement-Pending-Family-Boundaries.json); and only line 3 of nine status documents. The specification has 11 domains and 54 source references; the inventory has 23 families and 116 references. No required command remains blocked. There are no migrations, application configuration changes or deployments. Opt-in use requires a v7-capable captured runtime and matching updated accounting modules.

Cumulative scientific charges remain **79,339.66095319996 seconds** and **51,980,910,910 bytes**, against declared maxima of **168,240 seconds** and **107,122,524,160 bytes**. No scientific launch, qualification sample, cost experiment, timing pair, live-history rescan, production entropy or scientific reservation occurred. No qualified forecast, compression speedup or search improvement is established.

Next implement native lease byte and lifetime bounds, then resolve runtime/cache and finite observer obligations before considering resource qualification. The [sealed handoff](../TestResults/loadout-placement-pending-families-handoff-20260925.json) retains those obligations and the unchanged scientific gates.
