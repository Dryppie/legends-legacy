# Complete retained-family controller — implementation and zero-combat verification protocol

Frozen scope **14 September 2026** after the [completed UTC binding](Tower-Retained-UTC-Binding-Review.md). Target: offline `LL/tools/BalanceHarness`. Implement the dedicated controller for the [bound design](Tower-Retained-Family-Confirmation-Design.md), without allocating or launching confirmation. Preserve all prior reviews, sealed packages, gameplay and all 481,603 reservations, including the original unused 512 values.

New output: `TestResults/balance/tower-complete-family-controller-20260914`. Diagnostic limits: **zero combat preparations, zero fights, zero fresh balance values, zero retries/resumes, 1,800 seconds active diagnostic workload, 4 GiB total new retained output**. Correctness tests and compilation are timed separately, with their output counted. Synthetic test accounting events exercise durable starts/completions without constructing a combat runtime or reserving balance values. Expected exception fixtures are correctness tests, not combat retries.

1. Add a dedicated fixed contract for 43,879 cells, 580 anchor reasons / 560 anchors, 32 first-stage values and 256 separate second-stage values, 4,096 second-stage capacity, 2,434,784 maximum attempts, 24 hours and 64 GiB for a future study. Leave every previous contract and cap unchanged. These future-study constants do not expand this diagnostic envelope.
2. Bind the source to the UTC package seal `78b20e50a9af9a8bbd24e37fe09b0050d6a3a4687bc7a8b3dc8c7bf4e3ddfa79` and complete cell-file hash `ab889005721127743f9c5bc9444fa727b5b32b54b42b4c31203f99e501c9dbce`. Verify captured gameplay, content/settings, all original recipes/identities and the complete anchor/context family. Retain original timestamps. No outcome or recipe is dropped.
3. Implement whole-stage selection and assessment using alpha .025 per stage. Freeze selection and complete first-evidence hash durably before stage two. Capacity overflow stops Inconclusive; observed rates above 50% reject the candidate. Selected cells use only their second-stage results. Batch archives never define separate statistical families.
4. Reuse the existing compact writer with 32-record chunks, no retries, at most 256 cells per storage campaign. One outer durable journal, deadline and owned storage account span all batches, preparation, reconstruction and final publication. Starts flush before engine entry; returned completions are charged before observing cancellation. Failed evidence is retained and cannot resume or verify as complete. Verify every recipe/seed/outcome/roster and complete file inventories.
5. Require an externally reserved, frozen future schedule and setup/history evidence. The controller exposes no allocator. Validate exact distinct/disjoint schedules and complete refreshed history, including new registry members. Prepare the entire bound family and check saved participant hashes before any future engine entry. No real preparation or execution command is run in this task.
6. Run backend correctness tests only through `build/run-tests.ps1`. Fixed final filter: `FullyQualifiedName~BalanceHarnessTowerCompleteFamilyTests|FullyQualifiedName~BalanceHarnessTowerContextTests|FullyQualifiedName~BalanceHarnessTowerRuntimeComparisonTests|FullyQualifiedName~BalanceHarnessTowerRetainedAuditTests|FullyQualifiedName~BalanceHarnessTowerCeilingControllerTests|FullyQualifiedName~BalanceHarnessTowerStorageTests|FullyQualifiedName~BalanceHarnessTowerStagedTests|FullyQualifiedName~BalanceHarnessTowerMidpointTests`. Cover exact full-family capacity boundaries, multiplicity, no pooling, malformed evidence, seed exclusions, accounting interruptions, cancellation, setup/deadline/storage exhaustion, archive tampering and forbidden engine entry outside writers. Compilation/test fixes may be verified in new isolated artifact roots; no diagnostic workload may be retried.
7. After correctness passes, freeze producing source, executable and an independent diagnostic verifier. Run exactly **one** `tower-complete-family-inspect` against the sealed UTC source, with **900 seconds / 1 GiB** sublimits and no combat preparation. Verify the complete 43,879 original scenarios, fixed capacities, and all 290 native interval rows against the already independently checked table. Native approximate bounds must agree within **1e-8**. Then independently verify the resulting identity/count/capacity/interval receipt once, within 300 seconds. A failed check stops dependent work without retry.
8. Complete one preservation/history audit within 120 seconds and final sealing within 120 seconds; charge a further 60 seconds for short static reads and metadata. Include builds, captures, new reports, whole changed files, shared test TRX and a 2-MiB final metadata allowance in output accounting. Update active handoffs and a new measured review; never edit earlier reviews or sealed diagnostics.

The source-only diagnostic verifies implementation binding and arithmetic. It cannot certify combat parity for the changed execution orchestration, full-study throughput, actual second-stage selection capacity, practical acquisition or updated-checkout gameplay. Those limits remain explicit. Fresh seed reservation, bounded combat parity and confirmation launch require their own concrete frozen scope. Reliability **Fail 1/3**, adoption **Hold**.
