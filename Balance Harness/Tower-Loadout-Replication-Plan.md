# Fresh challenger confirmation and unchanged-v13 replication

13 September 2026. Authorized by the user's “Please proceed” after the proposed publication-recovery fix, challenger confirmation and independent replication. Scope: offline BalanceHarness, tests and Tower documentation.

## Fixed scope and publication behavior

Kharad remains Health **3.04881408** / Power **3.85370128**. Preserve ten level-40, tier-1, rank-2 Standard characters, the exact fixed equipment, five level-1 unascended/unevolved Essences, and hypothetical ownership. Practical acquisition remains unverified. No catalog/default promotion, gameplay changes or expansion to floors 6–11.

Completed compact chunks receive at most four directory-publication attempts, with 50/100/200 ms delays and receipt/data revalidation. These are filesystem attempts, not combat retries, and their time is charged to execution. Persistent failures retain pending files. Explicit zero-combat publication recovery validates the entire contiguous prefix, frozen identity, recipes, prepared reports, ordered seeds, record hashes and durable attempts before renaming. Normal resume retains its existing identity and budget checks. A failed top-level benchmark remains interrupted; its start marker and attempt journal must not be reset.

Preserve all historical sealed evidence and reviews. Freeze both following protocols, producing assemblies/content/settings, exact recipes, all schedules and this plan before either campaign's first fight. Exclude every integer array in the latest **472,856-reservation ledger**, including unused and constructor reservations. Allocate confirmation first with master seed **2026091302**, then replication with **2026091303**, excluding the confirmation reservations as well. No seed shopping or adaptive additional allocation.

## Confirmation

The complete family contains the six original external controls plus the previous v13 primary `team-df06d15779b79eec34ac53c14fb0e138` and secondary `team-ac85939be8c0d4020590070f30d580f5`. Use their exact saved ordered recipes. All eight receive the same **1,000 fresh seeds**, for **8,000 fights**, no combat retries or diagnostic replays. Execute cap **1,200 seconds**, retained confirmation package cap **1 GiB**. Run all cells even if an early rate exceeds 50%.

The ordinary unchanged 10–50% family evaluator remains authoritative for its assessment. Separately predeclare joint alpha .05: .025 across all eight rates (Wilson multiplier 16), and .025 across the two discordant probabilities for one paired comparison (Wilson multiplier 4). Compare the new primary against the strongest prior saved control `team-1a924a7cfff12298633bee909cdea4ad` on the exact common ordered seeds. Report both the observed difference and its adjusted interval; superiority requires a positive lower bound. Do not substitute another comparison after seeing outcomes.

Every observed rate above 50% is a ceiling breach. A joint lower bound above 50% additionally supports a true rate above the ceiling under the approximate interval procedure. An interval crossing 50% leaves that statistical conclusion unresolved; do not increase the sample adaptively. Historical breaches remain visible and are not erased by a new below-ceiling point estimate. This confirmation alone cannot establish near-optimality or choose a calibration setting.

## Independent replication

Use the unchanged `independent-loadout-composition-v13` algorithm and unchanged deeper-v4 comparator: three paired restarts, 384 candidates per arm, maximum 8,192 proposals per arm, eight paired discovery seeds, unchanged whole-party discovery rank and two finalists per arm. Retain the v13 128-vector library and every original operator, initial/fresh construction, random stream, fitness and nomination rule. No knowledge of the successful seven-copy recipe, saved Essence IDs/counts/ancestry or confirmation outcomes enters independent generation or ranking.

Both new saved recipes join all six original controls strictly as external controls. Up to 20 deduplicated complete recipes, preserving every origin. Nominate rank-one primaries before held-out fights. Measure all cells on 64 descriptive screening seeds and 256 separate validation seeds, always completing validation and retaining zero-win finalists. The existing fixed anchor remains `team-1abe76ca1891d97a91d484f0a3662048`.

The separate preparation profile `tower-loadout-replication-v1` changes only the external-control allocation: **18,432 discovery + 20 × 64 screening + 20 × 256 validation + 8 diagnostic fights = 24,840 maximum fights**. The eight fixed diagnostic repeats are the original four historical detailed parity cases and first-screen-seed replays of the three new primaries plus fixed anchor. No other repeats or combat retries. Execution cap **2,700 seconds**, retained replication package cap **2 GiB**. Verification is timed separately and executes zero fights. The work/export package is bounded to **256 MiB**.

Keep the original reliability rule: joint alpha .025 across the complete deduplicated rate family and .025 across six paired comparisons through 12 discordance intervals. Each new primary requires rate lower bound ≥10%, paired difference lower bound versus its same-restart v4 primary >0, and paired difference lower bound versus the fixed anchor ≥−10 percentage points. At least **two of three** new primaries must satisfy all three. The larger external family changes the rate multiplicity denominator, not the decision thresholds or generation policy. Report this replication separately from the earlier 1/3; do not pool or count the earlier secondary as another successful restart.

## Completion and decision

Verify captured inputs and complete compact evidence, reconstruct nominations and reliability, independently audit statistical arithmetic and same-arm module ancestry, preserve/export every finalist and control, and reconcile actual attempts, elapsed time and storage. Update active Markdown and write a separate completion review without rewriting historical reviews or frozen plans.

If replication fails, keep v13 experimental and identify a specific next hypothesis from the complete evidence; do not launch another policy or campaign in this allocation. If confirmation supports a breach, carry it into the next calibration design, but do not retune from this study. Even if reliability passes, practical acquisition and a separately authorized calibration/floor challenge remain outstanding.
