# Core portfolio versus team coverage: frozen comparison

16 September 2026. Target: offline `LL/tools/BalanceHarness`. Preparation is authorized under the existing allowance. **Live preflight, fresh allocation and combat require a specific new exception for 45 fresh values and 300 additional diagnostic seconds.** Previous seed approvals are exhausted. No authorization receipt exists for this comparison.

## Question and frozen inputs

Does core portfolio find a stronger team than team coverage at the same small evaluation budget? Team coverage is the baseline because its finalist left 62.18% mean boss health versus filler diversity's 92.84% in the last comparison; both had 0/32 wins. This is a comparison of complete search policies, including their different recipe pools. It does **not** isolate the causal effect of the new allocator alone. Do not pool historical combat outcomes into this comparison.

- Baseline: `independent-team-coverage-v1`, method `team-coverage`, 560-recipe pool.
- Candidate: `independent-team-core-portfolio-v1`, method `team-core-portfolio`, unchanged 768-recipe filler pool. The preceding zero-combat implementation increased used profiles from 43/198 to 96/198 and represented same-owner core pairs from 19/282 to 168/282 versus filler diversity. Construction took 0.4177 versus 0.3890 seconds. Combat benefit is unmeasured; the common four-Essence control subset still had zero selected owners.
- Each policy constructs and evaluates exactly 16 teams, with at most 16 proposals and existing 256-state/16-recipe per-core and 256-state/250,000-check allocation caps. Construction is finite and deterministic; a generation label is not an independent structural restart. No candidate refill, adaptive allocation, policy tuning, confirmation reselection or ability-order tuning. Controls and combat outcomes never enter construction.
- Both use the sealed `tower-deep-challenger-study-20260915` gameplay DLLs, content, settings and original timestamp: floor 5, existing +10% health/power context, level 40/tier 1/rank 2, neutral equipment, 80 eligible Essences and unrestricted copies. Ten characters comprise two five-player parties, each with five Essence slots. Equipped Essence/ability order is ordinal. Definitions differ only in policy/method.
- Fixed controls: `team-040e60d3dbc5c127321653c47ed3a9d3` and `team-49f6979895354870c89362d4abf214bb`. Independently compare both prepared participant sets/statistics with the captured controls.

Implementation seal: `tower-core-portfolio-20260916/files.json`, SHA-256 `ee9066843a2fad3a8828548c9a60889d200e7caa1cadfb83ac89851e149d9f14`. Retain its 83 passing tests and eleven old-policy parity fixtures without rerunning them. Adapt the sealed preceding comparison model, runner and audits into a new isolated package. Do not edit root harness implementation or any sealed package. Freeze all source, scripts, protocol, test source and captured dependencies before diagnostics; pin the compiled request before preparation.

## Exact allocation and analysis, conditional on approval

Namespace `tower-core-portfolio-comparison-v1`, master `2026091601`. Allocate exactly 45 unique values disjoint from the complete live reservation union: generation 1, discovery 4, selection 8, confirmation 32. Preserve all **482,776** reservations; successful allocation yields **482,821**. Rejected allocation candidates and interrupted attempts retain durable records.

1. Baseline first: 16 teams × 4 discovery seeds = 64 fights.
2. Candidate: 16 teams × the same 4 discovery seeds = 64 fights.
3. Freeze the top two per policy using existing rank: win rate, guardian health, survival, victory duration, ID. Merge identical recipes/context while preserving all origins. Screen the two to four teams on eight fresh shared seeds: at most 32 fights. Select one finalist per policy by screen victories, then nomination rank and ID.
4. Freeze both finalists and both controls; merge exact duplicates with all origins retained. Confirm the two to four members on 32 fresh shared seeds: at most 128 fights.

Maximum **288 fights**, including every repetition; **zero retries, resumes or replays**. A finalist plays 44 total, another screened team 12, an unscreened team 4 and each control 32. Duplicate discovery still consumes both charges; merging never creates replacement nominees. Incomplete stages stop dependent work. Native per-policy definition cost is 176; the wrapper runs discovery only and owns the combined 288-fight cap.

Primary endpoint: `team-core-portfolio-finalist-minus-baseline-finalist` paired confirmation victories; draws are non-wins. Also report four fixed finalist-minus-control contrasts. Existing simultaneous Wilson factors: 8 for rates, 20 for paired gain/loss bounds. Merged identical members have exact zero difference. Boss health, duration, missingness, group exposure and paired health differences are descriptive and cannot select confirmation winners. No reliability, optimality or default-adoption claim follows from this small comparison.

## Preparation within the existing budget

Incoming diagnostic workload **2,575.270 seconds**, leaving **124.730 seconds** under the approved **2,700-second** ceiling. Preparation has a **60-second aggregate cap**, with non-publication phases stopped by 45 seconds and 15 seconds reserved for closure. Compilation, tests, preparation, audits and publication count; source editing does not. Per-phase maxima: freeze 20, build 45, test-build 30, tests 20, request 10, preparation 20, audit 20 seconds, reduced by the aggregate/cumulative remainder. Zero fights, new seeds, retries and live registry scans during preparation. Exactly two control preparations and 32 fabricated generation evaluations, with combat-entry guards.

Run eight focused tests through `build/run-tests.ps1`: shared definitions/costs, context drift, separate nominations/incomplete discovery, screening ties/incomplete evidence, duplicate origins, paired extremes/misalignment, incomplete confirmation and durable interrupted attempt charging. The nomination test requires every proposal and structural trace to match the sealed `team.json` and `portfolio.json` fixtures. No broad combat-integration filter.

Native offline preparation reads the complete sealed prior ledger, uses 45 already-reserved labels solely as non-runnable schema fixtures, prepares two controls and exports 66 interval cases. Its proof explicitly says `sealed-history-fixture`. An independent Python reader checks the sealed union, identical input definitions/costs/schedules, participant statistics, intervals and test exports. Verify all **64 predecessor packages** unchanged before and after preparation. Readiness is `PreparedAwaitingSeedAndTimeException`; complete live preflight remains pending.

New package: `TestResults/balance/tower-core-portfolio-comparison-preparation-20260916`. Assemble source, review it and freeze this protocol before diagnostics. Run once in order:

```powershell
$python = 'C:/Users/HrHoe/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe'
$work = 'TestResults/balance/tower-core-portfolio-comparison-preparation-20260916'
& $python -B "$work/workflow.py" freeze
& $python -B "$work/workflow.py" build
& $python -B "$work/workflow.py" test-build
& $python -B "$work/workflow.py" tests
& $python -B "$work/workflow.py" request
& $python -B "$work/workflow.py" check
& $python -B "$work/workflow.py" audit
& $python -B "$work/publish.py"
```

Every phase writes create-new start/result/failure receipts. The first failure stops dependent work; publication preserves partial evidence. No replacement run or retry. Tests invoke `build/run-tests.ps1 -NoBuild -ArtifactsPath "$work/tests" -Filter FullyQualifiedName~BalanceHarnessCorePortfolioComparisonTests`.

## Proposed execution allowance and gates

After successful preparation, request **45 fresh values and exactly 300 additional diagnostic seconds**, carrying the measured remainder forward. Approval would raise this workflow's cumulative ceiling to **3,000 seconds**, leaving old experiments and caps unchanged. The last comparable live check and independent audit took 52.234 seconds; authorization/binding/run/native verification/independent audit/publication took 149.482 seconds. The incoming 124.730 seconds cannot cover that complete workload. These observations guide the request and are not a runtime guarantee. No extension applies before explicit approval.

Unexecuted gated commands: `authorize.py <actual-reply> <context>`, then `execute.py check`, `audit-preflight`, `bind`, `run`, `verify`, `audit-execution`, and `finish.py`. Authorization binds protocol/package hashes and requires at least 270 seconds available under the proposed ceiling. Complete live registry preflight and its independent audit must pass before allocation. Binding checks the registry and preparation seal again and requires at least 170 seconds remaining. Changed registry/union stops execution; no automatic expansion. Runtime and archive-only native verification recheck the external registry. The independent execution audit reconstructs records, schedules, nominations, finalists, all merged origins, intervals and seed/attempt charges, including exact generated party/trace parity with sealed fixtures.

Execution phase maxima: live check 90, live audit 20, binding 90, run 90, native verification 90, independent audit 20 seconds, each bounded by the lower remaining cumulative allowance after a 20-second closure reserve. Stop below 288 fights if a deadline/check fails. Preserve cancellation, durable seed intent/write-through journals, attempt-start charging, incremental accounting with full boundary reconciliation and archive verification. Retain detailed performance, CPU, allocation, memory and attempt evidence on success or failure.

The cumulative output cap stays **4 GiB**. Carry prior receipt bytes plus actual sealed implementation size. Preparation ≤192 MiB, execution ≤128 MiB, study **≤384 MiB** (reduced from the preceding adapter's 1 GiB); also charge 1 MiB for shared test output and one closure second. Assert that even the sum of these package caps fits the cumulative remainder. Native study accounting uses the reduced cap and boundary reserves. No deletion or reuse of evidence. Preserve unrelated dirty work and all root harness source; only the focused root test, new package, protocol/review and six active Markdown handoffs change.

V19 remains sealed with all 253 required recipes, no confirmation and the unused original 512 confirmation values. V19 Unresolved, reliability Fail 1/3, deep recovery 0/3, adoption Hold. No Kharad tuning, gameplay/configuration change, migration, deployment or 129,536-fight confirmation. A failed check or exhausted limit must retain evidence and identify the unresolved boundary.
