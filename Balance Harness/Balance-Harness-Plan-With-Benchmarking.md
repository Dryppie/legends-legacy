# Balance Harness Plan

**Current Tower priority — 12 September 2026:** [compact archive support](Tower-Compact-Archive-Review.md) now preserves complete non-event reports and ranking/replay behavior in shared, compressed bundles. The frozen 1,200-combat comparison measured 88.69% smaller ordinary archives and 10.31–15.17% lower overall runtime. Preparation reuse, unused checkpoint removal, resumable verification and campaign integration remain in the [performance prerequisite](Automatic-Tower-Team-Discovery-Plan.md#immediate-next-increment-bulk-performance). The Kharad portfolio retains a scoped balance Pass while search quality remains Fail. Keep the current gear/untrained-Essence budget and 10–50% policy; larger searches and floors 6–11 remain queued.

**Progression audit and calibration — 11 September 2026:** [floors 2–5 review](Tower-Progression-Floors-2-to-5-Review.md) records independent four-Essence searches on floors 2–4, their separately confirmed linked Health/Power calibration against every known breach, and Kharad's fresh **20.9%** strongest-control check. Compatible controls and calibrated top builds persist for future searches. Floors 6–11 and practical acquisition coverage remain open.

**Fresh search and Kharad follow-up — 11 September 2026:** [new independent searches](Post-Calibration-Tower-Team-Search-Review.md) confirmed Garran's strongest saved team at **34.8%** and found a stronger Kharad team at **59%**, triggering a separate calibration. The [expanded-portfolio follow-up](Kharad-Expanded-Portfolio-Calibration-Review.md) applied another **8% to both Kharad Health and Power**; its strongest of 122 parties confirmed at **25.25%**, and the full family passes. At that stage, main-dashboard searches retained **4 floor-1 / 6 floor-5 controls**. Broader progression, practical Essence access and further independent ceiling searches remain open.

**First retained-build calibration — 11 September 2026:** compatible saved builds now enter new Tower Lab searches automatically as fresh benchmark controls; completed future studies retain their generated finalists. The [retained-build calibration](Retained-Tower-Builds-Calibration-Review.md) applied linked Health/Power factors of **1.06 to Garran** and **1.56 to Kharad** relative to their pre-campaign inputs. The strongest of 57/106 retained parties confirmed at **34% / 24.25%**, respectively, and both frozen families pass the 10–50% policy. This uses the declared full Essence pool including Rare Essences; this historical result is followed by the fresh searches and expanded calibration above.

**Original independent-team pilot results — 11 September 2026:** increments 1–5 of [Automatic Tower team discovery](Automatic-Tower-Team-Discovery-Plan.md) are complete. The [fixed pilots](Automatic-Tower-Team-Pilot-Review.md) found a floor-1 generated primary at **859/1,000 wins (85.9%)** and five floor-5 generated finalists at **972/972 each**. Independent search found viable builds; both declared cohorts fail the 50% balance ceiling. These pilots use the full 80-Essence pool with hypothetical ownership, including Rare Essences. The retained-build follow-up above now calibrates these declared budgets; practical Essence access and the wider progression curve remain separate coverage. Wider progression balance remains unestablished; no bosses were tuned by these pilots.

**Latest user-authorized floor-1 tuning — 11 September 2026:** [linked Health/Power calibration](Tower-Floor-1-Linked-Tuning-Review.md) first restored original Health **1.27** / offense **1.24**, then selected **1.32 times both**, now **1.6764 / 1.6368** locally. The supplied party confirmed at **29.1% wins (291/1,000; 95% interval 26.37–31.99%)**, with zero draws. All 1,000 local parity reports and 70 other-floor regression pairs matched. Two HP-heavy alternatives were compared separately; the most HP-heavy exceeded the ceiling at 55.3%. The prior [offense-only 39.4% calibration](Tower-Floor-1-Tuning-Review.md) remains historical. This is scoped acceptance for that party/budget, without deployment; independent team generation, wider build coverage and the progression campaign remain separate work.

**Current Tower priority — user decision, 11 September 2026:** apply the approved [10–50% Tower acceptance policy](Tower-Balance-Acceptance-Policy.md) at four Essences on floor 1, five around floor 5, six around floor 10 and at least seven on floor 11. Every evaluated build at the intended floor budget must stay at or below 50%, with at least one reaching 10%; poor builds do not all need to meet the lower bound. The standalone evaluator and independent-search confirmation are integrated in the CLI and Tower Lab. The fixed floor-1 and floor-5 pilots are complete; practical Essence access and further cohorts remain to be defined for the broader [floor-1–11 progression audit](Boss-Specific-Essence-Loadout-Plan.md#next-priority-validate-the-progression-curve). Preserve above-ceiling specialists as controls. Earlier high-clear results and current search-completion statuses do not pass balance automatically. Dated pending-target statements and proposed next experiments below retain their historical context; this decision takes priority. The starter 50–90% policies remain separate.

**Current priority — user decision, 9 September 2026:** leave Phase 2 at its current state and defer its remaining integration work. The [first Phase 3 Tower slice](#next-implementation-phase-3-tower-slice) is implemented: floor 1 with a legal five-character party, independent gameplay-parity checks, saved reports and deterministic replay. Its fixed 20 trials produced 0 wins / 20 defeats and four matching detailed replays; see the [review](Tower-First-Slice-Review.md). Tower win rates remain descriptive until separate targets are approved; the starter's 50–90% band does not apply automatically. Broader Phase 3 coverage remains open.

**Latest Tower increment — 10 September 2026:** all 15 floors, 4–10-slot budgets and Tower Lab's **Find loadouts** workflow now include [five-role whole-party search](Tower-Whole-Party-Review.md). Controller candidates join Guardian/Restorer/two Strikers, with first-cell, repeated-group and alternating-group deployments compared at unchanged budgets. Earlier finalists remain controls; new finalists freeze before fresh confirmation. Independent optimization of every later character, iterative context search and ownership/training remain open. No numerical Tower gate or production tuning is introduced.

**Next generalist/control experiment:** [joint refinement of retained four-/five-slot parties](Essence-Loadout-Search-Plan.md#next-implementation-joint-refinement-of-retained-parties), with stronger discovery sampling and fresh frozen confirmation. The Controller/deployment increment has finished its 72,480-fight measurement and verification; it is not pending implementation. Dated sections below retain the scope and limitations of their historical increments. Phase 2 remains sufficient for now, with its remaining integration deferred.

**Boss-specific analysis and plan:** the [new strategy plan](Boss-Specific-Essence-Loadout-Plan.md) maps all 15 guardians to observed mechanics and candidate counters, including add clearing, focused damage, tanking, healing, prevention and control. Recommended first work is a structured boss inventory and separately versioned boss objective, followed by equal-cost joint-search/strategy-diversity comparisons and fresh confirmation. The existing generalist track remains a control; boss-specialist alternatives keep full all-floor transfer reports. No specialist build has been newly confirmed by this analysis.

Status: offline idle execution, original controls, the [First Hunt cohort](First-Hunt-Cohort.md), explicit baselines, paired comparisons, versioned goals and advisory CI are implemented in [BalanceHarness](../LL/tools/BalanceHarness/README.md). The selected level-5 Blood Grove and level-10 Crystal Creek checkpoints pass their approved **50–90% win bands** on the retained 8 September build. The [combined regression check](Starter-Regression-Review.md) repeated all **52,000 accepted trial identities** across eighteen cells with zero changed gameplay records or outcomes and four matching detailed replays. Win rates remain **89.13% / 76.21%** in Blood Grove and **74.40% / 55.45%** in Creek; all four intervals lie inside the band. That build passed **2,119 backend tests**. A repeatable local script preserves both accepted baselines and their schedules.

The [9 September evidence package](Starter-Baseline-Package.md) now preserves both accepted references, their exact executables and complete supporting evidence in a **1.59 GB ZIP**. All **428,158 files** restored and verified outside the checkout; four saved suites pass their existing checks and **eight detailed replays match**, using an isolated original runtime after the system updated. The user chose to keep it local, so off-device storage is deferred. The first hosted CI run passed on published main; hosted verification after publication and broader coverage remain open.

Target: offline balance tooling for the primary game service in `LL/`. This work does not require an API endpoint, Angular UI, chat-service changes, infrastructure changes, or a deployed service.

The [level-10 starter handoff](Crystal-Creek-Starter-Handoff.md) is also measured: 16,000 battles compare four costed preparation steps in Blood Grove and Crystal Creek. The primary Amulet + Goblin second-slot build confirms at 99.4% / 100% in Crystal Creek, while the original two-item, single-Essence build at level 10 loses every tested Creek fight. The user subsequently approved **50–90% wins at this checkpoint, with no duration target**. Both completed seed sets fail that scoped policy above the upper bound. The [starter acceptance review](Starter-Path-Acceptance.md) records current policy, live local checks and hosted CI separately; it supersedes earlier pending-target statements.

The [first Crystal Creek tuning experiment](Crystal-Creek-Tuning-Review.md) remains closed and Inconclusive. The subsequent [fine experiment](Crystal-Creek-Fine-Tuning-Review.md) completed **275,200 battles across 42 candidates** and selected Barrier **0.44** / Ice Needle **1.7**. Fresh confirmation reached **74.40% [72.44–76.26%]** and **55.45% [53.26–57.62%]**, both **Pass**. All 32 external controls, eight Blood Grove return cells and four replays matched. Removing creature ownership metadata preserves all gameplay in 36,800 repeated verification battles; the corrected definitions/mappings are local API content, preserving original player abilities. The linked review records its accepted local baseline and retained build. Blood Grove's later confirmation closes its separate acceptance question; the combined regression above subsequently verifies both checkpoints on the retained 8 September build.

Scope decision — 8 September 2026: the immediate requirement is a viable starter progression path. Every Essence does not need to win every encounter. Broad Essence comparisons remain optional diagnostics; the proposed Lumo Token replacement experiment is deferred. The user selected **Goblin Warrior + Sword + Heavy Chest for Blood Grove**, initially approved a 65–75% band, and later widened it to **50–90%, retaining the 70% aim**. The [selected reference](Blood-Grove-Starter-Reference.md) maps the items to one-handed Shortsword and Heavy Breastplate at the existing rank-0, unstyled level-5 defaults. The band applies independently to its two fixed encounters. All four initial 100-trial cells failed with 0% clears. The user then authorized the local equivalent of bonus 0.21 and a separately declared confirmation: version-12 content sets Blood Grove offense to 2.421 and leaves the other 13 areas unchanged. Both original fresh point estimates fit the band, but Raven uncertainty initially prevented baseline acceptance. The later fixed 10,000-trial confirmation passes both checks and has its own accepted local reference; the original result remains unchanged. Nothing was deployed. Level-1 references and other builds remain separate decisions. The subsequent level-10 decision enforces 50–90% wins and explicitly excludes duration.

The harness should answer three questions:

1. Can representative players clear the content intended for their progression stage?
2. Which builds, essences, and party compositions perform unusually well or poorly, and why?
3. Did a code or content change move those results outside the intended balance goals?

The first useful deliverable is a reproducible report for a small set of real encounters. Build search, a universal essence score, and automatic tuning should follow only after that report is trustworthy.

The implementation covers individual encounters with one to three fixed enemies, three progression stages (levels 1, 5 and 10), paired encounter seeds, saved content/inputs, battle replay, scorecards and parity tests against the normal idle-session path. The original controls have 12 cells/60 draft checks; the First Hunt cohort has 36 cells/180 draft checks. Complete suites can be explicitly accepted as comparison baselines and evaluated against their matching policies. See the tool README for exact commands and limits; the sections below distinguish implemented idle support from the wider design.

## 1. Existing Building Blocks and Gaps

The repository already contains useful foundations. Extend their preparation and execution paths instead of building a second combat implementation.

| Existing component | Reuse | Gap to address |
| --- | --- | --- |
| `AbilityBalanceSimulator` | Seeded team discovery, saved-candidate round robins, side swapping, parallel batches, matchup and essence summaries | It constructs runtime combatants directly. It is not yet a general progression or PvE benchmark harness. Its request has no per-participant essence level or ascension settings. |
| `CanonicalEquipmentBuildFactory` | Deterministic equipment references, real essence loadouts, progression rungs, role profiles, and no-essence controls | Add explicit benchmark fixtures and acquisition assumptions; a canonical reference is not automatically a typical player's build. |
| `CanonicalCooperativeRosterCatalog` | Stable roles and five-character cells: Guardian, Restorer, two Strikers, Controller | Include viable alternative compositions; these roles are benchmark conventions, not player classes. |
| `CombatPreparationPipeline` | Live/snapshot preparation, equipment and essence setup, content-specific activity, stable participant identity | Offline idle composition is implemented without an API or database; each additional content adapter still needs preparation parity. |
| `CombatEngineExecutor.ExecuteSimulationAsync` | Real ability compilation, progression modifiers, equipment behavior, and isolated execution | Supply the actual content rules explicitly and reproduce any content-specific outcome interpretation. |
| `WorldTowerCombatRuntimeFactory` | Shared Tower preparation, guardian scaling, stagger, party identity, and scouting modifiers | Benchmark party size from each floor's `RequiredSlots`, including multiple five-character cells. |
| `CombatResult` and `CompactCombatTelemetry` | Engine/content outcomes, ticks, participant statistics, summon/add windows, health-pressure samples | Inventory available measurements before promising per-essence damage, effective healing, or control attribution. |
| Existing correctness and parity tests | Combat preparation, canonical snapshot rehydration, telemetry behavior, outcomes | Harness execution, suite, comparison and goal tests now exist; extend parity coverage with each new content adapter. |

A concrete parity risk exists today: `AbilityBalanceSimulator` does not override `FastCombatEngineOptions.StartActiveAbilitiesOnCooldown`, whose default is `false`. Normal executor and Tower playback paths set it to `true`. The same underlying engine can therefore produce different results because its setup differs. Resolve and test this distinction before presenting simulator results as gameplay balance.

The executor also has a mutable compiled-essence cache. Start with a separate executor/dependency scope per worker; do not assume an existing service instance is safe to share across parallel battles.

## 2. Balance Goals and Metric Classification

Each scenario needs a written player-experience goal, an expected progression cohort, and measurable acceptance rules.

| Content | Proposed primary goal | Useful diagnostics |
| --- | --- | --- |
| Normal idle enemies | Reliable victories and, where relevant, an acceptable kill-time band | Health remaining, damage intake, ability usage |
| Elites | Intended victory rate at the expected progression point | Death timing, sustain, duration distribution |
| Dungeon encounter | Encounter clear rate under the specified starting state | Attrition, resources, wave performance |
| Complete dungeon run | Run completion rate with actual carryover/recovery rules | Failure encounter, remaining health/resources |
| Bosses | Clear rate and explicitly required mechanic checks | Time to kill, first death, add clearance, stagger |
| World Tower | Floor clear rate by progression, party composition, and scouting state | Role survival, mechanic pressure, difficulty curve |
| PvP | Matchup score distribution and bounded draw/side advantage rates | Duration, counters, composition coverage |

Rules:

- A primary metric or explicit guardrail can fail a benchmark. A diagnostic cannot silently become a failure condition.
- Duration is primary only when pacing, an enrage, or another content goal makes it relevant.
- Goals apply to named cohorts. An optimized build and an ordinary progression build need not have the same target.
- Do not require every essence or build to be equally good everywhere. Reward meaningful strengths and counters.
- Record both absolute target compliance and movement from a baseline. A stable result can still be badly balanced.
- Treat initial numerical targets as **draft hypotheses** until reviewed against intended gameplay. Observed results alone do not establish the desired target.
- Track intended exceptions explicitly, such as a boss difficulty spike or an intentionally weak negative control.

## 3. Scenario Definitions

A scenario is a versioned contract describing one combat situation, not just a pair of stat blocks.

Required fields:

| Field | Meaning |
| --- | --- |
| Identity | Stable ID, revision, description, tags, and content adapter |
| Fixture references | Character/party and encounter IDs with pinned revisions |
| Progression context | Level, accessible gear/essences, unlock state, and content position |
| Starting state | Health, resources, cooldown policy, buffs, party order, scouting/modifiers |
| Rules | Production ruleset reference, resolved tick limit, recovery, waves, overtime/revival where applicable |
| Sampling | Explicit seed schedule, orientation policy, repetition count |
| Outcome policy | Definition of victory/clear, draw, timeout, and mechanic failure |
| Goals | Metric, unit, bound, direction, confidence policy, and enforcement status |
| Diagnostics | Requested measurements and telemetry level |

Two scenario classes should stay visibly separate:

- **Gameplay scenarios:** legal, progression-appropriate characters against real content; eligible for gameplay balance checks after parity validation.
- **Mechanic probes:** controlled targets or intentionally isolated abilities; useful for explanation and engine verification, but not evidence of general player viability.

Coverage tags can include single target, adds, burst, sustained damage, armor, resistance, sustain, control immunity, stagger, and attrition. These are selectors over scenarios; they do not require separate simulator implementations.

An isolated dungeon fight must not be reported as a complete dungeon clear. Likewise, a single Tower floor does not model a multi-floor progression journey.

## 4. Benchmark Suite and Runtime Benchmarks

Use three balance suites with different purposes:

| Suite | Contents | Expected use |
| --- | --- | --- |
| Smoke | Small, fixed, representative cases and deterministic invariants | Local iteration and pull requests |
| Reference | More seeds, progression boundaries, alternative builds and parties | Intentional balance review and baseline comparisons |
| Exploration | Generated candidates and broad matchup coverage | Finding combinations and scenarios worth investigating |

The implemented [idle reference fixture](../LL/tools/BalanceHarness/Fixtures/idle-reference.json) contains **12 fixed idle cells**: levels 1, 5 and 10 × two legal builds × two real encounters. It uses 100 distinct seeds per cell, or 1,200 battles total. Alternative builds share encounter seeds for paired comparisons. Lumo Ruins, Blood Grove and Crystal Creek cover initial weapon ownership, the next area entry and the second essence-slot unlock. These are fixed-spawn measurements, not area-wide spawn-distribution estimates.

This is an initial sampling budget, not a claim that 100 seeds can prove narrow win-rate differences. The suite reports per-cell 95% Wilson clear-rate intervals, separate win/non-win duration distributions, remaining health and completeness. It remains advisory until the written ownership hypotheses, targets and sampling are suitable. A `--samples 3` override provides a 36-battle smoke run with the same fixtures.

The separate [First Hunt fixture](../LL/tools/BalanceHarness/Fixtures/idle-first-hunt.json) has 36 cells and 3,600 battles by default, or 108 battles at three samples per cell. It crosses the three actual First Hunt Essence choices with mace/wand, uses quest-reward equipment counts and fixes duplicate/mixed two-enemy encounters at levels 5 and 10. Random box outcomes are explicit conditions, and Essences remain untrained. It improves cohort coverage without silently replacing the original controls or claiming an area-wide spawn estimate.

Performance benchmarking is a separate track:

The [12 September Tower workload review](Tower-Competitive-Workload-Performance-Review.md) makes this track the immediate prerequisite for further large Tower runs. Measure the complete preparation/simulation/archive/verification path with a small frozen budget. Extend the existing shared archive patterns, eliminate repeated input/report payloads and unused playback checkpoints, isolate mutable battle state, and make chunk verification resumable. Record an overall elapsed-time/storage cap alongside combat counts; coordinate readers, writers and maintenance. The [implementation exit checks](Automatic-Tower-Team-Discovery-Implementation.md#next-increment-bulk-performance--planned) require parity and repeatable performance gains before expansion. Staged statistical sampling is a separate future policy, not an optimization to apply retrospectively.

- Record load/validation, preparation, combat execution, aggregation, and reporting time separately.
- Distinguish cold startup from warmed repeated execution.
- Record battles/second, simulated ticks/second, allocation/peak-memory measurements where supported, worker count, telemetry mode, build configuration, runtime, and machine identity.
- Compare timings on a controlled machine/configuration. Do not fail a gameplay balance assertion because a developer laptop was busy.
- First measure realistic budgets; provisional goals are under one minute for smoke and under ten minutes for a local reference run.
- Keep the existing database-backed `build/measure-idle-combat.ps1` benchmark separate. It measures a broader idle-processing path and has external local dependencies.

## 5. Character Profiles and Progression

A profile must describe a build that can exist at the stated progression point.

Pin:

- Character level and base attributes.
- Equipment identities, occupied slots, tier, rarity, quality, upgrades, and relevant behavior/set effects.
- Essence identities, equipped slots/order, levels, ascension, and other combat-affecting progression.
- Relevant passive unlocks, persistent bonuses, temporary effects, and their explicit inclusion or exclusion.
- Acquisition assumptions: what this player could reasonably own at this stage.
- Expected intent, such as balanced solo, sustain, damage, guardian, or control.

Use three cohorts:

1. **Entry:** newly eligible, incomplete but legal equipment/loadout.
2. **Reference:** an attainable build chosen to represent the intended content audience.
3. **Optimized:** a strong legal build within the same acquisition/resource budget.

An intentionally poor build is a separate negative control. It should not lower the expected standard for entry players.

Use the canonical factories as fixture-building inputs, then store the resolved builds. A fixture ID must not silently acquire new equipment or essences when a factory changes. Report both the source recipe and the materialized result.

Power rating is useful metadata and a coarse comparison aid. Do not use it as the sole definition of equivalent strength or as proof that an encounter is correctly tuned.

## 6. Build Generator

Build generation should follow fixed fixtures, not precede them.

Implement in increasing order of complexity:

1. Hand-authored fixtures.
2. One-slot legal substitutions around those fixtures.
3. Seeded random sampling from the progression-eligible catalog.
4. Archetype-constrained sampling.
5. A bounded optimization/search method only if earlier methods miss useful combinations.

Reuse gameplay validation for slot limits, essence-family restrictions, equipment hand rules, unlocks, and progression limits. Reject invalid fixtures with an actionable error; do not silently truncate a loadout or substitute fallback content.

Specify the sampling distribution. Uniform sampling of essences is not uniform sampling of archetypes or acquisition cost. Save generator version, constraints, seed, and every materialized candidate.

Define duplicate identity carefully: preserve slot/party ordering where it affects gameplay. Only deduplicate permutations proven to be equivalent.

Evaluate candidate builds on multiple encounter types. Reserve separate encounter/seed sets for evaluating search winners so that optimization does not merely memorize the discovery suite.

## 7. Party Creator

Use five-character cells as the starting composition unit, with total participants determined by the encounter.

For Tower:

- Read `RequiredSlots` from the floor definition.
- Preserve `PartyNumber` and stable slot order.
- Pin scouting bonuses and distinguish first-clear conditions from boosted repeat attempts.
- Test guardian scaling, stagger participant counts, and party-local targeting with the real runtime factory.

Recommended reference compositions:

| Composition | Purpose |
| --- | --- |
| Canonical balanced | Stable comparison point |
| Attainable mixed | A plausible group without perfect specialization |
| Damage-heavy | Finds burst strategies and missing sustain requirements |
| Sustain-heavy | Finds indefinite survival and timeout/draw behavior |
| Control-light | Tests how strongly success depends on control/stagger |
| Duplicate-heavy | Tests stacking and mandatory combinations where duplicates are legal |

Changing composition should initially preserve progression and acquisition budgets. Study gear improvements separately from role changes so their effects remain interpretable.

Keep multiple viable party templates; do not define balance as one required Guardian/Restorer composition.

## 8. Enemy and Encounter Profiles

Gameplay encounters should resolve real content IDs through the existing catalog/scaling logic.

The resolved snapshot must include:

- Enemy identity, level/scaling inputs, stats, native abilities, resistances, immunities, and target behavior.
- Adds, summons, waves, reinforcements, and recovery rules.
- Stagger, revival, fury, enrage/overtime, or other applicable mechanics.
- Content-specific outcome rules and exact simulation time limit.
- Party size, lane/party grouping, modifiers, and relevant starting state.

Select encounters at ordinary progression points and transitions: new gear tiers, new mechanics, bosses, and party-size changes. These boundaries are more informative than uniformly testing every possible level.

Use artificial high-armor, high-damage, and healing targets only as labeled mechanic probes.

Add content adapters one at a time. Tower already has a shared runtime factory. For content whose preparation or rules still live in a larger service, extract the smallest shared pure preparation/rules seam when implementing that adapter; do not copy formulas into the harness.

## 9. Combat Simulator and Gameplay Parity

Execution flow:

`Resolved fixtures → production preparation → content runtime/rules → CombatEngineExecutor → raw result → content outcome → metrics`

Use `ExecuteSimulationAsync` where it matches the adapter's needs. It accepts a ruleset and returns combat results without the normal entity-state synchronization. Multi-encounter simulations must explicitly reproduce gameplay state carryover using shared logic.

Important checks:

- Match cooldown initialization and basic-attack timing.
- Preserve equipment behavior and granted abilities, essence evolution/ascension, content-specific essence activity, and threat/tanking settings.
- Distinguish `EngineOutcome` from `ContentOutcome`; the harness must use the gameplay clear/win rule.
- Resolve all time-limit defaults explicitly. Current `CombatRuleset` and normal executor paths have different default/explicit limits.
- A gameplay draw or tick-limit result is a completed simulation. An exception or harness wall-clock cutoff is an invalid/incomplete run, not a combat loss.
- Each battle gets fresh mutable combat state. Never reuse entities mutated by a previous run.
- Detailed logging must not change outcomes or consume gameplay RNG.

Parity validation should exercise **both setup and execution**. Comparing two calls against the same already-prepared runtime cannot catch a broken profile or encounter adapter.

Use existing preparation and snapshot tests as foundations. For each gameplay adapter, compare independently prepared equivalent inputs through the normal content path and the harness path, then compare rules, participants, outcomes, duration, terminal state, and appropriate deterministic telemetry.

## 10. Simulation Matrix and Orchestration

The orchestrator owns workflow; it should not own damage, targeting, or scaling formulas.

1. Load suite and validate references.
2. Resolve and freeze content, builds, encounter rules, and seeds.
3. Expand selectors into concrete cells.
4. Print cell/battle counts and estimated cost before execution.
5. Execute with bounded workers, cancellation, and fresh state.
6. Persist per-battle summaries as work completes.
7. Aggregate, evaluate goals, compare a compatible baseline, and write the report.
8. Rerun selected cases with detailed telemetry when explanation is needed.

Avoid the full Cartesian product of every build, essence, party, encounter, and tier. Use fixed references, targeted substitutions, stratified exploration, and then deeper sampling of suspicious cells.

Record declared omissions. A cancelled or budget-exhausted run must be visibly incomplete and cannot establish a passing baseline.

A future resume capability should reuse only completed battle keys from the exact same experiment identity. Resuming under different content or rules starts a new experiment.

## 11. Determinism and Content Snapshots

A seed alone is insufficient.

Capture:

- Harness/report/schema versions and source revision.
- Whether the checkout was dirty, plus an immutable source snapshot reference or retained relevant patch sufficient to reproduce local code changes.
- Exact resolved content and hashes: combat abilities/statuses/summons, essences, equipment/progression, creatures, and the encounter catalogs/settings used.
- Profile/suite revisions, prepared-input hashes, resolved rules, engine options, and tick rate.
- Runtime/SDK, platform/architecture, build configuration, seed-algorithm version, and worker count.
- Fixed logical start time and deterministic participant/encounter identities.

Use a documented stable hash to derive seeds from master seed, scenario/cell identity, trial index, and random-stream purpose. Do not use process-randomized `GetHashCode()`, task order, or worker number.

Keep seed-schedule identity separate from content/code hashes: changing a damage coefficient should rerun the same seeds for a paired comparison. Use separate seed streams for build selection, encounter generation, and combat.

For PvP, run both orientations for each matchup seed and retain the pair identifier. Side swaps are paired observations, not twice as many independent samples.

Share immutable snapshots/catalogs only after confirming they are immutable in practice. Load no changing content during a run. Validate that one-worker and multiple-worker runs produce the same ordered battle results.

Default to bundled file-backed content and synthetic legal players. Avoid real account snapshots. If hydration currently requires persistence, factor reusable hydration from data access or use a disposable local fixture store while proving parity; never replace preparation with approximate stat calculations.

## 12. Metrics Collector and Outcome Semantics

Store one compact record per battle, not just the best combinations or a capped list of examples.

Minimum record:

- Run, cell, fixture, seed, and orientation identities.
- Completion status, engine outcome, content outcome, and termination reason.
- Duration in ticks and seconds using the recorded engine tick rate.
- Available participant damage/healing statistics and terminal health/alive state.
- Available mechanic measurements and telemetry capability/version.
- Reference to a replay bundle or failure details when retained.

Define metrics before implementing charts:

| Metric | Definition and caveat |
| --- | --- |
| PvE clear rate | Cleared attempts / completed valid attempts; report failures, draws, and tick-limit endings separately. Invalid simulations block acceptance rather than disappearing from the report. |
| PvP win rate | Wins / completed valid battles. |
| PvP matchup score | `(wins + 0.5 × draws) / completed valid battles`; label it as score, not win rate. |
| Duration | Show successful-clear and failed-attempt distributions separately, including median and tail percentiles where sample counts support them. |
| Effective healing | Health actually restored, excluding overheal; unavailable until telemetry supports that distinction. |
| Damage contribution | Distinguish actual health/barrier damage, overkill, and any raw attempted damage before ranking abilities. |
| Control contribution | Accepted control duration/interrupts or mechanic progress, accounting for immunity and overlap; do not equate casts with successful control. |
| Death/survival | Separate first downing, revival, permanent death, and final state where those mechanics exist. |
| Resource use | Record only resources actually modeled and exposed by the relevant content path. |

Missing instrumentation is `unavailable`, never zero. A required metric without support makes a scenario invalid. An optional diagnostic should display its limitation.

Duration on victories alone has selection bias: a build that loses its difficult fights can appear faster. Always present it beside clear rate and sample count. Tick-limit endings are capped observations, not genuine kill times.

Use compact telemetry for bulk runs. Retain a bounded, explicitly selected set of detailed examples: representative victories, failures, timeouts, and large changes. Store the selection policy and all their seeds; the first few fights are not necessarily representative.

## 13. Statistical Interpretation and Balance Assertions

Keep two different claims separate:

1. A fixed seed suite changed. This is a reproducible regression observation.
2. The underlying chance of winning is outside the desired range. This is an estimate with sampling uncertainty.

For independent binary PvE outcomes, report a 95% Wilson interval. As a rough worst-case guide near a 50% clear rate, 100, 400, and 1,600 independent battles provide uncertainty of approximately ±10, ±5, and ±2.5 percentage points. These are planning estimates, not guarantees for paired PvP samples or every metric.

Use predeclared sample sizes for acceptance runs. Exploratory resampling may investigate an outlier, but do not repeatedly sample until an ordinary confidence interval happens to pass. Confirm discoveries with a fixed-size run on reserved seeds.

For a target band:

- **Pass:** the interval lies fully inside the band.
- **Fail:** the interval lies entirely outside the band on a disallowed side.
- **Inconclusive:** the interval overlaps a boundary; the estimate does not support a decisive claim.
- **Invalid:** required data, metrics, or simulations are missing or erroneous.

Use the corresponding one-sided rule for a minimum or maximum. Equality belongs to an inclusive bound. Report inconclusive acceptance runs as needing review, never as passes.

For baseline comparisons, retain matched seed-level results and evaluate paired differences. A side-swapped PvP pair is one sampling cluster. Do not apply a binary-win interval directly to draw-adjusted scores or treat the two orientations as independent.

Every regression rule needs a practical effect threshold as well as uncertainty handling. For example, a five-percentage-point clear-rate decline is different from a five-percent relative decline. State the units and which directions are undesirable; easier content can also violate a difficulty goal.

Keep a small, declared set of gating assertions. Large exploratory rankings create many chances for false alarms; discoveries remain advisory until confirmed on reserved seeds/encounters.

The implemented `evaluate` command reads [draft idle goals](../LL/tools/BalanceHarness/Fixtures/idle-goals.json), validates exact cell coverage and the pinned fixture contract, and evaluates absolute clear-rate/mean-duration/mean-health goals or compatible paired baseline changes. Each check records inclusive bounds, units, eligible sample minimum, rationale and primary/guardrail/diagnostic role. Results are pass, fail, inconclusive or invalid.

The original control and First Hunt goals are `Draft`; the selected Blood Grove and primary Crystal Creek starter goals are `Enforced`. Enforced primary/guardrail goals require a written review reason; diagnostics cannot gate. An optional `diagnosticCells` list declares comparison-only cells without inventing targets for them. Required and diagnostic cells must be distinct and together cover the exact saved suite; missing, extra or overlapping declarations are invalid. Assessment includes draft findings while command enforcement considers reviewed checks. Invalid evidence always returns 2; enforced failure returns 1, enforced inconclusive returns 3, and a valid advisory or passing enforced evaluation returns 0. Reports freeze the policy and evidence hashes. The normal-approximation and per-check interval limitations remain visible; no multiple-testing correction or automatic policy promotion is applied.

## 14. Essence Analysis and Scoring

Use scenario-specific evidence before introducing an overall score.

Three levels of analysis:

1. **Observed performance:** results of builds containing an essence. Useful for discovery, but confounded by teammates, opponents, progression, and sampling frequency.
2. **Direct attribution:** damage, effective healing, statuses, summons, and procs attributed to their source when telemetry supports it. This explains events, but does not capture all indirect value.
3. **Marginal contribution:** rerun a matched build with one legal substitution, using the same seed schedule and encounters. This estimates the value of choosing that essence in that context.

Use replacements with comparable slot and acquisition/progression cost. A no-essence control measures the value of filling a slot; it does not establish that the tested essence is better than its alternatives.

Shared-seed comparisons reduce some noise but cannot guarantee identical random events after changing a build. Record paired results without claiming identical fight trajectories.

For synergy, compare four legal variants around the same reference: neither A nor B, A only, B only, and both. On a declared outcome scale, estimate the interaction as `M(A,B) - M(A,0) - M(0,B) + M(0,0)`, where `0` denotes the chosen legal replacement. Report the uncertainty and the reference choices; this is context-dependent and can be distorted by clear-rate ceilings.

Prefer a scorecard of damage, sustain, support, control, and matchup results. If an overall score is added later, publish its scenario weights, normalization, uncertainty, and version. Do not add raw DPS, healing, and control seconds together or count attributed damage again as separate marginal value.

Avoid declaring an essence useless merely because it has low damage or appears infrequently in generated builds. Check its intended role, exposure count, and measured substitutes.

## 15. Baselines and Historical Comparisons

A baseline is an immutable accepted run plus its exact experiment inputs. It is evidence of a reference state, not a replacement for balance targets.

Store small reviewed manifests, fixtures, targets, and accepted summaries in source control. Keep full battle records and replay bundles in an ignored output directory or retained CI artifacts, with checksums and locations recorded in the baseline manifest.

Comparison rules:

- Match stable cell IDs, seed schedules, scenario semantics, units, and scoring versions.
- Allow the intended code/content differences being evaluated and show their hashes/diffs prominently.
- Report changed profile definitions or target policies as experiment changes; compare unchanged cells where possible and mark the rest non-comparable.
- Do not reject every comparison because the content hash changed; testing such changes is the purpose of the harness.
- Keep separate views for stable fixture comparisons and newly generated current-progression fixtures. A generator update must not silently replace the comparison population.
- List added, removed, skipped, invalid, and non-comparable cells explicitly.

Baseline promotion must be an explicit local developer action after reviewing the report and stating why the new reference is accepted. Never overwrite a baseline automatically after a failure. This is a workflow safeguard for future tooling, not a requirement to obtain approval before editing this plan.

The implemented `baseline accept` command validates a complete saved suite and writes a new manifest with its reason, metrics version, relative bundle location, accepted summary and evidence fingerprint. `compare` validates both archives without requiring their old binaries, compares matching complete cells, lists added/removed/incompatible/incomplete cells, and emits advisory JSON/Markdown. Changed code/content is allowed and shown; changed fixture selections, rules, seeds or runtime/platform are excluded. Replay retains strict executable identity checks.

Clear-rate changes use paired gained/lost wins with a conservative approximate 95% interval from Bonferroni-adjusted Wilson intervals. Duration changes use only seeds won in both runs; health uses all matched attempts. Continuous mean intervals are exploratory normal approximations with at least 30 nonconstant paired differences. Versioned goals express draft targets and practical change thresholds. An advisory CI workflow verifies execution and evidence; gameplay-target approval and a multiple-testing policy remain open after the initial cohort review.

When an intentional engine fix changes results, preserve the old report, explain the behavior change, and review targets independently. Rebaselining should not hide an unexplained parity defect.

## 16. Outlier and Dominance Detection

Start with interpretable warnings:

- A progression-reference build misses its content goal.
- A floor or encounter transition produces an unexplained difficulty spike.
- An essence substitution is consistently harmful across its intended use cases.
- A combination outperforms alternatives across several encounter families at the same budget.
- Duplicate stacking sharply improves outcomes or removes a mechanic requirement.
- Sustain creates frequent tick-limit endings or unusually high draw rates.
- A role disappears from successful parties, or only one composition reliably succeeds.

Always attach sample size, effect size, coverage, and reproducible examples. A high rank in one sampled pool does not prove global dominance, and low usage in generated builds is not player pick-rate evidence.

Report tradeoffs: a build that is best against armor but weak against adds may be healthy specialization. Search for alternatives on the damage/survival/control tradeoff frontier instead of requiring one universal ranking.

## 17. Parameter Experiments and Automatic Tuning

Keep production catalogs and existing formulas authoritative. Avoid creating a second balance configuration containing copies of every game value.

Introduce a bounded local overlay only when manual experimentation is useful:

- Explicit allowlist of tunable fields with units, legal bounds, and step size.
- Source content hash, exact old/new values, and experiment rationale.
- Overlay applied to a private snapshot before compilation; cached results keyed by that snapshot.
- Sensitivity experiments changing one parameter or a small related group first.
- Multi-scenario objective with guardrails for progression, build diversity, and unaffected content.

Later search may rank candidate overlays using derivative-free search or another justified method. Penalize unnecessary magnitude/number of changes, use a fixed evaluation budget, and confirm candidates on held-out scenarios and seeds.

Output a proposed patch and before/after report. Never automatically change production files, rewrite targets to fit candidates, promote a baseline, apply migrations, or deploy a suggested adjustment.

## 18. Reporting and Developer Workflow

Start with JSON for tools and Markdown for review. Add CSV exports for cell/matchup tables if useful; a dashboard is optional later work.

The report should show:

1. Run identity, completeness, validity, input changes, and parity/coverage limitations.
2. Primary target results: pass, fail, inconclusive, and draft/advisory cases.
3. Largest practical baseline changes with uncertainty and sample counts.
4. Progression/encounter tables and, when coverage exists, Tower curves and PvP matchup matrices.
5. Diagnostics and concrete replay instructions for selected cases.
6. Runtime/memory results in a separate section.
7. Suggested investigations or parameter experiments with their evidence.

The current CLI supports the following workflow. Use the [tool README](../LL/tools/BalanceHarness/README.md) for runnable PowerShell examples and required options.

| Command | Implemented purpose |
| --- | --- |
| `run` | Save one fixed idle fight |
| `suite` | Run the reference fixture or an explicit suite JSON; `--samples 3` provides a smoke run |
| `replay` | Reproduce a saved fight; suite replay requires `--battle`, and `--detailed` enables the event log |
| `baseline accept` | Validate a complete suite and write a new reference manifest with a reason |
| `compare` | Write an advisory paired comparison against an accepted baseline |
| `evaluate` | Evaluate a saved suite against versioned goals and, for change metrics, a compatible baseline |
| `investigate-entry` | Run the fixed First Hunt/Fury/Armor Chest acquisition experiment |
| `investigate-pressure` / `investigate-pressure-fine` | Run the historical regional-bonus protocols against content without area overrides |
| `validate-blood-grove` | Confirm the fixed local candidate and current policy, checking all other areas, both control suites and four replays |
| `investigate-handoff` | Measure the fixed level-10 reward/Fury preparation steps in Blood Grove and Crystal Creek, with paired findings and eight replays |

Preflight validation is part of run creation; there is no standalone `validate` command or named `smoke` suite selector. [smoke-balance.ps1](../build/smoke-balance.ps1) now wraps two identical suites, disposable repeatability evidence, comparison, replay and draft evaluation for local/CI verification. Resume and broader content commands remain proposed.

Evaluation returns distinct exit statuses for valid advisory/passing enforcement (0), enforced balance failure (1), invalid evidence/configuration/coverage (2), and enforced inconclusive checks (3). Cancellation returns 130. Draft findings remain advisory, and successful execution does not certify unimplemented content types.

The minimum actionable finding is: which case changed, by how much, against which target/baseline, how certain the finding is, and how to reproduce it.

## 19. Suggested Project Boundaries

Keep the first implementation small enough for a solo developer to maintain.

| Location | Responsibility and status |
| --- | --- |
| `LL/tools/BalanceHarness/` | Implemented console entry point, offline composition, execution, metrics, baseline comparison, goal evaluation and reports |
| `LL/tools/BalanceHarness/Fixtures/` | Original idle reference and First Hunt suites with draft goals, plus the selected Blood Grove starter with its reviewed policy; baseline manifests can be versioned separately when accepted |
| Existing `Services.LL` combat/content code | Shared preparation, execution, rules, and minimal reusable seams needed by real content adapters |
| `LL/tests/EssenceSystem.Tests/` | Implemented harness correctness, parity, determinism, comparison and goal-evaluation tests |
| `build/smoke-balance.ps1` | Implemented local/CI workflow verification using a disposable same-revision reference; no gameplay baseline promotion |
| `.github/workflows/balance-harness.yml` | Implemented PR/manual harness tests and advisory smoke configuration, job summary and seven-day evidence retention; first hosted run passed on published main |
| Ignored `TestResults/balance/` | Current local output convention for summaries, battle records, snapshots, replay bundles and baseline/evaluation artifacts |

The tool may reference Core and Infrastructure. Core must not depend on the tool, API, Infrastructure, or Presentation. Keep harness-only schemas and scoring policies in the tool until a second genuine consumer requires shared contracts.

Load API-owned content files through an explicit content root without referencing the API assembly or starting its host. Register only required dependencies; do not start hosted services, reward writers, outboxes, or network integrations.

Do not build separate libraries/services for every conceptual box in the original plan. Begin with a few cohesive modules and extract boundaries only when implementation needs them.

## 20. Delivery Phases and Acceptance Criteria

| Phase | Deliverable | Complete when |
| --- | --- | --- |
| 0 — Trustworthy execution | Offline composition, one real idle encounter, explicit rules, parity fixture | Equivalent gameplay and harness inputs produce matching outcomes/state; repeated seeds and logging modes preserve results; no running API/shared database is needed. |
| 1 — Useful vertical slice | Twelve control cells plus 36 First Hunt cells, fixed fixtures/seeds, compact records and Markdown/JSON reports | Every battle is identifiable and replayable; errors/cancellation are visible; a local change produces an explainable report. |
| 2 — Regression workflow | Targets, baseline manifests, paired comparisons, sample/uncertainty rules, small CI suite | A deliberate behavior/fixture perturbation is detected by the appropriate check; unchanged inputs are stable; incompatible comparisons and inconclusive results cannot pass silently. |
| 3 — Content coverage | Tower first, then boss, dungeon, and PvP adapters in separate increments | Each adapter has setup/execution/outcome parity tests and representative progression/mechanic cases; multi-party Tower and content-specific timeout/recovery rules are covered. |
| 4 — Build/essence investigation | Legal substitutions, bounded generation, attribution where supported, synergy analysis | Findings include matched controls and reserved-seed confirmation; different viable builds and role tradeoffs can be inspected. |
| 5 — Tuning assistance | Bounded overlays and ranked proposed patches | Suggestions improve declared objectives without violating guardrails on held-out evaluation; acceptance and production edits remain explicit. |

Do not block phases 0–2 on a general build optimizer, complete telemetry attribution, UI work, economy simulation, or support for all content. These early phases should already answer whether representative idle progression changed.

Phases 0 and 1 are implemented for fixed idle encounters, including group combat, victory/defeat replay, legal partial equipment, paired seeds, scorecards and visible cancellation. Phase 2 includes explicit baselines, compatible paired comparisons, distinct evaluation outcomes and a [verified portable evidence package](Starter-Baseline-Package.md). Both original cohorts have draft policies, advisory CI and local smoke/reference verification; the first hosted workflow passed on published main. Blood Grove and Crystal Creek have explicitly accepted local references and pass all four reviewed checks through the [repeatable 52,000-battle regression](Starter-Regression-Review.md) on the retained 8 September build, which passed 2,119 backend tests. The First Hunt extension addresses starter choices and enemy counts; the [progression investigation](Blood-Grove-Progression-Review.md) tests unascended training/reinforcement and the [attainable-entry investigation](Blood-Grove-Entry-Review.md) tests Fury/all Armor Chest outcomes with declared budgets and separate confirmation seeds. These are bounded investigations toward Phase 4, not a general build search. Lumo Token replacements and broader Essence exploration are deferred. Off-device storage is deferred by user choice; publication, hosted integration and broader coverage remain open. Do not derive approved targets automatically from observed results.

### Phase 2 integration and follow-up

**Deferred by user choice — 9 September 2026.** The current local workflow is sufficient for now. The items below remain recorded for later; they do not block the first Tower increment. Preserve existing idle behavior, accepted baselines and local evidence while adding the adapter. Relevant regression tests still apply to shared code changes.

1. **Local packaging and recovery are complete.** Keep the [corrected evidence ZIP, checksum, runtime and recovery tools](Starter-Baseline-Package.md) together. Both accepted manifests and all closed budgets remain unchanged. The user chose local storage for now; an off-device copy remains deferred. These checks recover the original execution environment and do not certify later runtime or game changes.
2. Use the completed [local regression script](../build/check-starter-balance.ps1) after relevant game changes, with a tested build and new output directory. The first combined run preserves both fixtures and schedules, passes all four goals and leaves accepted baselines unchanged. Keep historical Inconclusive results and closed sample budgets intact; repeated schedules are not new independent evidence. Automatic hosted gameplay gating remains a separate workflow decision.
3. Review additional journey coverage separately. [Starter Path Acceptance](Starter-Path-Acceptance.md) records onboarding checks and two UI issues; other chest outcomes, complete natural spawn coverage and subjective acceptance remain unmeasured. Original controls and the 36-cell cohort stay diagnostic/draft, without requiring every Essence to win.
4. Keep the [first passing hosted CI run](https://github.com/Dryppie/legends-legacy/actions/runs/34266122671) as evidence for published main. Verify the updated working tree remotely after it is published. CI smoke-tests the two original draft cohorts with three samples per cell and disposable repeatability references; starter policies only gate explicitly selected evaluations. A hosted gameplay regression gate remains a separate workflow decision.

This is policy review and workflow integration. Full dungeon runs, broader Tower coverage, PvP, the complete Beta build matrix, acquisition pacing and automatic tuning still need their own coverage and validation.

### Next implementation: Phase 3 Tower slice

**First slice implemented — 9 September 2026.** The [review](Tower-First-Slice-Review.md) and [README](../LL/tools/BalanceHarness/README.md#first-tower-slice--9-september-2026) record commands, evidence and limits. The original implementation checklist below is retained as the scope of this increment. This is a historical heading; current completed coverage is all 15 floors and 4–10-slot whole-party search, and the current next step is linked at the top of this plan.

The primary game's offline harness in `LL/tools/BalanceHarness` now includes `tower` and Tower-aware `replay`. The fixture uses floor 1's actual five slots, five distinct level-10 starter-reward builds, stable party order/identity and no contributions. It reuses the production Tower runtime factory, shared snapshot rehydration, normal Tower playback and encounter outcome factory. Independent tests persist/reload equivalent snapshots through the normal database-backed path. Detailed simulation replay matches normal playback under the production cooldown/tick rules.

The fixed schedule produced **20 valid trials, 0 wins / 20 defeats, no draws, timeouts or incomplete trials**; the descriptive 95% Wilson clear interval is **0–16.11%**. Four preselected detailed replays match. Full inputs, 16 content files, execution identity, results, Markdown/JSON scorecards and a retained executable are saved separately in `TestResults/balance/tower-floor-1-20260909/`. Existing idle archive contracts, accepted starter evidence and production content remain unchanged by this slice. This completes the bounded first-slice deliverable, not Phase 3 overall; no Tower target or baseline was approved.

1. Inspect the current [Tower runtime factory](../LL/src/Infrastructure/Service/Services.LL/WorldTower/WorldTowerCombatRuntimeFactory.cs), [normal Tower service](../LL/src/Infrastructure/Service/Services.LL/WorldTower/WorldTowerService.cs), [floor catalog](../LL/src/API/API.LL/Data/world-tower/tower-floors.json) and existing preparation/parity tests. Follow the production setup, execution and outcome interpretation. Reuse those paths; extract only a small shared seam if needed, preserving dependency direction.
2. Select one existing floor with complete content and define one legal, explicitly materialized party. Read its actual `RequiredSlots`; preserve `PartyNumber`, stable slot order and participant identity. Record the floor, character progression, equipment/Essences, scouting state, selected content/settings, starting state and a small fixed validation seed schedule before running. Choose routine fixture details from current production rules and document them.
3. Implement the offline Tower adapter through the real preparation and combat rules, without requiring a running API or shared database. Extend the existing scenario, report and replay contracts where needed while preserving historical idle-archive compatibility. Record content, inputs and execution identity so a saved Tower trial can be replayed with its matching build.
4. Add independent parity tests against the normal Tower preparation/execution path with equivalent inputs and seeds. Check prepared participants/rules, terminal state and Tower outcome interpretation. Repeating two runs through the same harness setup alone does not establish gameplay parity. Check repeatability and reject invalid floor/party inputs visibly.
5. Run the fixed scenario, retain its valid results and Markdown/JSON report, and verify a detailed replay. Report wins, defeats and any other production outcomes descriptively. Run relevant backend tests through `build/run-tests.ps1`, and update this plan and the harness README with the implemented scope, commands, evidence and remaining limits.

**This increment is complete when** the fixed Tower scenario runs offline through production rules, independent parity and repeatability checks pass, invalid inputs are rejected, and saved results/report/replay work. Its success criterion is trustworthy execution and evidence, not a required win rate. A single floor does not certify the Tower difficulty curve or a multi-floor journey.

Keep the initial scope to this adapter and scenario. Hosted Phase 2 integration, renewed starter tuning, archive repackaging, Tower balance-target approval, broad party optimization, performance benchmarking and the boss/dungeon/PvP adapters remain later work. Preserve the reviewed starter fixtures, policies and evidence; no gameplay tuning, baseline promotion, deployment or shared-database change is part of this first Tower increment.

After this slice works, expand Tower coverage to representative progression/mechanic floors and alternative legal compositions, including multiple five-character cells where required. Then add boss, dungeon and PvP adapters as separate increments under the Phase 3 acceptance criteria.

### Tower benchmark workflow — implemented 9 September 2026

The next bounded increment is now implemented: [`tower-benchmark`](../LL/tools/BalanceHarness/README.md#automatic-tower-benchmarks--9-september-2026) generates five declared profiles into two fixed party cells, runs floors 1/3/5 using their actual five/five/ten slots, and optionally compares a matching saved reference in the same command. It rematerializes equipment from current balance content and resolves current Essence abilities automatically; manual recipe changes are needed only for intended loadout/progression changes. The local dashboard below now exposes this workflow through a button; build optimization remains future work.

The [catalog and measured review](Tower-Benchmark-Review.md) document budgets, fixed seeds, evidence and remaining limits. Two complete 120-battle runs compared **120 pairs with zero changed gameplay records**; all six first-trial detailed replays matched. Every measured cell had 0/20 wins, reported descriptively with no target band. **All 33 Tower tests and 2,234 backend tests passed** through the repository runner. Independent normal-path parity now includes mixed five-character and ten-character parties; temporary-content tests verify automatic response to equipment and Essence balancing changes. Changes to recipes/seeds/rules are incompatible comparisons, while changed coefficients/builds are recorded and compared. Saved evidence and cancellation are verified before a run can act as a reference.

This advances Tower coverage within Phase 3. It leaves Phase 2 integration, accepted starter evidence, the original Tower slice, production content and external environments unchanged. Broader parties/progression, scouting, approved targets, full Tower journeys and the boss/dungeon/PvP adapters remain separate work.

### Local Tower dashboard — implemented 9 September 2026

[`tower-dashboard`](../LL/tools/BalanceHarness/README.md#local-tower-dashboard--9-september-2026) now provides catalog/floor/party selection, profile previews, fixed sample/seed selection, a reference selector, run/cancel progress, verified saved results and deltas, Markdown/JSON downloads and detailed replay with event filtering/paging. The local host calls the existing benchmark/comparison/replay methods; it introduces no alternative combat path. New runs rematerialize current content, and every operation saves separately. Tower rates remain descriptive.

The [dashboard review](Tower-Dashboard-Review.md) records **2,238 passing backend tests**, including four new real-HTTP integration cases, and browser verification of reference/repeat equality, a ten-character replay and cancellation with completed trials preserved. The final UI fixes passed the four dashboard tests again. Retained evidence/builds are under `TestResults/balance/tower-dashboard-20260909/`; prior Tower runs and accepted starter evidence remain unchanged.

This is a loopback-only developer tool requiring the ASP.NET Core 10 shared runtime. Code edits need rebuild/restart; replay requires the matching retained build/runtime. Discovery is bounded to two directory levels and 2,000 directories under a configurable results root. There is one active operation, no queue/resume or visual recipe editor. Reference approval, broader parties/scouting/progression, full Tower journeys, hosted Phase 2 integration and other adapters remain deferred. No migration, deployment or production configuration change is introduced.

### All released Tower floors — implemented 9 September 2026

The default benchmark catalog now includes **all 15 released floors**, selected by default in the dashboard. Both existing parties run each boss with the production floor's actual 5/10/15 slots: **30 combinations and 600 trials** at the unchanged 20-sample default. Profiles and seed derivation remain unchanged, and earlier evidence is preserved. Selecting a historical reference still restores its original floor selection.

**49 Tower tests passed** through `build/run-tests.ps1`, including independent persisted normal-path parity on every floor, complete slot/PartyNumber mapping and HTTP all-floor comparison with a 15-character replay. A coverage assertion compares the explicit benchmark floor list with the production released-floor provider. Two full browser-driven runs completed 600 battles each, compared **600 pairs with zero changed records**, and **15 detailed replays matched**. Every cell recorded 0/20 wins descriptively. The [all-floor review](Tower-All-Floors-Review.md) records full-run evidence and limitations.

Every fight starts independently with the current declared builds. All-boss coverage does not establish representative progression budgets, acquisition, a continuous Tower journey or accepted difficulty targets. Results remain descriptive. Phase 2 integration, further profile design and the other adapters remain deferred. No gameplay content, migrations or deployment configuration changes are part of this increment.

### Tower progression presets — implemented 9 September 2026

The separate `tower-progression-v1` catalog adds early/mid/late parties over all 15 floors, with 900 trials at the default budget. Character levels 20/50/90, tier/rank budgets 1/0, 2/3 and 2/5, and 3/6/10 level-1 unascended Essences are declared ownership assumptions. Roles and equipment definitions stay fixed; additional Essences extend existing canonical suggestions. These are benchmark stages, not approved acquisition budgets or target audiences. Dashboard previews expose each budget, and the existing reference catalog remains unchanged.

**54 Tower tests passed**, including legal materialization of all 45 combinations, paired seeds, repeat/comparison/replay, catalog discovery and independent persisted normal-path parity for all three stages. Two complete 900-battle runs compared **900 pairs with zero changed records**, with seven diagnostic replays. Early lost every floor; mid cleared floors 1–10 and late cleared 1–11 in all 20 trials per cell, with defeats above those ranges. The [progression review](Tower-Progression-Review.md) records intervals, evidence and limits. These observations do not imply a target band or a boss tuning decision.

Ascension, quality/styles, alternative compositions, acquisition, continuous progression and Phase 2 integration remain deferred. No gameplay content, production service, migration or deployment changes are introduced.

### Tower factor investigation — implemented 9 September 2026

The separate `tower-factors-v1` catalog tests eight presets against all 15 floors: a level-50/rank-3/six-Essence control; level 70/90 variants; rank 4/5 variants; eight/ten Essences at level 90; and the late budget. Each declared parent/child pair changes exactly one recipe factor, with paired seeds and legal slots. The earlier reference/progression catalogs remain unchanged.

**55 Tower tests passed**. Two full 2,400-battle runs compared **2,400 pairs with zero changed records**; nine fixed replays and an additional observed-draw replay matched. At floor 11, the level-90 six-Essence preset won 4/20, eight Essences won 10/20 (one draw), and ten Essences won 20/20. Rank 4/5 at level 50 still won 0/20. No preset won on floors 12–15. The [factor review](Tower-Factors-Review.md) retains per-cell intervals, all-floor summaries, explicit parent relationships, evidence and statistical limitations. These exploratory measurements identify follow-up cases; they do not approve a target or tune a boss.

Single-Essence effects, alternative compositions, acquisition, ascension/styles, full journeys and Phase 2 integration remain deferred. No production content, runtime configuration, migration or deployment changes are introduced.

### Complete four-to-ten Essence party range — implemented 9 September 2026

`tower-essence-slots-v1` supplies every equipped Essence count from 4 through 10 at fixed level 90, Standard tier-2 rank-3 gear and the same role composition. Lower counts leave available slots empty, isolating loadout extensions from character-level changes. All seven parties cover all 15 released floors, for 105 combinations and 2,100 trials at the default budget. Existing catalogs and evidence remain unchanged.

**59 Tower tests passed**, including the exact range, legal preparation, unchanged non-Essence recipe fields, paired seeds, a repeated 105-trial smoke with zero changed records and replay of every count. Independent normal-path checks verify five/six-Essence behavior on floor 11. The dashboard exposes all seven choices; its full run completed **2,100 trials**, and seven retained replays matched. Floor-11 wins for counts 4–10 were 11, 20, 4, 7, 10, 18 and 20 out of 20 respectively, with one draw at count eight. The [slot-range review](Tower-Essence-Slots-Review.md) records all-floor results, evidence, limits and commands. This is conditional equipment/Essence ownership, not an acquisition model or target approval. Phase 2 integration stays deferred; no production content, migration or deployment changes are introduced.

### User Tower progression anchors — 9 September 2026

The user specified that floor 1 should start at about **four equipped Essence slots per character**, and floor 10 should be doable at about **six**. These are qualitative progression anchors; expedition RequiredSlots, a numerical clear-rate target, gear acquisition budgets and other floors' progression targets are separate. The starter 50–90% band still does not apply.

`tower-anchors-v1` checks the first unlock levels (30 and 50), using a conditional tier-1/rank-0 entry budget and the existing tier-2/rank-3 mid budget. Those levels/gear are modeling assumptions, not additional user-approved targets. Both parties run all 15 floors for context; prior level-90 comparisons remain intact. **62 Tower tests passed; all 600 measured trials completed and both checkpoint replays matched.** Floor 1's entry party won 0/20, leaving a gap to investigate for that assumed build; floor 10's six-slot party won 20/20. The [anchor review](Tower-Anchors-Review.md) records intervals and limitations. No numerical target or baseline was approved. Phase 2 integration remains deferred; this increment does not tune combat, change production content, migrate or deploy anything.

### Four-slot entry reinforcement investigation — 9 September 2026

The [entry rank review](Tower-Entry-Ranks-Review.md) adds `tower-entry-ranks-v1`: fixed level-30 four-Essence parties at Standard tier-1 equipment ranks 0–3, plus the unchanged six-slot checkpoint, all 15 released floors. **1,500 discovery battles** completed. Floor-1 wins were 0/0/3/5 out of 20 at ranks 0/1/2/3. A protocol recorded before measurement selected the lowest positive rank with any win (rank 2) for a disjoint 100-trial confirmation, retaining rank 0 and the six-slot control on floors 1 and 10. All **600 confirmation battles** completed using discovery's frozen content/settings and the same executable: floor-1 rank 0 won 0/100, rank 2 won 10/100, and floor-10 six-slot won 100/100.

The Restorer was the first casualty in 91/100 rank-0 and 92/100 rank-2 floor-1 confirmation fights. Next compare support protection and bounded legal four-Essence alternatives at fixed level/rank before choosing a floor-1 tuning overlay. This is diagnostic evidence, not approval of a build budget, numerical target or boss coefficient. No production tuning or baseline promotion occurred; Phase 2 remains deferred. **66 Tower tests passed** through the required runner in an isolated configuration (Release was locked by another test process); **eight replays matched**. The fixture, tests, reports and retained executable preserve earlier evidence and unrelated working-tree changes.

### Intended Uncommon floor-1 budget — 9 September 2026

**User clarification:** floor 1 should be beatable with four Essences per character and **Uncommon equipment, Standard/Fine quality, rank 1–2**. This supersedes the earlier Common-equipment assumption for the intended entry point; preserve those historical measurements. Level 30 and tier 1 remain modeling assumptions. The [Uncommon entry review](Tower-Uncommon-Entry-Review.md) adds `tower-entry-uncommon-v1`, testing all four quality/rank combinations on all released floors alongside the existing six-slot checkpoint.

All **1,500 discovery and 1,000 reserved-seed confirmation battles** completed. Floor-1 confirmation wins were Standard rank 1: **50/100**, Standard rank 2: **89/100**, Fine rank 1 and rank 2: **100/100** each. The six-slot floor-10 checkpoint won 100/100. These builds establish beatability within the clarified budget; no boss change or automatic win-rate target is applied. Mixed sets, other Essence compositions, acquisition timing and a numerical policy remain open. Phase 2 stays deferred.

**Nine replays matched** on the retained measurement build; **75 paired all-floor smoke trials** matched a newly compiled harness. New tests cover exact resolved budgets and independent normal-Tower parity, but execution is blocked by concurrent unrelated `CombatStyleReprisalEngineTests.cs` compilation errors after the required runner was attempted three times. Rerun the Tower filter when that work builds; do not count the previous increment's 66 passes as a current result. Evidence, executables and source snapshots are retained separately; no production content, configuration, migrations or deployment changes were made.

### All-floor progression and composition benchmark — 9 September 2026

The [curve review](Tower-Curve-Review.md) adds `tower-curve-v1` and catalog schema 2: explicit legal per-floor cells, three compositions and a provisional 4–10-slot progression across all 15 floors. Floor-1 gear and the floor-10 six-slot anchor come from the user; all interpolation and later gear budgets remain hypotheses. The preview shows budgets by floor range. Schema 1 remains supported without altered serialized hashes; a real 600-battle historical archive still verifies with zero changed comparison records.

**900 discovery plus 900 reserved-seed confirmation battles completed; 14 replays matched.** Balanced/support/pressure confirmation wins were 57/78/0 at floor 1, 100/52/0 at floor 10, and 18/8/0 at floor 15 (100 trials each). Floor 10 uses provisional Uncommon Standard tier-2 rank-2 gear. Support's remaining 48 floor-10 trials are draws, with a verified production 600-second tick-limit replay. All three compositions had zero discovery wins at floors 4, 7, 12 and 13 under the provisional budgets. Next confirm floors 4/7 and test bounded loadout alternatives before deciding on tuning; no numerical gate or baseline is promoted.

The earlier Uncommon compilation block is resolved: **70 existing Tower tests passed**, then **79 passed** including curve materialization, invalid maps, legacy hash compatibility, repeat/replay and independent normal-Tower parity. No verification remains blocked. Evidence and executables are retained separately; accepted starter archives and unrelated changes are preserved. Phase 2 remains deferred. No production content/configuration, migrations or deployments changed.

### Bounded Essence build search before boss tuning — 9 September 2026

The user identified Essence selection as a major determinant of player/build quality. Legal reference parties alone do not establish strong builds. The [search review](Tower-Essence-Search-Review.md) documents a new one-button workflow that enumerates all 16 combinations of four authored role substitutions while fixing the balanced composition, equipment, levels, Essence progression and 4–10-slot per-floor budgets. Every candidate covers all 15 released floors and fills RequiredSlots through production Tower preparation. The original balanced Essence selections remain the control.

Discovery uses eight trials per candidate/floor. It selects two generalists and up to three distinct floor winners alongside the control, writes selection before confirmation, then tests every finalist on all floors with 30 trials from a disjoint seed set. Content/settings and execution remain frozen. Reports regenerate from verified bundles and include recipes/rationales, discovery ranks, held-out paired gains/losses and role-level diagnostics; detailed replay retains ability statistics/events. No post-confirmation reselection or baseline promotion occurs. This is a bounded hypothesis search, not an exhaustive optimizer or acquisition model. Floor-1 gear remains within the user's Uncommon Standard/Fine rank-1–2 budget; floor-10 six-slot and later gear assumptions remain distinct.

**86 Tower tests passed**, including independent normal-Tower parity for three new build/floor cases, all 240 legal floor/candidate preparations, unchanged non-Essence budgets, deterministic selection, held-out seeds, cancellation, preserved output, tamper rejection, replay and HTTP integration. The initial frozen-settings casing error was corrected before the passing run. Phase 2 integration stays deferred. Boss tuning remains paused; Tower results are descriptive and the starter 50–90% band is not applied. This increment introduces no production content/configuration, migrations or deployment changes and preserves earlier evidence and unrelated work.

The full browser experiment completed **4,620 valid battles**, comprising 1,920 discovery and 2,700 confirmation trials. Candidate 05, changing only the Guardian and Striker Essences, won **330/450 confirmation fights** against control's **218/450**; floor-12 wins improved from 0/30 to 13/30 at unchanged gear/progression. All six finalists won 30/30 on floor 10, while floors 4 and 7 stayed at zero and floor 13 had rare wins. **17 distinct detailed replays matched**, covering slots 4–10, victories, defeats and a draw; a browser replay also matched. Reports and executable/source evidence are retained separately. Next investigate counter-builds and provisional budgets for the unresolved floors, then decide whether boss changes are warranted. The bounded pool cannot certify optimal Essence selection.

### Planned broader Essence loadout search — 9 September 2026

The user requested a plan for finding the best character Essence loadouts. The [Essence loadout search plan](Essence-Loadout-Search-Plan.md) specifies five future increments: legal budgets/catalog and ordering identity; bounded whole-character search with diverse starts and one-/two-slot mutations; complementary party search; frozen confirmation and evidence; and the dashboard/export workflow. It retains all 15 floors and 4–10-slot cohorts, separates generalists from specialists, and compares the search method with random sampling at equal battle budgets. It reports best-found loadouts for a declared context, not universal Essence rankings or global optimality.

The next implementation task is the legal search contract, allowed-pool validation, slot-order/identity audit and mechanics metadata inventory. A small four-slot search pilot follows before the full cohort expansion. Existing-build regression checks stay separate from reoptimization after balance changes. This is a documentation plan, not completed implementation or additional measured evidence. Phase 2 remains deferred; no boss tuning, baseline promotion or production change is authorized by this plan alone.

### Essence selection and confidence intervals clarified — 10 September 2026

The [loadout plan's selection principle](Essence-Loadout-Search-Plan.md#selection-principle-clarified--10-september-2026) records the distinction discussed with the user: select whole legal loadouts by actual party combat performance at matched budgets/seeds, explore individual and combined substitutions, and confirm frozen finalists on unused seeds. Mechanic metadata proposes candidates; individual Essence damage/healing totals do not establish loadout quality. Retain contextual generalists, specialists and uncertain alternatives.

Wilson 95% intervals quantify clear-rate uncertainty; paired intervals quantify changes against a control. Neither is a universal Essence score or the current discovery selector. The implemented four-substitution search ranks total wins, then remaining guardian health, survival and stable ID. Broader catalog exploration and joint party optimization remain planned. This clarification changes no ranking code, statistical threshold, historical evidence, Tower target or deferred Phase 2 scope.

## 21. Verification Strategy

### Controller and whole-party deployments — 10 September 2026

The [whole-party review](Tower-Whole-Party-Review.md) records schema 3: search all five first-cell roles, then test first-cell-only, repeated and alternating five-role deployments throughout the legal party. Every preceding cohort finalist remains a control. Joint discovery freezes the best deployment for each method/seed and the full matched trio for one family before fresh-seed confirmation. Deployment mappings and paired comparisons are retained separately, and Tower Lab exposes the new scope while keeping the earlier four-character search.

The seven-cohort batch starts at six slots, then covers 4/5/7/8/9/10 at the same provisional budgets, with a 72,480-fight cap declared before combat. Six slots uses three search seeds per method; the other cohorts use two. Full-party deployments overwrite both authored contexts identically, so duplicated results cannot establish independent robustness. Actual combat accounting explicitly includes context identity. No boss, gear, Essence or production rule tuning is introduced, and Phase 2 remains deferred.

The batch completed all **72,480 fights**, with 176 joint candidates, 88 frozen finalists and all 30 prior controls. **161 Tower tests passed**, all archives reconstructed, **58 detailed replays matched** and **80 exported normal-Tower fights were identical**. The rendered browser completed another 9,120-fight study and passed run/cancel/results/download/export/replay checks. Six-/eight-slot deployment comparisons improved; four-slot search did not beat the retained best, while ten-slot new finalists cleared every floor in 10/10 per context and tied the best retained party. The review contains every cohort's results, the audited unused-catalog differences from concurrent work, and the preserved evidence package. Next work is deeper joint refinement from strong retained four-/five-slot parties with more discovery samples and fresh confirmation, without changing their budgets or automatically tuning Tower.

### Progression loadout search and dashboard — 10 September 2026

The [progression search review](Tower-Party-Progression-Review.md) records the next scoped B–E increment: explicit 4–10-slot budgets, legal ordered extensions of historical starts, four first-cell character searches, joint screening, fresh confirmation and the Tower Lab **Find loadouts** workflow. Each search covers all 15 floors and production RequiredSlots. The batch predeclares six separate cohorts, runs six slots first with floor 10 as the priority, and caps total work at 41,040 combats. Level and gear vary between cohorts under provisional curve assumptions; their scores cannot isolate the effect of extra Essence slots.

Schema 2 ranks minimum priority-floor gains over each context's own control, then all-floor gains and existing tie-breaks. Wilson 95% remains descriptive. Both methods receive identical historical starting hypotheses, room for fresh exploration and equal actual combat budgets. Exact definitions and reserved schedules are saved before combat, excluding 1,578 historical seeds plus preceding cohorts. Dashboard jobs preview cost, suggest fresh seeds, support cancellation, verify saved reports, display exact finalist recipes, export normal-Tower scenarios and replay archived fights. The local results-folder seed ledger does not cover every external concurrent process.

**144 Tower tests passed** through `build/run-tests.ps1`, including all slot/floor preparation checks, six-/ten-slot end-to-end archive tests and six independent normal-Tower parity cases on floors 1/10/15. An isolated rendered-browser run completed **5,040 battles** and checked cancellation, verified results, JSON/recipe download and replay without page errors. Detailed cohort measurements and archive/export checks are recorded in the linked review.

The six-slot control and all five searched finalists won **40/40 floor-10 confirmation fights in each context** at level 50 with provisional Uncommon Standard tier-2 rank-2 gear. This supports the user's six-slot anchor under that budget; it does not establish optimal loadouts, a guaranteed win rate or an approved gear/acquisition budget. Controller/later-cell optimization, ownership/training, broader context robustness, resume and scheduling remain open. No production tuning, baseline promotion, migration or deployment occurred; Phase 2 remains deferred and accepted starter evidence is preserved.

The progression batch completed all **41,040 combats** and confirmed 26 parties across six budgets. Full semantic reconstruction passed for every archive; **44 detailed replays matched**, and **140 normal-Tower exported fights** reproduced exactly. All six studies and the browser run share the same content/settings/executable fingerprint, with 2,730 distinct reserved seeds and no cross-study overlaps. At ten slots, one frozen finalist won 298/300 with original allies and 300/300 with alternative allies, including 18/20 and 20/20 on floor 15. The review keeps every cohort separate and records uncertainty and remaining limits. Controller/later-cell search at fixed budgets and stronger method replication are the next bounded extension.

### Character robustness and joint-party search — 10 September 2026

The first scoped C increment adds `tower-party-search` and `tower-party-verify`; see the [joint-party review](Tower-Party-Search-Review.md). It searches first-cell Guardian, Restorer and both Strikers using both ally contexts during selection, then screens bounded joint combinations and freezes parties before fresh all-floor confirmation. Selection uses minimum per-context gains against unchanged controls; Wilson 95% remains descriptive. Both methods receive identical historical Restorer A/B/C starts and equal actual combat budgets. Specialist alternatives and asymmetric Strikers are retained.

The four-Essence, Uncommon Standard rank-1 study completed **29,760 combats** across all 15 floors: 17,280 character discovery, 2,880 joint screening and 9,600 confirmation. Three distinct seed schedules exclude 873 declared historical seeds. All seven searched finalists won 40/40 on floor 1 versus 26/40 for the control. The strongest observed all-floor party came from random seed 9677, with 195/600 original-context wins and 199/600 with alternative later-cell allies, versus 46/600 for each control. No guided-method superiority or optimality is established; all-floor totals describe the fixed matrix.

The corrected verifier reconstructed proposals, every score, both selections and confirmation from all 29,760 archived fights without differences. **125 Tower tests passed**, including independent normal Tower parity for joint parties on floors 1/10/15; six party-search tests passed after final Markdown formatting and seven passed after a method-party deduplication regression fix. **14 detailed replays matched**, and forty normal-Tower exported results were identical after JSON decoding. The prior reliability package's 38,072 checksum-listed files remain unchanged. New evidence/executables/source snapshots: `TestResults/balance/tower-party-search-20260910/`; complete run size is approximately 634 MB. No required verification remains blocked.

The command is CLI-only. Controller/later-cell optimization, 5–10-slot searches, other gear cohorts and dashboard integration remain open. No production tuning, baseline promotion, migration, deployment or Phase 2 integration is included.

### Search reliability and ally-transfer verification — 10 September 2026

The [reliability review](Tower-Loadout-Reliability-Review.md) records **35,280 completed combats** (17,280 discovery, 18,000 confirmation), using four search seeds, six discovery samples per floor and forty new confirmation samples. The Standard rank-1 four-Essence entry budget was evaluated on all 15 floors with both original and fixed candidate-05 allies. A shared union of fifteen finalists was frozen before confirmation and tested fully in both contexts. The new schedules had zero overlap with the 183 declared historical exclusions.

Every finalist won 40/40 on floor 1 with stronger allies, while some won only 18–19/40 with original allies versus that context's 25/40 control. Historical A/B and a new random-search candidate won 40/40 in both. Guided search led two all-floor confirmation comparisons, random led five and one tied; no dependable guided-search advantage or optimality claim is established. Strong allies can mask weak target loadouts, so future selection changes must retain contrasting-context evidence and use a new confirmation schedule.

**116 Tower tests passed** through `build/run-tests.ps1`, including independent normal-Tower parity with alternative allies on floors 1/10/15. The verifier rebuilt discovery and confirmation results, **13 detailed replays matched**, and forty exported normal-Tower results matched the compressed originals after JSON decoding. All 11,384 earlier pilot evidence files remain unchanged, and the new reader verifies that legacy archive. Lossless gzip reduced report payload size by **87.74%**; the complete 35,280-battle archive is approximately **655 MB**. Evidence/executable/source: `TestResults/balance/tower-loadout-reliability-20260910/`.

This extends B and scoped evidence/transfer work; all roles/5–10-slot studies, joint party search, wider ally/ownership budgets, specialist retention and the dashboard remain open. No production code/configuration, boss tuning, migrations or deployments changed. Phase 2 remains deferred; no numerical Tower gate or baseline was promoted. No verification command remains blocked.

### Four-Essence character-search pilot — 10 September 2026

The [pilot review](Tower-Loadout-Pilot-Review.md) records the first bounded B implementation: whole-loadout candidates, diverse starts, single/double/order mutations, fixed allies/identities, exact caching and equal-actual-cost random search. The default searches the first-cell Restorer at all 15 floors in four separate Uncommon Standard/Fine rank-1/2 gear cohorts, with four Essences and level 30 throughout. Floor-1 wins are the predeclared primary objective; later-floor four-slot coverage does not replace the six-slot floor-10 progression cohort.

**108 Tower tests passed** through `build/run-tests.ps1`, including new independent persisted normal-Tower parity cases. **8,760 battles completed** (5,760 discovery, 3,000 unused-seed confirmation). Frozen controls and each search arm's winner were confirmed on every floor; duplicate finalists merged. Two shared alternatives won 10/10 floor-1 confirmation fights at Standard rank 1 versus the control's 8/10, while another guided finalist regressed to 6/10. All gains/regressions, pointwise Wilson intervals and paired differences remain descriptive. Guided search showed no clear advantage over equal-cost random search in this small pilot; investigate precision/generation before adding complexity.

The archive verifier reconstructed scores and selection, **11 detailed replays matched**, and ten exported-recipe fights matched the pilot byte for byte. All **3,192** earlier foundation evidence files remain unchanged. New evidence/executable/source snapshots: `TestResults/balance/tower-loadout-pilot-20260910/`. Initial local NuGet access and a disposable export-settings omission were resolved; no verification remains blocked.

The CLI supports search, verification, export and replay. Multiple ally contexts, other roles, 5–10-slot search measurement, joint party optimization, specialist retention, matched-cost authored-search comparison and dashboard controls remain open. This is a scoped B pilot with supporting evidence/export functionality, not completion of B–E. No production code/configuration, boss tuning, migration, deployment or baseline promotion changed; Phase 2 remains deferred. The user's later withdrawal left the existing slot-order policy unchanged.

### Loadout foundation verification — 10 September 2026

The [loadout foundation](Tower-Loadout-Foundation-Review.md) completes increment A of the broader search plan: strict legal contexts/allowed pools/pins, ordered proposal preparation, fixed identity comparisons, a structured mechanics inventory and replayable order probes. The default covers all 15 floors and counts 4–10 in 165 contexts: **652 accepted/prepared proposals, eight recorded family-rule rejections, 80 definitions from 77 families**. These are preparation probes, not additional ranked builds.

The reference factory's new optional identity vector preserves original instance identities across Essence changes and leaves absent-field serialization/default behavior unchanged. **12 of 18 paired order probes changed gameplay, including one outcome change**, so future search must preserve and mutate slot order. **99 Tower tests and all 2,514 backend tests passed** through `build/run-tests.ps1`; **14 detailed replays matched** and a real historical 600-battle archive verifies with zero changed records. Evidence/executable/source snapshots are retained at `TestResults/balance/tower-loadout-foundation-20260910/`. The first failing identity test was corrected before this verification; no command remains blocked.

Next implement increment B's four-slot search pilot, including ordered mutations, diverse starts and equal-cost random-search comparison before unused-seed confirmation. Remeasure earlier strong candidates with pinned identities. Transitive mechanic inference, trained/owned-account budgets, joint party optimization and expanded dashboard controls remain open. Phase 2 stays deferred; no boss tuning, baseline promotion, production configuration, migration or deployment change is introduced. Earlier evidence and unrelated working-tree changes are preserved.

**9 September Tower slice:** [20 Tower harness tests and all 2,157 backend tests passed](Tower-First-Slice-Review.md), through `build/run-tests.ps1`. The fixed 20-trial run completed with no invalid/cancelled/missing battles, and four detailed replays matched. Verification logs, TRX and checksums are retained with the separate Tower evidence. The initial sandboxed commands were blocked by local NuGet/build-output access; the authorized runner subsequently completed. This adds no starter samples and makes no Phase 2 integration claim.



When implementing, run backend tests through the repository entry point:

```powershell
./build/run-tests.ps1 -Filter 'FullyQualifiedName~BalanceHarness|FullyQualifiedName~CombatStyleHarness|FullyQualifiedName~CombatPreparationPipelineTests|FullyQualifiedName~CanonicalEquipmentBuildFactoryTests|FullyQualifiedName~EquipmentHandRuleTests|FullyQualifiedName~CompactCombatTelemetryTests|FullyQualifiedName~FastCombatEngineOutcomeTests|FullyQualifiedName~AbilityBalanceSimulatorRegressionTests'
```

The filter includes the `BalanceHarness` and separately named `CombatStyleHarness` test classes. Add affected content-specific tests for each new adapter. Use the complete `./build/run-tests.ps1` suite when a change affects shared combat/preparation behavior. The dated increments below retain their original results and test counts; they are not claims of fresh verification on a later checkout.

Meaningful new verification should cover:

- Invalid or inaccessible loadouts and unresolved IDs are rejected.
- Identical input snapshots/seeds produce identical results across repeated runs and supported worker counts.
- Each adapter matches production preparation, rules, and content outcome interpretation.
- Summary/detail telemetry preserves gameplay and units, including draw/timeout and revival cases.
- Comparator behavior for improvement, regression, unchanged, missing, incompatible, and inconclusive data.
- A deliberately changed coefficient or broken setup causes the expected result/check to change.
- Cancellation and bounded execution produce partial artifacts without a false passing status.
- Performance budgets on representative content without treating noisy timings as gameplay failures.

`BalanceHarnessTests` covers idle-path parity across progression builds and duplicate/mixed enemy groups, repeatability, logging-mode independence, replay integrity and invalid/cancelled execution. It now includes both encounters for the selected Goblin Warrior/Shortsword/Heavy Breastplate reference. `BalanceHarnessSuiteTests` covers the original schedule, paired/reordered seeds, partial/two-handed loadouts, statistics and cancellation. `BalanceHarnessComparisonTests` covers accepted-evidence integrity, paired statistics, content changes, incompatible/incomplete runs and historical comparison. `BalanceHarnessGoalTests` covers interval boundaries, small samples, policy validation and enforcement outcomes. `BalanceHarnessFirstHuntTests` covers the 36-cell/180-check contract, authored starter choices, reward budgets, independent duplicates, group validation/replay/comparison and legacy hash compatibility. `BalanceHarnessProgressionTests` adds training recipe/snapshot validation, legacy compatibility, unchanged unascended behavior and trained archive/replay/comparison checks. `BalanceHarnessEntryTests` verifies every armor/style budget against production Forge quotes, complete experiment/replay evidence, paired schedule rejection, output preservation and cancellation. All 78 harness tests passed for the selected-starter increment, alongside its 400-battle measurement and two selected replays. Both existing cohort smoke workflows passed in the preceding increment; they were not rerun for this fixture-only change. The earlier 43,200-battle attainable-entry matrix and its 24 replays remain historical evidence. See the cohort and investigation reports for limits. Hosted CI, other-adapter and controlled performance checks remain open.

The reviewed Blood Grove working-band increment adds policy/fixture coverage and statistical-boundary tests, bringing the harness filter to **79 passing tests**. Both saved starter runs were evaluated through the verified evidence reader: each has two failed checks, no invalid checks and the expected enforced-failure exit code 1. No new combat runs, content changes or CI configuration changes were needed to record and evaluate this target.

`BalanceHarnessPressureTests` now covers legal single-value overlays, nonsecret settings capture, fixed selection, full discovery/confirmation/control evidence, output preservation and cancellation. **83 harness tests passed** for the pressure increment. Its full 18,200-battle run, four preselected replays and both complete control comparisons finished; the selected candidate failed the reviewed band and was not promoted. This is one bounded tuning investigation toward Phase 5, not a general optimizer or completed tuning-assistance phase. Production content, existing fixtures/goals and CI configuration are unchanged.

The separate fine-pressure increment extends that coverage to both workflows, the 11-value fine grid, increased discovery budget, fresh/excluded seed schedules and distinct two-decimal bonus labels. **85 harness tests passed**; the 24,600-battle investigation and its four replays/control comparisons completed with one failed and one inconclusive target check. Saved evidence also supports a damage-rounding threshold explanation for the steep response. Both completed experiments remain unpromoted, and production content, recipe/goals and CI configuration are unchanged.

The later [50–90% policy revision](Blood-Grove-Band-Review.md) supersedes the initial band without changing battle evidence or the confidence method. Six saved runs were re-evaluated: two passing, eight failing and two inconclusive checks, none invalid. Both tuned candidates now have a passing mixed-pair check and an inconclusive Raven-pair check. Historical pressure protocols retain v1 explicitly; current evaluations use v2. Verification for this revision is recorded in the linked review.

The [local candidate increment](Blood-Grove-Local-Validation.md) adds area-specific offense and transition-ceiling validation, all-area stat checks and a fixed local confirmation/control/replay command. After updating the stronger-enemy comparison overlay to respect the local override, **all 2,066 backend tests passed** through the repository runner. The declared 21,600-battle run completed without invalid evidence, all 32 external control cells stayed identical and all four replays matched. The policy remains Inconclusive, so no viable baseline was accepted. Regional content is now version 12; no migration, deployment or hosted CI change was made.

The subsequent [level-10 handoff increment](Crystal-Creek-Starter-Handoff.md) added an initially advisory checkpoint fixture and costed preparation comparison without changing production data. **96 harness tests passed**, including eight added cases for reward/gate budgets, actual Forge quotes, saved evidence and four normal-idle parity comparisons. Both fixed 8,000-battle seed sets and eight replays completed with no invalid/cancelled/missing battles. The level-5 archive remained unchanged. The later [acceptance increment](Starter-Path-Acceptance.md) adds the reviewed level-10 policy, historical archive compatibility, 108 passing local tests, both cohort smokes, live local checkpoint observations and the first passing hosted workflow on published main. Neither increment converts the level-5 inconclusive result or level-10 above-band result into a passing baseline.

The later [fine Creek confirmation](Crystal-Creek-Fine-Tuning-Review.md) and [Blood Grove acceptance](Blood-Grove-Acceptance-Confirmation.md) established separate passing local references without changing those earlier results. The [combined regression](Starter-Regression-Review.md) then verified both on the retained 8 September build: **52,000 repeated battles**, zero changed gameplay records/outcomes, four matching replays and **2,119 passing backend tests**.

The [9 September recovery check](Starter-Baseline-Package.md) restored **428,158 files**, passed all four saved-suite checks and matched **eight fixed replays** using the original runtime. Its **15 archive/command checks** passed. This verifies the local package and retained evidence; it adds no independent samples, performs no baseline promotion and does not replace backend testing of later code or runtime changes.

## 22. Remaining Decisions for Broader Balance Gates

The following are design choices still to be established from intended gameplay:

1. Do the initial levels 1, 5 and 10 represent the intended player journey well enough for enforcement?
2. Are the fixture's stated gear/essence ownership assumptions attainable at each point, and what acquisition budget should become authoritative?
3. Which clear-rate and pacing bands describe the desired experience, with what practical change tolerances?
4. Which Tower scouting states and alternative party compositions should be considered ordinary?
5. Are the initial 15-minute CI job limit, three-minute smoke limit and seven-day artifact retention suitable after hosted measurement?
6. Which measurements are worth adding to telemetry for the first useful essence analysis?

These decisions can be recorded incrementally while using the implemented runner and advisory evaluator. They prevent treating initial numerical proposals as established balance requirements.

## Source Map

Repository references used to ground this plan. The idle tool, reference suites, baseline workflow, goal evaluator, reviewed starter bands and advisory smoke/CI exist, including the first passing hosted run. Broader player-cohort enforcement, publication and hosted verification of current local changes, and further analysis remain future work.

- [Ability simulator](../LL/src/Infrastructure/Service/Services.LL/Combat/Engine/AbilityBalanceSimulator.cs) and [request/report contract](../LL/src/Core/Application/Interfaces/Services/LL/Essences/IAbilityBalanceSimulator.cs).
- [Combat engine options](../LL/src/Infrastructure/Service/Services.LL/Combat/Engine/FastCombatEngine.cs), [executor](../LL/src/Infrastructure/Service/Services.LL/Combat/Engine/CombatEngineExecutor.cs), and [ruleset contract](../LL/src/Infrastructure/Service/Services.LL/Interfaces/Combat/Resolution/ICombatEngineExecutor.cs).
- [Preparation pipeline](../LL/src/Infrastructure/Service/Services.LL/Combat/Layers/Resolution/CombatPreparationPipeline.cs).
- [Canonical build factory](../LL/src/Infrastructure/Service/Services.LL/PowerRatings/CanonicalEquipmentBuildFactory.cs) and [cooperative rosters](../LL/src/Infrastructure/Service/Services.LL/PowerRatings/CanonicalCooperativeRosterCatalog.cs).
- [Tower runtime factory](../LL/src/Infrastructure/Service/Services.LL/WorldTower/WorldTowerCombatRuntimeFactory.cs), [floor definitions](../LL/src/API/API.LL/Data/world-tower/tower-floors.json), and [region-boss resolver](../LL/src/Infrastructure/Service/Services.LL/RegionBosses/RegionBossCombatResolver.cs).
- [Combat result](../LL/src/Core/Domain/Models/Combat/CombatResult.cs) and [compact telemetry](../LL/src/Core/Domain/Models/Combat/CompactCombatTelemetry.cs).
- [Canonical build and snapshot parity tests](../LL/tests/EssenceSystem.Tests/CanonicalEquipmentBuildFactoryTests.cs), [telemetry tests](../LL/tests/EssenceSystem.Tests/CompactCombatTelemetryTests.cs), and [backend test entry point](../build/run-tests.ps1).
