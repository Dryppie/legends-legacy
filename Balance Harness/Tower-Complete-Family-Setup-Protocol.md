# Complete retained-family external setup — seed-free verification protocol

Frozen **14 September 2026** after [verified v4 parity](Tower-Complete-Family-Parity-V4-Review.md). Target: offline `LL/tools/BalanceHarness`. Prepare and verify the concrete external setup inputs for the [implemented complete-family contract](Tower-Retained-Family-Confirmation-Design.md). No full study, fresh balance value, combat preparation or fight is authorized here. All 481,603 historical reservations, including the original unused 512 values, remain required.

New package: `TestResults/balance/tower-complete-family-setup-20260914`. Limits: **zero combat preparations, zero fights, zero fresh balance values, zero diagnostic retries/resumes; 1,800 seconds active diagnostic workload and 4 GiB new retained output**. Correctness tests and compilation are separately timed; their output counts toward 4 GiB. Use a separate synthetic domain and literal values for allocator fixtures. Never evaluate a candidate in the real future allocator domain in this scope.

1. Implement a seed-free `tower-complete-family-setup-inspect` command. Verify the existing sealed source, complete family, required history, live registry membership, every history hash, and equality of the authoritative ledger to the complete union. Preserve captured gameplay and every prior source identity. Output a non-executable setup receipt with native execution identity and measurements; do not write `seeds.json`, a registered ledger or a launch protocol.
2. Bind the future external allocator rule: SHA-256 of UTF-8 `domain U+001F invariant-master U+001F label U+001F zero-based-ordinal`, first four digest bytes as signed little-endian Int32; domain `tower-captured-v19-complete-family-v1`, master **2026091423**, labels **first** then **second**, counts **32 / 256**, at most **100,000 candidate derivations per stage**. One exclusion set carries complete history and all accepted values across both stages. This master is an allocator parameter, not a generated combat value. Any later allocation must durably retain its intent, candidate/acceptance accounting and all reservations before combat. This scope runs only the rule's separate-domain fixtures.
3. Freeze exact selected correctness tests before invoking `build/run-tests.ps1`. Filter: `FullyQualifiedName~BalanceHarnessTowerCompleteSetupTests|FullyQualifiedName~BalanceHarnessTowerCompleteFamilyTests`. The new class contains **19 cases**; the unchanged complete-family class contains **52**, for **71 cases**. All selected controller writers are synthetic; no combat integration class is selected. Persist the complete TRX, source hashes, command and elapsed time. Implementation fixes may receive a separately recorded correctness build; diagnostics cannot retry.
4. Build one inspection-only host against the sealed captured-v19 gameplay DLLs, using the v4 retained harness source plus the new setup source. No gameplay assembly is rebuilt for the native diagnostic. Its only entry point invokes the public setup inspector and exposes no allocator, preparation or execution command. Freeze compiler/source/executable hashes, the setup request and expanded command before native execution.
5. Run exactly **one native setup inspection**, at most **600 seconds / 64 MiB native output**, using the UTC source `tower-retained-runtime-v2-20260914`, its audit's complete required history and the authoritative midpoint ledger. The native command validates all 43,879 source scenarios but performs zero combat preparation. Run exactly **one independent Python check**, at most **120 seconds**, reconstructing registry membership, reservation union/hash, counts, source bindings, allocator metadata, gameplay identity and zero-work assertions. Do not generate prospective values in either check.
6. Run one final preservation audit within **120 seconds**, then seal within **120 seconds**. Charge a further **60 seconds** for short static reads plus the measured initial baseline capture. Persist all phase timings and byte accounting, including setup/build/test output, whole changed checkout files, shared TRX and a **2 MiB metadata allowance**. Copy final Markdown into the evidence package; write `evidence-files.json` last. A failed native/independent/preservation check stops dependent work with its evidence retained.

The future full-study envelope remains 2,434,784 attempts / 24 hours / 64 GiB, with zero retries. It does not enlarge this diagnostic or any old experiment cap. This setup cannot establish full-family second-stage selection capacity, combat across the 256-cell outer batch boundary or full-study throughput. Fresh reservation, final runnable protocol/setup charge and execution require a separate concrete scope. Reliability **Fail 1/3**, adoption **Hold**; original v19 remains sealed Unresolved with no confirmation in that experiment.
