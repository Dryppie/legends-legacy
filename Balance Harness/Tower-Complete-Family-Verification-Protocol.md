# Complete retained-family controller — captured source verification protocol

Frozen **14 September 2026**, before tests or diagnostics. Target: offline `LL/tools/BalanceHarness`. This is a new verification package after the [stopped controller verification](Tower-Complete-Family-Controller-Review.md); that package and its eight unintended test fights / two resumes remain sealed under `6cb84e3ffb1e6e0ce3893cb9721ff10dc708fa01e817b3ca32da6f8e1018ea25`. No earlier result is amended.

Output: `TestResults/balance/tower-complete-family-verification-20260914`. Limits: **zero combat preparations, zero engine fights, zero new balance reservations, zero diagnostic retries/resumes, 1,800 seconds diagnostic workload, 4 GiB total new retained output**. Builds and correctness tests are timed separately; their output counts. Charge setup, freeze, diagnostic command walls, preservation, 60 seconds for short static reads and the full 120-second seal allowance. No confirmation launch, tuning, gameplay changes, old cap changes or deployment.

1. Capture the dirty checkout baseline and reviewed source. Preserve unrelated work. Inspect the eight selected test classes and their helpers before execution. The selected controller loops use injected synthetic writers, observations and accounting events; no combat preparation or engine executes. Expected error/cancellation fixtures are correctness tests, not diagnostic retries. Freeze the exact 215 expected test names from the preceding TRX, excluding both cases of `Cancel_resume_reconstructs_selection_and_committed_work_without_accepting_tampering`.
2. Run exactly one correctness invocation through `build/run-tests.ps1`, with isolated `test-build` output and the filter below. Copy the shared TRX immediately and require exact equality with the frozen 215 names, all Passed, with neither excluded case present. A failure stops dependent diagnostics; no retry in this scope.
3. Build a separate captured executable from the sealed UTC proof's captured harness source plus the three unchanged new `TowerCompleteFamily*` implementation files. A dedicated inspection-only entry point invokes the actual public `TowerCompleteFamilyInputs.Inspect` exactly once. It exposes no run/prepare command. Pin all producing sources and executable files. Reference the same captured v19 Application, Common, Domain and Services.LL assemblies as the completed UTC proof, and verify their hashes.
4. Freeze the executable, independent verifier, runner, source seals and input hashes before diagnostic execution. Run one public source inspection against `tower-retained-runtime-v2-20260914`, whose seal is `78b20e50a9af9a8bbd24e37fe09b0050d6a3a4687bc7a8b3dc8c7bf4e3ddfa79` and cells hash is `ab889005721127743f9c5bc9444fa727b5b32b54b42b4c31203f99e501c9dbce`. Native sublimits: **900 seconds / 1 GiB**. The entry point persists elapsed/CPU/allocation/peak-working-set/GC measurements and an existing `TowerPerformanceTrace` command scope. The public inspection uses a private trace guard, so nested source/hash timing percentages are unavailable; do not infer them from the outer scope.
5. Independently check the native receipt once, within **300 seconds**: all 43,879 original scenarios, exact source seal, canonical ordered cell hash, unique inventory/legacy/UTC identities, one context, 580 reasons / 560 anchors (327 historical, 253 midpoint, 20 overlaps), full capacity arithmetic and all **290** interval rows. Compare native bounds with the prior independently calculated table (`interval-arithmetic.json`, hash `c6bfe437cf3ed79c5d59530d8311f974158c14a179e58048f348e59387db6cf7`) within **1e-8**, including endpoint/monotonicity/confidence/rate checks and the first-stage 0–1 / second-stage 48–91 win bands. Preserve failures and stop dependent work without retry.
6. Run one preservation audit within **120 seconds**: baseline checkout/source, prior reviews, full preceding controller and UTC package inventories, all 203 UTC external input bindings, complete history membership and the union of all **481,603 reservations**, including the original unused 512. Record concurrent unrelated changes without reverting them. Update seven active Markdown handoffs and a new measured review. Count all new package files, whole changed checkout files, shared TRX and a 2-MiB final metadata allowance before sealing within 120 seconds. Never write to a sealed package.

Exact test filter:

```text
(FullyQualifiedName~BalanceHarnessTowerCompleteFamilyTests|FullyQualifiedName~BalanceHarnessTowerContextTests|FullyQualifiedName~BalanceHarnessTowerRuntimeComparisonTests|FullyQualifiedName~BalanceHarnessTowerRetainedAuditTests|FullyQualifiedName~BalanceHarnessTowerCeilingControllerTests|FullyQualifiedName~BalanceHarnessTowerStorageTests|FullyQualifiedName~BalanceHarnessTowerStagedTests|FullyQualifiedName~BalanceHarnessTowerMidpointTests)&FullyQualifiedName!~Cancel_resume_reconstructs_selection_and_committed_work_without_accepting_tampering
```

This verifies captured implementation source binding and arithmetic only. Production full-family preparation, compact-batch execution, reconstruction parity, practical selection capacity and full-study throughput remain separate gates. No seed allocator or executable confirmation binding is created. Reliability **Fail 1/3**, adoption **Hold**; sealed v19 retains its original Unresolved/Hold scope and no confirmation of its own.
