# Captured-v19 ceiling screen — reviewed preparation and execution boundary

Frozen **14 September 2026** after [complete zero-combat materialization](Tower-Portfolio-Ceiling-Preparation-Review.md). This handoff preserves the [statistical design](Tower-Portfolio-Ceiling-Recalibration-Plan.md). Status: **MaterializedSeedBindingPending**. It authorizes no fights or seed allocation. The current preparation executable exposes only `prepare`, `check` and `math-fixture`; a combat entry point and final bound protocol remain required before execution.

## Verified inputs

- All 253 confirmed recipes, exact identities/Essence order, 112 controls and 141 generated parties are retained at each factor **1.00, 1.04, 1.08 and 1.12**. All 119 ceiling-only additions remain included. All 1,012 cells materialize successfully.
- The [prepared archive](../TestResults/balance/tower-ceiling-screen-preparation-20260914/materialized/prepared-files.json), [request](../TestResults/balance/tower-ceiling-screen-preparation-20260914/request.json), [input-copy receipt](../TestResults/balance/tower-ceiling-screen-preparation-20260914/inputs-receipt.json) and [source continuity](../TestResults/balance/tower-ceiling-screen-preparation-20260914/source-continuity.json) bind the reviewed outputs and producing implementation.
- Application, Common, Domain and Services.LL DLLs match the completed captured-v19 confirmation. The isolated preparation harness differs only by its added preparation contract and entry point. Any future execution adapter is another harness build and must record its own source/assembly hash while preserving the four gameplay DLLs, settings, content and prepared participants.
- The four `definition-template.json` files have empty seed arrays and all **481,219 exclusions**. They cannot execute or pass ordinary definition validation. Probe seed 1694558530 was reused only for materialization and is not a future screen seed.

## Required binding before a combat command can be enabled

1. Verify every prepared/source hash and audit registered seed history for changes since the retained ledger. Preserve all old and unused values. No historical outcome is a new trial.
2. Only after separate authorization, allocate exactly **128 new shared values** using the frozen policy/master 2026091421 and rejection procedure. Save the complete expanded ledger durably before work starts. Bind the exact same ordered array to every one of the 1,012 cells; retain the full exclusions. Do not use the materialization probe or any prior reservation.
3. Create four ordinary schema-1 executable definitions of **253 × 128 = 32,384 fights** each. Per-definition limits remain within the existing 1,000-cell / 100,000-fight contract; total study allocation is **129,536**, not four independent global allowances. Revalidate exact recipes/content/settings and the final execution identity. Bind all new files and source/driver/command hashes into a final immutable execution protocol.
4. Integrate the four sequential calls to the existing compact runner under one global durable attempt journal, cancellation deadline and storage owner as specified below. Test the integration without combat, including refusal of empty/unbound schedules, missing factors, changed identities, attempted resume and cap drift. Freeze any further diagnostics separately; this screen reserves zero replay/retry fights.
5. Review the concrete bound protocol before launch. Prepared input parity does not itself verify an execution adapter that has not yet been built.

## Global execution contract to retain

The future controller must use one `TowerRescreenAttempts` journal across all factors, charging and flushing each start before simulation, then flushing completion. An interrupted start retains its charge. Use one owning `TowerStorageAccountant` spanning every factor directory and all metadata/pending files, with the existing nested ownership contract for compact batches. Check cancellation and remaining time/bytes before factor creation and at existing attempt/storage boundaries. Closed subtrees remain immutable until mandatory full audits; cached accounting never replaces archive verification.

Preserve the fixed order: factor ascending, recipe ID ordinal, then exact shared seed order; sequential `prepared-v1` execution and chunks of 32. The global ceilings remain **129,536 attempted fights, 14,400 seconds and 8 GiB total new output**, including copies, logs and reconstruction. **Zero retries, replays, discovery fights or automatic resume.** Use the still-absent `TestResults/balance/tower-captured-v19-ceiling-screen-20260914` output; never write into a sealed package. A failure, cancellation, identity mismatch or exhausted limit preserves evidence and stops, without extension or partial Pass.

Each complete factor archive must pass the native compact verifier, including its exact definition, content/executable identity, all 253 cases × 128 seeds/outcomes, journal and final inventory. Independently reconstruct every count and shared schedule before calling the whole-screen selector. Per-factor ordinary assessment uses a smaller family and is only an internal report; the correction decision must use **all 1,012 cells with alpha .05/1,012**. The reviewed selector and independent 129-count fixture establish this arithmetic, not evidence from future trials.

The unchanged selection rule requires all 253 upper bounds ≤50%, at least one lower bound ≥10%, and strongest observed rate in [15%,40%]. Among eligible nonbaseline factors, choose the maximum rate closest to 30%, then the smaller factor. No eligible setting means Unresolved at the fixed grid/caps. Retain every observation, including above-ceiling findings. No pooling, interpolation, optional top-up or factor pruning is allowed.

## Interpretation

At most, a completed screen can identify **CandidateForFullFamilyConfirmation**. The **43,879 retained recipe/context entries** still need strict materialization, context/history coverage audit and a separately bounded statistical/capacity protocol. Existing staged limits remain 20,000 cells and 500,000 fights. Do not shrink that family or carry old-setting outcomes forward.

Current-checkout gameplay is a separate producing version. Neither these prepared copies nor a future captured-v19 screen authorize current-content application, catalog promotion or deployment. Existing reliability remains **Fail 1/3**, adoption **Hold**, and family assessment **Fail / Fail**.
