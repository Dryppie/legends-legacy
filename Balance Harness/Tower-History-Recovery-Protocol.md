# Complete history scan and abandoned reservation recovery

16 September 2026. Target: the offline BalanceHarness. Authorized scope: reduce complete registry traversal cost, audit the failed V3 allocation through a separate recovery record, and verify history validation against the existing 150-second binding allowance. No combat, preparation, fresh seeds, retries, resumed studies or gameplay changes.

## Frozen diagnostic scope

Use `TestResults/balance/tower-history-recovery-20260916` for new evidence. Preserve the sealed V3 study, readiness and execution packages. The execution receipt starts this scope at 3,524.420524455597 cumulative seconds; the existing ceiling remains 3,840 seconds and 4,429,185,024 bytes. This scope allows at most **300 additional diagnostic seconds and 48 MiB**, including preparation, builds, tests, audits, temporary fixtures, publication and failures. Source editing is excluded. Reserve five seconds for publication; stop dependent work at the first failure. No retry under this protocol.

Before execution, freeze source, tests, executable assets, commands and the workflow by SHA-256. Use the same sealed gameplay assemblies as the V3 launcher. Copy the original sequential scanner into the diagnostic only; the candidate uses four bounded directory workers. Both enumerate the complete registry, reject links, include hidden entries, enforce the same two-million-directory cap, and exclude only this new evidence subtree. No cached membership or archive pruning.

Execute these phases once, in order, within the shared 300-second budget (individual ceilings are not additive entitlements):

1. Freeze inputs and checkout preservation inventory: 15 seconds.
2. Compile the isolated candidate/diagnostic: 30 seconds; compile isolated tests: 25 seconds.
3. Run the frozen scanner, recovery, launcher and version test classes through `build/run-tests.ps1`: 40 seconds. Literal fixture labels only; no production allocator or combat engine in tests.
4. Audit the failed V3 study and write an external recovery record: 15 seconds. Verify its exact sealed inventory, failure marker, complete recorded allocation transcript, schedules and Pending delta. Recompute only the already recorded candidate values. Preserve all 45 values permanently; do not publish Complete in the original study.
5. Run the captured sequential scanner over the real registry once: 100 seconds maximum. Persist full membership and trace, including failure.
6. Run candidate history Refresh plus Recheck once, with explicit pinned recovery records for abandoned Pending studies: 150 seconds maximum, also limited by the remaining shared budget. Require identical membership and the complete 482,911-value union. Persist individual scan timings, counts, CPU, allocation and peak memory. This exercises history validation, not allocation or complete binding.
7. Independently audit saved results and preservation: 10 seconds. Publish and seal: five seconds, plus any unspent phase time within the shared cap.

The workflow clamps each process deadline to the remaining aggregate allowance, records owned-process termination, and counts all elapsed diagnostic phases. Stop if there is insufficient budget. The expected test count and exact source list are frozen in `setup-pins.json` and the workflow before its first execution.

## Interpretation and next boundary

The reference runs first; machine load and filesystem caches are not controlled. Report one paired observation, not a universal speedup or historical attribution. A timeout or failed parity check remains unresolved. The old 150-second failure included allocation and other binding work; this check measures the history portion only.

Recovery is opt-in, bound to unchanged source hashes, and supplies reservation exclusion values only. Default Pending rejection and original no-retry/no-launch rules remain. Preserve all 482,911 reservations, including the failed V3 study's 45, the earlier failed comparison's 40, and V19's separate 512 unused values and 253 recipes. V19 reliability remains Unresolved; later reliability Fail 1/3, deep recovery 0/3; adoption Hold. No new balance execution is authorized.
