# Corrected group-diversity verification

15 September 2026. Continue the authorized implementation with the exact policy and diagnostics in the [implementation protocol](Tower-Group-Diversity-Implementation-Protocol.md). The initial package is sealed after 78/79 tests passed; its new-policy full-search case exposed a missing explicit policy/method entry in the provenance validator's loadout-operator allowlist. The correction adds only that entry and retains every parent-count, identity and ancestry requirement. No policy schedule or fixture assertion changes.

Use a new `tower-group-diversity-verification-20260915` package. Preserve the failed package and its **9.218 seconds** of diagnostics; carry **550.237 cumulative seconds** and the entire retained output chain forward. The original 300-second implementation diagnostic allowance still applies across both packages, within 1,800 seconds cumulative. Both packages together remain below 512 MiB and the complete chain below 4 GiB. Each compilation is capped at 300 seconds and recorded separately. Zero combat/seed allocation, preparations, replays or unchanged-scope retries.

Freeze corrected source, unchanged fixtures/scripts and captured inputs before execution. Preserve all 23 preceding sealed packages before/after. Reuse the already completed reference's 128-evaluation old-variation hash and 38 constructor outputs; do not run it again. Build the corrected candidate and its tests once each, with the same captured gameplay DLLs and no restore/gameplay build. Run exactly the same **79 backend cases through `build/run-tests.ps1`** against the corrected executable. On failure stop dependent diagnostics and retain all evidence.

Only after those pass, run the previously unexecuted captured-input constructor once: 19 requests per group/count, variation and diversity policy (57 total), complete greedy order and 1,956 positions covering two breadth/count/filler cycles. Then run the unchanged independent audit once, verifying legacy output parity, novelty order, stable seeds, schedules, recipes, legality, counts and prefix coverage. These are first executions of the deferred construction/audit stages, not repeats of measured outcomes. No target-gameplay fitness callback or combat entry.

Publish the corrected result with the preceding failure still visible and charged. Update six active handoffs. Keep all 482,506 reservations, v19's 253 recipes and unused 512 confirmation values, fixed ability order, unchanged gameplay and adoption Hold. No new balance study, Kharad tuning, deployment or 129,536-fight confirmation.
