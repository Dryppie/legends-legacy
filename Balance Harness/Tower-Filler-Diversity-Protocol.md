# Deterministic filler diversity: implementation and zero-combat verification

15 September 2026. Target: offline `LL/tools/BalanceHarness`. Implement the generic construction change supported by the [saved diagnosis](Tower-Team-Coverage-Diagnosis-Review.md). The root checkout is dirty; capture it before editing and preserve unrelated work. Existing policies and sealed packages retain their behavior.

## Frozen implementation

Add opt-in `independent-team-filler-diverse-v1` / `team-filler-diverse`; keep all defaults and previous policies unchanged. This policy uses the existing team-wide roles, shared copies, legal loadout repetition, allocator, candidate budget and fixed ordinal equipped Essence order.

Expose complete-loadout construction with a validated explicit provider permutation. This changes traversal order only; every emitted recipe remains canonical. Existing constructor entry points continue using their exact ordinal traversal and validation. The new policy visits cores by ordinal ID, then unrestricted focus followed by the five ordinal roles. The unrestricted focus has one pass; each role has at most three. Each pass requests at most one recorded recipe. If a pass exhausts its feasible space, stop the remaining passes for that focus. All passes share the unchanged **256-state / 16-recorded-recipe per-core budget**; no retries or uncharged search expansion.

Before each pass, sort providers by their non-anchor filler exposure in previously retained distinct pool recipes, then SHA-256 of the canonical string array `[version, coreId, role-or-empty, invariant-pass-ordinal, providerId]`, then ordinal ID. Exposure updates only when a new distinct recipe enters the pool; its anchor Essences are excluded. Save each pass's core, focus, ordinal, provider-order hash, exposure hash, and complete bounded constructor result. The pool remains sorted/deduplicated by canonical recipe hash. No controls, combat scores, fresh seeds or generation-label optimization enter construction.

Register the new policy at all existing structural/composition-only gates and provenance checks. Existing structural trace fields retain their schema, with the new explicit version. Allocator caps stay **256 states / 250,000 candidate checks / 16 parties**, with one generation label, at most 16 proposals/evaluations, ten characters and up to five Essence slots. Cancellation and charged attempt/checkpoint behavior remain unchanged. No gameplay, content, configuration, ability-order tuning, default adoption, migration or deployment changes.

## Exact verification

Freeze source, test files, scripts, captured inputs/DLLs, predecessor manifests and this protocol before diagnostic execution. Build once against the same captured gameplay assemblies in a new isolated package: `TestResults/balance/tower-filler-diversity-20260915`.

- Run **71 backend tests** through `build/run-tests.ps1`: the existing 59 constructor/allocator/structural/diversity/team tests plus 12 new tests. The new tests cover provider permutations/canonical output, invalid permutations, exhaustive small-case equivalence, limits/cancellation, bounded pool schedules/exposure, exhausted focus handling, input immutability/metadata order, registrations/invalid contexts, generated collective coverage/canonical order, missing roles without evaluation, checkpointed failure/cancellation, and independence from the generation label. Fake evaluation callbacks and combat-entry guards are mandatory.
- Replay the seven existing old-policy fixtures (224 synthetic evaluations), then captured structural, diverse and team policies (16 each), the new policy (16), and the new policy with reversed metadata (16): **304 synthetic evaluations**, zero fights. Require exact full output parity for all **ten existing policies** and exact new-policy metadata-order parity. Each evaluation returns the same fabricated zero-win/100-health fixture; do not use saved combat outcomes for generation.
- Construct one additional captured new pool for pass/exposure evidence. No runtime/control preparations or combat. Independently reconstruct every bounded constructor pass in Python using the frozen traversal, state/recipe charging, role witnesses, focus termination and exposure updates. Verify pool hashes, all retained teams, shared copy legality, collective roles, canonical identities and trace counters.
- Measure all 80 provider frequencies, distinct pool/used recipes, non-anchor filler exposure, pairwise team replacement distances, and exact/subset coverage against the fixed controls **after generation only**. Reuse the sealed 191 control subsets, including all sizes one to five. Report concentration and coverage changes descriptively, including regressions. No requirement to rediscover a control, and no post-result retuning or rerun.

The new tests' exhaustive fixture compares all 512 combinations of small role requirements, copy masks and slot counts against the existing complete constructor using an alternative provider order. This proves unchanged feasibility when exhaustive, not combat quality. Existing test invariants, both complete/incomplete generation behavior and old serialization must pass.

## Hard limits and closure

Start from **2,300.927 diagnostic seconds**, leaving **99.073 seconds** under the already approved cumulative **2,400-second** cap. This implementation's complete measured workload is capped at **80 seconds**, including preservation, compilation, tests, captured fixtures, independent audit and publication. Non-publication phases stop by 65 seconds, retaining 15 seconds for closure. Per-phase maxima: freeze 20, build 25, test-build 15, tests 15, captured 10, audit 10 seconds, always limited by the remaining aggregate allowance. No extension, reset or old-cap increase.

**Zero fresh balance seeds, zero actual fights, zero runtime preparations, zero retries/resumes/replays of diagnostic commands.** The synthetic fixtures are not balance evidence. Preserve all **482,731 reservations**, including the unused original 512 confirmation values. No new live registry scan or allocation. No Kharad tuning, v19 modification, 129,536-fight confirmation or additional combat study.

New retained output is capped at **192 MiB**, also subject to the unchanged cumulative **4 GiB** cap. Carry forward the latest receipt's prior bytes plus actual sealed diagnosis-package size and a 1 MiB shared-test allowance. Keep full source/command/TRX/performance evidence, before/after hashes and all **58 predecessor packages** unchanged. A one-second closure allowance is charged. Editing source and Markdown is engineering work, not diagnostic execution.

Run once in order: `workflow.py freeze`, `build`, `test-build`, `tests`, `captured`, `audit`, then `publish.py`. Every phase writes create-new start/result/failure receipts and enforces cancellation/deadline/output limits. First failure stops dependent work; publication preserves the failure. Do not modify a frozen diagnostic or create a replacement run to get a pass. Publish measured effects and limitations, actual remaining budget, exact reproducible commands and active Markdown handoffs. Reliability remains Fail 1/3, deep recovery 0/3, v19 Unresolved and adoption Hold regardless of construction coverage.
