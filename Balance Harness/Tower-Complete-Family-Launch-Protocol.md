# Complete retained-family launch accounting: frozen verification protocol

Frozen 15 September 2026 before execution. Target: offline `LL/tools/BalanceHarness`. This scope implements and checks the accounting extension only. It authorizes **zero fresh values, zero preparations, zero fights, zero retries and zero resumes**. The existing reservation and its ten initial files must remain byte-for-byte unchanged. Full study execution remains a separate user decision.

## Fixed diagnostic scope

New evidence root: `TestResults/balance/tower-complete-family-launch-20260915`. The initial dirty-checkout baseline has 4,307 files. Preserve unrelated work; concurrent UI observations are limited to `LL/src/Presentation/ll/`, `docs/ui-rework-verification/` and exact `UI_REWORK_IMPLEMENTATION_PLAN.md`. Explicit protected inputs override that exception.

- Exactly **146 zero-combat tests** through `build/run-tests.ps1`: the previous 108 named cases plus 38 launch fixtures. `producing/test-selection.json` freezes every expected case and the exact filter. Synthetic attempt events are fixture writes, not combat.
- One captured host build from the previous sealed request source with only Program, complete-family inputs/run and the new launch adapter overlaid. Application, Common, Domain and Services.LL remain the captured-v19 DLLs.
- One `tower-complete-family-launch-check` using the final immutable launch request. It verifies source recipes, reservation proofs, setup maps, executable identity and current historical registry. No preparation, allocation or engine execution.
- One independent audit of the complete reserved union, original unused 512, unchanged study bytes/timestamps, twelve predecessor seals / 14,029 indexed files and protected checkout inputs.
- Persist exact commands, exit codes, elapsed time, detailed native trace, failures and final evidence. A failed command or deadline stops its scope without retry. No full-study, completed-study verification or reserve-bind command may run here.

The aggregate diagnostic workload is limited to **1,800 seconds and 4 GiB of new output**. Maximum phase allowances: tests 600 seconds, build 300, native check 600, independent audit 180, sealing 60 and static/setup work 60. Actual phase times plus conservative closeout charges must fit the aggregate bound. Tests and captured builds use isolated output inside the producing package. PowerShell 7 runs the repository test wrapper; existing host NuGet configuration access may require sandbox escalation.

## Immutable reservation and additional accounting

Study: `TestResults/balance/tower-complete-family-reserved-20260915`. Protocol SHA-256: `c0b8ccab56ba01002e2d4bd8268a587329b07d7605348117f5a7354af4fba003`. Completed reservation evidence seal: `988b8d5bca7311f9801db51ea5488b2bcafd66f287f5c6522b961e6d914739aa`. Preserve 32 first-stage and 256 second-stage values, all 481,891 reservations and the 5,391-file / 1,559,084,956-byte / 2,117.2720002000005-second base setup charge. No rebinding or protocol rewrite.

A separate versioned launch request binds the new captured executable, producing seal and exact additional file map. Only the harness assembly may differ from the original execution identity; runtime, architecture, OS and four gameplay assembly hashes must match. The legacy commands keep their original strict identity behavior.

Effective setup time is original setup time plus measured producing setup plus **2,400 seconds reserved upfront** for late work. Effective study storage is original 64 GiB minus original setup bytes, exact additional artifact bytes and **64 MiB reserved upfront**. The late allowance covers native check (600 seconds), independent audit (300), report/closeout (300), run administration (300) and future standalone verification (900). Control output is capped at 62 MiB, including request, logs, ready snapshot and later operation receipts; 2 MiB covers whole changed Markdown. Failure metadata has a further 64 KiB reserve within control output. The request resides in control and is charged there, avoiding a self-hash or recursive inventory amendments.

The producing directory is sealed before request creation. The control directory remains exclusively owned and append-only; each check/run/verify has a durable single-use start, a bounded result or failure, and a fixed deadline. A ready-file snapshot seals current control evidence without claiming that later run receipts already exist. No recursive study scan is added per fight. Original source/preparation, selection, compact batches, durable attempt charging, cancellation and reconstruction remain in the shared controller. Run and verify bind the exact request hash and tightened envelope in `started.json`.

## Future execution boundary

The concrete final request and commands are assembled for review after successful tests/check/audit. They retain the original **2,434,784 maximum attempts, 24-hour total, 64-GiB total, zero-retry** study envelope and the exact reserved schedules. Full-family capacity, combat across the 256-cell boundary and full-study throughput remain unverified. Reliability stays **Fail 1/3**, adoption **Hold**; v19 itself remains Unresolved with all 253 retained recipes and no confirmation inside its sealed experiment. No Kharad/content changes, old cap changes or deployment.
