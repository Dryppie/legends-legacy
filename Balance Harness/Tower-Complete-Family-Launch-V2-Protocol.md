# Complete retained-family compact receipts: frozen verification protocol

Frozen **15 September 2026**, before executing the corrected candidate. The user approved all three steps: fix compact completion receipts, test full-size synthetic run/verify paths, and build/validate a captured executable for an execution decision. This scope runs **zero fresh balance values, zero preparations and zero fights**. It does not launch the full study, retry/resume any sealed attempt, tune gameplay, change old caps or deploy anything.

## Fixed scope and inputs

Target: offline `LL/tools/BalanceHarness`. New package: `TestResults/balance/tower-complete-family-launch-v2-20260915`; immutable producing inputs live in `producing`, bounded post-build evidence in `control`. The initial dirty-checkout baseline contains **4,311 files**. Preserve unrelated work. Concurrent UI observations are limited to `LL/src/Presentation/ll/`, `docs/ui-rework-verification/` and exact `UI_REWORK_IMPLEMENTATION_PLAN.md`; protected input hashes override this exception.

Preserve the entire stopped launch package with seal `0ee1aae438b60440fd5d6b7964ed0686f71ca5b548eb1e64fcfff8ef7b5b61ea`, including its failure marker. The reserved study's original protocol hash remains `c0b8ccab56ba01002e2d4bd8268a587329b07d7605348117f5a7354af4fba003`. Its ten initial files, exact 32 + 256 schedules and all **481,891 reservations** remain unchanged, including the original unused 512. No original ledger or prior experiment is rewritten.

The producing capture copies the stopped candidate's complete captured source and overlays only `TowerCompleteFamilyLaunch.cs`. Four gameplay DLLs remain the captured-v19 Application, Common, Domain and Services.LL binaries. Runtime/OS/architecture are checked against the original reserved identity. Test sources and all other producing/protected hashes are frozen in `producing/scope.json` before execution.

## Implementation and regression boundary

The `complete-family-launch-v2` adapter publishes a typed compact summary: outcome, logical trial count, cell/selection counts and exact assessment/inventory file hashes. The complete assessment remains in its native study archive. Before publication, the adapter compares the verified controller result with the saved assessment and checks its inventory binding. Standalone verification must reproduce the same summary as the earlier run receipt. The 64-KiB record limit, 64-KiB failure reserve, control cap and no-overwrite behavior stay intact.

Run and verify expose internal callback boundaries so zero-engine fixtures execute their actual outer start, deadline, load, result publication, failure and no-retry paths. Public commands still bind and validate real inputs and invoke the existing shared study controller. No CLI fixture/bypass path is added. The controller's search, preparation, selection, batches, durable attempt charging, cancellation and archive verification are unchanged.

Exactly **160 test cases** execute once through `build/run-tests.ps1` in PowerShell 7: the prior 146 cases plus 14 receipt regressions. Exact case names and filter are frozen in `producing/test-selection.json`. The new cases cover:

- Full **43,879-cell** synthetic assessments for both completed second-stage selection and first-stage capacity stop. Both actual outer run and verify adapters must publish receipts at most 65,536 bytes, retain full assessments, bind raw file hashes, preserve scientific outcome/status and reject overwrite. Persist old full-result serialization size and corrected run/verify sizes/times in the TRX output and extracted metrics.
- Seven tampering cases: summary count, assessment hash, inventory hash, request hash, run status, assessment bytes and inventory bytes.
- Four interrupted completion cases: cancellation, changed assessment, exhausted control publication budget and disagreement between saved assessment and returned controller result.
- One failed-prerequisite case proving the outer run stops before any study callback.

Every synthetic archive is local temporary evidence; no combat runner, gameplay roster/input or balance seed is created. Synthetic attempt notifications in reused controller fixtures do not execute combat.

## Bounded commands and completion checks

Run one selected repository test invocation, one captured build, one corrected native launch input check and one independent preservation audit. No command retry or resume is permitted. On failure, preserve the command, log, elapsed time and failure evidence. The captured check uses the final immutable request and verifies the real 43,879 recipes, reserved schedules, producing identities, storage/time accounting and live historical registry without preparation or combat.

Maximum aggregate diagnostic workload: **1,800 seconds / 4 GiB of new output**. Phase maximums: tests 650 seconds, captured build 250, native check 600, independent audit 180, sealing 60 and static/setup 60. Actual times and conservative setup/closeout charges must remain below the aggregate cap. Builds use isolated output; PowerShell 7 and existing host NuGet configuration are used through the required repository wrapper. No package manager changes.

The independent audit must verify **13 predecessor packages / 15,419 indexed files**, all protected producing/input hashes, the baseline outside permitted changes, all **147 historical paths / 481,891 reserved values**, the unused original 512, unchanged study bytes/timestamps, and the exact corrected envelope. Zero run/verify start markers may exist in the new control directory at diagnostic closeout. The stopped request remains blocked.

## Resource accounting and final decision

The original base remains **5,391 files / 1,559,084,956 bytes / 2,117.2720002000005 seconds**. New additional setup includes the complete stopped launch package, reservation observer package and corrected producing package, with no overlapping base paths. Carry forward the stopped diagnostic's **280.47-second** charged work once, plus measured corrected baseline/freeze/tests/build and 60 seconds for capture/sealing. Do not carry the previous candidate's unused future-operation allowances as completed work.

Deduct a fresh fixed **2,400 seconds / 64 MiB** before any future study: check 600 seconds, independent audit 300, report/closeout 300, run administration 300 and standalone verification 900; control files 62 MiB and whole changed documents 2 MiB. The producing seal is finalized before the immutable request. Later control evidence is covered by the fixed allowance; the ready snapshot records existing files while permitting later single-use run/verify receipts after approval.

Final deliverable: measured compact receipt regression results, passing tests, captured executable and one successful real-input check, independent preservation/accounting evidence, active Markdown updates, and the concrete full-study request/commands for a user decision. The future study retains **2,434,784 maximum attempts / 24 hours total / 64 GiB total / zero retries** with the exact existing schedules. No combat is authorized by this verification request.

Reliability remains **Fail 1/3**, adoption **Hold**; v19 itself remains **Unresolved** with all 253 required recipes and no confirmation inside that sealed experiment. Full-family selection capacity, combat across the 256-cell boundary and whole-study throughput remain unmeasured.
