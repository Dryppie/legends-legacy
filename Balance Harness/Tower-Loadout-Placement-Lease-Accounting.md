# Loadout placement: checked leases and selected metadata accounting

Current status: Python writer leases check release results and preserve an earlier failure; success reporting follows cleanup. Native writer leases retain their FileStream contract with opt-in lifecycle counters and distinct ownership-probe outcomes. Verification passes 212 backend tests and 316 Python tests. Selected metadata calls are counted, while whole-process and scratch coverage remain incomplete. Both compressed guards and the 1,806/1,800-second resource gate remain closed.

The target is the offline Balance Harness and its diagnostic owner. `run-proposal-affinity-study.py` now checks the Win32 `CloseHandle` return value, using the captured last-error code when it fails. This correctness check applies with or without diagnostic accounting. A release failure raises if the body succeeded; if the body already failed, its original exception is preserved and gains a cleanup note. Both handles are still visited during stack unwinding. Failed acquisition never causes an invalid handle to be closed. Unknown release state is recorded without retrying the uncertain release.

The owner now uses its lease stack as a context around work and publication. Success output and the successful return occur only after release. A release failure after otherwise successful work adds `failure.json`, prevents success output and makes the diagnostic supervisor fail. Earlier completion/closeout files remain retained as failed evidence; they do not make an archive with a failure marker valid. Acquisition and watchdog-start failures also unwind acquired handles while preserving the initial exception. The watchdog covers lease cleanup. The existing final archive measurements still precede cleanup, so this change does not establish a complete elapsed boundary.

`OwnerFileCounters.operation` records attempts, completed API returns and thrown failures. The owner applies it to selected directory inventories, symlink/junction queries, storage `lstat` calls, final payload `stat` calls, and lease acquisition/release. Lease observations capture their collector when acquired, so a later nested scope cannot take their cleanup counts. Metadata counters count operations, not bytes. A disappeared racing lease or renamed pending file can produce a failed stat followed by a valid storage sample; these outcomes remain distinct. Existing `os.walk` error suppression and path-query semantics are preserved, so an API completion does not prove complete filesystem coverage.

`TowerCompactResume.cs` keeps the original `FileStream` return contract, sharing flags, buffer size and DeleteOnClose behavior. `TowerWorkAccounting.Leases.cs` adds opt-in observations for parent-directory ensure, exists/link-target queries, acquisition and explicit release. The observed stream retains `SetLength` and `Flush`, captures its collector, and observes `Dispose`, `Close` and `DisposeAsync` through the same release boundary. A release is observed once; failure preserves the thrown exception, records unknown state and does not trigger an automatic retry. Native finalization and native `using` exception ordering remain outside this added guarantee; ordinary finalization is not labeled an observed release.

`TowerProposalStudyRun.cs` uses the counted ownership-probe helper. A sharing/lock rejection during acquisition means the enclosing owner holds the lease: the API acquisition failed but the logical probe completed successfully. An available lease is closed and reported as missing ownership. Other errors fail the probe. The exception filter now surrounds acquisition only, so a release failure cannot be mistaken for proof of another owner's lease.

The [updated boundary inventory](Tower-Loadout-Placement-Lease-Boundaries.json) preserves the prior inventory as an immutable reference and pins 55 source references across 16 selected families. It records the implemented boundaries and their limits. Other metadata paths, native finalization, in-memory parsing/serialization, initial authentication, external/transient scratch and outer-monitor work remain incomplete. The resource model is still a draft: no new limits, coefficients, measurements or qualified forecast are supplied. Kernel/job totals overlap application counters and remain separate.

The [verification package](../TestResults/loadout-placement-lease-accounting-verification-20260924) retains logs, TRX results, source snapshots, literal fixtures, process observations and fresh runtimes. The [handoff](../TestResults/loadout-placement-lease-accounting-handoff-20260925.json) pins the evidence and continuation constraints. Final verification passed **212 backend tests and 316 Python tests**, with no skips:

- Ten new backend cases exercise real exclusive leases, DeleteOnClose, missing/held/error probes, stream compatibility, three disposal paths, collector capture and injected release/metadata failures. The previous 202 backend cases also pass.
- Twenty new Python cases cover real lease exclusion, invalid handles, metadata failures, failed release results, original `BaseException` preservation, two failed releases, collector capture, directory/storage observations, acquisition/watchdog failure cleanup and success suppression. Scientific worker commands in owner integration fixtures are routed; their process observations are explicitly synthetic.
- The 13 prior read tests, 280 diagnostic Python regressions and three real native audit process cases pass. The latter verify successful production audit and rejection of changed content or a resealed false input identity, using a fresh retained runtime.

The full literal native audit still reconstructs 12 roots, 24 trajectories, 12 catalogues, 12 physical held-out members, 15,744 trial bindings and one endpoint, with one completed 65,536-byte entropy read. Input identities are real; outcomes are fabricated draws. No audit callback is injected. This is correctness evidence, not a scientific run or coverage of the maximum 36-member held-out layout.

```text
./build/run-tests.ps1 -ArtifactsPath .artifacts/loadout-lease-accounting-final-20260925 -Filter 'FullyQualifiedName~BalanceHarnessLeaseAccountingTests|FullyQualifiedName~BalanceHarnessRemainingReadTests|FullyQualifiedName~BalanceHarnessNativeAuditSuccessTests|FullyQualifiedName~BalanceHarnessReceiptPublicationTests|FullyQualifiedName~BalanceHarnessWorkerReceiptTests|FullyQualifiedName~BalanceHarnessWorkAccountingTests|FullyQualifiedName~BalanceHarnessWriteAccountingTests|FullyQualifiedName~BalanceHarnessFileAccountingTests|FullyQualifiedName~BalanceHarnessContentAccountingTests'
python -B -X utf8 build/test-proposal-lease-accounting.py -v
python -B -X utf8 build/test-proposal-remaining-reads.py -v
python -B -X utf8 TestResults/loadout-placement-lease-accounting-verification-20260924/run-verification.py
python -B -X utf8 build/test-proposal-native-audit-success.py -v
git diff --check -- <changed files>
```

Exact export paths, pins and commands are retained in the package. The first native build failed with five compile errors because an initial disposable-only wrapper did not preserve existing stream consumers. The corrected implementation retains FileStream and its added compatibility cases pass. The final isolated build has 45 warnings and zero errors. The initial failure log/build directory and first Python success fixture are retained; final Python verification uses the whitespace-cleaned test source and a fresh export. No required command remains blocked. Test/process durations are correctness observations, not resource qualification samples.

Changed files comprise the owner launcher/accounting module, native lease acquisition/probe callers, one native accounting partial, two test files, this report, the new inventory, scoped LF attributes and line 3 of nine existing status documents. Existing accounting methods, the supervisor, process helpers, independent auditor, codecs, CLI accounting default and first compressed guards remain unchanged. Historical document bodies, prior inventories/reports, sealed packages and retained runtimes remain intact.

Recorded cumulative charges remain **79,339.66095319996 seconds** and **51,980,910,910 bytes**; declared maxima remain **168,240 seconds** and **107,122,524,160 bytes**. The first failed pair remains failed. No scientific launch, qualification, cost experiment, timing pair, live-history scan, production entropy draw, scientific reservation, encounter preparation or combat occurred. Literal input materialization did occur. The audit floor remains 1,806 seconds against a 1,800-second limit.

Next specify complete retained/pending/lease/log/scratch/runtime-cache domains and their lifetimes, including failed cleanup. Identify enforceable ownership or conservative bounds for external/transient files, and close the outer-monitor boundary before proposing any measurement. Sampled size maxima remain samples, not a true storage high-water mark. Remaining metadata operations must stay explicitly outside coverage until implemented and verified. There are no migrations, application configuration changes or deployments. Future diagnostic packages must bind the updated Python sources and rebuilt native producer; historical runtimes must not be replaced.
