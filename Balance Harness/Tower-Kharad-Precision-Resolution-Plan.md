# Kharad complete-family precision resolution

Prepared 13 September 2026. This plan becomes frozen when the new package writes `protocol.json`, before its first combat. It implements the [prospective design](Tower-Kharad-Precision-Resolution-Design.md) and preserves the completed [Inconclusive family study](Tower-Kharad-Full-Family-Review.md).

## Exact scope and selection

Reconstruct all **17,821** registered recipes and both source stages from the sealed complete-family campaign, including compact outcome archives, original selection, content, settings, producing execution, source indexes, reports and charged attempts. Pin its protocol and final manifests. Its protocol SHA-256 is `dc1bdaaf5bbde2edf98ca95d302067c109143b50f005c3280a8d2164dc71e6b5`; its final file-manifest SHA-256 is `4656ba889374a71889dd32476843e7121ce47b77c6cc3548767e9ca92ff9fd25`.

Retain original first-stage bounds at alpha **.025** over **17,494** recipes. Tighten **all 361** second-stage bounds to alpha **.0125**, then select every remaining unresolved recipe. The independently checked selection is exactly `team-db3598435c3754b58e2b6d9c10ff7be2`. A different selection stops preparation; it does not expand the experiment. The largest retained second-stage upper is **49.31504%**, with **17** supported viable retained teams. The resolved first-stage upper remains **49.19684%**.

Measure that exact saved recipe at the unchanged isolated +16% candidate: Health **3.5366243328**, Power **4.4702934848**. Preserve the source content files and sanitized settings byte for byte. Gameplay assembly hashes, runtime, OS and architecture must match the source; only the harness assembly may change to implement this decision. Preserve producing source and executable snapshots.

Budget remains ten level-40, tier-1, rank-2 Standard characters with five level-1 unascended/unevolved Essences, exact fixed gear, no styles or contributions, and all 80 eligible Essences under hypothetical ownership. Recipes stay external to independent generation, ancestry and ranking.

## Frozen sample and limits

| Allocation | Limit |
| --- | ---: |
| Fresh recipes | **1**, exact complete tightened selection |
| Fresh seeds / recipe | **1,000** |
| Total fresh attempted and completed fights | **1,000** |
| Retries, diagnostic fights, optional extensions | **0** |
| Total run time, including source reconstruction | **1,800 seconds** |
| New study package storage | **1 GiB** |

Import every array of the [475,054-reservation source ledger](../TestResults/balance/tower-kharad-full-family-20260913/seed-ledger.json), including unused historical reservations. Allocate exactly 1,000 disjoint fresh seeds using a deterministic study-specific namespace, and freeze all **476,054** reservations before combat. Do not pool the prior 75/192 observation with the new sample. Preparation and verification forbid combat; fixture tests use separate test content/schedules and supply no study outcomes.

The time cap includes source reconstruction and measurement in the run invocation. A durable global journal records each start and completion. An interruption remains incomplete: no resume, automatic retry, marker reset, extension or replacement schedule. The previous source package is read-only. The one-GiB limit covers the new study directory, including captured binaries, manifests, new evidence and reports; previously sealed source packages and development work are separately retained references.

Resource basis: the full-family captured reconstruction took 442.7 seconds; the earlier 16,384-fight confirmation took approximately 267 seconds. These historical observations support a thirty-minute cap with margin for verification and filesystem overhead. They are planning estimates, not measured performance for this run. Check disk space before freezing.

## Composite decision and completion

Use `tower-composite-precision-wilson-95-v1`: source first alpha .025, tightened source second alpha .0125, fresh family alpha .0125, total .05. The fresh adjustment uses the complete selected fresh family, while retained bounds keep their complete original stage-family denominators. Final refreshed bounds use only the fixed fresh sample. Every source and fresh observed rate above 50% rejects acceptance. Missing, duplicate, extra or replaced evidence, recipes, producing hashes or seeds also reject acceptance.

Pass requires all 17,821 final upper bounds ≤50% and at least one supported ≥10% lower bound per required cohort. For the fresh one-team sample, **460 or fewer wins out of 1,000** supports the upper ceiling. More than 500 wins rejects acceptance; an uncertain result remains Inconclusive and stops. Wilson coverage is approximate, with no lifetime repeated-study guarantee.

Run relevant backend tests through `build/run-tests.ps1` after a successful build. Freeze the protocol, this plan, recipes, seed ledger, source references, candidate content/settings, producing sources/binaries, passing test receipt and resource limits. Execute once, reconstruct all source and fresh archives with the captured executable under a no-combat guard, independently audit the composite arithmetic and conservation of the full family, and seal a separate review and application decision.

Application remains a checked review decision; this study changes no live balance file. Candidate independent-search reliability **Fail 0/3**, practical acquisition, unsearched recipes and floors 6–11 remain separate requirements. Live Kharad stays at Health **3.04881408**, Power **3.85370128** during this work. No migrations or deployments.
