# Complete retained-family controller — bounded production parity v2 protocol

Frozen **14 September 2026**, after the [stopped parity setup](Tower-Complete-Family-Parity-Review.md), before new tests or diagnostics. Preserve that package under seal `a94b566cdf232667756741e3e71550eacb1e379e75c49ef1ebe2ee0c85766ff3`. Its extraction is not reopened. This new attempt uses the corrected `role` / `minimumSamples` cell schema, with typed native validation before combat and durable timing/failure receipts. Target: offline `LL/tools/BalanceHarness`. New output: `TestResults/balance/tower-complete-family-parity-v2-20260914`. This diagnostic cannot launch the full confirmation contract, reserve seeds, resume or alter any sealed study.

## Fixed cases, comparisons and limits

Select by ordinal UTC cell hash, independently of outcomes: the first non-anchor, **`0000b8a0159100cfee696335c83a6b6908665d193492bd665a5df56cbdeaab68`**, then the first midpoint anchor, **`02aba209053fea61609745b10bf1e7c367e9633cd7b0a264faa5ee78beb8d2a0`**. The latter retains midpoint origin **`team-eb63c3547f4c75de71105c01ecd9a989`**. Original recipes, character/Essence order, timestamps, anchor reasons and participants are unchanged. These two diagnostic cells do not replace or reduce the required 43,879-cell confirmation family.

First-stage values are the **first 32** entries, in existing order, from `tower-captured-v19-ceiling-screen-20260914/seed-ledger.json.shared`. Second-stage values are **all 256**, in existing order, from `tower-captured-v19-midpoint-20260914/seed-ledger.json.shared`. They must be distinct, disjoint and already registered/used. Freeze every literal value, recipe, input path/hash and command in the new request/protocol before execution. This is replay/parity use only. The diagnostic's internal batch definitions exclude only these exact 288 values from the full historical exclusion union; the authoritative registry remains untouched. The full-study fresh-seed validator and public run contract remain unchanged and reject this diagnostic binding.

1. Review the dirty baseline and use the already extracted internal preparation and batch/reconstruction adapters, unchanged from the stopped package. Check the builder against the actual five-field cell schema before reading the retained inventory. After the input extraction, run one native zero-preparation check of typed definitions, identities, schedules and the selected saved anchor archive. Include that check in setup accounting. No public cap or bypass option is added. Run the same reviewed **215 fixture tests** through `build/run-tests.ps1`, using the previous verification's exact filter/allowlist, which excludes both combat-capable staged cancellation/resume cases. Compile the candidate against the same four captured v19 gameplay DLLs. Compilation/test repairs may occur before the producing files are frozen; no diagnostic retry is allowed.
2. Freeze the producing sources/executables, corrected test selection, selected scenarios, both schedules, complete history, prior package seals, content/settings and independently written verifier. Stream the retained inventory once to capture the two original scenarios; this setup is charged. Use unchanged midpoint content (+10% Health/Power) already sealed in that study, without editing any content file.
3. Reference: run the **unchanged sealed midpoint executable** once through `tower-balance-run`, prepared execution, one worker, one 32-record chunk, zero retry reserve, **32 maximum attempts**, **120 seconds / 256 MiB**. Its definition contains only the selected non-anchor and first-stage values. Preserve its complete archive. A scientific assessment exit code of 1 is acceptable only with a complete verified 32-trial archive. If its first-stage upper bound at the diagnostic divisor of one exceeds 50%, stop without selecting another case or launching the candidate. This is a declared parity feasibility gate, not a balance assessment.
4. Candidate: call the shared production controller loop once with exactly **288 maximum attempts**, one second-stage place, **1,080 seconds / 2 GiB**. Its preflight verifies the complete sealed source, then calls the unchanged production preparation algorithm for **all 43,879 cells** before any candidate fight. Verify every saved participant hash and persist every input/plan/participant hash plus detailed existing trace measurements. Only the two fixed diagnostic cells enter the small loop. The full study's constants, family validator and public entry point remain unchanged.
5. Run **32 first-stage fights**, verify the actual compact batch and compare every full report digest, summary, prepared roster, outcome, guardian health and duration with the reference. Freeze selection through the production durable publication path. Run **256 second-stage fights** for the anchor and compare them with its **256 sealed midpoint reports**, using the identical gameplay/content/recipe/seed inputs. Do not rerun those reference reports. Physical archive case/index labels may differ; no gameplay field may differ. Native archive verification precedes comparison. Invoke the shared production reconstruction before final publication, then independently reconstruct once more without combat. Retain the controller journal, each child accounting receipt and full file inventories. No detailed replay or combat repetition is added.
6. Independently verify all new compact records and hashes, exact ordered schedules, all 43,879 preparation rows against the sealed participants, both stage decisions, all 288 report comparisons, global and child durable charges, producing identities and exact inventories. Bound this one independent verification to **120 seconds**. Check preservation/history once within **120 seconds** and seal within **120 seconds**, with **60 seconds** charged for short static reads/metadata. Record any mismatch or resource stop and do not retry dependent work.

Total new combat: **32 reference + 32 candidate first stage + 256 candidate second stage = 320**, below 512 including all repetitions. **Zero fresh balance seeds, retries, resumes or additional replays.** Expected preparation calls: **43,879 full-family + 1 reference reusable + 2 candidate reusable = 43,882**, with no combat during full-family preparation or verification. The diagnostic-wide bound is **1,800 seconds / 4 GiB new retained output**; count setup, freezing, both native commands, independent verification, preservation and sealing. Builds and fixture tests are timed separately, but their output counts. The setup/check/freezing allowance is **120 seconds**, reference 120, candidate 1,080, independent verification 120, preservation 120, sealing 120 and static metadata 60: **1,740 seconds**, leaving a further 60 seconds of global headroom. Abort before combat if measured setup/check/freezing exceeds 120 seconds. External command watchdogs persist failures; the input extraction itself is limited to 100 seconds. Source/script/build preparation is timed separately from compilation and included in the setup charge. External watchdogs and the native candidate deadline enforce these bounds. No timing-driven repetitions or extensions.

## Evidence and remaining scope

Pin the completed source-verification seal `52061b1d68f628ccce2f7b98fb667a37d88dbf5b91252fa3babdf0b6705bf6a4`, UTC source seal `78b20e50a9af9a8bbd24e37fe09b0050d6a3a4687bc7a8b3dc8c7bf4e3ddfa79`, complete cell-file hash `ab889005721127743f9c5bc9444fa727b5b32b54b42b4c31203f99e501c9dbce`, midpoint study seal `8a23325bea8fe71f62be5f313b6e49cf29814c276572122e1970c2412d0a2300`, and all selected saved-archive files. Preserve all **481,603 reservations**, including the original unused 512, all older reviews and unrelated checkout work. The previous eight-test-fight/two-resume scope violation remains sealed.

This verifies production preparation and a small actual two-stage execution/reconstruction path. It does not establish full-family second-stage capacity, 256-cell batch-boundary combat parity, full-study throughput or new statistical evidence. Tests cover synthetic batch/capacity/cancellation/tampering boundaries. A full 2,434,784-attempt study still needs a separate concrete external allocator/reservation/setup binding and execution scope. Reliability **Fail 1/3**, adoption **Hold**; no gameplay tuning, deployment, migrations, confirmation launch or old-cap increase.

The producing diagnostic entry point supports only typed input checking and the fixed two-cell parity execution. It cannot call the full-study public Run, allocate values or alter full-family validators. The reference uses the unchanged midpoint executable; the candidate uses captured gameplay DLLs plus unchanged current controller sources. Frozen script and executable hashes are required before either combat command. Known fixture/build errors may be repaired before producing freeze, but any real input/diagnostic failure stops dependent execution without retry.
