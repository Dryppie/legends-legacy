# Complete retained-family reservation — approved execution protocol

Frozen **15 September 2026**. The user's “Please proceed” answers the explicit request to reserve exactly **288 new values**, with **zero fights**, under **600 seconds / 64 MiB / zero retries**. This is the authorized exception to the earlier zero-fresh-seed limit. It does not authorize a full study, preparation, gameplay changes, old-cap changes or deployment.

Use the exact [prepared request and executable](Tower-Complete-Family-Request-Review.md). Request: `TestResults/balance/tower-complete-family-reservation-request-20260915.json`, SHA-256 **`bc264a3468b7a127ca7f8d27ba15ab9d9dfb87eba394a54d565d4dfa6e621a5c`**. Captured executable package: `tower-complete-family-request-20260915`, seal **`5ddd0e9d91babb95759c0f68a2ae713ceb5ddf5323a218be64bff984202aed7d`**. Do not change either. Target study: `TestResults/balance/tower-complete-family-reserved-20260915`; observer evidence: `TestResults/balance/tower-complete-family-reservation-20260915`.

1. Freeze command, hashes, checkout baseline and this protocol before invocation. Within **20 seconds**, verify the approved request, all 5,391 effective setup inputs (including the original request), captured executable/runtime metadata, unchanged producing files and the saved exact 108 passing zero-combat test cases. Reuse tests/builds because implementation is unchanged. Confirm the new study is absent. Do not derive a candidate during these checks.
2. Invoke `dotnet <captured BalanceHarness.dll> tower-complete-family-reserve-bind <approved request>` exactly **once**. The unmodified request retains its 600-second/64-MiB native cap; use a stricter **400-second external watchdog** to reserve time for verification/reporting within the approved total. Capture stdout/stderr and exact wall time. Never retry, resume or overwrite. Preserve any failure, Pending marker, unmatched start and returned values. Do not call run, combat preparation or controller verification of a nonexistent completed study.
3. After a successful invocation, run one independent read-only audit within **120 seconds**. Reconstruct only recorded candidate derivations in first/second order, all history/cross-stage rejections, schedules, the 481,603-value historical exclusion set and completed 288-value reservation. Check the durable start/candidate pairing, receipts, exact protocol fields, every frozen file, effective setup bytes/hashes, original request hash and the absence of combat starts/attempts/batches. Verify all **11 predecessor packages / 13,999 indexed files**, protected inputs and checkout preservation. Traverse live history once and require exactly the original 145 paths plus the study's two registered history files, with union **481,891** and the original unused 512 intact. No novel candidate beyond recorded ordinals may be evaluated.
4. Reserve **60 seconds** for static work and closeout. Total charged active work is measured preflight + native + audit plus this allowance, at most **600 seconds**. Count the study, observer package, whole changed Markdown files and a **2 MiB metadata allowance** together against **67,108,864 new bytes**. Update the seven active handoffs, create a measured review, retain final document copies, inventory/hash the new study read-only and seal observer evidence last. Keep observer files outside the study so its exact frozen initial inventory remains unchanged. Record the observer output separately for any future full-study accounting; do not silently amend the approved request or published protocol to include it.

A native failure or audit/limit failure stops dependent execution; preserve evidence and state what is incomplete. No new reservation retry or replacement schedule is authorized. On success the new study ledger becomes the complete 481,891-value reservation history, while every original ledger remains unchanged. The published setup time charge is the previous **1,517.2720002000005 seconds + the full 600-second binding allowance = 2,117.2720002000005 seconds**; the bounded reporting work above fits within that same reserved allowance and is not added twice.

The executable's reservation result provides wall time and durable file timestamps, not a persisted detailed phase trace. Report those limits honestly. This operation makes no throughput claim. Reliability remains **Fail 1/3**, adoption **Hold**; original v19 remains sealed **Unresolved**, with all 253 recipes and no confirmation inside that experiment. Full-study execution and its resource accounting/decision remain separate.
