# Refinement V3 launcher integration

16 September 2026. Target: offline `LL/tools/BalanceHarness`. Implement an explicit comparison version selecting `independent-discovery-refinement-novel-v3`, while omitted requests retain V1. Keep canonical scenario identity, schedules, fixed ability order, caps, attempt charging, cancellation and archive verification unchanged.

## Preparation boundary

Evidence directory: `TestResults/balance/tower-refinement-launcher-v3-20260916`. Incoming authority is the sealed novelty-closure package (`files.json` SHA-256 `5e47cc7e6112ba4d4aef09203b90d831e2678c90aa9e8421f4842b17b15c53d6`): 3,341.7534694555843 diagnostic seconds used and 221,651 bytes remaining under 3,600 seconds / 4 GiB. Count retained preparation artifacts too. Editing is excluded from diagnostic time; executed checks are charged.

Before any build, allow one dirty-checkout snapshot (10 seconds), then one preparation audit/publication (10 seconds), at most 192 KiB combined output. Snapshot tracked modifications and untracked source files by hash; preserve unrelated files. The audit checks selected source hashes, Python syntax, exact fact count, frozen input hashes, scoped whitespace and Markdown links, and records the remaining allowance. It does not compile C# or claim tests pass. No retries; preserve the first failure and skip dependent checks.

## Verification awaiting output authorization

The existing output allowance cannot hold the harness DLL. Request exactly **64 MiB additional cumulative diagnostic output**, solely for this integration's builds/tests/evidence, before executing the following frozen scope. This changes no experiment cap. The cumulative time ceiling stays 3,600 seconds; this scope uses at most 180 seconds including failure publication. Zero real fights, combat preparations, combat replays, fresh seed allocations and retries. Synthetic fixture labels remain outside the real registry.

1. Freeze once (15 seconds): verify the preparation seal, current selected source/tests, immediate prior seal, captured gameplay/dependency hashes and cached restore assets. Copy only the five changed harness files and selected tests/fixtures. Compile all other harness source from the existing sealed storage-profile and novelty packages. Preserve separate compiler intermediate directories. No restore, gameplay build or build-server reuse.
2. Harness build once (30 seconds), then test build once (25 seconds), using the pinned gameplay assemblies, isolated projects and output directory. An assembly resolver loads unchanged dependencies from the sealed package. No executable combat diagnostic is included.
3. Run exactly **60 backend facts** once (60 seconds), through `build/run-tests.ps1 -NoBuild`: ten gate, twelve comparison/controller, ten launcher, eight preflight, ten shared-executable, and ten new V3 integration tests. Use a dedicated retained temporary fixture directory outside `TestResults/balance`; count all its bytes plus a 1 MiB allowance for shared test output. Literal fixture labels and fabricated journal events exercise binding/accounting without a generator or combat engine.
4. Audit once (25 seconds): require all 60 executed/passed and zero failed; verify exact test identities, unchanged gameplay/search sources, legacy omission/default contract, unchanged sealed failed study, zero production allocation, unrelated-checkout preservation and scoped whitespace. Report failures without a rerun. No whole-registry scan or historical combat replay.
5. Publish once (15 seconds, reserved even after failure): record every phase duration, output including retained fixtures, source/binary/test hashes, precise failure or completion, changed files and six active handoffs. Seal evidence without deleting historical output to recover space. Compilation and fixture checks establish launcher correctness, not stronger builds or combat throughput.

The ten new facts cover: legacy field omission/default definition parity; supported V3 pair and unchanged shared gameplay; unknown/mixed/policy-mismatched versions; version-specific driver receipt; request/authorization version binding; preflight-version rejection before fixture allocation; preflight-request rejection before fixture allocation; complete V3 bind/run/reconstruction; incomplete V3 gate stopping subsequent stages; and V1 nominations rejected for V3. Existing cases retain cancellation, storage, durable charges, no retries and archive-tamper coverage.

Prepared workflow: `python -B TestResults/balance/tower-refinement-launcher-v3-20260916/workflow.py <phase>`, sequential phases `freeze`, `build`, `test-build`, `tests`, `audit`, `publish`. It must require an explicit output-authorization receipt before `freeze`. First failure permits only `publish`. Full commands and deadlines are retained per phase. Reproduction uses a new separately bounded directory; never rerun a sealed package.

Preserve all 482,866 reservations, including the failed comparison's 40 unused values and V19's separate 512 unused values and all 253 recipes. No real comparison is authorized. V19 reliability Unresolved, later reliability Fail 1/3, deep recovery 0/3 and adoption Hold remain. No Kharad tuning, gameplay content/configuration change, migration or deployment.
