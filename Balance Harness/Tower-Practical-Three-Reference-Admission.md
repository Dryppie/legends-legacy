# Three-reference practical search: native admission

**Execution completed — 22 September 2026:** The [single admitted search and both audits](Tower-Practical-Three-Reference-Execution.md) passed with **3,528 fights** and `IncumbentRetained`. Reference `96b94357…` won **762/1,000**, versus **700 /624** for the original controls. All **551,408 values** are now excluded across **232 files**. The run is closed. The admission evidence and historical no-search status below remain unchanged.

22 September 2026. Target: offline `LL/tools/BalanceHarness`. **ThreeReferenceNativeAdmittedNoReservation**. Native admission passed for the confirmed `96b94357…` recipe and both original controls. The complete live registry remains **550,367 exclusions across 229 history files**. No search, combat, allocation or repeated native admission occurred.

The [implementation](Tower-Practical-Three-Reference-Search.md) is now connected to the [verified reuse bundle](Tower-Confirmed-Team-Reuse.md) through a concrete captured-runtime request. This establishes input validity and compatibility for the captured gameplay scope. It supplies no new strength or balance result.

## Admitted inputs

The [request](../TestResults/three-reference-native-admission-20260922/preset/request.json) and [unscheduled template](../TestResults/three-reference-native-admission-20260922/preset/template.json) retain floor 5, ten level-40 characters, two five-character groups, five Essences per character, rank 2 /tier 1 /standard equipment, actor identities, the allowed pool and `OwnedCopies=null`.

| Choice | Frozen value |
| --- | --- |
| Request | `tower-practical-three-reference-allocated-search-v1` |
| Generator | `retained-composition-three-references-v1` |
| References | `399bc776…`, `8287f779…`, `96b94357…` |
| Selector | `tower-staged-incumbent-tie-v1` |
| Existing tie designation | `confirmed-399bc7760fb0cf790a5d8ac4` |
| Candidate/proposal limits | 46 evaluated candidates; at most 256 proposals |
| Discovery | 8 trials per candidate; 368 fights |
| Selection | Five nominees; 32 trials each; 160 fights |
| Confirmation | 1,000 trials per distinct recipe; at most four recipes /4,000 fights |
| Maximum total | **4,528 fights**, one root, no diagnostics or replay reserve |
| Practical interval family | Ten quantities; improvement must pass against all three references |
| Prospective allocation | Master `20260922`; domain `practical-three-reference-pilot-20260922` |
| Prospective accepted values | 1,041: one construction +8 discovery +32 selection +1,000 confirmation |
| Proposed cumulative limit | 1,800 seconds /2 GiB |
| Precharged admission allowance | **600 seconds /512 MiB** |
| Remaining proposed execution limit | **1,200 seconds /1,536 MiB**, including launch checks and publication |
| Intended new search output | `TestResults/balance/tower-practical-three-reference-20260922`, absent at closeout |

The new request explicitly accounts for the third reference. It does not reuse the completed two-reference experiment's allowance. The converter preserved both controls, the existing selector and its designation, the prospective allocation choices and resource limits. Importing the confirmed recipe did not make it a new primary or default. A selected reference is measured once during confirmation; unused fights cannot extend another stage.

The native check's `declaredValues=1041` counts temporary validation labels, not allocated values. A future 1,000-trial confirmation remains a practical pilot and does not guarantee sufficient precision to demonstrate a five-point gain.

## Runtime and native evidence

The capture retains the preceding gameplay dependencies, all 16 content files and effective settings. Only the four harness runtime files were replaced with the tested three-reference build. Portable symbols matched **196 producing source documents**. **562 methods**, including the preset and allocation admission paths, resolved without executing their bodies. All loaded execution assembly hashes matched their captured files.

```text
Execution hash: 8fbd85a5e3c669fcb588dbf4d021fa7616d6dff2a1a8885d7298897fb26f03a0
Settings hash:  f9587e8941a021443aecef37dccf0d5dad250a28df32673cacf7f2df50b12b74
```

The [preset receipt](../TestResults/three-reference-native-admission-20260922/preset/preset.json) records the exact three references, source and output hashes, five nominees and cost. The subsequent [native admission](../TestResults/three-reference-native-admission-20260922/native-check.json) returned `ReadyNoReservation` after preparing one native input for each reference and verifying the full history, including the pinned abandoned-reservation recovery. Its combat-entry guard remained active. Context, preset and native-check processes all exited successfully with zero active descendants in their owned Windows Jobs.

## Closeout correction and accounting

The original Python closeout check failed **after native admission passed**: it compared the nested dependency `runtimes/win/lib/net7.0/System.Management.dll` with a filename-only lookup. The corrected check compares complete paths relative to the runtime root and retains every hash check.

The [original failure](../TestResults/three-reference-native-admission-20260922/failure.json), original package and original manifest remain unchanged. A separately bounded, read-only reconciliation authenticated that seal plus the appended failure, checked every runtime dependency against the reuse context, reconstructed the exact request conversion, and refreshed the complete live history. Two negative checks rejected incorrect manifest and failure pins. Native preparation was not repeated.

- [Reconciled admission receipt](../TestResults/three-reference-admission-closeout-20260922/receipt.json)
- [Closeout package pin](../TestResults/three-reference-admission-closeout-20260922-pin.json)
- [Original native package manifest](../TestResults/three-reference-native-admission-20260922/files.json)
- [Preparation and read-only verification helper](analysis/prepare-three-reference-admission.py)

Original preparation took **139.954 seconds**, including its failed closeout. Reconciliation took **46.157 seconds**, for **186.111 seconds** of observed operational work. The original package is **48,659,913 bytes**; reconciliation adds a small separately recorded evidence package. The proposed request charges the entire **600-second /512-MiB** admission allowance conservatively. Earlier engineering work remains separately disclosed under the accepted treatment; no complete historical engineering total is inferred or reset.

The original manifest excludes the subsequently appended failure. Consumers must authenticate both through the reconciled closeout receipt; removing the failure or treating the original manifest alone as a successful package is incorrect. The closeout manifest SHA-256 is:

```text
65cde20aabb7deaffcd889b0e685a9bd26830d63be4678db75a2f4506956d213
```

## Verification and next step

Verification comprised the source-symbol and runtime checks, public preset command, public native allocation check, independent saved-evidence reconciliation, two negative pin checks, full history comparisons and scoped syntax/Markdown checks. The initial wrapper failure is retained and reconciled. No required check remains blocked. Backend tests were not repeated because this step changed no C# code; the preceding implementation's tests remain pinned in the admission package.

Changes are the preparation/verification helper, captured request/runtime/evidence packages, this report, and current pointers in the implementation, reuse, state, README and practical-search guides. There are no gameplay changes, application configuration changes, migrations, database operations or deployments.

The admitted search has since completed within its remaining 1,200-second /1,536-MiB allowance; its outcome and current history are linked above. Admission itself did not launch that search. The completed 44,000-fight experiment remains closed with its historical NotAssessed balance. The later practical search reports a separate captured-cohort balance Fail; V19 reliability and the 162 current-family context exceptions remain unresolved.
