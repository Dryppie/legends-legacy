# Team coverage comparison: readiness

**Completion update — 15 September 2026:** the specifically approved comparison has now completed and passed native plus independent verification: **288/288 fights**, 45 new values, **482,731 total reservations**, zero retries. Both finalists and controls won 0/32; improved win probability remains unestablished and adoption remains **Hold**. See the [execution review](Tower-Team-Coverage-Comparison-Execution-Review.md) for outcomes, the **108.978-second remaining diagnostic allowance at publication**, and the next saved-data diagnosis.

**Historical preparation record:** the text below records readiness before approval. Its pending-approval statements, proposed counts and unexecuted-command labels describe that earlier state; they are not current instructions or unused authorization. The approved 45 values have all been consumed. The sealed preparation package and its original document snapshot remain unchanged.

15 September 2026. **VerifiedLivePreflightAwaitingSeedException**. All eight backend tests passed. Complete reservation-history verification, both control preparations and the independent preflight audit passed. Zero fights, zero new seed values and zero retries. Completed control preparations: 2.

The [frozen protocol](Tower-Team-Coverage-Comparison-Protocol.md) compares the previous structural diversity policy with team-level role coverage and permitted loadout repetition. Each gets 16 teams and four discovery fights per team. The top two from each policy are screened on eight shared seeds; each winner and the two fixed controls then receive 32 confirmation fights. Maximum **288 fights**; a finalist plays 44 total. Both policies have a 16-proposal cap and deterministic construction. A generation label does not create an independent structural restart. Fixed ordinal ability order remains.

The new runner/model adapt the sealed preceding comparison. They preserve durable seed intent/journals, attempt-start charging, cancellation, storage reconciliation, native archive verification, separate nominations, duplicate-origin merging and adjusted paired intervals. Gameplay DLLs/content/settings, character templates, timestamp, equipment and controls are identical. No root harness implementation changed. The prior implementation's 59 tests and nine old-policy parity fixtures are reused without rerunning them.

The eight new tests ran through `build/run-tests.ps1`, covering budgets/input equality, context drift, independent nominations, incomplete evidence, screen ties, exact duplicate merging, paired interval extremes and interrupted attempt charging. Synthetic scoring and combat guards ensure zero fights. Live preflight separately verifies the complete union of 482,686 reserved values, materializes both definitions with historical fixture labels only, prepares both controls, and exports 66 interval checks. An independent reader compares captured participant identities and statistics, schedules, input equality, costs, history and interval calculations. Build logs, source/DLL hashes, test TRX and all receipts are retained. No full gameplay build or full backend suite was run.

| Preparation phase before publication | Seconds |
| --- | ---: |
| audit | 6.344 |
| build | 3.531 |
| check | 56.609 |
| freeze | 4.453 |
| request | 0.125 |
| test-build | 1.719 |
| tests | 2.672 |

Every phase, including compilation and publication, is charged against the existing cumulative 2,400-second ceiling. The [completion receipt](../TestResults/balance/tower-team-coverage-comparison-preparation-20260915/control/completion.json) records actual remaining time and output, a one-second closure allowance and a 1 MiB shared-test allowance. Preparation has a 150-second cap and all new output remains within the cumulative 4 GiB ceiling. All 54 predecessor packages were verified unchanged; active Markdown snapshots and unrelated dirty tracked-file hashes are preserved.

**Allocation and combat are pending specific approval for 45 fresh values, at most 288 fights, zero retries/resumes/replays, the measured remaining time and the unchanged cumulative 4 GiB output cap.** This request is required by the user's original zero-fresh-seed instruction; previous seed approvals are exhausted. No time extension is requested. Binding rechecks the complete live registry and preparation seal before allocating. It requires at least 170 diagnostic seconds remaining and stops on any mismatch. Successful allocation would extend 482,686 reservations to 482,731. Readiness does not authorize execution or establish combat strength.

## Reproducible commands

These are the recorded commands, not instructions to rerun a sealed or failed directory. Artifact generation and diagnostics use create-new outputs; each diagnostic phase runs once and dependent work stops after failure.

```powershell
$python = 'C:/Users/HrHoe/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe'
$work = 'TestResults/balance/tower-team-coverage-comparison-preparation-20260915'
& $python -B "$work/assemble.py"
& $python -B "$work/workflow.py" freeze
& $python -B "$work/workflow.py" build
& $python -B "$work/workflow.py" test-build
& $python -B "$work/workflow.py" tests
& $python -B "$work/workflow.py" request
& $python -B "$work/workflow.py" check
& $python -B "$work/workflow.py" audit
& $python -B "$work/publish.py"
```

The test phase invokes `build/run-tests.ps1 -NoBuild -ArtifactsPath "$work/tests" -Filter FullyQualifiedName~BalanceHarnessTeamCoverageComparisonTests`. Isolated builds use frozen gameplay DLLs and cached restore metadata.

The following commands remain **unexecuted and gated on the actual new approval**. `authorize.py` takes the user's verbatim approving reply and its context as two arguments; neither may be invented. The receipt binds the actual protocol/preparation hashes and cannot grant more time.

```powershell
# Only after recording the actual specific user approval with authorize.py:
& $python -B "$work/execute.py" bind
& $python -B "$work/execute.py" run
& $python -B "$work/execute.py" verify
& $python -B "$work/execute.py" audit-execution
& $python -B "$work/finish.py"
```

The independent execution audit will require every generated party and construction trace to match the sealed diversity/team-coverage fixtures, reconstruct all combat archives, nominations, screen winners, complete confirmation family, schedules, paired contrasts and durable charges, and retain health/duration missingness. These execution checks have not run. Any failure preserves evidence and forbids retries.

Changed files: one new comparison test class, the isolated runner/model and workflow/audit package, this protocol/review, and six active Markdown handoffs. No gameplay/configuration changes, migrations or deployment implications. V19 retains 253 required recipes and the unused 512 original confirmation values; v19 Unresolved, reliability Fail 1/3, deep recovery 0/3 and adoption Hold. No Kharad tuning or large confirmation.
