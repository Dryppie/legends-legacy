# Practical Tower selection diagnostic: admission package

**Subsequent disposition:** The [completed diagnostic](Tower-Practical-Selection-Diagnostic-Execution-Review.md) completed all **4,640 fights** and both built-in audits in **248.562 seconds /152.36 MiB**, returning **NoSelectionMissDemonstrated**. The frozen primary won **712/1,000**, versus **632/1,000** and **628/1,000** for the anchors and **598/1,000** for the other challenger. This is encouraging fixed-team evidence, while adoption remains **Hold** and both anchors remain recommended. The scope is closed; **2,089** new reservations bring the permanent union to **486,374**, with no new Pending reservation. No further experiment is queued. This document and its JSON retain the frozen prelaunch proposal. Prelaunch revalidation passed; the admission helper now intentionally refuses its absent-output check. Use the execution reader for the completed evidence.

17 September 2026. Target: the offline BalanceHarness. **The gameplay proposal is frozen and its public native check passed with zero fights and zero new reservations. Launch awaits an explicit decision on one 4,640-fight run with 1,380 seconds /512 MiB of additional allowance and capped operational-failure risk.** The producing runtime, unscheduled template, request, content identities, complete history requirements and prior accounting are concrete. No gameplay command has run.

The [machine-readable review](Tower-Practical-Selection-Diagnostic-Admission.json) is reproduced by the [preparation/verification script](analysis/practical-selection-admission.py). The immutable package is at `TestResults/selection-diagnostic-admission-20260917`. Its [proposed request](../TestResults/selection-diagnostic-admission-20260917/request.proposed.json), [pending decision](../TestResults/selection-diagnostic-admission-20260917/decision.json), [native check](../TestResults/selection-diagnostic-admission-20260917/native-check.json) and [manifest](../TestResults/selection-diagnostic-admission-20260917/files.json) are ready for review. The script can prepare/check or verify; it has no gameplay-launch path.

## Concrete scope decision

Approve or decline **one** diagnostic of the unchanged `retained-composition-incumbents-v1` kernel:

| Stage | Work | Maximum fights |
| --- | --- | ---: |
| Discovery | One construction root; 64 evaluated parties; at most 256 proposals; eight trials each | 512 |
| Selection | Four nominees, including both incumbents, on 32 trials each; freeze one primary and all four recipes | 128 |
| Confirmation | All four frozen nominees on the same fresh, post-freeze 1,000-value panel | 4,000 |
| **Total** | Native reconstruction, independent audit and publication included in the run | **4,640** |

Keep the fixed floor-5 Kharad cohort: ten level-40 characters, five Essences each, two five-character subgroups, fixed equipment, the admitted Essence/family pool, canonical ordering and both supplied anchors. The original template is unchanged except for the diagnostic identifier; its generation/stage schedules remain empty. The request fixes master **2026091702** and domain **`tower-practical-selection-diagnostic-20260917-v1`** before any value is derived. The master was chosen once for this dated scope; no candidate schedules were generated or compared.

The public runner may reserve **at most 2,089 new values**: 41 construction/discovery/selection values and up to 2,048 eligible distinct words from the single post-freeze entropy batch. A completed confirmation panel uses the first 1,000 accepted values; every exposed eligible tail value remains excluded. The 484,285-value history can therefore grow to at most **486,374**. No values are reserved by this admission package.

The decision must explicitly accept these limits and consequences:

* Native combat, candidate generation and complete diagnostic-verifier cost remain unmeasured at this workload. The caps can stop an otherwise legal run before it produces a valid result. The successful resource probe is not a completion guarantee.
* Any admission, integrity, entropy, deadline or storage failure is terminal. There is no retry, resume, replacement panel, refill, replay, sample extension, search-master shopping, phase transfer or automatic second run.
* Exposed values remain excluded after failure. Interrupted diagnostic reservations, especially an unresolved entropy draw, may leave Pending history that blocks later allocation. Existing recovery commands do not provide an automatic recovery/resume path for this protocol.
* A complete run may report `SelectionMissDemonstrated` or `NoSelectionMissDemonstrated`; incomplete evidence yields neither completed conclusion. It does not establish method reliability, global optimality or a stronger replacement recommendation. Both anchors and adoption Hold remain unchanged.

The measured non-combat costs support presenting this bounded diagnostic for approval. They do not justify claiming guaranteed completion or a likely selection miss. The approximately 81.09% detection bound in the [design](Tower-Practical-Selection-Diagnostic-Design.md) remains conditional on its sampling model, a true 16-point miss and operational completion; it is not an unconditional success probability.

## Budget and prior accounting

| Component | Charged/proposed seconds | Charged/proposed bytes | State |
| --- | ---: | ---: | --- |
| First resource probe | 180 | 256 MiB | Closed; failed path check |
| Corrected resource probe | 180 | 256 MiB | Closed; measured workload completed |
| This package preparation and native check | 120 | 64 MiB | Closed; completed |
| **Prior charges carried into the request** | **480** | **576 MiB** | Fully charged; no refund |
| **Proposed additional diagnostic allowance** | **1,380** | **512 MiB** | Awaiting explicit run decision |
| **Proposed cumulative request ceiling** | **1,860** | **1,088 MiB** | Prior charges plus proposed run |

Thus the proposed run has **23 minutes /512 MiB available**, while the native request records **31 minutes /1,088 MiB cumulatively** and `priorSeconds=480`, `priorBytes=603979776`. This preserves the earlier run proposal's available resources without hiding the probe or preparation charges. Earlier closed gameplay studies remain historical read-only inputs; their allowances are not transferred or reopened.

The four nontransferable phase ceilings remain unchanged:

| Phase | Seconds | Maximum output growth |
| --- | ---: | ---: |
| Admission, reservation and native setup | 180 | 96 MiB |
| Search and nomination | 120 | 64 MiB |
| Confirmation | 660 | 256 MiB |
| Audit and publication | 360 | 16 MiB |
| **Sum** | **1,320** | **432 MiB** |

Within the 1,380-second /512-MiB run allowance, the two-second /4-MiB closeout reserve leaves **58 seconds /76 MiB** beyond the phase sums for enclosing setup and other accounted overhead. That is not transferable phase capacity. An extra public verifier, standalone audit or replay after completion is not included: the run already requires native reconstruction and its independent audit before publication.

The [successful resource probe](Tower-Practical-Selection-Diagnostic-Resource-Probe-02-Review.md) took 125.776 seconds /168.57 MiB and covered native history/copy/input/report operations. Its search and confirmation phases reused saved reports. The older 1,408-fight workflow and the sizing review's stress multipliers remain qualified comparisons, not evidence of current full-workload combat completion.

## Frozen identities and native admission

The package contains the measured **26-file producing runtime**, the complete unscheduled template, all **202 required history-file hashes**, the existing Pending recovery mapping and receipt hashes, source pins, the exact proposed command, and the pending decision. The request pins live content through all **16 data hashes**, native settings and the execution identity. Raw application settings and secrets were not copied; the public check read the live settings and required exact semantic agreement. Later drift must stop launch, not trigger an automatic rebind.

The intended gameplay output is the absent direct registry child `TestResults/balance/tower-practical-selection-diagnostic-20260917`. Preparing the package did not create it or write a reservation-ledger filename. The prior probes remain outside the registry and unchanged.

| Identity | SHA-256 |
| --- | --- |
| Native canonical request hash | `a7989e15bf3a39d86397a0856288948f5003c975525c4f2bee90bd3908944e07` |
| Proposed request file | `d0baa036cd43c0e70edc8f33dd9b8db375ddaad1e9c77f33ddd43e039ee02136` |
| Unscheduled template file | `6e2e39295d44625241f363a036c2ca3566f57d08906c63cdd49ba06d710f86c6` |
| Package manifest | `3dbb77e0097c1f8fa791fdbe8b37894a55c84b0b65643d93a18039206dbf4661` |

The exact public command executed was `tower-selection-diagnostic-check` against the proposed request using the copied producing runtime. It returned **`ContractValidNoReservation`**, **484,285 historical values**, **4,640 maximum fights**, **2,089 maximum new reservations**, **zero fights** and **zero new values**. Its `resourceFeasibilityEstablished` field correctly remains **false**.

The check's owned Windows process completed in **17.000 seconds**, with exit code zero and an empty process job. Package preparation through sealing and receipt publication took **21.125 seconds**, retaining **25,734,559 bytes**, inside its fixed 120-second /64-MiB allowance. Full preparation charges are carried forward despite lower measured consumption. These timings measure preparation/checking, not combat performance.

## Verification and launch boundary

```powershell
python -B "Balance Harness/analysis/practical-selection-admission.py" verify
```

Verification checks the exact **39-entry package inventory** plus its manifest and receipt; template/request semantics and budget arithmetic; producing source/runtime/content pins; all required history/recovery hashes; empty execution schedules; the public native-check receipt and completed owned process; the pending decision; the absent run output; and absence of reservation artifacts. The successful probe's independent reader also authenticated its evidence during preparation. A second verification reproduced the JSON byte-for-byte. All **439 local Markdown links** and whitespace checks passed. Of 4,977 baseline files, seven received the intended Markdown updates and **4,970 remained byte-identical**. No required command was blocked. The helper's preparation mode refuses an existing package and cannot invoke the gameplay command.

After explicit approval, revalidate the saved package against this review, require the same source/runtime/content/history identities and absent output, and execute the one [frozen command](../TestResults/selection-diagnostic-admission-20260917/command-after-approval.json). The public runner repeats live native admission under its registry lease before deriving values. If inputs or the resource proposal change, stop and make the changed proposal reviewable; approval of these hashes is not approval of substituted inputs or higher caps.

At preparation close, authorization covered the admission package, and the sealed decision remained `AwaitingExplicitRunDecision`. The subsequent explicit “Please proceed” approved the concrete single-run proposal and capped-failure risk. That later disposition is recorded separately in the [execution approval receipt](../TestResults/selection-diagnostic-execution-20260917/approval.json); the historical package and its pending decision remain byte-identical.

Added this review, its JSON and the preparation/verification helper, and updated current Markdown pointers. No production harness, gameplay code, optimizer or statistical gate changed. Existing binaries were copied without rebuilding; backend tests were not rerun because backend source did not change. The public native admission check and artifact verification passed. No migrations, application configuration changes or deployments are required.
