# Four-party native admission recovery

**AA, BB, AB and BA all passed native preparation and the independent record audit.** The producing runtime matches the captured cohort. This admits the four frozen preparation inputs; it establishes no stronger team and authorizes no combat.

One isolated helper compilation succeeded. Each party was prepared exactly once, with ten friendly characters and one guardian. The independent audit checked 40 characters and 200 Essence assignments against the matrix, including complete scenario party records, prepared identities and subgroup numbers, progression and fixed Essence order. The battle-start guard recorded zero callbacks; preparation timings contain no engine/executor activity.

| Party | Result |
|---|---|
| AA | PreparedAndIndependentlyChecked |
| BB | PreparedAndIndependentlyChecked |
| AB | PreparedAndIndependentlyChecked |
| BA | PreparedAndIndependentlyChecked |

## Changed tooling and design

The recovery lives in a new evidence package. Its [input script](../TestResults/balance/tower-subgroup-native-admission-recovery-20260916/prepare.py) applies the previously reviewed correction: compare relative file paths, including nested DLLs, with the historical 26-file runtime manifest. The closed attempt's source and evidence were not edited. The [setup record](../TestResults/balance/tower-subgroup-native-admission-recovery-20260916/setup.json) identifies copied source hashes and the correction.

The isolated [C# helper](../TestResults/balance/tower-subgroup-native-admission-recovery-20260916/Admission.cs) references the retained runtime through [Resolver.cs](../TestResults/balance/tower-subgroup-native-admission-recovery-20260916/Resolver.cs). The workflow permits one direct compiler invocation using installed .NET references, with no restore, package installation, production rebuild or build server. Native preparation is restricted to `CreateInput` and `PrepareAsync`; the historical battle callback rejects any battle entry. Cancellation is set inside the external preparation deadline.

The input stage binds the historical source/build records, runtime inventory, captured execution/settings, 16 content files, full matrix parties and installed compiler/framework files before native execution. Only an already reserved historical confirmation value may identify preparation inputs. It is not allocated again, and no independent panel is selected. The original recipes, equipment, level/tier/rank, neutral identity treatment and fixed order remain frozen. `OwnedCopies=null` continues to mean the captured full-pool assumption, not account-specific inventory admission.

Native input/prepared hashes and adapter party fingerprints retain distinct meanings. The [independent audit source](../TestResults/balance/tower-subgroup-native-admission-recovery-20260916/audit.py) imports no helper/generator code and compares frozen recipe and prepared actor records, while computing independent file hashes. It does not reimplement all native serialization rules. Prepared Essence order is checked; engine ability compilation is not performed. A preparation pass cannot authenticate a combat outcome export or authorize promotion.

## Verification

- `workflow.py input`: passed.
- `workflow.py compile`: passed.
- `workflow.py prepare`: passed.
- `workflow.py audit`: passed.

Publication verifies the parent package, available input/source pins, dirty-file preservation, document links and resource arithmetic. Native runtime/source/content files and sealed experiments remain unchanged. No backend test suite ran: `build/run-tests.ps1` is reserved for backend test execution, while this scope uses an isolated preparation command and independent Python record audit.

## Remaining boundary and resources

The [decision](../TestResults/balance/tower-subgroup-native-admission-recovery-20260916/decision.json) does not authorize combat. A future comparison still needs an explicitly authorized independent panel with exclusion reconciliation, a frozen native four-party definition and 1,024-fight envelope, and native outcome extraction plus archive/attempt-ledger authentication. The four recipes, simultaneous statistical gates and stopping rule remain unchanged. No stronger party is established by preparation.

This recovery uses the already allocated 60 seconds /6 MiB; there is no new transfer. The [completion receipt](../TestResults/balance/tower-subgroup-native-admission-recovery-20260916/completion.json) charges the full ceilings of executed phases, including inspection/setup, failures, outputs, publication and final checks. Unexecuted phase allowances remain unused. Charged this attempt: **60 seconds /6291456 bytes**. Remaining engineering: **0 seconds /312056 bytes**. Remaining cumulative diagnostic/output allowance: **1657.222728 seconds /83234062 bytes**. All prior charges and carried run/audit usage remain unchanged.

Changed files are the isolated recovery scripts/helper and evidence, this review, and a current notice in the original search review if its before-image was captured. There are no gameplay-content changes, migrations, persistent configuration changes or deployment actions. Any helper runtime configuration is isolated to this evidence package. No new seeds, combat, replay, benchmark or model fit occurred. All 483,732 exclusions, V19's 253 required recipes/512 unused confirmation values, V19 Unresolved, adoption Hold and closed experiment decisions remain preserved.
