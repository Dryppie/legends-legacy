# Team coverage: saved construction diagnosis

15 September 2026. Offline BalanceHarness analysis only. Freeze this protocol and its Python readers before executing them. This scope follows the completed 288-fight team-coverage/diversity comparison; it does not repeat that experiment or change search/gameplay code.

## Question and exact evidence

Locate the structural gap between the generated recipes and the two fixed controls: absent ingredients, absent compatible combinations in the bounded pool, or later allocation choices. The controls performed better on remaining boss health in the previous comparison, but all four confirmed teams won 0/32; neither causal combat benefit nor a win-rate improvement may be inferred from structural overlap.

Read only these existing evidence groups:

- `tower-team-coverage-20260915`: `input.json`, `mechanics.json`, `pool.json`, `controls.json`, `diverse.json`, `team.json`, and the captured constructor, team-coverage and structural-adapter source files. The sealed implementation already verifies control feasibility and the 560-recipe pool.
- `tower-joint-loadout-construction-v2-20260915/captured.json`: reconstruct the previous diverse policy's 680 distinct five-Essence recipes from saved records only.
- `tower-team-coverage-comparison-study-20260915`: both discovery reports, nominations, shortlist, finalists, family, quality and seed ledger. Do not regenerate parties, read combat records for new endpoints, reselect finalists or replay any fight.
- `tower-team-coverage-comparison-execution-20260915`: descriptive metrics, independent execution audit, completion and manifests. Preserve the native/independent checks already completed; do not rerun them.
- The preceding preparation's frozen common preservation helper and manifests, plus the new diagnosis scripts/protocol and current checkout hashes. Preservation checks may enumerate and hash the 57 predecessor packages; only the listed saved inputs are decoded for diagnosis. No new complete live-registry scan or seed allocation.

Freeze absolute paths and SHA-256 hashes in `TestResults/balance/tower-team-coverage-diagnosis-20260915/freeze.json`. Store the current dirty checkout before analysis; edit only this protocol/review and six active Markdown handoffs. Preserve all sealed snapshots and unrelated work.

## Fixed calculations

1. Verify the saved 16 proposals/evaluations for each policy against the prior construction fixtures. Reconstruct existing discovery ranks, four nominations and both saved finalists without changing selection. Describe all 32 candidates: recorded mean discovery health, loadout repetition, distinct recipes and exact pool membership. Retain the existing four-member confirmation results as descriptive context.
2. Reconstruct both saved pools and all 48 core/role-focus summaries. Verify full-recipe legality, union/deduplication and state/recipe charges. Count exact recipes used by generated teams, and all 80 provider frequencies in each pool. Count non-anchor filler appearances separately over the 768 recorded team-pool recipes, preserving that denominator rather than treating duplicate recipes as new builds.
3. For every distinct full control recipe, preserve control/character origins and derive: legal families/slots; authored roles; contained authored cores; compatible executed focus routes; exact pool/candidate membership; nearest pool overlap and up to three ordinal witnesses. If a legal extension is absent, classify each eligible saved route by its recorded `recipe-limit`, `state-limit` or `exhausted` stop. Report routes with no eligible core separately. An exhausted legal route that omits a valid target is a failed consistency check, not evidence of truncation.
4. Enumerate every size-1 through size-5 subset of the control recipes, at most 620 distinct subsets (20 character recipes × 31). For each, count control owners, pool recipes and generated character placements containing it, separately by policy. Sort summaries deterministically by control-owner count and ordinal IDs. This is retrospective structure analysis, not a new target or optimized candidate library.
5. For the 1,128 unordered pairs of 48 authored cores, retain each distinct legal union of at most five Essences that is contained in a control recipe. Report whether its individual cores and union occur in either pool. This tests whether compatible anchors are present separately but not together; it does not introduce joined cores into generation.

All calculations are finite: 80 providers, 48 cores, 288 saved focus records, 32 saved candidates with ten characters each, at most 20 distinct control recipes and 620 control subsets. No search expansion, constructor execution, generated candidate evaluations, random-number calls, runtime preparation or combat. A separate Python audit uses integer bitmasks for containment/count checks instead of the analysis reader's sets, and verifies saved ranks, origins, pool provenance, bounds and the sealed reservation union. No new backend test suite is needed for this read-only Python evidence scope; prior implementation/comparison test results are retained.

## Budget and failure behavior

Start from **2,291.022 cumulative diagnostic seconds**, leaving **108.978 seconds** under the existing **2,400-second** ceiling. This diagnosis has a **60-second total diagnostic cap**, including freeze/preservation, analysis, independent audit and publication. Non-publication work stops by 45 seconds, reserving at least 15 seconds for closure. Individual caps: freeze 20 seconds, analysis 15 seconds, audit 10 seconds, publication the remaining allowance. All successful and failed phases receive create-new elapsed-time receipts; no retries, resumes, alternative fixture reruns or repeated diagnostics. Editing source/Markdown is not diagnostic execution. First failure stops dependent work; publication preserves partial evidence and the reason.

Retain **zero fights, zero runtime preparations, zero fresh seed values and zero retries**. All **482,731 existing reservations** remain intact, including the original unused 512 confirmation values. The latest 45-value approval is exhausted. Verify the saved completed ledger; do not claim a new live global registry audit.

New output is capped at **16 MiB**, also subject to the unchanged cumulative **4 GiB** ceiling. Carry forward the exact preceding preparation/execution/study sizes, including their final manifests and the existing shared-test allowance; do not reset or increase old caps. Publish a conservative one-second closure allowance and retain before/after document snapshots and hashes. No deletion or modification of old packages.

Commands run once in order: `diagnose.py freeze`, `analyze`, `audit`, then `publish` (closure only after a failure). Freeze the executable script before the first command. The completion receipt and `files.json` seal the new package. Report observed counts, exact input references, elapsed time, bytes, verification results, limitations and whether a generic construction change is supported. Any such implementation needs its own frozen zero-combat scope; this diagnosis does not implement or run it.

V19 remains sealed with all 253 required recipes and no confirmation; reliability Unresolved. Current reliability Fail 1/3, deep recovery 0/3, adoption Hold. No Kharad tuning, ability-order optimization, gameplay/configuration edits, deployment, 129,536-fight confirmation or new combat experiment.
