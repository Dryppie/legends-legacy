# Loadout placement: bounded monitor terminal persistence

Current status: optional v2 monitor budgets persist and verify one terminal record in a separate fresh directory. Hard external/transient storage, blocking calls, memory and complete observer/cleanup lifetime bounds remain unresolved. Both compressed guards and the 1,806/1,800-second gate stay closed.

The target is the offline Balance Harness. `OwnerProcessMonitor(..., monitor_accounting=True, monitor_io_budget=declaration)` now also accepts `tower-proposal-monitor-io-v2`. V2 adds `terminalRoot` and `maxTerminalBytes` to the six v1 byte limits. The terminal root must be a canonical absolute path with an existing parent. Before owner launch, the monitor validates that it is fresh and disjoint from the monitor package, protected evidence, driver and optional runtime directories, then creates it once. Directory identity is checked before persistence, after writing and after readback. An existing, linked, overlapping or replaced destination fails. These checks do not impose OS filesystem confinement.

After constructing the original monitor terminal snapshot, the monitor makes one attempt to publish `terminalRoot/monitor-call.json`. Its UTF-8 encoding is capped before the exclusive file open. It shares `maxTotalWriteBytes`, `maxFileReadBytes`, `maxTotalReadBytes` and the remaining monitor deadline with the preceding work. V2 permits at most twelve hashes, three publication attempts and four bounded inventories; each inventory stops no later than the fourth returned entry. No failure refunds a reservation or resets a poisoned budget. There is no extra manifest in the terminal directory: the verified file hash is returned in memory.

The original three-file monitor package is unchanged. The persisted terminal record equals the original in-memory snapshot and retains its earlier boundary and self-persistence exclusion. Under v2, its outcome explicitly describes the monitor run body before terminal persistence. A `Complete` saved snapshot therefore cannot prove the later persistence or process exit succeeded. `terminal_publication_observation` separately reports `Verified` with the closed file's readback hash, or `Failed` without a verified hash, along with final selected I/O counts. That result remains in memory and excludes its own construction/persistence and the caller's later console and exit work. No additional observer process or recursive persistence chain is introduced.

Failure handling preserves partial files, prepared directories and the original error. A process failure can still have its terminal record persisted if its shared budget remains usable. A failed main publication poisons that budget, so later terminal persistence stops without new writes. A snapshot or destination-preparation failure leaves no terminal publication result because persistence was never attempted. Short writes remain within the original reservation; failed/zero writes, flush/close failures, changed contents, read-budget exhaustion or an exhausted deadline prevent verified persistence. V1 and default callers retain their previous behavior and schemas.

This closes selected terminal-file byte accounting, not the full observer boundary. Potentially blocking filesystem calls still have no hard time guarantee; encoding may allocate large tokens; external mutations, transient/runtime writes, caller console/exit and final result persistence remain uncovered. The saved record's I/O snapshot excludes its own terminal write; the final publication result includes that selected write/readback and explicitly excludes persistence of the result itself. `wholeProcessCoverage`, `usableForAdmission`, `wallClockBounded`, `memoryBounded` and `externalMutationBounded` remain false.

Verification passes **494 Python tests** in 22 groups, including **29 new terminal-persistence cases**. These cover exact v1/v2 declarations, copied limits, canonical/fresh/disjoint roots, protected-root generators, replaced directory identity, shared byte/read/deadline exhaustion, short/zero/partial writes, close/flush and original-error preservation, poisoned scopes, snapshot/result construction failure, changed readback and no retries. A retained routed fixture runs a real enclosing owner and four nested literal jobs with the runtime profile and all twelve hash slots. Scientific commands are intercepted; no native encounter preparation or combat occurs. All 465 existing Python regressions pass. The preliminary 27-case smoke run and 493-test regression run are excluded from the final total; their retained evidence and intermediate sources remain separate.

```text
python -B -X utf8 TestResults/loadout-placement-monitor-terminal-verification-20260925/run-tests.py
python -B -X utf8 TestResults/loadout-placement-monitor-terminal-verification-20260925/finalize.py --check-only
git diff --check -- <changed files>
```

The [verification package](../TestResults/loadout-placement-monitor-terminal-verification-20260925) retains commands, passing logs, source snapshots, literal fixtures and historical pins. The [resource specification v14](Tower-Loadout-Placement-Resource-Boundaries-v14.json) has 11 domains and 81 source references; the [boundary inventory](Tower-Loadout-Placement-Monitor-Terminal-Boundaries.json) has 28 families and 164 references. Current hashes and anchor lines are checked. Only line 3 changes in the nine status documents; their historical bodies remain byte-identical.

Changed files comprise the monitor and I/O-budget modules, new terminal tests, LF attributes, this report, the specification and inventory, and nine status lines. No backend source or test changed, so backend builds/tests were not rerun. The previous 376 backend passes and captured runtimes remain authenticated historical evidence. No required command is blocked. There are no migrations, application configuration changes or deployments. Optional v2 activation requires matching captured monitor and budget modules plus a fresh terminal destination; v1/default activation remains compatible.

Cumulative scientific charges remain **79,339.66095319996 seconds** and **51,980,910,910 bytes**, against declared maxima of **168,240 seconds** and **107,122,524,160 bytes**. This step performs no scientific launch, native encounter preparation, combat, scientific reservation, production entropy, resource qualification, cost experiment, timing sample or live-history rescan. No qualified forecast or search improvement is established. The [sealed handoff](../TestResults/loadout-placement-monitor-terminal-handoff-20260925.json) preserves the evidence and closed gates.

Next resolve hard external/transient storage and the remaining finite observer/cleanup lifetime obligations, including blocking calls, memory and caller console/exit. The final publication result must not be treated as a new recursively observed process or as proof that the remaining boundary disappeared.
