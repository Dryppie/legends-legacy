# Filler diversity versus team coverage: frozen comparison

15 September 2026. Offline `LL/tools/BalanceHarness` only. Prepare and verify the package under the existing allowance. **Fresh allocation, live preflight and combat are conditional on a specific new approval for 45 fresh values and 300 additional diagnostic seconds.** Previous seed approvals are exhausted. No approval receipt exists yet.

## Question and identical gameplay inputs

Does generic filler diversification improve the strongest team found at the same small search budget? Broader Essence coverage alone is insufficient: the preceding zero-combat result increased the pool from 560 to 768 recipes and provider coverage from 35/80 to 80/80, but reduced distinct used recipes from 96 to 81 and increased construction time from 0.0823 to 0.3592 seconds. Exact control recipes remain absent. This comparison retains those regressions and does not tune the policy further.

- Baseline: `independent-team-coverage-v1`, method `team-coverage`.
- Candidate: `independent-team-filler-diverse-v1`, method `team-filler-diverse`.
- Each constructs/evaluates exactly 16 teams, at most 16 proposals, with the existing 256-state/16-recipe per-core and 256-state/250,000-check allocation caps. Construction is finite and deterministic; its generation label is not an independent restart. Controls and combat results never enter recipe construction. Equipped Essence/ability order stays ordinal. No refilling, adaptive allocation, reselection on confirmation or policy tuning.
- Use the sealed `tower-deep-challenger-study-20260915` gameplay DLLs, content, settings and original timestamp: floor 5, the existing +10% health/power context, level 40/tier 1/rank 2, neutral equipment, 80 eligible Essences and unrestricted copies. Ten characters comprise two five-player parties, each character with five Essence slots. Both definitions differ only in policy/method.
- Fixed controls: `team-040e60d3dbc5c127321653c47ed3a9d3` and `team-49f6979895354870c89362d4abf214bb`. Identical participants/context are independently checked against prior captured preparations.

Implementation source is sealed by `tower-filler-diversity-20260915/files.json`, SHA-256 `d5406313b1d257e2c9216ba4710affb33781a31f1b05dd812624de1c0b25c340`. Its 71 tests and ten old-policy parity fixtures remain evidence. Adapt the preceding tested comparison model, runner, audits and resource controls into a new isolated package. Do not edit any sealed original or root harness implementation. Freeze source, scripts, protocol, test source and captured dependencies before diagnostics; pin the compiled request before preparation.

## Exact allocation, conditional on approval

Namespace `tower-filler-diversity-comparison-v1`, master label `2026091512`. Allocate exactly 45 unique values disjoint from the complete live reservation union: generation 1, discovery 4, selection 8, confirmation 32. Preserve all **482,731** prior values. Successful allocation yields **482,776** reservations. Rejected allocation candidates and interrupted attempts retain durable records.

1. Baseline first: 16 teams × 4 discovery seeds = 64 fights.
2. Candidate: 16 teams × the same 4 discovery seeds = 64 fights.
3. Freeze the top two per policy using existing rank: win rate, guardian health, survival, victory duration, ID. Merge identical recipes/context with all origins preserved. Screen the resulting two to four teams on 8 fresh shared seeds: at most 32 fights. Select one winner per policy using screen victories, then prior nomination rank and ID.
4. Freeze both winners and both controls. Merge exact duplicates with every origin retained. Confirm all two to four members on 32 fresh shared seeds: at most 128 fights.

Maximum **288 fights**, including all repetitions; **zero retries, resumes or replays**. Each finalist plays 44 fights total, another screened team 12, an unscreened team 4, each control 32. Duplicate merging never creates replacement nominees. Discovery overlap still consumes both charges. Incomplete stages stop dependent work. Native per-policy definition cost is 176; the wrapper owns the combined 288-fight cap and runs only each definition's discovery phase.

Primary endpoint: `team-filler-diverse-finalist-minus-baseline-finalist` on paired confirmation victories; draws are non-wins. Also report the four fixed finalist-minus-control contrasts. Existing simultaneous Wilson factors remain 8 for rates and 20 for paired gain/loss bounds. Merged identical members have exact zero paired difference. Boss health, duration, missingness and paired health differences are descriptive only and cannot select confirmation winners. This small exploratory comparison cannot establish reliability, optimality or justify default adoption. No pooling with earlier studies.

## Preparation under the existing budget

Incoming cumulative workload **2,320.929 seconds**, leaving **79.071 seconds** under the currently approved 2,400-second cap. This preparation has a **60-second aggregate maximum**, stops non-publication phases by 45 seconds and reserves 15 for closure. Compilation, tests, preparation, audits and publication all count. Engineering editing time does not. Per-phase limits: freeze 20, build 45, test-build 30, tests 20, request 10, prepare 20, audit 20 seconds, always reduced by the aggregate/cumulative remainder. Zero fights, fresh seeds, retries and live registry scans during preparation. Exactly two control preparations and 32 fabricated generation evaluations, with guards rejecting combat.

Run eight focused backend tests through `build/run-tests.ps1`: identical budgets/input definitions, context drift, separate nominations/incomplete discovery, screen ties/incomplete evidence, duplicate origins, paired extremes/misalignment, incomplete confirmation and durable interrupted attempt charging. The nomination test additionally requires all 16 proposals and structural traces per policy to match the sealed `team.json`/`filler.json` fixtures exactly. Reuse the 71 implementation tests without rerunning them. No broad filter that could include combat integration tests.

The native offline preparation reads the complete **sealed prior seed ledger**, materializes both definitions with 45 already-reserved labels solely as non-runnable schema fixtures, prepares both controls and exports 66 interval cases. Its proof explicitly says `sealed-history-fixture`; it is **not a complete live preflight**. The independent reader reconstructs the sealed union, checks all captured participant identities/statistics, identical definitions/costs/schedules, interval calculations and test exports. Full preservation hashes cover 59 predecessor packages before and after preparation. Readiness status is `PreparedAwaitingSeedAndTimeException`, never `VerifiedLivePreflight`.

New package: `TestResults/balance/tower-filler-diversity-comparison-preparation-20260915`. Run once: `workflow.py freeze`, `build`, `test-build`, `tests`, `request`, `check` (native `prepare`), `audit`, then `publish.py`. Each phase writes create-new start/result/failure evidence. First failure stops all dependent work; publication preserves partial evidence. No replacement run or retry.

## Proposed execution allowance and gates

After successful preparation, ask for **45 fresh values and exactly 300 additional diagnostic seconds**, carrying the measured remainder forward. Approval would make the cumulative ceiling **2,700 seconds** for this workflow; all old experiments and their caps stay unchanged. The preceding comparison measured 56.609 seconds for live preparation, 6.344 for its audit and 149.671 for authorization/binding/execution/native verification/independent audit/publication. These are planning observations, not a runtime guarantee. Current 79.071 seconds cannot cover that complete workload. No extension is applied before explicit approval.

`authorize.py` requires the actual reply/context, binds protocol/preparation hashes and requires at least 270 seconds available under the proposed ceiling. Only then run `execute.py check`, `audit-preflight`, `bind`, `run`, `verify`, `audit-execution`, `finish.py`. Full live registry preflight and its independent audit must pass before allocation. Binding rechecks the live registry and package seal and requires at least 170 seconds remaining. Any changed registry or reservation union stops; no automatic expansion. Runtime checks and verification recheck the external registry. Native verification is archive-only, without combat. Execution audit also requires exact party/trace parity with the sealed implementation fixtures.

Per-phase maxima: live check 90, live audit 20, binding 90, run 90, native verification 90, independent audit 20 seconds, always bounded by the lower remaining cumulative allowance after reserving 20 seconds for closure. Commands may stop below the 288-fight maximum if a deadline or check fails. No retry or unapproved extension follows. Cancellation, durable seed intent/write-through journals, attempt-start charging, incremental accounting with full boundary reconciliation, and exact archive verification remain mandatory. Both successful and failed workflows retain performance/CPU/allocation/memory and durable attempt evidence.

The cumulative output ceiling stays **4 GiB**. Carry forward the prior receipt's bytes plus actual sealed implementation size. Preparation ≤192 MiB, execution ≤128 MiB and study ≤1 GiB, all subject to the smaller cumulative remainder. Charge a 1 MiB shared-test allowance and one closure second. Do not delete or reuse output. Unrelated dirty checkout work and all root harness source must stay unchanged; the only new root C# file is the focused comparison test, plus protocol/review and six active Markdown handoffs.

V19 remains sealed with 253 required recipes, no confirmation and the unused original 512 confirmation values preserved. Reliability Fail 1/3, deep recovery 0/3, v19 Unresolved, adoption Hold. No Kharad tuning, ability-order optimization, gameplay/configuration changes, migration, deployment or 129,536-fight confirmation. If checks or limits stop work, publish the exact unresolved boundary and preserve all evidence.
