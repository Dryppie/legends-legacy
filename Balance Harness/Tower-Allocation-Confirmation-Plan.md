# Fresh confirmation of the saved allocation nominees

Frozen 14 September 2026 before allocating seeds or executing this study. Target: offline `LL/tools/BalanceHarness`. This is a separate confirmation study of saved recipes, with policy `tower-allocation-confirmation-v1`. The [original allocation experiment](Tower-Search-Allocation-Review.md) remains stopped at its declared time cap, with unresolved reliability. It receives no resume, extension, replacement outcomes or updated conclusion.

## Fixed question and inputs

Confirm the exact **94 recipes** and **six screened primaries** in `TestResults/balance/tower-search-allocation-20260913/comparison.json`. Preserve recipe ordering, slots, loadouts, gear, all original and screened nominees, their complete provenance, the 74 earlier controls, the anchor and strongest preceding control. The 20 newly selected recipes remain identifiable separately from the 74 controls. No new discovery, screening, candidate generation, nominee replacement or outcome-based family reduction.

Source provenance is the complete stopped campaign inventory and its sealed work package, `tower-search-allocation-work-20260913`. Its zero-combat stopped audit reconstructed all generation and screening, grouped ranks, selection and the 94-recipe family. Preparation verifies both full inventories, then copies and hashes the required source artifacts, manifest and audit receipt into the new package. Subsequent check and verification use that self-contained frozen snapshot. The source audit is reused; the stopped run is never completed or reinterpreted.

Kharad remains at Health **3.5366243328** / Power **4.4702934848**. Preserve the same level-40, tier-1, rank-2, ten-character, five-untrained/unevolved-Essence budget, fixed gear and hypothetical full ownership of all 80 Essences. Content, settings, gameplay assembly hashes and runtime must match the stopped source. Only the offline harness assembly changes to implement this workflow.

## Fresh allocation and unchanged decision rule

Master seed **2026091401**; allocate **512** deterministic, unique shared confirmation seeds in the new policy namespace. Exclude every value in the current 479,021-reservation ledger, the source ledger and every original generation/stage schedule, whether used or unused. Bound seed allocation to 100,000 attempts. Freeze the resulting ledger and definition before the first fight. The resulting complete ledger has **479,533 reservations**. No diagnostic fights or seeds.

All 94 recipes receive all 512 fresh trials: **48,128 starts and completions** are required for a complete result. Do not pool old confirmation, discovery or screening outcomes. Missing, duplicate, reordered, partial or out-of-scope trial evidence invalidates publication.

Use the existing joint evaluator unchanged: alpha .025 across all 94 confirmation rates, alpha .025 across nine paired differences (18 discordance intervals), approximate Wilson coverage. The three isolated-pair primaries must each have rate lower bound >= .10, paired improvement lower bound > 0 over their matching deep primary, and anchor-difference lower bound >= -.10. **At least two of three** must pass. Report comparison with the fixed strongest prior control separately, without changing this gate. Report ordinary family acceptance and joint family acceptance separately from reliability. No lifetime repeated-study, full generated-family, near-optimality or acquisition claim; a fresh result concerns these saved nominees, not a new replication of candidate generation.

## Measured resource envelope and stop rules

The read-only [profile](../TestResults/balance/tower-allocation-confirmation-work-20260914/profile.json) executed zero fights. A complete storage scan of the 50,741-file allocation discovery archive took .846–.938 seconds, versus .413–.470 seconds for 25,397 lineage discovery files. Inventory hashing took 3.850 and 2.267 seconds respectively. This establishes scaling cost per scan, not an exact retrospective attribution of the stopped run's elapsed time.

A prior dense confirmation archive contained 2,660 files and 412,601,038 bytes. Storage scans took .032–.034 seconds. Reconstruction of one 16,384-report batch took 11.991 seconds, buffering at most 32 reports. The source's partial confirmation completed 11,763 fights in 307.48 seconds, suggesting approximately 21 minutes for 48,128 fights before final verification; slower screening throughput suggests approximately 23 minutes. These are estimates, not guarantees.

Freeze **3,600 seconds**, **4 GiB (4,294,967,296 bytes)** and **48,128 fights**, with **zero retries**, no resume, extension or optional continuation. Use the existing compact format, prepared execution, 32-report chunks and three dense batches of 32, 32 and 30 recipes. Keep shared storage/inventory code and all integrity checks unchanged. Record elapsed execution, durable attempt accounting, storage and stage timings to assess the actual envelope. Include preparation snapshots, executable and source audit copies in the outer storage cap. Time starts when the execution marker is written; preparation and later read-only reconstruction are separate. Interruptions remain preserved incomplete evidence, never a reliability Fail or Pass.

## Verification and delivery

Build the harness and run relevant backend tests through `build/run-tests.ps1`. Test frozen source membership, control retention, nominee matrix and shortlist bindings; fresh history union; changed, partial, duplicate and pooled evidence; unchanged statistical results and the 2/3 gate; inventory changes and repeated-run refusal. Check the actual frozen package using its retained executable before combat. Execute once in `TestResults/balance/tower-allocation-confirmation-20260914`, then reconstruct every saved report with zero new fights and verify exact manifests, complete accounting and computed quality.

Export every full recipe with its fresh outcomes, provenance and control/new status; include a focused nominee comparison and reproducible analysis. Update the six active entrypoint Markdown files and create a new review after verified results. Retain producing source and documentation snapshots in the new work package. Verify the stopped source inventories remain unchanged and bind the previously sealed historical preservation chain. No source/default/catalog promotion, migrations, configuration changes or deployment.
