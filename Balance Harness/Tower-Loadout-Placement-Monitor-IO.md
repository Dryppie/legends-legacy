# Loadout placement: selected monitor I/O limits

Current status: optional monitor budgets cap selected hash reads, console retention and two receipt writes. External/transient storage, blocking calls and the complete observer lifetime remain unresolved. Both compressed guards and the 1,806/1,800-second gate stay closed.

The target is the offline Balance Harness. `OwnerProcessMonitor(..., monitor_accounting=True, monitor_io_budget=declaration)` accepts `tower-proposal-monitor-io-v1` with six nonnegative signed-64-bit integer limits: `maxFileReadBytes`, `maxTotalReadBytes`, `maxConsoleBytes`, `maxObservationBytes`, `maxManifestBytes` and `maxTotalWriteBytes`. Declarations are copied. These are diagnostic API limits supplied by a trusted caller; no scientific resource limits or launcher defaults have changed. Terminal accounting is required because the final record includes publication and verification work.

Selected source, console and receipt hashes reserve the observed file length plus one EOF-probe byte before opening. Both per-file and cumulative allowances include that probe, even when it returns zero bytes. At most eleven hashes are attempted: six source bindings including the I/O module, one optional runtime-profile binding, two console hashes and two receipt verification hashes. Reads use at most 65,536 bytes per call. A short read fails without retry; growth, changed identity/size, invalid results and failed reads prevent a trusted hash. Reservations are never refunded. Failed read progress stays unknown, and mandatory stream close preserves an earlier failure. Same-size concurrent rewrites and arbitrary external mutations are not prevented by this API.

The console cap uses the existing process helper's parent pipe writer, which limits retained merged stdout/stderr. The monitor checks the returned cap declaration. An overflowing owner is terminated and drained; retained failure evidence can still be published within the separate receipt budget. Pipe buffers, worker output attempts, external mutations and complete process memory remain outside this cap.

Only `owner-process.json` and `files.json` may be published, each with one exclusive create attempt. Canonical UTF-8 encoding is checked against the corresponding per-file allowance and the remaining cumulative receipt allowance before opening. Short writes advance within the original reservation; a failed or invalid write stops immediately, retains partial files and records unknown progress. Flush, sync, close and verification failures prevent verified publication. Close is attempted even when accounting the open completion or close fails. The cumulative receipt-write allowance excludes the separately capped console. Two directory inventories enumerate incrementally and stop no later than the fourth returned entry, instead of materializing an arbitrary directory listing.

The existing three-file monitor package remains the publication format. Its owner observation binds the declaration and I/O-module hash. The in-memory terminal observation additionally retains reservations, accepted bytes, attempted/completed hashes, publication attempts, read/write calls and inventory counts through verification. It excludes its own construction and any caller persistence. The encoder may allocate a large individual token before checking encoded size, so these output limits do not establish a serialization-memory bound. No hard wall-clock bound follows from checks around potentially blocking filesystem calls. `wholeProcessCoverage`, `usableForAdmission`, `memoryBounded`, `wallClockBounded` and `externalMutationBounded` remain false.

Verification passes **465 Python tests** in 21 groups, including **37 new cases**. Coverage includes exact declarations, copied state, EOF reservations, per-file/total read exhaustion, short/growing/failed reads, hash/close failures, finite hash and inventory attempts, canonical Unicode limits, cumulative receipt limits, exclusive-create collisions, short/zero/partial writes, flush/sync/accounting failures, verification exhaustion, console overflow and preservation of original process errors. The retained routed fixture exercises all eleven hashes with the runtime profile, a real enclosing owner job and four nested literal jobs. Its scientific worker commands are intercepted; it runs no combat or native encounter preparation. Existing default monitor behavior and all 428 prior Python regression cases also pass. The earlier smoke and 464-test runs are retained as preliminary evidence and excluded from the final total.

```text
python -B -X utf8 TestResults/loadout-placement-monitor-io-verification-20260925/run-tests.py
python -B -X utf8 TestResults/loadout-placement-monitor-io-verification-20260925/finalize.py --check-only
git diff --check -- <changed files>
```

The [verification package](../TestResults/loadout-placement-monitor-io-verification-20260925) retains exact commands, logs, before/after source snapshots, routed process evidence and historical pins. The [resource specification v13](Tower-Loadout-Placement-Resource-Boundaries-v13.json) has 11 domains and 76 current source references; the [boundary inventory](Tower-Loadout-Placement-Monitor-IO-Boundaries.json) has 27 families and 157 references. All reference hashes and anchor lines are checked. Only line 3 changes in the nine status documents; their historical bodies remain byte-identical.

Changed files comprise the new `proposal_monitor_io.py` module and tests, monitor integration and mandatory-close handling, LF attributes, this report, the resource specification and inventory, and the nine status lines. No backend source or backend test changed, so no backend build/test rerun was needed. The prior 376 backend passes and captured runtimes remain authenticated historical evidence, not new passes. No required command is blocked. There are no migrations, application configuration changes or deployments. Optional activation requires capturing the matching new module with the monitor; existing callers remain compatible.

Cumulative scientific charges remain **79,339.66095319996 seconds** and **51,980,910,910 bytes**, against declared maxima of **168,240 seconds** and **107,122,524,160 bytes**. This step performs no scientific launch, native encounter preparation, combat, scientific reservation, production entropy, resource qualification, cost experiment, timing sample or live-history rescan. No qualified forecast or search improvement is established. The [sealed handoff](../TestResults/loadout-placement-monitor-io-handoff-20260925.json) preserves the evidence and closed gates.

Next resolve the hard external/transient storage treatment and the remaining finite observer/cleanup lifetime obligations, including terminal persistence and exit. Runtime-directory sampling and these selected I/O caps do not close those gaps; adding another recursive observer would only move the remaining observer work.
