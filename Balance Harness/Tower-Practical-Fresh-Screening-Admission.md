# Fresh-screening comparison: captured-runtime admission

23 September 2026. Target: offline `LL/tools/BalanceHarness`. **Historical admission complete; the [single scientific execution](Tower-Practical-Fresh-Screening-Comparison-Execution.md) is now verified and closed as `DoNotPromoteFreshScreening`.** This report records the no-reservation admission stage. The tested fresh-screening harness passed compatibility with the original captured gameplay dependencies, content, settings and three reference teams. Admission itself made no strength claim.

## What passed

The retained DLL and portable symbols matched **202 producing source documents**. **664 methods** resolved against the captured dependencies, including screening validation, membership freeze, nomination, comparison execution and native archive reconstruction. A combat guard protected reconstruction of the stored engineering fixture: **23 screen members**, **five nominees** and the final selected output reproduced exactly. These are saved synthetic measurements, not fresh scientific evidence.

The public native check returned `ReadyNoReservation`. It validated both pipeline definitions and prepared the three real captured reference inputs for each arm: **six preparations**, with zero combat. The original non-harness runtime files, content and effective settings remain byte-for-byte pinned. Only the four tested harness runtime files are substituted. Neither the generator nor the incumbent-tie selector changed during admission.

Full historical scans before and after the native check reproduced **584,171 exclusions across 236 files**, including the supported abandoned-reservation recovery. Native validation independently checked the same history. Subsequent read-only verification reproduced the package conclusions and live history without repeating native preparation. Every owned child tree was empty on completion.

| Admission phase | Seconds | Result |
| --- | ---: | --- |
| Captured runtime, symbols and stored screening reconstruction | 1.266 | Passed |
| Public native reference-input and history check | 21.578 | Passed |
| Entire admission through sealing | 198.265 | Passed within 600 seconds |

The package retained **102,014,476 bytes (97.29 MiB)**, plus its small external pin. Its request conservatively charges the full **600 seconds /512 MiB** admission allowance. The new comparison retains **10,200 seconds /5.5 GiB** for its single execution, both audits and publication, within the **10,800-second /6-GiB** cumulative envelope. Native execution is capped at **10,080 seconds /5.25 GiB**; later audits share the remaining deadline.

The authenticated historical linear estimate is **8,587.387 seconds /3,134.161 MiB** at the maximum 72,672 fights, including the source run's audit/publication costs. It fits the allowance but is not a guaranteed upper bound. No battle was run here to calibrate or improve that estimate.

## Changes and engineering verification

The [admission helper](analysis/prepare-reference-exploration-admission.py) now accepts an explicit `--screening` mode. It binds the new implementation proof, all four tested harness files, 202 producing documents, the current history, plan and output path. It authenticates the earlier source coverage plus the three screening implementation files, preserving the existing version-specific behavior.

The [runtime adapter](analysis/reference-exploration-context.ps1) adds an optional screening check. It resolves the new methods and reconstructs saved membership, nomination and final selection against the retained runtime. Its initial engineering smoke check used the already-tested build's dependencies and performed no real reference preparation. The actual admission then repeated that compatibility check against the original captured dependencies.

**Twelve screening admission-helper tests and seven legacy helper tests passed.** They cover proof/version mismatches, failed test receipts, settings/history drift, protected nomination settings, complete tested binaries, nested dependency preservation, source coverage, literal fixture pins, resource arithmetic and owned-process termination. The adapter smoke check passed in **1.516 seconds**. Python syntax, PowerShell parsing, source bindings, local links and final publication are checked separately.

No C# implementation changed, so the [154 backend and 41 Python tests from implementation](Tower-Practical-Fresh-Screening-Implementation.md) were not repeated. No required command remains blocked. Engineering checks and documentation remain separately disclosed; complete historical engineering totals remain unknown, and the **18,180-second /13,584-MiB** prior-charge ledger remains unchanged. This admission's 600-second /512-MiB charge belongs to the new comparison envelope.

## Concrete package and next step

- [Admitted request](../TestResults/practical-fresh-screening-admission-20260922/request.json), [template](../TestResults/practical-fresh-screening-admission-20260922/template.json), [native check](../TestResults/practical-fresh-screening-admission-20260922/native-check.json) and [admission receipt](../TestResults/practical-fresh-screening-admission-20260922/admission.json).
- [Retained runtime compatibility](../TestResults/practical-fresh-screening-admission-20260922/context.json), [package manifest](../TestResults/practical-fresh-screening-admission-20260922/files.json) and [external pin](../TestResults/practical-fresh-screening-admission-20260922-pin.json).
- [Read-only verification](../TestResults/practical-fresh-screening-admission-engineering-20260922/admission-verification.json) and [engineering completion](../TestResults/practical-fresh-screening-admission-engineering-20260922/completion.json).

```text
Protocol:           tower-practical-fresh-screening-comparison-v1
Plan SHA-256:       13fc98e45afb7a96403918ffe0233fd1dcb375a52fb9446b63053911cc94c263
Admission manifest: e6eae89b36a30b4bf167c988ff3202c1148880765f559c0d314e67f7d4818caa
Request SHA-256:    9e7c7a8140cbc1d0006178bb99a264887d268a70448845da84c71fc9f24e1756
Template SHA-256:   e334fc889b02d31a5b42697b69a4e586aa051e77bf94928fd5b28863cd2ec38c
Execution hash:     d0ec8a1538af2c62c1bda13a1311fefd28fd9d44aa5202a527afbf96e72d6d80
```

The completed commands were:

```powershell
python -B -X utf8 'Balance Harness/analysis/prepare-reference-exploration-admission.py' self-test --screening
python -B -X utf8 'Balance Harness/analysis/prepare-reference-exploration-admission.py' self-test
python -B -X utf8 'Balance Harness/analysis/prepare-reference-exploration-admission.py' prepare --screening
python -B -X utf8 'Balance Harness/analysis/prepare-reference-exploration-admission.py' verify --screening --live --manifest-sha256 'e6eae89b36a30b4bf167c988ff3202c1148880765f559c0d314e67f7d4818caa'
```

Python used the bundled runtime. These preparation commands record completed work: the new-package tests and `prepare` require an absent admission package and must not be repeated or resumed. Read-only `verify` is the appropriate prelaunch check while the scientific output is absent.

The [single prospective comparison](Tower-Practical-Fresh-Screening-Comparison-Execution.md) has now completed using this retained request, runtime, auditor and launcher after a fresh prelaunch check. It assigned **12,588 fresh values**, permanently reserved the full fresh tail and completed **55,672 fights**, including 12,672 search fights. The allocation is closed. No retry, resume, replacement restart or extension is permitted. Prelaunch verification is historical and must not be rerun against the now-existing scientific output.

The original and offset comparisons remain closed without promotion. No default or team adoption, migration, application configuration change, database operation or deployment follows from admission.
