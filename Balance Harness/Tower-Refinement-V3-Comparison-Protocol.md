# V3 refinement versus team coverage: frozen comparison

16 September 2026. Target: offline BalanceHarness. Prepare a new comparison with the verified V3 launcher; no allocation or combat is authorized until the exact request receives approval. Earlier fresh-value approvals are exhausted. The previous failed study and every old experiment remain sealed.

## Scientific contract

Compare `independent-team-coverage-v1` with `independent-discovery-refinement-novel-v3`, explicitly using `tower-discovery-refinement-comparison-v2`. Both arms use identical captured gameplay/content/settings, character templates, equipment, timestamp and schedules. Keep ordinal equipped ability order fixed. V3 selects parents only from completed same-arm discovery evidence; confirmation never informs search or selection. Controls remain `team-040e60d3dbc5c127321653c47ed3a9d3` and `team-49f6979895354870c89362d4abf214bb`.

| Stage | Maximum teams | Fights each | Maximum attempts |
| --- | ---: | ---: | ---: |
| Baseline discovery | 16 | 4 | 64 |
| V3 discovery | 16 | 4 | 64 |
| Shared selection, two nominations per arm | 4 | 8 | 32 |
| One finalist per arm plus two controls | 4 | 32 | 128 |

Maximum **288 charged fight attempts**, including interruptions, with **zero retries, resumes or combat replays**. Both arms retain the 16-proposal/16-candidate cap. Incomplete discovery stops the entire comparison before nominations/selection; no refill. Duplicate recipes retain all origins and may reduce later fights. Select finalists by selection wins, then frozen discovery rank and ID. The primary result is V3-minus-baseline confirmation win rate with the existing adjusted paired interval. Boss health and duration are descriptive. This pilot cannot establish global optimality or reliability; adoption remains Hold.

Request exactly **45 fresh values**: one generation label, four discovery seeds, eight selection seeds and 32 confirmation seeds. Use the existing durable allocator, its unchanged V1 derivation namespace, and frozen master `2026091603`; do not derive candidates during preparation. Preserve all **482,866 existing reservations**, including the failed study's 40 unused values and V19's separate 512 unused values and 253 recipes. Production binding rechecks the complete live registry and rejects drift/Pending history.

## Paths and budgets

New readiness: `TestResults/balance/tower-refinement-v3-comparison-readiness-20260916`.
New study: `TestResults/balance/tower-refinement-v3-comparison-study-20260916`.
New execution evidence: `TestResults/balance/tower-refinement-v3-comparison-execution-20260916`.

Preparation is authorized within existing limits: **40 diagnostic seconds / 24 MiB**, including the copied runtime, thin host, checks and publication. Current cumulative ceilings are **3,600 seconds / (4 GiB + 64 MiB)**. Use the verified V3 package's actual sealed size and retained-fixture charge for prior accounting; conservatively reconcile its completion time against its phase receipts. No full registry scan, seed allocation, actor preparation, generated candidate or combat in readiness.

The proposed execution requires explicit approval for **45 fresh values, an additional 240 cumulative diagnostic seconds and an additional 64 MiB cumulative output**. Proposed ceilings become **3,840 seconds / (4 GiB + 128 MiB)**. These extensions are not applied by preparation and change no old experiment cap.

- Study limit: **360 seconds / 84 MiB**, including binding, launch and native reconstruction. Binding gets 150 seconds, conservatively charged in full; launch gets the remaining 210 seconds, including its live refresh. A host cancellation token covers the complete 360 seconds.
- Post-approval execution task: **400 seconds / 4 MiB evidence**, including native process containment/cleanup, independent audit and publication. Native process gets at most 364 seconds, audit at most 30 seconds and publication five seconds plus the existing one-second closure charge.
- The complete proposed reservation is actual readiness bytes plus **88 MiB** for study and execution evidence. The preparation audit must establish this fits after the requested output extension, and that 400 execution seconds fit after the requested time extension. No run starts without the whole envelope.
- Recalculate the component floor from fourteen complete compact historical arrays, the actual retained assembly bundle and four captured content copies. Reports and temporary writes remain outside that minimum; fitting the minimum does not guarantee completion. Cap exhaustion stops with evidence retained.

## Exact readiness checks

Freeze each phase before execution; first failure skips dependent phases and permits only publication. Phase maxima: freeze five seconds, thin-host build ten, native inspection eight, independent audit seven, publication five; five seconds remain for closure overhead. No retries.

1. Freeze the verified launcher seal (`029a43eaf865c5d18ccb1b0f6a1b5c2cb6bacbe19db9f9bf37d7d5109d967f3b`), its 60-test TRX, captured assets, unchanged live harness source, current dirty checkout, scripts and protocol. Reuse those 60 passing tests from `build/run-tests.ps1`; no harness/search/gameplay code changes justify repeating them.
2. Assemble a private runtime directory from the existing captured executable asset set, replacing only `BalanceHarness.dll` with the exact tested V3 library (`c6fb342f9d876dec9aa37fd2a14ee6ad9c03f64dc55f20283369ed86d0d2ebcd`). Retain the complete captured dependency manifest/runtime configuration and unchanged dependency bytes. Record all origins/hashes. The library is invoked through the sealed thin host; it is not claimed to have a standalone executable entry point. The shared study archive retains that library and all gameplay dependencies; reproduction also uses this sealed host.
3. Compile only the unchanged thin host from the earlier sealed readiness package, with copy-local disabled, captured references, no restore and build servers disabled. Its assembly name retains access to the existing friend API. No gameplay or harness rebuild.
4. Execute only `inspect`: validate the explicit V3 request, current execution identity, complete 482,866-value ledger, version-specific driver receipt, content and controls; write an unsigned authorization template. No public live-registry Check and no allocator. Three mandatory registry traversals remain inside future binding/launch.
5. Independently check the request/template, history union/hash, V3 version, exact asset provenance, output floor and full resource reservation. Reuse the unchanged raw-record/rate reader against 128 existing confirmation records/four rates; no fights or replay. Preserve unrelated dirty files and source hashes.
6. Publish readiness or the first failure, exact time/output, commands and six active Markdown handoffs. Seal the package. Request approval only if readiness passes.

After approval, record `approval.json` and `authorization.json` in the new execution directory, bound to the readiness seal and request hash. Run the prepared `execution.py` once. It calls the verified public ReserveAndBind/Run APIs, then checks the new study's inventories, producing assets, policy identities, allocation transcript, schedules, raw compact records, charges, fixed order, finalist selection, family origins and adjusted statistics independently. Failure retains all derived values and forbids reuse, retry or resume. No fourth global registry scan.

```powershell
$python = 'C:/Users/HrHoe/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe'
$work = 'TestResults/balance/tower-refinement-v3-comparison-readiness-20260916'
& $python -B "$work/workflow.py" freeze
& $python -B "$work/workflow.py" build
& $python -B "$work/workflow.py" inspect
& $python -B "$work/workflow.py" audit
& $python -B "$work/publish.py"
# Only after explicit seed/time/output approval:
& $python -B "$work/execution.py"
```

Use only `publish.py failure` after a readiness failure. No sealed V19 changes, 129,536-fight confirmation, Kharad tuning, ability-order optimization, gameplay/content changes, migrations or deployment. V19 reliability Unresolved, later reliability Fail 1/3, deep recovery 0/3 and adoption Hold remain unchanged.
