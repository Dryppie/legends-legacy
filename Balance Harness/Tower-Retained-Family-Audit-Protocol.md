# Captured-v19 retained-family audit — frozen zero-combat protocol

Frozen **14 September 2026** after the [complete midpoint candidate](Tower-Portfolio-Midpoint-Review.md). The user requested proceeding with the **43,879-entry inventory audit and independent confirmation design**. Target is the offline `LL/tools/BalanceHarness`. This scope implements and executes the audit; it does not allocate or execute another balance study.

## Fixed sources and preservation

The input is `tower-portfolio-ceiling-diagnosis-20260914/retained-family.json.gz`, SHA-256 `8173a6535d12dacdfd6e71eda939520dfeabb7e4399eae7db7d98837feb21fcf`. Retain all 43,879 ordered entries and every origin. Recompute the descriptive keys and original ten-source union from its source provenance, checking source hashes against sealed manifests. Do not select entries by outcome or silently discard incompatible contexts.

Use the completed midpoint's isolated +10% content/settings and byte-identical captured-v19 gameplay assemblies. Its `midpoint-files.json` SHA-256 is `8a23325bea8fe71f62be5f313b6e49cf29814c276572122e1970c2412d0a2300`. Reuse its first already-reserved shared value for materialization only. No simulation or new schedule is permitted. All **481,603 reservations**, including the **512 unused original v19 values**, remain excluded. Audit all registered ledger/history paths and their complete array union; record any additional exclusions rather than altering an old ledger.

New artifacts go under `TestResults/balance/tower-retained-family-audit-20260914`; capture the dirty checkout before edits. Preserve previous reviews, plans, experiments, content and executables. No deployment, gameplay application, catalog promotion or old cap change.

## Exact audit and resource envelope

**Zero fights, zero fresh seeds, zero retries/resumes; 1,800 seconds total diagnostic workload and 4 GiB total new output.** Native materialization is additionally bounded by **1,500 seconds / 1 GiB**, reserving time and output for preparation, history audit, independent verification and final metadata. Count builds/captures and repository documentation in output accounting; compilation and correctness-test time is recorded separately from diagnostic workload. Freeze exact script, source, executable and input hashes before the once-only primary command.

1. Run `build/run-tests.ps1 -Filter 'FullyQualifiedName~BalanceHarnessTowerRetainedAuditTests' -ArtifactsPath TestResults/balance/tower-retained-family-audit-20260914/test-build`. Tests use synthetic entries/participants and pure validations, with no engine invocation. Build the captured producing harness from the preceding captured source plus the new audit class. Code/build failures may be repaired before producing inputs freeze; retain every attempted command result.
2. One frozen Python orchestration pass verifies the descriptive source union, origin mappings, complete registered seed history and input seals. It copies the compressed retained family, freezes a native request and invokes the captured `tower-retained-audit <request.json> <new-output>` exactly once.
3. Strictly deserialize every scenario, rejecting unknown fields, nonempty schedules and incompatible floor/time/preparation/budgets. Normalize only production-equivalent party/equipment ordering and implicit identity defaults for keys; retain original scenarios and all actor/Essence identities. Calculate native recipe/context/equipment hashes. Materialize every eligible entry at +10%, persist compressed per-entry participant descriptions and input/participant hashes, and retain every invalid entry with a reason. A row issue yields `RetainedFamilyIssues`; an I/O, cancellation, source-integrity or resource failure stops and preserves partial output. An incomplete audit cannot claim a complete family.
4. Keep context groups and exact aliases visible. Never merge entries with different materialized participants. Match all 253 midpoint recipes by native context/recipe identity and require their saved prepared participant hashes. A candidate family requires every retained entry accounted for, zero invalid entries, all 253 midpoint matches and verified provenance/history.
5. One independent read-only verification pass checks every result row, descriptive key, origin count, native identity relation, participant digest, complete coverage/alias/context summaries, required midpoint matches and the exact native inventory/hash seal. No second materialization or combat replay. Retain performance calls/times, CPU, allocations, memory, bytes, commands and failures. Seal the complete engineering package after documentation/preservation checks.

## Design deliverable

After the audit, write a concrete independent confirmation protocol for the exact compatible family and contexts. State complete family/stage multiplicity, viability per required context, fresh seed exclusions, worst-case fight/time/storage budgets, stopping rules and required new implementation capacity. No pooled old outcomes, interpolation, post-hoc family reduction or nominal-95% reset per partition. Existing **20,000-cell / 500,000-fight** staged limits remain unchanged. Any new execution contract requires separate implementation and frozen binding; this audit does not authorize that confirmation's combat or seed allocation.

Reliability **Fail 1/3**, adoption **Hold** and previous family **Fail / Fail** stay historical. A complete materialization audit establishes a runnable retained family, not its win rates, acquisition feasibility, global optimality or live gameplay acceptance.
