# Practical pilot with incumbent nomination

**Propose one fresh practical search using the verified `retained-composition-incumbents-v1` policy, followed by independent confirmation of the selected party and both admitted anchors.** The fixed decision distinguishes retaining an incumbent from discovering a demonstrably stronger challenger. The [implementation and its 40 passing checks](Tower-Incumbent-Selection-Implementation-Review.md) establish correct nomination, accounting and reconstruction; they do not establish combat strength.

This pilot measures the practical output against both anchor recipes on a shared fresh confirmation panel. The earlier pilot remains contextual evidence and is not a contemporaneous comparison arm. Its failed result is not relabeled, pooled with new outcomes or reused as confirmation. One construction root cannot establish algorithm superiority or search reliability.

## Frozen target and search

Use the current complete runtime retained in `TestResults/balance/tower-incumbent-selection-20260916/runtime`, after verifying its compiled/source/dependency identities and passing native admission receipts. The producing harness has the verified incumbent nomination change; no harness or gameplay rebuild is proposed for this pilot.

The target remains Kharad/floor 5: ten level-40 characters in the existing subgroups, five Essences per character, Standard tier-1/rank-2 fixed equipment, neutral identity vectors, no contributed damage or styles, one context and ordinal outcome-independent ability order. Preserve the admitted 85-Essence /82-family pool. `OwnedCopies=null` assumes sufficient copies; this is conditional legal-team finding rather than a player-inventory or acquisition claim.

The only supplied references are `team-040e60d3dbc5c127321653c47ed3a9d3` and `team-49f6979895354870c89362d4abf214bb`. Preserve their canonical current-cohort recipes and evidence ancestry. They are both starting candidates, charged inside the discovery allowance; historical fitness is not carried into the new search.

Use exactly one `retained-composition` method/root, 64 complete evaluated parties, at most 256 proposals including rejected/duplicate opportunities, and the existing fresh schedule, parent retention and mutation operators. Nominate both exact admitted incumbents plus the best two distinct non-incumbent challengers by the existing discovery ranking. Inherited reference ancestry does not make a descendant an incumbent. Freeze the four in discovery-rank order before selection. Missing incumbent evidence or fewer than two challengers cannot produce a valid selection stage.

Select one party using the existing `tower-staged-zero-win-health-v1` rule: wins first; health breaks only zero-win ties, followed by frozen discovery rank and stable ID. Positive-win ties keep existing behavior. There is no new incumbent preference or confidence threshold for selection. Eligibility does not guarantee that noisy selection chooses the stronger team.

## Fights and fresh values

| Stage | Fixed maximum | Fights |
| --- | --- | ---: |
| Discovery | 64 parties, including both starts ×8 values | 512 |
| Selection | 2 incumbents +2 challengers, each ×32 separate values | 128 |
| Confirmation | Selected party +2 anchors, each ×256 new values | 768 |
| **Total maximum** | No diagnostics or combat replays | **1,408** |

Request exactly **297 fresh values**: one construction root, eight discovery values, 32 selection values and 256 confirmation values. All four panels are mutually disjoint and excluded against the full live reservation/history union. Shared values within each stage permit paired comparisons. Confirmation outcomes cannot enter generation, nomination or selection.

The latest complete production allocation ledger is the previous pilot's `binding/seed-ledger.json`, containing all **483,343** exclusions: its 483,046 historical values plus its 297 reserved values. The two newer fabricated archive ledgers contain only literal labels already included in that union; this planning check does not replace a new complete registry scan. Readiness and binding must use the recovery-aware production `Refresh`/`Recheck`, require the prior complete ledger and old history sources, discover the newer ledger files, and preserve the original Pending artifact with its existing mapped recovery receipt. Any changed/unexplained registry membership, bytes or union blocks allocation. Do not reuse the older helper's hard-coded 483,046 expected count or old authoritative-ledger choice.

No master or candidate balance value is chosen or derived in this planning step. After authorization, pin one master and the distinct domain `retained-incumbent-practical-pilot-v1` before derivation. Before each candidate derivation, durably append a Start record; append its candidate/acceptance outcome and retain every accepted value. Collisions consume derivation attempts within the existing bounded adapter. Pending/interrupted reservations cannot be discarded. All 297 accepted values remain excluded even if a later stage stops or confirmation merges.

Both anchors always enter the frozen confirmation family. If selection returns an anchor, exact-recipe merging preserves selected and reference origins and uses 512 confirmation fights: **1,152 total**. The saved 256 fight slots cannot fund another proposal, seed, candidate or run. The maximum remains 1,408; novelty must not be forced by discarding an anchor after seeing outcomes.

## Decision fixed before allocation

Require a Complete study, exact input/ledger/attempt integrity, native reconstruction and an independent saved-evidence audit. Completed scientific exit codes 0/1/3 are acceptable only with actual Complete status and matching reconstruction. Cancellation, incomplete discovery, invalid artifacts, changed inputs or exhausted resources yield no demonstrated result.

For complete verified evidence:

- **Incumbent retained:** the selected canonical recipe equals either anchor. Publish its fresh confirmation result and the other anchor's result, with both origins preserved. This is not a novel discovery or evidence of improvement over both anchors, even if it beats the other anchor.
- **Demonstrated improvement in this pilot:** a selected challenger is distinct from both anchors, its adjusted confirmation win-rate lower bound is at least 10%, and it exceeds each anchor by at least five observed percentage points with a strictly positive adjusted paired lower bound.
- **Improvement not demonstrated:** a novel challenger fails any of those thresholds. Distinguish a worse point estimate from insufficient precision; health and survival remain descriptive rather than replacement endpoints.

Retain the fixed family of seven Bernoulli quantities: three party win rates and gained/lost-win probabilities for the two paired contrasts. Compute each with existing `TowerBalanceEvaluator.Wilson(count, 256, 7)`. A contrast interval is `[gains.Lower - losses.Upper, gains.Upper - losses.Lower]`. Keep family size seven when a recipe merges; the duplicated rate and self-comparison do not permit reducing the adjustment. This is approximate Bonferroni-Wilson coverage, not an exact guarantee.

The existing generic study's pointwise paired descriptions and 50% balance ceiling remain separate from this decision. Keep above-50% teams visible, including the known anchors. Their earlier 63.67% and 60.55% results are context only; this pilot uses its own independent confirmation. A high team win rate can coexist with a generic balance failure. Do not filter a strong team out to obtain balance acceptance.

## Readiness and execution

Reuse the sealed harness runtime, matching gameplay dependencies, current native admission and the 40 passing implementation tests. Do not repeat search-quality fixtures or rebuild production code. Create a new isolated launcher/test package using the previous durable pilot adapter and owned Windows process-tree guard. Store readiness-only fixtures outside the live history registry. Keep document snapshots narrow and retain cleanup space inside each hard output ceiling.

The focused readiness work must verify:

1. The exact new policy, domain, two anchors, budget, stage counts and current execution/content/settings hashes; preservation of the old pilot and implementation packages.
2. Full recovery-aware history reconciliation using the 483,343-value complete ledger, including the prior pilot's binding/study ledgers and the newer fabricated ledgers. Recheck membership and bytes immediately before fresh derivation. A partial known-file check is insufficient.
3. The adapted reservation/launch binding, mismatched authorization rejection, Pending interruption behavior, durable attempted/completed counters and once-only launch. Use literal synthetic callbacks for adapter tests; never call a production candidate allocator in a test.
4. All decision branches from fabricated outcomes, including incumbent retention, novel success, novel failure, above-50% results and incomplete evidence. Verify serialized receipts use distinct `verificationStatus`/`studyStatus` names: the old anonymous `status`/`Status` collision must not recur.
5. The exact source template for the independent audit. It must expect the new nomination policy and incumbent-retained status, verify all four nominees against discovery evidence, independently reproduce selection/recipe merging, recount saved outcomes and match the seven-component decision.

Run the focused backend checks through `build/run-tests.ps1`. The previously verified production behavior may be reused only while source/runtime pins match. Readiness failures may receive bounded corrections and test reruns within the readiness envelope before allocation; every attempt is preserved and charged. Any change to search/selection policy, cohort, decision thresholds or resource envelope requires a new prospective review. Gate fresh allocation on passing readiness, never on a test process's exit code alone.

Launch once with a durable marker, external 1,408-attempt limit, soft cancellation before the hard deadline and owned-process-tree termination/cleanup. The generic state machine has no outcome-based futility rule; complete discovery and selection lead to the fixed confirmation family even if all selection wins are zero. Stop on integrity/cap failure and retain partial evidence. There is no combat retry, resume, second root, optional extension, automatic sample increase or renamed follow-up.

Afterward run native saved-study reconstruction and an independent ledger/outcome/recipe/statistics audit with a zero-combat guard. Retain exact recipes, all proposals and ancestry, frozen shortlist/selection/family, compressed outcome files, the producing runtime, source/byte inventories and cumulative accounting. No replay is authorized. If the pilot retains an incumbent or fails the challenger-improvement gate, report that result and close this pilot; it does not trigger another minor variant.

## Resource envelope and authorization requested

The [latest sealed receipt](../TestResults/balance/tower-incumbent-selection-20260916/completion.json) leaves **2,737.9506077827637 diagnostic seconds /485,291,603 bytes overall** and **112.9958801384491 engineering seconds /4,675,865 engineering bytes** before this planning work. These balances carry forward. This planning/publication scope is capped at **10 seconds /1 MiB**, charged to engineering; the [planning receipt](../TestResults/balance/tower-incumbent-practical-pilot-planning-20260916/completion.json) records actual conservative charges.

Request a **16-MiB byte transfer from unused run allowance to engineering**, with no time transfer and no overall increase. Component caps would be engineering **1,045 seconds /496 MiB**, run **2,255 seconds /496 MiB**, audit **300 seconds /32 MiB**. This gives the isolated launcher and test dependencies space without copying the entire verified runtime into readiness output.

| Work | Time ceiling | New-output ceiling |
| --- | ---: | ---: |
| Focused readiness, including corrections and failed attempts | 90 seconds | 16 MiB |
| Fresh binding and one combat run, including cleanup | 1,200 seconds | 300 MiB |
| Saved-evidence verification and publication | 120 seconds | 16 MiB |

These are stopping ceilings, not runtime predictions. The previous 1,408-fight pilot's binding/run used 83.531 seconds and about 81.4 million bytes of charged output, but this does not certify the new run's cost. The new scope fits within remaining overall limits after planning, and the readiness time fits within the current engineering balance; no time transfer is needed. Do not spend spare capacity across phases or increase ceilings after observing results.

**Approval requested:** the 16-MiB transfer, then focused readiness and, conditional on every gate passing, one allocation of at most 297 fresh values, one run of at most 1,408 fights, and the fixed saved-evidence audit. No allocation/run is currently authorized by the implementation receipt. This request grants no gameplay tuning, V19 confirmation, block-search revival, deployment, second root or extra combat samples.

## Planning work

This turn adds this prospective protocol and a non-executable proposal/preservation/accounting package, and updates current README/handoff notices. Static checks cover source/runtime pins, sealed implementation and old pilot inventories, known-ledger arithmetic and synthetic-label containment, links, unrelated dirty files and scoped `git diff --check`. No complete live registry scan, build, backend test, native preparation, benchmark, seed derivation, combat or replay is run in planning. Full live reconciliation belongs to readiness. No production code, gameplay content, migrations, persistent configuration or deployment changes are made. All 483,343 exclusions remain preserved; adoption stays Hold, V19 reliability Unresolved, and the earlier failed comparison remains Closed.
