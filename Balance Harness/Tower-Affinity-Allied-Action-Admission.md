# Allied-action comparison: runtime and resource admission

24 September 2026. Target: the offline `LL/tools/BalanceHarness`.

**The current runtime is qualified and the frozen twelve-root allied-action comparison is admitted. It has not been launched.** The [pinned admission package](../TestResults/affinity-allied-action-admission-20260924/admission.json) permits the separately bounded v4-versus-v5 comparison. Qualification used zero combat, zero entropy draws and zero new reserved values. Scientific rules, production defaults and previous evidence remain unchanged.

The [declaration](Tower-Affinity-Allied-Action-Admission-Declaration.json) fixed a single **900-second / 1-GiB** qualification attempt, its tested executable and helpers, consumed evidence, probe coverage, resource formula and no-retry/no-resampling rule before execution. The full allowance is charged even on failure. This successful attempt recorded **185.469 seconds / 400,658,017 bytes**. No scientific campaign allowance has yet been spent.

## Qualified runtime and context

The runtime retains all captured gameplay dependencies and content. Only the four tested harness files replace their captured counterparts. Both baseline and candidate materialized and prepared **568 saved recipe instances at two historical values each**: 1,136 preparations per runtime, **2,272 total**. Materialized-input and prepared-participant hashes agree for every instance under a guard that throws on combat entry. No roots were sampled or screened for the future campaign.

Coverage includes the four earlier first-wave arms, both earlier preservation arms, and both complete waves on all twelve saved allied-action roots. The candidate reconstructs all 30 saved arm exports exactly. The full rebuilt inventory matches the captured inventory hash `f06496a3079c4b9aa8d3366ac94e58de994ee7317fd41996eb90905a802d45c6`.

All **230 producing source documents** match the portable PDB, whose CodeView identity matches the assembly. Required generation, allied-action protection, comparison, validation and archive methods resolve under the captured dependencies. The production retention copier preserves all **27 runtime files**, including the executable and symbols. The qualified execution hash is `4507332791edbb470590cf5d5c15d0bf3fbd9c191bcc3e3cb57f84f095e99b53`.

The [qualified plan](../TestResults/affinity-allied-action-admission-20260924/plan.json) has native hash `da1f5148683c37c60432281faaddc7b5104aecebd7b2aecd97d32fcd01b15563`. Compared with the unchanged original native plan, only its scope binding changes. The qualified context changes only episode identity, execution identity and accumulated history exclusions; policy, inventory, benchmark, affinity IDs, allocation, selector and analysis remain fixed.

## History and resource gate

The complete authoritative scan and explicit pending-history recovery reconciliation yielded **766,415 permanently excluded values across 260 files**. Native input validation agreed. The full history was rechecked before sealing; launch must check it again. No entropy allocation file or scientific output directory was created.

The forecast takes upward-only maxima of prior admitted floors, the fixed native-audit probe, current guarded preparation, the completed preservation pilot scaled from 15,744 reports to the 21,888 ceiling, and paired old/new owned-fixture phase costs. Storage growth enters the audit inventory scaling before admission. Faster new observations cannot lower inherited floors. The margin remains **2**, including the **120-second publication reserve**, with no borrowing between partitions.

| Partition | Forecast | Fixed limit |
| --- | ---: | ---: |
| Native time | 6,693.288 s | 9,000 s |
| Audit/publication time | 1,763.718 s | 1,800 s |
| Native storage | 5,168,096,099 bytes | 5,905,580,032 bytes |
| Audit/publication storage | 34,989,702 bytes | 536,870,912 bytes |

All four estimates fit their own hard limits. Audit/publication time has **36.282 seconds** of forecast headroom, with the publication reserve already included. This is a planning estimate, not a guaranteed bound. The owned launcher must stop and retain incomplete work if any actual limit is reached. The total remains **10,800 seconds / 6 GiB** under `tower-proposal-resource-envelope-v2`.

## Verification and changed files

The new admission suite passed **28 tests**, covering declaration pins and immutable rules; all twelve saved roots and both complete waves; materialized-input/participant equality; gameplay dependency drift; producing sources and required entry points; source manifests; all four resource partitions; upward-only floors; metadata/storage growth; and rejection without borrowed time or timing retries. Two initial synthetic forecast assertions were corrected for the new fixed 15,744-report denominator before declaration; the initial test log is retained. No qualification was run during those tests.

The declared run passed baseline/candidate qualification, retention, native input validation, independent Python audit of the sealed owned allied-action fixture, repeated authoritative history checks, and final owned-launcher admission validation. Post-run verification reconstructed the context and complete resource formula from saved observations without resampling. Existing **125 backend tests and 67 Python tests** remain authenticated; no backend source changed, so no rebuild or backend rerun was needed. No required command remains blocked.

Changed files are the new `prepare-affinity-allied-action-admission.py`, `affinity-allied-action-admission-context.ps1`, and `test-affinity-allied-action-admission.py`; the pinned declaration; this report; `.gitattributes` rules preserving their declared bytes; and line 3 of nine current-status documents. Their historical bodies and all 114 inherited pins remain unchanged. Previous admission scripts and the frozen scientific design are untouched.

The verification commands were:

```text
python -B -X utf8 "Balance Harness/analysis/test-affinity-allied-action-admission.py"
python -B -X utf8 "Balance Harness/analysis/prepare-affinity-allied-action-admission.py" --declaration "Balance Harness/Tower-Affinity-Allied-Action-Admission-Declaration.json" --pin aad2fbc9b3b2e3a5af089a4ab512b533796362f722b59bbcdbe6368bdf081036
```

The PowerShell helper passed parser validation before execution. Python used the bundled runtime. Every native qualification subprocess ran in an owned Windows Job with deadline/storage checks and zero surviving processes. The admission manifest SHA-256 is `c8eb52d39b57fc385a5e88330a8ec621908a3345ef3d12c33360cbdcb361e68d`; request SHA-256 is `2e99570c1d6f5aebc72f55bb32c4d64949d0923b1c5d4cc9ad3dea16b5253784`. The [verification handoff](../TestResults/affinity-allied-action-admission-handoff-20260924.json) retains the source and evidence pins.

## Accounting and next step

Cumulative recorded work is **40021.394953 seconds / 31,616,661,801 bytes**. The full qualification charge raises cumulative declared maxima to **104,820 seconds / 65,716,355,072 bytes**. Unit tests and handoff documentation remain separate engineering verification. The prior recognition result remains `CompleteDiagnosticOnly`, and the source preservation pilot remains `NoObservedOutputDifferentiation`; admission provides no efficacy evidence.

Next is one owned execution of the admitted twelve-root comparison, after revalidating package pins and live history at launch. It retains the 4,380-value requirement, 21,888-fight ceiling, all-24-output freeze, unchanged validation gates and all-root decision rules. One entropy exposure must retain its entire newly exposed tail; incomplete work keeps its charges and reservations, with no replacement roots or retry.

There are no migrations, application configuration changes, deployments or gameplay-default changes. The opt-in request explicitly selects the allied-action comparison and v2 resource envelope.
