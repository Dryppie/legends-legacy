# Five-candidate, three-reference confirmation implementation

23 September 2026. Target: offline `LL/tools/BalanceHarness`. The frozen v1 confirmation protocol is implemented. **The [first captured-runtime admission failed before native preparation](Tower-Practical-Three-Reference-Confirmation-Admission.md).** The [verification improvement and prospective resource amendment](Tower-Practical-Three-Reference-Confirmation-Resource-Amendment.md) are now complete; next is explicit v2 accounting implementation and tests before another admission. No scientific allocation or combat has run, and no candidate has acquired a new strength recommendation.

The [plan](Tower-Practical-Three-Reference-Confirmation-Plan.md) and [unchanged JSON](Tower-Practical-Three-Reference-Confirmation-Plan.json) remain authoritative. JSON SHA-256: `c6544a63a569de5197c4e2a35749c01a14388478e6a9986509d991c64bd6a74f`. Native version: `tower-practical-three-reference-confirmation-v1`. The older six-candidate/two-reference protocol keeps its existing version, exact family, 5,500-sample endpoint, recommendation behavior and serialized request identity.

## Implemented behavior

- Exactly five frozen challengers, then the three retained references. Complete scenario, role, reference-ID, content and settings hashes reject substitutions or reordering. Runtime and complete historical exclusions bind at admission.
- One 52,000-byte batch supplies 13,000 signed little-endian words. The first 6,500 fresh unique values form the shared panel; every fresh tail value remains reserved. All durable interruption boundaries preserve the no-refill/no-resume rule. Unresolved starts block later allocations and legacy recovery cannot clear them.
- Seven slices per team, at most 1,000 seeds each: 56 scenario bindings and exactly 52,000 journalled terminal reports. Native reconstruction checks input, cache, scenario, report, ordinal and seed identities without combat.
- Family 38 covers eight win rates and the gain/loss rates of fifteen signed contrasts. Qualification requires at least 728 wins, net gain at least 325 against **each** reference, and a positive adjusted paired lower bound. Every qualifier is recommended in frozen order, followed by all three references. Incomplete evidence produces no recommendation. Balance remains `NotAssessed`; search reliability and candidate-to-candidate superiority remain unassessed.
- The Python auditor independently reads direct reports and recounts all eight ordered rows and fifteen contrasts. It checks the full panel before pairing and independently validates all result fields before preserving native JSON number encodings.
- The Python owner holds both exclusive registry/output leases through execution, both audits and publication. Every child starts suspended in a hidden Windows Job; a completed root with a surviving descendant cannot count as a completed job. Both audits and the final publication barrier use one deadline. Failed audits, changed proposals, exhausted storage or interrupted sealing leave failed evidence and never create a valid completion.

## Resources and accounting

| Scope | Time ceiling | Storage ceiling |
| --- | ---: | ---: |
| Whole new study | 7,800 seconds | 4 GiB |
| Separately charged captured-runtime admission | 600 seconds | 512 MiB |
| Execution owner, including audits/publication | 7,200 seconds | 3.5 GiB |
| Native execution, including cleanup | 6,000 seconds | 3 GiB |
| Both audits, final barrier and publication | 1,200 seconds | 512 MiB |

Unused native allowance cannot enlarge the audit reserve. Copied executable/content files, reports, journals, temporary files, logs and publication bytes count within the enclosing storage checks. Completion includes its manifest size; a separate terminal receipt binds that manifest and measures through sealing, including its own retained bytes. The owner continues checking the original deadline through the terminal write.

The accepted **18,180-second /13,584-MiB historical charge ledger remains separately disclosed**. Complete historical engineering totals remain unknown. Implementation fixtures are engineering work, outside this new scientific allowance. Previous studies remain closed and charged.

## Verification and retained evidence

Evidence directory: [implementation receipts](../TestResults/three-reference-confirmation-implementation-20260923). **81 distinct backend tests passed**: 80 focused tests plus the full 52,000-report fixture against the same final build. The earlier combined pass contains 76 passing tests. **18 Python tests passed**, including a complete independent recount, hostile row ordering, actual descendant cleanup, lease exclusion, storage reserves and interrupted publication.

The full fixture uses literal entropy, synthetic inputs and terminal reports under a combat guard. It measures reconstruction overhead; it does **not** establish captured-runtime compatibility or scientific combat feasibility. Legacy regression coverage includes exact family/threshold parity, request serialization, resource limits, native input preparation, reservation failures and process cancellation. Unchanged legacy long publication campaigns were excluded from the focused filter; the new complete archive and publication failure fixtures cover the changed paths.

The final-runtime literal fixture took **322.786 seconds** and retained **70,638,563 bytes** across its isolated input/output directory. The full independent history scan took **97.796 seconds** and confirmed **633,313 values /240 ledgers**, unchanged. These are individual engineering observations, not a global engineering-cost total or a combat forecast. The tested harness DLL hash is `e322f21ff216f00143de6fa3c33d6fd9873ab8b6cb8e219e76f05e17fe7f107a`; its PDB hash is `e1afe06374a39c93349ca98b1a8e5ae32ae7fa60d293df825a84298e209f9e7d`.

The initial test attempt could not read the user's NuGet configuration within the sandbox; rerunning the same repository wrapper with the required access succeeded. The first built test run exposed an incorrect fixture expectation: at this sample size, its 325-net-win example already had a positive paired lower bound. The fixture was corrected without changing the frozen decision rule. Initial logs remain retained. No required verification command remains blocked.

The scoped diff check passed. The repository-wide check found unrelated existing trailing spaces in `LL/docs/strongholds-mechanical-progression-revision.md`; that document was not changed by this work.

One repeat of the real descendant-process fixture exposed a Windows temporary-working-directory teardown race after the Job reported zero active processes. The test now uses a stable execution directory, matching the launcher, while retaining its timeout and empty-job assertions. That failed log is also retained.

Backend commands use `build/run-tests.ps1`, with an isolated `-ArtifactsPath` beneath the evidence directory. Python checks use `python -B -X utf8 build/test-three-reference-confirmation.py --fixture <retained-literal-fixture>`. Exact logs, source/runtime hashes, per-fixture timing/storage and the read-only authoritative-history check are retained with the implementation receipt.

## Admission handoff

Use the retained scientific gameplay dependencies/content/settings from the completed three-reference comparison admission. Substitute only the four tested harness artifacts (`BalanceHarness.dll`, `.pdb`, `.deps.json`, `.runtimeconfig.json`), then check their compatibility against those retained dependencies. Do not substitute gameplay assemblies built from the dirty current checkout.

The new request reuses the fixed-family request structure, with version `tower-practical-three-reference-confirmation-v1`, an empty `phases` dictionary, exact 7,800-second /4-GiB totals, and the full admission charge of 600 seconds /512 MiB backed by its receipt. Its `threeReference` object binds absolute `planPath`, `auditorPath` and the tested `auditorHash`. The definition carries the exact eight teams from the plan, the admitted execution identity and refreshed complete exclusions. The native canonical team hash is `3ec3b0e93a491ef4837babbf5d8bf6f98745369c53e9be4a6e21f430a590ef32`.

Available commands:

```text
BalanceHarness tower-three-reference-confirmation-check <admitted-request.json>
BalanceHarness tower-three-reference-confirmation-verify <completed-output>
python build/run-three-reference-confirmation.py --request <admitted-request.json> --harness <retained-runtime/BalanceHarness.dll>
```

The launcher is implemented, but no runnable scientific request has been admitted or launched. The first admission failed after 499.765 seconds when the PowerShell helper could not resolve the ASP.NET configuration assembly. Its full 600-second /512-MiB charge is preserved. The separate read-only closeout confirmed 633,313 values across 240 ledgers unchanged. Future admission still must refresh history and leases, authenticate source/runtime bindings, establish relevant resource headroom and finish within a prospectively reconciled allowance; the corrected host has not been used to retry the failed package.

Changed source groups are the shared fixed-family controller/profile/reservation/archive, the new owned native route, Python owner/auditor, backend/process fixtures and Python tests. Markdown pointers now lead here. There are no application configuration changes, migrations, database operations, deployments or infrastructure changes.
