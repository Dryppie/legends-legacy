# Kharad calibration after v13 replication

Frozen study design, 13 September 2026. Target: offline `LL/tools/BalanceHarness`, isolated content copies and saved evidence. No gameplay implementation, live catalog or deployment changes are part of this study.

The [unchanged-v13 replication](Tower-Loadout-Replication-Review.md) found two successful primaries at 225/256 and 244/256. The previous challenger separately confirmed at 548/1,000. Current Kharad uses Health **3.04881408**, Power **3.85370128**. Four already-paid first-screen-seed replays are inspected before freezing inputs; they are descriptive evidence, not causal ablations.

## Fixed scope and allocation

- Ten level-40, tier-1, rank-2 Standard characters; five level-1 unascended/unevolved Essences; exact existing equipment; no styles/contributions; all 80 eligible Essences under hypothetical ownership. Acquisition remains unverified.
- Import all **20** seed-free finalists and controls from `tower-loadout-replication-20260913/selected-family.json`, including zero-win builds. Preserve exact ordered recipes, identities and provenance.
- Screen linked Health/Power factors **1.00, 1.02, 1.04, 1.06, 1.08, 1.10, 1.12, 1.16, 1.20, 1.30, 1.40**, calculated independently from the current baseline. Only these two floor-5 fields may differ. Every factor tests all 20 builds on the same **64 fresh seeds**: **14,080 fights**.
- Select among factors whose maximum observed build rate is **15–40%**, minimizing distance from **30%**, ties to the smaller factor. This interior preference is a selection rule, not acceptance. No monotonicity assumption, interpolation, additional grid points, pooled results or adaptive extension. If none qualifies, retain the complete screen and stop with no candidate.
- At the selected setting, run unchanged `independent-loadout-composition-v13`: both `coverage-deep-joint` and `loadout-composition-joint`, three fresh paired restarts, **384 candidates per arm**, eight separate discovery seeds: **18,432 fights**. References are absent from the discovery definition and generator inputs. Preserve all 2,304 evaluations and ancestry.
- Before opening confirmation outcomes, nominate discovery rank one and rank two from every arm using the unchanged whole-party fitness order. The confirmation family is the exact-recipe union of these 12 nominations, all 20 saved controls, and **every newly evaluated party with an observed discovery rate above 50%**. Do not replace primaries after held-out results. Maximum **64** distinct confirmation recipes; overflow is an explicit unresolved-coverage stop, never truncation.
- Confirm that complete frozen nominated family on **512 fresh shared seeds**, at most **32,768 fights**. Ordinary `bonferroni-wilson-95-v1` acceptance uses the entire declared family; every adjusted upper must be at most 50%, with at least one adjusted lower at least 10%. Any observation above 50% rejects acceptance.
- Separately describe search reliability with joint alpha .025 over all confirmation rates and .025 over six paired comparisons (two discordance intervals each). Wilson multipliers are twice the complete family size and 24 respectively. A primary qualifies only if its adjusted lower is at least 10%, paired lower improvement over its same-restart deeper-v4 comparator is positive, and paired lower difference against the fixed saved anchor is at least −10 percentage points. At least two of three are required. This is a new measurement at changed content, not another replication of the previous setting.

## Bounds, reproducibility and decision

Maximum **65,280 combat attempts**, **5,400 seconds** across execution phases, **4 GiB** for the study package, **zero combat retries or diagnostic replays**. Each started/completed battle is durably counted; campaign journals and compact receipts independently account for the same work. Existing publication retries do not repeat combat. A technical interruption preserves evidence and requires an explicit recovery decision; do not rerun a started phase or reset its clock/journal.

Import every integer in every array of the latest **474,187-reservation** ledger, including unused/constructor reservations. Allocate disjoint screen, generation, discovery, unused contract-selection and confirmation schedules with deterministic master seed `2026091304`. Freeze all schedules, content variants, definitions, executable and producing-source hashes before the first battle. Exact confirmation recipes are deterministically frozen after discovery and before the first confirmation attempt. Verify compact archives using the captured executable with a zero-combat guard; independently reconstruct arithmetic, selection, family coverage and source preservation.

The final application decision must distinguish a verified candidate, acceptance of the nominated family, independent search reliability, and complete relevant-family coverage. The earlier **2,918-party family**, intervening unshortlisted searches and unshortlisted new evaluations are retained as outstanding coverage unless actually confirmed here. A nominated-family Pass does **not** replace that complete-family requirement or authorize a live setting change. Deliver the exact candidate and a concrete apply/hold/reject decision; retain all breaches and recipes for the next bounded confirmation. Do not launch the historical campaign implicitly. Later floors and practical acquisition are separate work.

Historical sealed reviews and packages remain unchanged. Update the active handoff and status Markdown after verification. Backend checks, when relevant, run through `build/run-tests.ps1`.
