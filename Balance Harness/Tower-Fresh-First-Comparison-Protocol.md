# Fresh-first V5 versus baseline: bounded comparison

16 September 2026. Target: offline BalanceHarness. Preparation only is authorized. Reuse the [85 verified backend facts](Tower-Fresh-First-Refinement-Review.md) and exact tested V5 assembly; no harness/gameplay rebuild or backend test rerun.

## Frozen scientific design

Compare `independent-team-coverage-v1` (sixteen fresh proposals) with `independent-discovery-refinement-fresh-first-v5` (twelve fresh proposals, then up to four single-Essence/one-slot edits of the best completed parent), selected by `tower-discovery-refinement-comparison-v4`. Both arms share captured gameplay assemblies, content, settings, equipment, timestamps and schedules. Keep fixed ordinal ability order, the V4 local-edit algorithm/random namespace, ranking, nominations and selection unchanged. No Kharad or gameplay tuning. This 75/25 allocation is exploratory, not a demonstrated optimum.

| Stage | Maximum teams | Fights per team | Maximum attempts |
| --- | ---: | ---: | ---: |
| Baseline discovery | 16 | 4 | 64 |
| V5 discovery | 16 | 4 | 64 |
| Top two per arm: selection | 4 | 8 | 32 |
| One finalist per arm plus two controls | 4 | 32 | 128 |

At most **288 charged attempts**, zero retries, resumes or combat replays. Incomplete discovery stops both arms before nomination, with every charge retained and no refill. Merge identical later-stage recipes while preserving every origin; merged nominees/finalists reduce actual fights. Selection uses wins, original discovery rank, then ID. A finalist can play 44 fights across stages. Controls remain `team-040e60d3dbc5c127321653c47ed3a9d3` and `team-49f6979895354870c89362d4abf214bb`.

Primary endpoint: V5-minus-baseline confirmation win rate with the existing adjusted paired interval. Boss health, duration and local-parent changes are descriptive. Identical merged finalists imply an exact zero difference between those same recipes, not proof of policy equivalence over other searches. One exploratory search cannot establish reliability or global optimality; selection/confirmation never feed discovery.

Request exactly **45 fresh values**: one generation label, four discovery, eight selection and 32 confirmation seeds. New master **2026091606**, unchanged durable allocator and V1 derivation namespace. Do not derive candidate values during preparation. Exclude all **483,001 existing reservations** from the completed V4 study's ledger. Retain older Pending sources and their external recovery receipts as exclusions only; no failed study is resumed, rebound or released.

## Compact preparation within existing authority

New directories: `TestResults/balance/tower-fresh-first-comparison-readiness-20260916`, `tower-fresh-first-comparison-study-20260916`, and `tower-fresh-first-comparison-execution-20260916`.

Start from the sealed V5 implementation receipt: **3,931.3432723556225 seconds / 4,613,166,874 bytes**. Current cumulative caps remain **4,260 seconds / 4,613,734,400 bytes**. Preparation allows **45 diagnostic seconds / 512 KiB**, leaving room beneath both caps. Charge twenty seconds conservatively for initial inspection. Source/script drafting is excluded. Phases run once: freeze 5 s; host-only build 5 s; native inspect 5 s; independent audit 5 s; publication 4 s, with one second reserved for sealing. All share the 45-second allowance. Stop at the first failure, retain evidence and do not retry.

Snapshot the dirty checkout, compressing the hash inventory. Freeze the exact protocol, scripts, source/input manifests and commands. Reference the sealed tested harness and captured dependencies without copying a runtime; compile only the small reference-only host against cached restore assets. Its production API body remains unchanged. Compress active-document snapshots instead of duplicating their full text; do not remove or rewrite old evidence to reclaim output.

Verify the V5 implementation seal, 85-test TRX, all relevant source/test/dependency pins, previous V4 readiness/execution seals and complete study inventory. Advance the saved registry snapshot by the V4 study's completed ledger/input. Native `inspect` validates the request/version, controls, content/settings/gameplay identity, current 483,001-value ledger, recovery bindings and matching V5 driver receipt; it writes only an unsigned authorization template. It must not create the study, derive labels or refresh the live registry.

The independent preparation audit checks identical inputs, the latest 45-value historical increment, protocol/template/hash equality, versions, prospective caps and recovery pins. Reuse the saved-record reader against 128 confirmation records/four rates, and exercise the V5 local-edit reader on the sealed twelve-fresh/four-edit synthetic output. No new proposals or fights. The execution reader must preserve merged-origin handling, verify actual counts from archive membership and inspect all four local edits and the twelve-fresh prefix. Production binding and launch retain their complete live history scans, cancellation, durable charges and native archive reconstruction. No extra standalone full registry scan is scheduled.

## Exact prospective approval

Request **45 fresh values**, at most **288 attempts**, **zero retries**, plus **120 additional cumulative diagnostic seconds** and **112 MiB additional cumulative output**. Only specific approval raises the cumulative ceilings to **4,380 seconds / 4,731,174,912 bytes**. Earlier seed approvals are exhausted; no old experiment cap changes.

- Study: **360 seconds / 84 MiB**, including **150 binding seconds charged in full**; launch/live refresh/combat/native reconstruction share the remaining 210 seconds.
- Complete execution: **400 seconds**, including staging, process cleanup, independent audit and publication.
- Producing runtime copied only after approval: **24 MiB**, verified against the pinned manifest.
- Execution evidence outside that runtime: **4 MiB**. Study/runtime/evidence reserve **112 MiB** in total, including partial failure output.

Before requesting approval, prove the actual preparation plus the full 400-second/112-MiB execution reservation fit the proposed cumulative caps. Recompute the study minimum from fourteen compact history arrays, one runtime and four content copies; reports and transient writes are additional, so completion is not guaranteed. Do not sign an allocation/execution authorization during preparation.

After specific approval, bind the user's decision to the readiness seal and request hash, validate all frozen assets, and invoke the frozen wrapper once. Preserve the first failure and every derived reservation. Publish measured counts, native/whole-task timings, final bytes, verification and limitations. If finalists merge, report that fact explicitly. No policy-strength claim follows from an identical recipe.

All **483,001 reservations** remain excluded, including failed allocations and V19's 512 unused values. V19 retains all 253 required recipes and no confirmation; reliability Unresolved, later Fail 1/3, deep recovery 0/3 and adoption Hold. No configuration, migration, deployment, ability-order tuning or sealed-study modification.
