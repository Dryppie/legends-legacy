# Corrected same-group variation verification

15 September 2026. The user authorized proceeding with corrected zero-combat verification. Target: offline BalanceHarness. This separate package preserves the failed implementation run and its charges. **Zero fights, combat replays, preparations or fresh balance values.** All **482,461 reservations**, including v19's unused 512, stay preserved. No search policy, gameplay, ability ordering, ranking, nomination, candidate/attempt limits or archive/accounting code changes.

## Fixed inputs and correction

The current harness source matches the failed implementation package's source hashes, so reuse its captured candidate executable and previously successful old-policy reference hash. Do not rerun the old reference or rebuild gameplay/harness code. Rebuild only the tests. Use the prepared two-overlapping-core fixture and its exactly-one-group precondition; add an explicit assertion that the complete-search case actually evaluates a guided variation proposal. The test count remains **63**. Preserve the failed one-core fixture, binaries, logs and TRX unchanged.

Reuse the unexecuted captured-content driver from that package: the frozen 214-group catalogue, existing captured mechanics and input-only recipe budget, and synthetic construction label 17. No control recipe or combat result enters construction. The independent Python audit also compares the full catalogue with the previously sealed catalogue and checks its recorded hash; it retains valid rejection outcomes without changing input labels or rerunning construction.

## Exact sequence and diagnostics

1. Snapshot dirty checkout status, current harness/test hashes, six active documents and the shared TRX. Freeze the protocol, workflow/audit/preservation scripts, test fixture source, copied captured executable, captured inputs, catalogue and old parity references before any diagnostic. Verify **16 sealed packages**, including the failed implementation package's `files.json` SHA-256 `4c0c687a28ecf0c4553c119a3c777146bd064d3cf085d02132477a266e590e53` and all 15 packages it preserved. Do not enumerate unrelated campaign trees.
2. One isolated test-assembly build against the unchanged captured candidate DLLs and cached dependencies. Build cap 300 seconds, measured separately. No reference/harness/gameplay rebuild or reference execution.
3. One `build/run-tests.ps1 -NoBuild` invocation with the composition, joined, group/count and group-variation class filter: **63 cases**, consisting of the existing 48 cases and corrected 15 variation cases. All four classes guard combat entry. Validate exactly 63 executed, all passed, no skipped; preserve TRX and any failure receipt even when the wrapper exits nonzero. Do not retry.
4. Only after all tests pass, invoke the captured candidate's construction driver once. Enumerate exactly **1,956 schedule positions**: **1,712 guided choices** spanning two complete 214-group sweeps and **244 uniform choices**. These are schedule records, not filled teams or fights. Separately construct **exactly 19 fresh requests**: 17 guided and two uniform. Save every recipe/rejection, group/count/filler/placement trace and `TowerPerformanceTrace` timing. No evaluation, extra requests, preparation or combat.
5. One independent Python recount of those saved outputs: exact catalogue identity/hash; unique group visits per sweep; counts 1/5/10/5 then 2/6/1/6; uniform positions; shared/changed stream identities; all 19 traces; legal known IDs, slot/family/ownership limits, canonical recipe hashes and exact final group counts on accepted requests; nested reservation targets and fixed-count filler differences when available. Report observed failures/duplicates without retry or selecting different labels. No claim of strength or controlled combat speedup.
6. Final frozen-input, copied-binary, current source and sealed-package preservation checks. Publish measured results, limitations, commands and six active Markdown updates; seal this new package. Final preservation/publication may run after a failure, but dependent tests/construction/audit stop.

Use `TestResults/balance/tower-group-variation-verification-20260915` only. Additional diagnostic workload at most **120 seconds**, within the prior implementation's remaining 300-second allowance and cumulative **263.066 / 1,800 seconds**. Each diagnostic command is bounded by the remaining time; builds are separately measured. New output at most **128 MiB**, combined with the failed implementation within its 512-MiB envelope and all preceding comparison/diagnosis output within 4 GiB. No old cap increase. **Zero retries/resumes**; preserve any failure and remaining uncertainty.

This is targeted zero-combat construction/parity verification, not a full backend suite or a balance comparison. Historical reliability Fail 1/3, deep recovery 0/3, sealed v19 Unresolved and adoption Hold remain unchanged. No Kharad tuning, named-build optimization, ability-order search, configuration changes, migration, deployment or large confirmation.
