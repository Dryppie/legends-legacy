# Group-completion saved-trajectory audit

15 September 2026. Target: offline BalanceHarness evidence analysis. The user authorized inspection of the completed completion-versus-diversity comparison. **Zero fights, fresh seeds, preparations, replays, candidate construction or scoring callbacks.** No executable, gameplay, policy or sealed package changes. The comparison's 45-value exception is exhausted.

## Frozen scope

Read the sealed `tower-group-completion-comparison-{preparation,study,execution}-20260915` packages. Analyze **88 evaluations and 92 proposals** (45 diversity baseline, 47 completion), all parent references, full finalist ancestry, fresh roots, parent eligibility, module-library hashes and recorded donor uses. Reconstruct saved fitness ordering, both top-two nominations and frozen wins/rank/identity screening. Keep rejections and duplicates separate from evaluated teams; never invent scores for them.

Retain every requested group/count/filler, including incomplete bundles, recorded completion steps, trace evidence and recipe. Both policies use the diversity schedule; the saved prefixes are 17 guided plus two uniform baseline requests and 18 guided plus two uniform completion requests. Reconstruct the metadata-derived 214-group order using the existing generation label; verify both prefixes and each control group's exact and containing-group positions. Untested positions are not predicted outcomes.

For both nominees per policy, retain discovery health/rank, the eight saved screen outcomes and health values/missingness, and the frozen winner. For every catalogue group present in either control, recount generated owners, module availability/uses and finalist owners. Define the shared control set descriptively as Essences present on at least eight characters in each control; recount its complete co-location on every evaluated team and its exact control loadouts. This reference analysis must not feed generation.

Extend the saved-data reader with completion-step accounting: reconstruct the prefix from reserved insertions, validate exact before/after additions, retain category/provider/recipient details and count newly supplied shared-control Essences. At each saved step, list shared-control providers compatible with still-uncovered group owners under current slots, family and shared-copy constraints. This metadata-only list does not draw randomness, score providers, propose new recipes or claim that a skipped provider would be stronger. Retain near-complete shared sets that lacked one component before a step. Recount final shared-set owners across all evaluated recipes, module donors and finalist ancestry. Quantify observed additions and retention without attributing causal combat effects.

## Verification and limits

Freeze protocol, scripts, seals, input hashes and checkout hashes before diagnostics. Reuse all 13 saved-data fixtures and add exactly **three completion-reader fixtures**: repeated-owner and multi-step accounting without mutating the input; eligible shared providers with category/copy/family/slot exclusions; malformed trace rejection and no-trace handling. **16 Python fixtures**, one analysis pass and one independent verification pass. The independent verifier reconstructs graph/ranking/library/screen/bundle/order results without importing the analysis engine, and separately checks completion steps, alternatives, exact control-loadout matches and all shared-set counts from source records.

The prior **103 backend cases through `build/run-tests.ps1`** and four metric fixtures remain sealed. There are no backend edits or new combat requiring repetition. New diagnostic workload including freeze/preservation, fixtures, analysis, verification and publication is capped at **120 seconds**, each diagnostic subprocess at most **45 seconds**, zero retries. Carry forward **1,172.992 seconds** within the cumulative **1,800-second cap**, retaining preceding failures/corrections. New output at most **32 MiB**, cumulative retained output at most **4 GiB**. Preserve **33 preceding sealed packages** before/after. On a failed check or cap, stop dependent diagnostics and preserve evidence.

Publish the measured review, reproducible commands and six active handoffs. Preserve **482,596 reservations**, v19's unused 512 and all 253 recipes, fixed ability order and unchanged gameplay. Historical reliability Fail 1/3, deep recovery 0/3, sealed v19 Unresolved and adoption Hold. No Kharad tuning, deployment, default promotion, larger combat study or 129,536-fight confirmation.
