# Composition-only search pilot

15 September 2026. Target: offline BalanceHarness against the unchanged captured-v19 guardian Health/Power +10% candidate. The user asked to proceed with a small search-quality pilot. The original zero-fresh-seed restriction still requires a specific exception before the 85 values below may be allocated. Preparation, compilation and zero-combat verification are authorized. No historical study is resumed or modified.

## Frozen design

One independent `independent-composition-only-v1` / `composition-only-joint` arm, **48 distinct evaluated builds**, at most **768 proposals**, FreshEvery 4. The first 12 measured builds are fresh constructions. Full captured eligible pool, existing equipment/attributes, floor 5, ten characters (two five-player parties), five Essences each, level 40, tier 1, rank 2. No owned-copy restriction is introduced. Preserve the captured neutral character/Essence identities and UTC timestamp. Sort Essence IDs ordinally within each character before identity, deduplication, evaluation and nomination. No ordering search or gameplay tuning.

| Stage | Builds | Fights per build | Maximum fights |
| --- | ---: | ---: | ---: |
| Discovery | 48 | 4 | 192 |
| Screen | Top 4 by existing discovery fitness | 16 | 64 |
| Confirmation | Top 2 screened builds plus 2 fixed controls | 64 | 256 |
| Total | | | **512** |

Screen ranking is wins descending, then the frozen discovery rank, then ordinal build ID. Both finalists freeze before any confirmation outcome. The two controls are the two saved external references in the sealed deep-challenger definition; neither enters generation, parents, modules or screening. Apply the same fixed Essence ordering to both controls. Use the same scenario ID, non-Essence context and paired schedule for all cases within a stage. Deduplicate exact confirmation recipes while retaining every finalist/control origin; do not fill a freed seat or spend the unused fights. Retain all 48 evaluated recipes, rejected/duplicate proposals, screening outcomes and confirmation results, including zero wins and early >50% observations. An incomplete discovery stops the pipeline.

This is an exploratory search-quality pilot, not a full retained-family acceptance study or a historical-policy comparison. One shallow restart cannot establish reliability, optimality or an improvement in search quality over the old policy. Four discovery fights are noisy; screening reduces but does not eliminate selection error. Fixed-order controls are new measurements, not pooled with their old ordered results. Unconfirmed discovery/screen ceiling observations remain explicitly unresolved. Historical reliability Fail 1/3, deep recovery 0/3, sealed v19 Unresolved and adoption Hold remain unchanged.

## Seeds and resources

After the specific exception only: **85 fresh values**: one generation root, four shared discovery values, 16 shared screening values, and 64 shared confirmation values. Exclude the full current union of **482,286 reservations**, including all unused historical reservations. Expected final union 482,371. Domain `tower-composition-only-pilot-v1`, master 2026091503, stages in order `generation`, `discovery`, `selection`, `confirmation`, zero-based candidate ordinal per stage, existing StableRandom derivation, at most 100,000 candidate attempts per stage. Durable Pending intent and Start/Candidate journal precede use; an interruption is preserved and never retried. No fresh candidates are derived during preparation.

Maximum diagnostic workload **1,800 seconds** including preparation, allocation/binding, combat, native reconstruction and independent audits. Native combat stage maximum **900 seconds** and also the remaining total budget. Maximum new output **4 GiB**, split preparation below 1 GiB and study below 3 GiB. Zero retries, resumes, replays or additional fights. Build/test/edit/report time is measured separately; waiting for the user is excluded. Once-only stage receipts preserve failures. Existing owned storage accounting, compact archives, cancellation and durable per-attempt charging remain enabled. Verification enters no combat.

## Preparation and result rules

Freeze this protocol, adapter, workflow, captured dependencies/content, source definition, complete reservation registry and synthetic fixture inputs before diagnostic execution. Reuse the verified composition-only source snapshot; compile the pilot adapter against exactly the same gameplay assemblies. Run focused tests through `build/run-tests.ps1`. The real-input check must validate the exact 512-fight definition using explicitly marked historical fixture labels, inspect the entire current reservation registry, materialize both fixed-order controls, and verify their common non-Essence context against generated scenarios. Fixture labels cannot be bound or launched. No candidate search-quality conclusion comes from synthetic scores.

Before binding, require completed independent preparation verification and a recorded authorization naming the protocol hash, 85 fresh values, 512 fights and unchanged time/storage limits. Verify all input pins again. The executable pipeline runs discovery, freezes the top four, screens them, freezes two finalists and both controls, then confirms the deduplicated family. Never use confirmation outcomes for additional search or reranking of the prespecified primary.

Report all confirmation rates with Wilson intervals using eight family seats (.025 total alpha for at most four rates). Report four prespecified paired finalist-minus-control contrasts using Wilson bounds for gained/lost wins with 16 family seats (.025 total alpha across eight discordance intervals); identical merged recipes have a structural zero difference. Keep the full allocation when recipes merge. An interval spanning zero is unresolved. These are approximate simultaneous intervals; the pilot cannot establish adoption or balance acceptance.

Persist phase wall/CPU/allocation/memory measurements, detailed TowerPerformanceTrace timings, fights started/completed, exact recipes, seed ownership, native archive reconstruction and an independent audit of selection/statistics/accounting. On any failure or limit, preserve all evidence, stop dependent stages, and state what remains unverified. No deployment, application configuration change, migration, Kharad tuning or change to old experiment caps.
