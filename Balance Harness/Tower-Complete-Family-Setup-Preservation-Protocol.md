# Complete-family setup preservation closure — read-only protocol

Frozen **15 September 2026**, following the user's instruction to proceed after the [setup review](Tower-Complete-Family-Setup-Review.md). Target: offline Balance Harness evidence and active handoffs. Close the incomplete final preservation check in a **new** package, `TestResults/balance/tower-complete-family-setup-preservation-20260915`. Preserve the old stopped scope, failed receipts and seal unchanged. No old command is resumed or rerun in its output directory.

This scope permits **one read-only preservation audit**, with **300 seconds** maximum, and final documentation/packaging within **120 seconds**. Charge the measured freeze plus a **60-second static-review allowance** and the full **120-second seal allowance**. Aggregate active diagnostic limit remains **1,800 seconds**, and this scope narrows new retained output to **128 MiB**, below the original 4-GiB bound. **Zero tests/builds, runtime preparations, fights, fresh seed candidates, reservations, retries or resumes.** A failed audit retains its evidence and ends dependent work; there is no automatic extension.

## Frozen inputs and comparisons

1. Pin the setup package's seal to `15850337e1bc5e5428d16e0f447e4562636f32fe10405475fc5e707c7709d741`. Verify all **1,406** indexed files, including its native/independent receipts, 71-test TRX/allowlist, producing freeze, failed preservation record and final documents. Verify the seven earlier package seals named by its original `closeout-scope.json` and all **8,613** indexed files in those packages: controller, source verification, parity v1–v4 and UTC binding. Compare exact relative inventory membership as well as hashes; each manifest itself is additional to its indexed files.
2. Reconcile the original **4,280-file checkout baseline** with the setup scope's **12 final changed-file hashes**. Protected source, tests, historical Markdown and other unrelated non-UI files must match the applicable recorded hash. The three implementation/test files must also match their frozen engineering hashes. Record new checkout files separately; only this scope's two new Markdown documents, the setup scope's declared new files and the concurrent UI paths below may be additional.
3. Treat `LL/src/Presentation/ll/`, `docs/ui-rework-verification/` and the exact file `UI_REWORK_IMPLEMENTATION_PLAN.md` as concurrent work outside this Tower task. Record changes/additions/deletions and available hashes without overwriting them or requiring their bytes to stop changing. Preserve a current snapshot of the previously flagged UI README, its original baseline hash and the hash recorded at the prior stop. **This exception applies only to checkout comparison. It never exempts a file from a frozen Tower source/input/history/archive check.** The UI's correctness is not assessed here.
4. Verify the setup protocol's **205 external input bindings** and **184 producing files**. Check its saved native/independent success receipts, exact 71-name passing TRX and zero-preparation/fight/new-value assertions without invoking their producing commands. Preserve the original test-host failures and all earlier diagnostic failures.
5. Discover registered history once using the existing three filenames (`seed-ledger.json`, `prior-seed-ledger.json`, `history-input.json`) under `TestResults/balance`, excluding only this new package. Reject linked history directories/files, enforce a two-million-directory traversal limit and require exact membership of the **145 frozen paths**. Hash every input, deduplicate the **102 distinct hashes**, reconstruct the sorted Int32-array union and match the authoritative midpoint ledger plus native union hash. Require **481,603 reservations** and explicit inclusion of all **512 unused original v19 confirmation values**. No allocator is invoked and no new registered ledger is written.

## Recorded result and finalization

Before execution, freeze the exact scripts, this protocol, reference seals, checkout expectations, expanded audit command and limits in `scope.json`. The audit writes a durable execute-once start marker, per-phase timings, complete checked-file records, UI observations, history summary and a final outcome. Bound all paths to their declared root and check the deadline during traversal and file hashing. Retain partial records if any check fails.

After a pass, update the seven active handoffs and add a new measured review. Preserve every historical review/protocol, including the setup scope's **SetupVerifiedPreservationIncomplete** result. Finalization checks changed Markdown links/whitespace, captures the final files, accounts for the whole new package and whole changed checkout documents plus **2 MiB metadata allowance**, then writes `evidence-files.json` last. No scientific verification, test suite or old package audit is repeated during sealing.

Closure establishes preservation of the completed setup work; it does not allocate the future 288 values or authorize a full study. The future durable reservation wrapper, final executable/protocol/setup-charge binding and separately scoped execution remain necessary. Full-family selection capacity, combat across the 256-cell batch boundary and full-study throughput remain unmeasured. All prior caps remain unchanged. Original v19 is sealed Unresolved with no confirmation in that experiment; later reliability **Fail 1/3**, adoption **Hold**.
