# Refinement novelty — diagnostic failure preserved

16 September 2026. The diagnostic probe failed to compile (CS0122): its assembly name `Probe` cannot access the internal `TowerPerformanceTrace` combat guard. This was a diagnostic setup error. No search reconstruction, backend tests, combat or candidate implementation ran. Dependent phases were skipped; zero retries.

Correct the probe assembly identity to the already authorized friend assembly `EssenceSystem.Tests` and its launch filename in a new frozen diagnostic scope. Then locate the v2 duplicate before implementing a bounded construction fix. The v2 result remains 15/16 synthetic teams; no new policy or strength result is established. Do not launch another combat comparison or allocate seeds.

The intended fix was not implemented. The previous role-preserving v2 implementation and its 20 passing tests remain prior evidence, not new test results. The exact unexecuted correction is recorded in [correction-not-executed.json](../TestResults/balance/tower-refinement-novelty-20260916/correction-not-executed.json). Neither the failed probe nor its frozen inputs were edited after execution.

The [frozen protocol](Tower-Refinement-Novelty-Protocol.md) required first-failure preservation and zero retries. The attempted command was:

```powershell
& 'C:/Users/HrHoe/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe' -B 'TestResults/balance/tower-refinement-novelty-20260916/diagnose.py'
```

Do not rerun that sealed command. Its exact build command and exit accounting are in [command-0.json](../TestResults/balance/tower-refinement-novelty-20260916/probe/command-0.json) and [exit-0.json](../TestResults/balance/tower-refinement-novelty-20260916/probe/exit-0.json); the [compiler log](../TestResults/balance/tower-refinement-novelty-20260916/probe/command-0.log) records CS0122. The process job was empty after exit, and no timeout or output cap caused the stop. Diagnostic execution consumed **1.453 seconds** before publication. The [completion receipt](../TestResults/balance/tower-refinement-novelty-20260916/completion.json) includes publication and exact remaining cumulative resources.

The candidate harness build, test build, all 28 planned backend tests through `build/run-tests.ps1`, four captured verification searches and dependent candidate audit were **not run**. The publication check verified the previous sealed role-validity package, frozen probe inputs and 158 current harness source files against their prior hashes. No harness/gameplay source was changed in this scope; unrelated checkout files were not edited. A new complete unrelated-file baseline was not captured before this early failure.

Changed files: this protocol/review, six active Markdown handoffs, and the isolated diagnostic evidence package. No gameplay, migration, configuration, deployment, cap or ability-order changes. All **482,866 reservations** remain, including the failed comparison's **40 unused values** and v19's separate **512 unused values** and **253 recipes**. V19 reliability Unresolved, later reliability Fail 1/3, deep recovery 0/3 and adoption Hold remain unchanged.
