# Three-reference positive-tie comparison admission

23 September 2026. The single captured-runtime admission completed as **`ThreeReferenceTieComparisonAdmittedNoReservation`**. Both selector definitions passed native validation; all **six reference inputs** were prepared, with **zero fights and zero fresh values**. The subsequent [single scientific execution](Tower-Practical-Three-Reference-Tie-Comparison-Execution.md) is now verified and closed as **`NoSelectorDifferences`**. This admission report records the preceding zero-fight preparation.

## Captured runtime and checks

The [admission package](../TestResults/three-reference-tie-admission-20260923/admission.json) retains the original gameplay assemblies, nested dependencies, content, effective settings and three exact reference recipes from the reconciled three-reference capture. Only the four tested harness files were replaced: DLL, portable symbols, dependency manifest and runtime configuration. The retained gameplay package was not rebuilt from the dirty working tree.

Portable-symbol checks bind **203 producing source documents** to the tested DLL. **791 methods** resolved against the retained dependencies, including comparison execution, reservation, both selectors and archive reconstruction. The [compatibility receipt](../TestResults/three-reference-tie-admission-20260923/context.json) also reproduces both output choices from the same pinned five-nominee engineering fixture under a combat guard. These literal measurements exercise the changed tie rule; they are not evidence of a real strength gain.

One public `tower-incumbent-tie-comparison-check` validates both definitions and prepares three references for each. It returned **`ReadyNoReservation`**, confirming 24 shared searches, 12,672 search fights and at most 60,672 total fights for the future comparison. Python scans before and after that native check agreed with its history validation: **600,552 permanently excluded values across 238 files**. No allocation, entropy batch or scientific output directory was created.

The [new admission helper](analysis/prepare-three-reference-tie-admission.py) fixes this protocol's paths, source pins, prior charges and captured-input contract. [Ten helper tests](analysis/test-three-reference-tie-admission.py) passed, covering template/history drift, missing or changed harness files, nested dependency preservation, implementation/accounting pins, process ownership, resource forecasting and refusal to resume an existing package. The shared [runtime context helper](analysis/reference-exploration-context.ps1) adds an explicit `ThreeReferenceTie` mode; older modes retain their behavior. Its PowerShell syntax check passed. No C# changed in this admission step, so the previous 241 backend and 18 Python implementation tests were not repeated.

## Sealed inputs and resource allowance

| Binding | SHA-256 |
| --- | --- |
| Admission manifest | `90b9a5a9815454ba1169d64f61cc8b7a950597cfa56bc1c9380c53155538f1c5` |
| Concrete request | `24dd41a1ec3a3b42125e81210b3f27ffc277d996c9ade2dcd3e5efb6528ce6a2` |
| Unscheduled template | `deef08eaa90b74e5857dc48827f38f7d371215a8a5d92fd599316c91f6c5d594` |
| Captured execution identity | `8b6fdc194e72566c0d56686ebe5aa24f4fd2bf11560e10043354af1d99881d8b` |
| Effective settings | `f9587e8941a021443aecef37dccf0d5dad250a28df32673cacf7f2df50b12b74` |
| Frozen plan | `2ae905286313000e04170f45acd849c552251954487c99186d4cea1b96fcdf24` |

Admission took **182.437 seconds**, retaining **53,229,937 bytes**, within its fixed **600-second /512-MiB** allowance. The full allowance remains charged, leaving **10,200 seconds /5.5 GiB** for the single execution, both audits and publication within the 10,800-second /6-GiB cumulative envelope. Context, native validation and enclosing worker receipts prove their owned process trees finished empty. The outer allowance included authentication, copying, checking and sealing; it was not restarted for individual steps.

The latest completed screening execution supplies a rough linear cost estimate at the new maximum fight count: **3821.442 seconds /1,381,761,383 bytes**. That estimate includes historical audit and publication cost, and fits the remaining allowance. It is not a guaranteed upper bound; the owned launcher still enforces absolute limits with no extension or retry.

The [external read-only verification](../TestResults/three-reference-tie-admission-review-20260923/verification.json) authenticated the sealed package, checked all content/runtime/source pins and admission receipts, rejected an incorrect manifest pin, and rescanned live history. It added zero native preparations, fights or values and took **48.578 seconds**. This follow-up, helper development/tests and documentation are separately disclosed engineering work. Full historical engineering totals remain unknown; preserve the **18,180-second /13,584-MiB** prior-charge ledger without treating it as a total.

## Next step and reproducibility

The [concrete request](../TestResults/three-reference-tie-admission-20260923/request.json) binds `tower-three-reference-tie-comparison-v1`, both captures, the plan, auditor, history and original 600-second /512-MiB admission charge. The [closed execution](Tower-Practical-Three-Reference-Tie-Comparison-Execution.md) used its one comparison through the [retained owned launcher](../TestResults/three-reference-tie-admission-20260923/run-three-reference-tie-comparison.py), using the retained `runtime/BalanceHarness.dll`. It runs both selectors on the same 24 direct searches, freezes all 48 choices, then confirms only differing recipes. Identical outputs retain null absolute wins. No old allocation can be reused or resumed, and neither defaults nor team recommendations change automatically.

Completed verification commands:

```powershell
python -B -X utf8 'Balance Harness/analysis/test-three-reference-tie-admission.py'
python -B -X utf8 'TestResults/three-reference-tie-admission-20260923/prepare.py' verify --manifest-sha256 90b9a5a9815454ba1169d64f61cc8b7a950597cfa56bc1c9380c53155538f1c5 --live
```

Python used the bundled runtime. The second command is read-only and does not repeat reference preparation. The one-shot `prepare` and scientific execution are closed and must not be rerun. The historical admission verifier requires the scientific output to be absent; use the execution closeout receipt for the completed run. Logs, helper snapshots and publication checks are retained in the [engineering review](../TestResults/three-reference-tie-admission-review-20260923/completion.json) and its [manifest](../TestResults/three-reference-tie-admission-review-20260923/files.json). The initial helper test used exact floating-point equality for the forecast; it was corrected to a nine-decimal tolerance before admission. No required command remains blocked.

Changed repository files are the new admission helper and tests, the context helper, this report and seven documentation pointers. No gameplay code, application configuration, database migrations, deployment or infrastructure changes occurred. The closed screening decision remains `DoNotPromoteFreshScreening`, and the captured cohort's separate balance failure remains unresolved.
