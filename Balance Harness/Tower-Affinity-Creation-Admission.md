# Affinity creation: runtime and resource admission

**Subsequent execution and diagnosis — 23 September 2026:** The [single creation pilot](Tower-Affinity-Creation-Pilot-01-Execution.md) completed 19,840 fights and all verification, reaching `Inconclusive`. Its [saved-stage review](Tower-Affinity-Creation-Stage-Review.md) is also complete with no new fights or allocations. This admission is consumed. The original admission record below is historical; its output must not be relaunched or reused.

23 September 2026. Target: the offline `LL/tools/BalanceHarness`.

**The creation comparison is admitted for one future execution.** The [sealed receipt](../TestResults/affinity-creation-admission-20260923/admission.json) is `ProposalStudyAdmittedNoReservation`. Admission completed in **184.297 seconds**, retaining **121,932,833 bytes**. It performed zero combat, production entropy draws, new-value reservations or scientific launches. It provides no evidence that the creation policy produces stronger teams.

The [declaration](Tower-Affinity-Creation-Admission-Declaration.json) fixes a separate creation admission, output, tested build, implementation hashes, source manifests, forecast formula and full **900-second /1-GiB charge**, with no retry or timing resample. The preservation admission scripts and historical scientific archives remain unchanged.

## Runtime and history

The final harness from the [owned-controller implementation](Tower-Affinity-Creation-Study-Implementation.md) is now qualified against the captured gameplay runtime. Only its four tested harness files replace the captured runtime files. All other dependencies and captured content retain their original hashes.

- All 48 exported recipe/reference instances across four arms were materialized and prepared using the same two previously declared values: **96 preparations per runtime, 192 total**. Every input and prepared-participant identity matched between the captured and candidate runtimes.
- The candidate reproduced all four saved export arms and the full typed inventory. Producing symbols authenticated **223 source documents**; **777 method bodies** resolved, including creation and generation entry points.
- The production runtime-retention method preserved all **27 runtime files**, including portable symbols, within its existing 64-MiB limit.
- Two independent complete history scans agreed on **688,598 permanently excluded values across 248 files**. The native `tower-proposal-study-check` independently reconstructed that history and returned `InputsVerifiedNoReservation` for the creation design. Existing abandoned-reservation recovery pins were retained.
- The externally pinned independent audit of the saved creation fixture passed again. Its literal reports are engineering evidence, not newly simulated combat or scientific results.

The [qualified plan](../TestResults/affinity-creation-admission-20260923/plan.json) changes only the scope hash from the [earlier prospective plan](Tower-Affinity-Creation-Comparison-Plan.json). Context changes are limited to the qualified execution, new scope ID and refreshed exclusions. The original plan remains byte-for-byte unchanged. Candidate/control policies, roots, panels, decision thresholds, all-output freeze and scientific limits remain fixed.

## Resource decision

The [forecast](../TestResults/affinity-creation-admission-20260923/resource-forecast.json) uses the maximum of the prior admitted estimate, the frozen native audit-cost probe, current preparation and creation-fixture costs, and twice the completed preservation-study costs scaled from 17,536 to 21,888 fights. Creation/preservation fixture ratios can only increase costs. All storage floors are applied before scaling the audit hashing estimate. The existing factor-of-two margin and 120-second publication reserve remain.

| Partition | Forecast seconds | Hard seconds | Forecast bytes | Hard bytes |
| --- | ---: | ---: | ---: | ---: |
| Native work | 4,239.253 | 9,000 | 4,860,555,295.20 | 5,905,580,032 |
| Audit/publication | 1,692.865 | 1,800 | 34,989,702 | 536,870,912 |
| Total | 5,932.118 | 10,800 | 4,895,544,997.20 | 6,442,450,944 |

Both partitions fit. **Audit time has about 107 seconds of forecast headroom.** These are historical and sampled estimates, not guaranteed upper bounds. Exhausting either partition invalidates an incomplete run; neither partition can borrow from the other. No limit was increased and no timing was repeated to obtain admission.

The full admission allowance is charged despite the smaller observed use. Recorded cumulative charges are now **21,615.172 seconds /17,012,918,789 bytes**, including prior failures and reviews. Cumulative previously declared maxima plus this admission are **30,960 seconds /21,541,945,344 bytes**. The separate, unconsumed scientific ceiling is **10,800 seconds /6,442,450,944 bytes**; adding it to recorded charges gives **32,415.172 seconds /23,455,369,733 bytes**. Development builds and synthetic engineering tests remain separately identified.

## Verification and changed files

New implementation files are [prepare-affinity-creation-admission.py](analysis/prepare-affinity-creation-admission.py) and [test-affinity-creation-admission.py](analysis/test-affinity-creation-admission.py). The preparation script authenticates its inputs, holds registry/output leases, uses bounded owned Windows processes, rejects runtime or scientific-plan drift, refreshes history and seals the admission without allocating values. It retains failure evidence and the full charge if an attempt fails.

**37 Python tests passed:** 19 new creation admission cases, 12 shared admission cases and six resource cases. They cover evidence tampering, changed scientific limits or retry policy, complete arm coverage, runtime/input drift, increasing storage costs, partition exhaustion and unchanged historical declarations. The exact build's existing **140 passing backend tests** and producing sources were authenticated; no C# changed and no backend rebuild was needed.

```powershell
python -B -X utf8 'Balance Harness/analysis/test-affinity-creation-admission.py'
python -B -X utf8 'Balance Harness/analysis/test-proposal-affinity-admission.py'
python -B -X utf8 'Balance Harness/analysis/test-proposal-affinity-resource-admission.py'
```

The [one admission command's log](../TestResults/affinity-creation-admission-run-20260923.log), all six successful owned-process receipts, full tests and producing-source copies are retained. Each child process exited with an empty owned job. The [post-seal verifier](../TestResults/affinity-creation-admission-verification-20260923/verify.py) and [verification result](../TestResults/affinity-creation-admission-verification-20260923/verification.json) recheck the external pin, all package bytes, consumed historical evidence, qualified plan/context, source identity, history-file pins, arithmetic and complete charge ledger without another timing sample or combat run.

```powershell
python -B -X utf8 'TestResults/affinity-creation-admission-verification-20260923/verify.py' --pin 554071201e9c01f99dbe084e5561df22fa7c97b785d823802d39a808898fa884
```

Other changed files are the declaration and this report, plus current-status links in the harness README, practical-search guide, tool-gap review, independent assessment, preservation-admission report and creation study-implementation report. There are no migrations, application configuration changes, deployments or gameplay-default changes. No required verification command was blocked. The scientific launcher has deliberately not been run in this admission step.

## Next execution

The next step is the single frozen creation-versus-single-edit development comparison: **12 paired roots, 3,948 fresh values and at most 21,888 fights**. The launcher must use the admitted request and captured runtime directly, with the external admission pin below. It rechecks history and structural feasibility before allocation; all 24 searches must freeze before held-out observation. Both audits and final publication verification must pass. No replacement, refill, retry or immediate policy adoption is permitted by this plan.

| Artifact | SHA-256 |
| --- | --- |
| Admission `files.json` | `554071201e9c01f99dbe084e5561df22fa7c97b785d823802d39a808898fa884` |
| Declaration | `0c9e345ace7c68d9f35486e6eae68ceff7af00d64b4d4a54b82d02b59a56af85` |
| Admitted request | `c4966e4d3e820cb076ad1cb6f064bb6e5a3c37c7f117b2da7a34e980e9eaa134` |
| Qualified plan file | `e59094d0886153a13527360ed2f48e1df6a912b805e0ad666d94e769ce5362fe` |
| Final harness DLL | `bf2e5c1f283ea566184099fe1686ed8e9de1774f6ea0afdd5dd8c79a65a77cf4` |

The canonical qualified plan hash is `fe511dedfef4ccce2975f3ae0b0078fe685b96d87c1cb02075e10baa9776f034`; the qualified execution hash is `0914db13a42eb02793d00cc5c237490151a3b2125f0c1525b111a1643fde51f5`. The admitted [request](../TestResults/affinity-creation-admission-20260923/request.json) targets the still-absent `TestResults/balance/tower-affinity-creation-pilot-01-20260923` directory.
