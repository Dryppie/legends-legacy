# Joint role construction: corrected verification scope

15 September 2026. Target: offline BalanceHarness. The user's “Please proceed” authorizes corrected preparation and the still-unexecuted verification described in the [failed-scope review](Tower-Joint-Loadout-Construction-Review.md). The failed package remains sealed, with no retry or modification.

Use `TestResults/balance/tower-joint-loadout-construction-v2-20260915`. Apply the saved explicit-UTF-8 correction to the copied workflow, carry the failed package's completion counters, and add it to preservation. Run the freeze with `python -X utf8=0` so success exercises explicit encoding under the legacy Windows default. All Python text reads in the new workflow use an explicit encoding. The constructor, 11 backend tests, native metadata adapter and independent audit remain byte-identical to the failed package. Freeze those source hashes before any build or diagnostic.

The [original construction protocol](Tower-Joint-Loadout-Construction-Protocol.md) supplies the unchanged structural semantics: joint coverage around an authored core, most-constrained missing category first, canonical Essence IDs, family/remaining-copy limits, evidence witnesses and explicit incomplete-search status. No combat scoring, reference recipes, RNG labels, ability-order search or party allocation. Structural category coverage is not a guarantee of targeting, uptime or strength; spare-slot filler is outside the API.

Execute once in sequence: setup/freeze; one isolated adapter build; one isolated test build; **11 backend tests through `build/run-tests.ps1`**, including the **448-case exhaustive synthetic fixture**; the **48-core captured metadata diagnostic** using the unchanged **80 Essences / five required authored roles / five slots / 256 states / 16 recipes per core**; independent verification of every recipe/witness and brute-force feasibility for exhausted cores; publication and sealing. No previous construction, test or battle run is repeated. Builds use captured DLLs and cached restore metadata, with no restore or dirty gameplay rebuild; each is measured engineering work capped at 300 seconds.

Carry **1,758.194 diagnostic seconds**, including the failed freeze and its closure, and the failed package's actual final bytes. The unchanged cumulative cap remains **1,800 seconds / 4 GiB**. This new package is capped at **40 diagnostic seconds and 192 MiB**, with each diagnostic phase at most 15 seconds and ten seconds reserved for publication. Stop all dependent phases on failure or a limit; preserve partial evidence with zero retries. Do not increase, reset or borrow another experiment's allowance.

Verify all **45 predecessor packages** and existing harness source before/after; preserve unrelated dirty work. Update a separate v2 review and six active Markdown handoffs. Report executed commands, measured costs and anything unverified. No policy integration or promotion, gameplay/content tuning, configuration, migrations or deployment.

Zero new fights, battle preparations, seeds or replays. Preserve all **482,641 reservations**, v19's 253 recipes and unused 512 confirmation values. Reliability Fail 1/3, deep recovery 0/3, v19 Unresolved and adoption Hold remain unchanged. No Kharad tuning, ability-order optimization or 129,536-fight confirmation.
