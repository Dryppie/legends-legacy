# Fresh-first V5 comparison readiness

16 September 2026. **ReadyAwaitingSpecificApproval**. The V5-versus-baseline request passed native preflight and independent readiness checks. The exact tested harness and all 483,001 reservations are pinned; zero new values or fights.

The [protocol](Tower-Fresh-First-Comparison-Protocol.md) freezes baseline team coverage versus V5 twelve-fresh/four-local allocation under comparison-v4. Both arms share captured gameplay, content, settings, equipment and schedules. Fixed ability order and search/selection caps stay unchanged. Discovery: 16 teams per arm × four fights. Selection: up to four teams × eight fights. Confirmation: two finalists plus two controls × 32 fights. Maximum 288 charged attempts; a finalist can play 44 fights. Incomplete discovery stops both arms before nomination, without refill. The adjusted paired confirmation win-rate difference is primary; boss health is descriptive.

The exact tested V5 harness is referenced in place during readiness. A small host-only build adds a pinned dependency resolver; its production API call body is unchanged. Native inspection loaded the tested DLL and validated the request without deriving labels, creating a study or scanning the entire history tree. The 85 passing backend facts from `build/run-tests.ps1` and the completed implementation audit were reused without reruns. The independent reader checked 128 saved confirmation records and four rates; no combat replay.

The study's counted component minimum is **73.04 MiB**, leaving **10.96 MiB** beneath its 84-MiB cap. The producing runtime is **17.54 MiB**, beneath its separate 24-MiB cap. The minimum includes fourteen historical arrays, one archived runtime and four content copies; reports and transient writes are additional. Completion is not guaranteed. The complete prospective 400-second/112-MiB execution reservation fits after the requested extensions.

Readiness uses at most 45 seconds/512 KiB within existing authority. The proposed study uses 360 seconds/84 MiB, including 150 binding seconds charged in full. Complete execution uses 400 seconds, with a separate producing runtime capped at 24 MiB and other execution evidence capped at 4 MiB. The complete runtime is copied only after approval. All output, including partial failure output, is charged. No extra full registry scan is scheduled; production binding/launch retain their complete scans and fail if history changes.

Await approval for exactly 45 fresh values, at most 288 attempts, zero retries, and cumulative extensions of 120 seconds plus 112 MiB. After approval execute this sealed request once, capped at 400 seconds. Current cumulative ceilings remain **4,260 seconds / 4,613,734,400 bytes**. Only approval raises them to **4,380 seconds / 4,731,174,912 bytes**. Earlier approvals are exhausted; the authorization template is unsigned and no execution authorization exists. Master **2026091606** is frozen, but no value has been derived.

[Exact request](../TestResults/balance/tower-fresh-first-comparison-readiness-20260916/request.json), [native inspection](../TestResults/balance/tower-fresh-first-comparison-readiness-20260916/inspection.json), [readiness audit](../TestResults/balance/tower-fresh-first-comparison-readiness-20260916/readiness-audit.json), [completion/resource receipt](../TestResults/balance/tower-fresh-first-comparison-readiness-20260916/completion.json). Run the frozen `workflow.py` phases `freeze`, `build`, `inspect`, `audit`, `publish` once; exact commands and native timings are retained. Only after the bound approval and authorization receipts exist, invoke `python -B TestResults/balance/tower-fresh-first-comparison-readiness-20260916/execution.py`. The execution reader also checks each V5 local edit, its best completed parent and the unchanged twelve-fresh recipe prefix.

Changed files: this protocol/review, isolated host/request/reader/execution scripts and six active Markdown handoffs. No harness/search/gameplay source, configuration, migration or deployment changes. No fresh seed, combat, replay, preparation service, backend test or harness build ran. A host-only compilation is charged. Historical seals and unrelated checkout changes are preserved.

All 483,001 reservations remain excluded, including the failed V3 allocation's 45 unused values, earlier failure's 40 and V19's separate 512 unused values and 253 recipes. The successful previous comparison accounts for the latest 45-value increment. V19 reliability Unresolved; later Fail 1/3, deep recovery 0/3; adoption Hold. V5 strength remains untested.
