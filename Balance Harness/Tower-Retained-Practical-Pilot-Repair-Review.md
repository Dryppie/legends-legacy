# Practical pilot: input-budget repair verified; history integration stopped

**The approved budget repair works. Both builds passed, and 27 of 28 zero-combat tests passed. The pilot remains stopped at history readiness: no fresh values were allocated and no combat ran.** The one remaining failure is in the isolated launch wrapper: it selected a history reader that rejects all Pending records, despite an existing, verified recovery receipt for the abandoned V3 reservation.

The change in `TowerBossDiscoveryContract.cs` extracts the existing generator-input copy. `TowerBossImprovement.cs` now validates the actual supplied-search definition and copies those inputs directly, instead of temporarily substituting two independent methods. The exact pilot stays at **1,408 fights**, including 512 discovery fights; no fictitious second discovery method is counted. Generation, mutation, retention, ordering, selection and confirmation rules remain unchanged. The new permanent `BalanceHarnessImprovementInputProjectionTests.cs` covers exact budgets for the standalone and older retained policies, insufficient-budget rejection, the independent-mode boundary and isolation from mutable input arrays.

The passing suite includes both regression cases, all 13 retained/paired parity cases, corrected journal-reader and authorization checks, the seven-component decision and scientific-exit cases, and the complete/cancelled synthetic study paths. The full synthetic study verifies 64 evaluated parties, four nominations, one finalist, both confirmation anchors, exact-duplicate merging, and the fixed discovery/selection counts. The saved paired-v1 trajectory also matches the prior artifact exactly. These are workflow and compatibility results, not combat-strength evidence.

Native preparation admitted the same two anchors from the complete rebuilt runtime, and executable retention passed. The [identity comparison](../TestResults/balance/tower-retained-practical-pilot-repair-20260916/identity-transition.json) confirms that **only `BalanceHarness.dll` changed**. Runtime, platform and all four gameplay assembly hashes match the prior admission. Content/settings, legal-team assumptions and reference recipes remain fixed. The exact 1,408-fight definition now passes native validation.

The failed test reached `TowerCompleteFamilySetup.History`, which correctly rejects unresolved Pending state. The actual source is `tower-refinement-v3-comparison-study-20260916/history-input.json`. Its external receipt already records `AbandonedPermanentlyReserved`; the existing `TowerRefinementComparisonLaunch.Refresh` reader explicitly supports this pinned recovery. The prior fresh-first study used that mapping successfully. The pilot wrapper omitted it. This is an integration error in the wrapper, not a reason to rewrite the old Pending file or release its values.

The [independent diagnosis](../TestResults/balance/tower-retained-practical-pilot-repair-20260916/history-integration-diagnosis.json) verifies the old receipt, complete ten-file failed-study inventory, all 45 recorded Start/Candidate pairs, deterministic values for those recorded rows only, complete schedules and permanent no-retry marker. All **45 values already belong to the unchanged 483,046-value authoritative exclusion ledger**. No unmatched allocation start was found. Full live registry reconciliation has still not passed in this pilot; the receipt audit does not substitute for that gate.

The failed readiness package is preserved with an explicit `Failed` marker, and cannot bind or launch. It contains proposed source files for a narrow continuation: use the existing recovery-aware Refresh/Recheck functions from both readiness and binding, with the exact prior mapping and all recovery evidence pinned. No production source or harness rebuild is needed for that continuation. The original Pending artifact remains unchanged and still cannot authorize a retry or resume of its abandoned study.

**Proposed continuation:** the [concrete integration proposal](../TestResults/balance/tower-retained-practical-pilot-repair-20260916/history-integration-proposal.json) allows **45 diagnostic seconds /8 MiB** to compile only the small launcher/history test against the retained verified runtime and run one complete history integration check through `build/run-tests.ps1`. Reuse the 27 passing checks; do not copy/rebuild the gameplay runtime or rerun unrelated tests. It requests **15 seconds from unused run allowance, with no byte transfer**: engineering would become 865 seconds /384 MiB and run 2,435 seconds /608 MiB. Audit and overall ceilings remain unchanged. Only after every gate passes may the original one-shot 297-value /1,408-fight pilot proceed within its unchanged execution and verification caps. This continuation has not been run and is not automatically authorized by this receipt.

The approved transfer for this repair was **75 seconds /48 MiB**: engineering is now capped at 850 seconds /384 MiB and run at 2,450 seconds /608 MiB. The [completion receipt](../TestResults/balance/tower-retained-practical-pilot-repair-20260916/completion.json) charges this build, failed readiness, diagnosis and publication. Earlier failures and the planning overrun remain charged. Overall limits stay at 7,980 seconds /5,804,916,736 bytes. No run allowance has been spent on combat in either pilot-readiness package.

Verification ran isolated offline restore/Release builds and `build/run-tests.ps1 -NoBuild -ArtifactsPath TestResults/balance/tower-retained-practical-pilot-repair-20260916/tests` with the four-class filter saved in `control/readiness-tests-command.json`. Result: **27 passed /1 failed /0 skipped**, with all owned processes confirmed exited. Preservation checks cover the prior sealed failure, pinned dependencies/content, unrelated dirty files, exact source correction and scoped `git diff --check`. Fresh binding, combat execution and saved-study reconstruction were not run because readiness failed. The proposed history integration has not been compiled or tested.

Changed files are the two harness source files, one permanent regression-test file, this review, the README and five handoff notices, plus isolated evidence/launcher files. There are no gameplay changes, migrations, persistent configuration changes or deployment implications. The failed block-search comparison remains Closed, adoption Hold and V19 reliability Unresolved.
