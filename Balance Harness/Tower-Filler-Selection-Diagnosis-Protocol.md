# Filler diversity: saved team-selection diagnosis

16 September 2026. Offline BalanceHarness evidence analysis only. Freeze this document, the readers and exact input hashes before diagnostics. No harness/gameplay edits, generation, constructor/allocator calls, runtime preparation, combat, fresh seeds or retries.

## Question and evidence

Locate where control-associated combinations are missing: recipe construction, deterministic team allocation, discovery nomination or screening. The completed comparison confirmed both finalists and both controls at 0/32; filler diversity left 92.84% mean boss health versus 62.18% for team coverage and 32.02–33.74% for controls. These are descriptive observations. Structural overlap does not identify causal contributions or establish that a control recipe is necessary or optimal.

Read these sealed inputs only for diagnosis:

- `tower-filler-diversity-20260915`: input, mechanics, controls, pool, team/filler proposal fixtures, measurements, and captured allocator/structural/team/filler source.
- `tower-team-coverage-20260915/pool.json`: preceding 560-recipe pool.
- `tower-team-coverage-diagnosis-20260915/analysis.json`: fixed catalogue of 191 control subsets, independently rederived from the controls.
- `tower-filler-diversity-comparison-study-20260915`: both discovery reports, nominations, shortlist, finalists, family, screen and confirmation evidence, quality, completed seed ledger and final manifest.
- `tower-filler-diversity-comparison-execution-20260915`: saved descriptive metrics, independent audit, completion and manifests; preceding preparation preservation helper and manifest.

Freeze absolute paths/SHA-256 hashes in `TestResults/balance/tower-filler-selection-diagnosis-20260916/freeze.json`. Preservation checks may enumerate/hash all 62 predecessor packages, without rerunning their diagnostics or the global live registry. Capture current dirty checkout hashes; edit only this protocol/review and six active Markdown handoffs. All C#, sealed evidence and unrelated changes remain unchanged.

## Fixed calculations

1. Verify both saved 16-team outputs against the implementation's parties/traces. Reconstruct the original discovery ranks, two nominations per policy and screen winner using the exact existing win/rank/ID rule. Preserve all origins and full confirmation family; no reselection. Describe every candidate's recorded four-fight wins/health, rank, nomination/finalist status, distinct recipes and maximum repeated loadout. Also describe the two predefined allocation schedules (odd-numbered teams favor variety, even-numbered teams favor reuse); do not pool stages or claim causality.
2. Read and validate both saved pools: 560/768 full five-Essence recipes, 80 eligible providers, family/copy legality, five authored role categories and contained authored cores. Controls contain 20 character placements and ten distinct recipes. Recompute all 191 subsets of sizes one through five. Report exact/subset presence in each pool, all 160 generated placements per policy, nominees and finalists. Retain complete provider/role/core counts for pools and teams.
3. Explain the **320 already-saved placements**, ten slots for each of 32 teams, without producing alternatives. For each saved prefix, score every compatible saved pool recipe using the captured comparator: descending newly covered roles, ascending exposure in earlier completed teams, ascending current uses on variety teams/descending on reuse teams, descending previously unused Essence IDs, ordinal recipe hash. Exposure updates only after a completed saved team. Inputs use identical pools in all ten slots, no owned-copy restriction, minimum one distinct recipe and maximum ten uses. Require the saved choice to rank first at every step, and full role coverage in every saved team. Any mismatch is a failed explanation; stop rather than substituting another path.
4. Save each step's selected recipe, comparator values, top ten indices, and survivor counts after each comparator key. For every pool recipe retain exact minimum rank, summed ranks and selected-owner count across the 160 observed prefixes. Verify that 16 × 11 states and 160 × pool size checks match saved traces; this establishes the observed straight paths, not behavior on unexplored paths.
5. For **every one of the 191 subsets**, find the best-ranked compatible containing recipe at every observed prefix. Count no-pool, unavailable, chosen and losing decisions; classify each loss by the first differing comparator key, and retain minimum/maximum best rank and up to three ordinal decision witnesses. The report's examples are fixed as the most frequent control subset of sizes two, three and four (tie by ordinal IDs), plus exact full-recipe counts. No favorable subset selection after results.

All operations are finite: two pools, 32 candidates, 320 observed prefixes, **212,480 option scores**, 191 subsets, 80 providers and 48 authored cores. No full tree traversal, new team construction, search expansion, counterfactual fight, random-number call or optimizer proposal. Sorting saved options to explain the saved first choice is an archive calculation, not an allocator rerun. Do not infer how absent/unselected recipes would fight.

## Independent audit

A separate Python reader uses integer bitmasks for role/Essence containment and independently calculates comparator components, complete ranks, tie-group sizes, every subset's decision counts/witnesses and per-recipe aggregate ranks. The analysis uses sets and dictionaries. Audit all saved nominations/finalists against existing evidence, pool legality, subsets, provider/role/core counts and the completed reservation union. Compare the unchanged source comparator to the five declared keys. Retain previous native/independent combat verification and backend test receipts; do not rerun them. No new backend suite is needed for this read-only Python scope.

## Limits, closure and publication

Start at **2,543.598 cumulative diagnostic seconds**, with **156.402 seconds remaining** under the approved **2,700-second** ceiling. This scope permits **60 seconds total**, including preservation, analysis, audit and publication. Non-publication work stops by 45 seconds, reserving 15 seconds for closure. Phase maxima: freeze 20, analyze 20, audit 20 seconds, reduced by aggregate/cumulative remaining time. First failed check or reached limit stops dependent work; no retries, alternative fixture runs, resumes or additional combat. Editing scripts/Markdown is engineering work, not diagnostic execution.

**Zero fights, preparations, new seeds and retries.** Preserve all **482,776 reservations**, including the unused original 512 confirmation values. The latest 45-value approval is exhausted. Check the sealed completed ledger, not a new live registry.

New output ≤**16 MiB**, also under the unchanged cumulative **4 GiB** cap. Carry exact sealed preparation/execution/study sizes and the prior 1 MiB shared-test allowance forward, including their final manifests. Charge one publication closure second. Never delete/overwrite previous evidence.

Run once: `diagnose.py freeze`, `analyze`, `audit`, `publish`. Every phase uses create-new start/result/failure receipts and time limits; publication is the only dependent work allowed after failure. Seal the new package with `files.json`. Publish complete counts, score witnesses, source/input references, actual time/bytes, reproducible commands, limitations and a bounded next recommendation. Any implementation or further diagnostic needs a separate frozen scope; this task changes no policy and starts no experiment.

V19 remains Unresolved with all 253 required recipes and no confirmation. Current reliability Fail 1/3, deep recovery 0/3, adoption Hold. No Kharad tuning, ability-order optimization, gameplay/configuration change, migration, deployment, old-cap increase or 129,536-fight confirmation.
