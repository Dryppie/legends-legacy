# Bounded structural party allocation

15 September 2026. Target: offline BalanceHarness. The user authorized proceeding from the verified joint loadout constructor to bounded selection and placement across characters. Implement a standalone allocator; existing search policies and gameplay remain unchanged.

Input: a family map, candidate full loadouts for each numbered character slot, one shared available-copy inventory, explicit minimum distinct recipes and maximum uses of one recipe. Canonicalize Essence IDs and deduplicate identical recipes within each slot. Validate exactly the requested Essence-slot count and same-character family uniqueness. Choose the unfilled character slot with the fewest currently compatible recipes; visit less-used recipes and new Essence coverage first with stable hash ties. The traversal is deterministic and outcome-independent. Every branch charges copy usage across the complete roster, then restores it on backtracking. Return only complete slot assignments, recipe IDs, copy totals and distinct-recipe counts.

Cap the API at ten characters, five Essence slots, 1,024 input recipes per character, 4,096 states, 1,000,000 candidate compatibility checks and 64 retained parties. Separate candidate-check accounting bounds work even when a node scans large pools. A cap returns incomplete status; no capped negative result proves infeasibility. A fully exhausted search is complete only over the supplied pools and constraints. No ability-order variants, battle calls, RNG seeds or reference outcomes enter allocation. Candidate pool position is not a combat ranking.

Freeze `TestResults/balance/tower-joint-party-allocation-20260915` before execution. Two isolated engineering builds, each at most 300 seconds, use captured DLLs and cached restore metadata. Run **10 new backend tests once through `build/run-tests.ps1`**, including **243 exhaustive inventory/roster-size cases** over a three-recipe synthetic pool. Verify all complete assignments against brute force, constrained-slot placement, global copy shortages, diversity/repetition constraints, canonicalization/deduplication, state/check/party caps, empty pools, metadata, cancellation and input immutability. Reuse the existing 11 constructor tests and 448-case proof by unchanged source/binary/TRX hashes; do not regenerate its 768 saved recipes.

Captured diagnostic: derive the pool from the verified 768 recipe records (740 distinct). Use every distinct recipe with **exactly five** Essence IDs; preserve and report excluded shorter recipes instead of adding filler. Ten characters are two five-player parties. Run exactly three metadata scenarios against that fixed pool: **unrestricted-diverse** (unlimited copies, at least ten distinct recipes, at most one use each); **five-copies-diverse** (five copies of every allowed Essence, at least five distinct recipes, at most two uses each); **one-copy-diverse** (one copy per Essence, at least ten distinct recipes, at most one use each). Each call is capped at **256 states / 250,000 candidate checks / 16 retained parties**. Inventory scenarios are construction fixtures, not gameplay/content changes or new balance runs. Persist all placements, identities, copy/diversity counts, termination reasons, CPU/allocation/peak-memory and wall-clock metrics. No strength or targeting claim.

Independently verify the pool against the exact saved records; every returned placement, canonical recipe/party hash, role coverage, family/copy/diversity constraint and counter cap; and the integrity of all saved inputs. For an exhausted negative case, identify a shared-Essence bottleneck when every candidate requires more copies than the scenario supplies; otherwise report the native negative proof as not independently closed. Synthetic exhaustive tests independently verify general allocation semantics. Do not enlarge the captured pool or caps to obtain a result.

Carry **1,768.944 diagnostic seconds** and actual final bytes from the preceding package. Setup/freeze, tests, captured allocation, independent audit and publication share at most **25 additional diagnostic seconds**, within the unchanged **1,800 seconds / 4 GiB**. Each diagnostic phase is at most ten seconds, reserving eight seconds for publication; the new package cap is 192 MiB. Engineering builds are separately measured. Stop dependent work on any failure or limit, preserve evidence, zero retries. Explicit UTF-8 reads throughout.

Preserve all **46 predecessor packages**, existing source and unrelated dirty work before/after. Update the implementation review and six active handoffs. Zero fights, new seeds, battle preparations or replays. Preserve **482,641 reservations**, all 253 v19 recipes and unused 512 confirmation values. Historical reliability Fail 1/3, deep recovery 0/3, v19 Unresolved and adoption Hold remain unchanged. No policy promotion, Kharad/content tuning, ability-order search, 129,536-fight confirmation, cap increase, configuration, migration or deployment.
