# Frozen discovery parity closure diagnostic

Prepared 14 September 2026 after explicit authorization to close the remaining performance parity gate. Target: offline BalanceHarness. This is a new diagnostic; the previous failed diagnostic and sealed v19 packages remain untouched.

## Exact schedule and limits

One candidate execution, zero reference executions. Reuse the completed 34-fight reference at `TestResults/balance/tower-discovery-performance-20260914-implementation/measured/reference`. Its exact `definition.json`, content/settings and retained gameplay DLLs define the comparison; the new candidate changes only harness code. This is the previous diagnostic gameplay scope, not a claim of v19 gameplay equivalence.

Before any combat, validate every reference inventory/report, input hash and gameplay/runtime identity. Then, in ascending order, create independent neutral fixtures with **0 and 9,216 existing archives**, each with exactly **16 new eleven-file candidate writes**. Exercise actual campaign and nested batch writer leases (3 and 2 neutral bytes), growing outer and child neutral journals (2 and 8 bytes per sample), nested accounting, pending/final publication and exact final audits. These fixtures execute no combat and are not attempt journals or balance evidence. Keep all fixtures. Record each sample, preparation, initialization, finalization, file/directory visit counters, CPU, wall time and memory. No timing threshold causes a repetition.

After both fixtures pass, run **34 candidate fights** using the retained reference definition verbatim:

1. Pass 0: `saved-deep`, then `saved-portfolio`, eight trials each.
2. Pass 1: the same order and eight trials each.
3. Reconstruct the completed 32-trial candidate campaign under an explicit no-combat guard using normal durable archive verification.
4. Replay `tower.0001` in detailed mode once for each case, in the same order, and compare complete typed report hashes with retained reference detailed reports.
5. Verify durable outer and campaign attempt counts, exact full report digests, final inventories, unchanged frozen inputs and final output bytes.

The recipes are the previously retained deep `team-77416926dedbf2bf6abc7ccb7f782f89` and portfolio `team-1b3d503fe18c185829cf1d18a135ff29`. Each case uses these eight existing values in order: `629336171, -203679512, -78889043, 888394724, -244917489, -85237506, 381152043, 902396678`. Worker count 1; prepared combat; chunk size 32; zero retries. No seed allocation, search, screening or confirmation runs.

New measured workload cap: **900 seconds and 256 MiB**, including preflight, fixture preparation, verification and final diagnostic inventory. Candidate combat retains the reference's **600-second / 256-MiB** bounds. New package including builds/source evidence is limited to **1 GiB**. All diagnostics together remain within the original **512 fights / 1,800 seconds / 4 GiB** authorization: the prior stopped diagnostic used 42 fights and 246.045 seconds; this execution adds at most 34 fights and 900 seconds. Separate preservation checks also count toward the combined wall-time limit. Compilation and correctness tests are excluded from diagnostic workload timing.

Freeze source snapshots, execution DLLs, all reused reference/content files and this protocol into the machine definition before execution. Preserve a hash receipt and successful zero-combat CLI preflight. If a measured check fails or a cap is reached, preserve evidence, stop and report the unresolved gate; no rerun/resume or cap extension. Never overwrite an existing diagnostic output.

## Interpretation

Require exact compact results and detailed replays, 34 started/completed attempts, completed campaign reconstruction, closed leases, exact inventories and constant candidate file/directory visit counts across the two fixture sizes. Timing remains descriptive. The old measured **622.54× incremental / 4.50× sixteen-write lifecycle** comparison remains historical evidence; this richer nested fixture does not create a new paired speedup estimate. Mandatory hash audits stay enabled. Decide whether this closes the engineering gate while explicitly preserving the unmet broader 5× target and unknown whole-run speedup.

V19 retains all 253 required recipes, zero confirmation fights, reliability **Unresolved**, adoption **Hold**, ordinary/joint **NotRun / NotRun**, and all **480,707 reservations**, including **512 unused confirmation values**. Do not modify v19, the old diagnostic, gameplay/content/Kharad, deployment or old caps. The 129,536-fight confirmation remains a separate unstarted task.

The exact build, preflight, diagnostic and preservation commands and outcomes will be retained in the new package `TestResults/balance/tower-discovery-performance-20260914-parity-closure` and linked from the closure review.
