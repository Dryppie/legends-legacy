# Distinct elite loadouts: completed opt-in pilot

Status: **pilot complete; reliability Fail 0/3**. The [pilot review](Tower-Loadout-Diversity-Review.md) records 9,224 fights, both campaign reconstructions and eight matching fixed replay checks. The [precombat plan snapshot](../TestResults/balance/tower-loadout-diversity-20260913/implementation-plan.md) preserves the specification before allocation. The [implementation review](Tower-Loadout-Diversity-Implementation-Review.md) retains its separate 264-test and exact-comparator evidence. The earlier [assessment](Tower-Loadout-Diversity-Assessment-Review.md) and its frozen proposal remain unchanged.

## Exact policy boundary

Implemented opt-in `independent-loadout-diversity-v11` with exactly `["stagger-reservation-joint", "loadout-diversity-joint"]`. The first method preserves v10's stagger-reservation arm, including its random stream, constructor, operators, measurements and serialization. The second method uses the same v10 rules and changes only the selection of elite parents before mutation. Keep all defaults unchanged.

The new arm's four-member elite selector must:

1. Sort the current arm's complete measurements with the existing `TowerBossGeneration.Rank` function.
2. Compute each recipe's signature from ascending numeric party slots and ordinal-sorted Essence IDs within each slot. Preserve slot identity, all IDs and multiplicities. Use an unambiguous canonical representation; never concatenate with a delimiter that legal IDs can contain. Operate on copies; never reorder saved builds.
3. Scan rank order and add the first representative of each previously unseen signature until the existing `BeamSize` is reached.
4. If fewer signatures exist, append unselected recipes in rank order until `min(BeamSize, evaluated count)` is reached. Preserve this two-pass selection order; no random draws occur in the selector.

The rank-one recipe is always first. All ordered recipes remain evaluated, saved and cached under their original ordered identity. The selected representative can change as measurements arrive. The signature is a diversity preference within this fixed input/gear context, not a combat-equivalence key or a cross-context cache key. Retain order mutation and the ability to revisit a stronger ordering.

Use the resulting beam in the existing exploration and parent-selection code. Preserve `ExplorationSize=4`, its capability-pattern-first/fallback algorithm, the one-in-four exploration-parent branch, uniform choice within the selected pool, and the existing beam/exploration recombination union. Membership of exploration and recombination alternatives may change because their input beam changed; do not add a second selection heuristic. Preserve the fresh schedule, initial allocation, proposal limits, all operators, v10 count sampler, provider eligibility, category order, family/ownership checks, core insertion behavior and filler ordering.

Do not alter `Rank`, fitness, discovery shortlist rules, the separately frozen rank-one primary/rank-two exploratory selection, confidence evaluation or acceptance rules. Grouping does not add a tie-break criterion to final ranking. No recovery quota, timing deadline, conditional-provider exclusion, preferred Essence ID or reference-derived count enters this policy. A higher structural signature count is not evidence of a stronger party.

## Implementation contract and completed verification

Target the offline `LL/tools/BalanceHarness` and focused tests only. Follow the existing policy/version integration rather than refactoring the generator. Expected integration points are `TowerBossGeneration.cs`, policy/provenance guards in `TowerBossDiscoveryContract.cs`, and v11 admission to the existing v10 metadata/constructor validations in `TowerBossPartyGenerator.cs`, `TowerPartyCoverage.cs` and `TowerStaggerReservation.cs`. Add a small pure selector/helper only where it makes the rule independently testable. Ordinary and compact verification must accept the new policy; factory/Tower Lab defaults, content, catalogs, gameplay and older serialization must remain unchanged.

Use explicit method recognition so the new method enables the v10 stagger/compatible-defense path, while only `loadout-diversity-joint` enables the new selector. Preserve the comparator's fresh operation/provenance and stream. Give the new method appropriate fresh provenance without changing constructor behavior or metadata meaning. New policy admission must not accidentally enable v11-only behavior for older versions or unrelated methods. Do not add public tuning parameters or change the generation input boundary to include references.

The [implementation protocol](../TestResults/balance/tower-loadout-diversity-implementation-20260913/protocol.json) and review record completion of the following verification requirements. Before editing, verification preserved all 19 packages including the assessment, all prior sealed reviews and then-current source/content/assemblies/catalogs. Freeze implementation-check bounds separately before any production constructor reconstruction/probe. Preserve the dirty tree. Required checks include:

- Pure selector cases: empty/fewer-than-four inputs, all same signature, all distinct, mixed fallback, tied combat fitness/stable ID order, repeat determinism, and immutable input collections. Check rank-one retention, capacity and one representative per signature when at least four exist. Verify an improved ordering becomes its group's representative without removing other ordered measurements.
- Signature cases: reordered dictionary enumeration; numeric slots including 2 and 10; within-character order permutations; cross-character swaps; different quantities/variants; case-sensitive IDs and safe delimiter encoding. Swapping character loadouts must remain distinct because fixed gear/positions can differ.
- Integration checks: only the new method uses the selector; unchanged exploration/recombination algorithms receive the selected pool; no added random draw; ordinary/compact archive/provenance verification; old fields and serialized output remain unchanged. Validate that reference additions/removals/reordering cannot affect independent construction or parent pools.
- Exact reproduction of all six archived v10 arms and the three `stagger-reservation-joint` comparator arms under the new policy, using saved discovery callbacks under a separately bounded implementation protocol. These are historical compatibility checks, not new combat evidence. If a new-arm synthetic constructor probe is needed, freeze its callback/proposal/time/storage limits and any seed reservations first; do not allocate seeds merely to populate traces.
- Run relevant backend tests through `build/run-tests.ps1`, including existing regression groups and focused new selector/contract checks. Verify gameplay/content/catalogs unchanged and seal an implementation review and updated handoff before a pilot.

The assessment's 87,381 abstract sequences establish the proposal's arithmetic/selection properties only. They do not replace production C# tests, archive parity or reference-isolation verification.

<a id="prospective-pilot-envelope-not-an-allocation"></a>

## Completed pilot allocation and result

The [separately frozen pilot](../TestResults/balance/tower-loadout-diversity-20260913/protocol.json) completed the comparison below in 252.30 execute seconds, with zero retries. Reliability is Fail 0/3; family assessments are Fail / Fail. Its [completed zero-combat v11 diagnosis](Tower-Loadout-Diversity-Diagnosis-Review.md) is now complete. Its closed allocation was: three paired restarts, 96 evaluated complete parties per arm, eight shared discovery seeds, discovery-selected primary and exploratory secondary per arm, and the six existing controls outside generation. All selected recipes used 256 disjoint shared validation seeds. Original source associations were preserved, and no primary was replaced after validation.

The frozen bounded envelope was **4,608 discovery + at most 4,608 validation + eight fixed diagnostic fights = at most 9,224 fights**, with zero retry allocation, at most 2,048 proposals per arm, 600 seconds for measured execution/reconstruction/replay phases, 1 GiB total retained package and 512 MiB per campaign. The executable protocol froze all schedules, comparisons and fixed diagnostics before combat; it is now closed. No sample extension or automatic repeat follows a failure.

Keep the existing reliability gate: at least two of three new primaries need adjusted rate lower bound at least 10%, paired lower improvement over the same-restart comparator above zero, and paired lower difference against the fixed anchor at least minus ten percentage points. Joint nominal alpha .05 stays split .025 over the full rate family and .025 over six paired comparisons, with discordance component .025/12; the ordinary evaluator remains separately .05/family. Preserve approximate Wilson and repeated-study caveats, every observed >50% breach and the intended-cohort viability requirement. Never pool closed studies.

Exclude every array in the latest [472,194-seed ledger](../TestResults/balance/tower-loadout-diversity-20260913/seed-ledger.json), including unused and constructor-only reservations. Any later implementation reservations also become exclusions before a pilot. Keep all zero-win recipes, rejections, parent provenance, frozen selections and outcomes regardless of result. Any parent-pool diagnostics must remain observational and outside fitness/selection tuning.

Kharad stays **Health 3.04881408 / Power 3.85370128**. Preserve ten level-40, tier-1, rank-2 Standard characters, five level-1 unascended/unevolved Essences each, exact fixed gear, no styles/contributions and all 80 eligible Essences under hypothetical ownership. Controls may inform only their predeclared reference measurements; their recipes, IDs, counts, ancestry, fitness and held-out results cannot enter independent construction, grouping or ranking. No practical acquisition claim, near-optimality claim, boss tuning, default/catalog promotion, floor expansion, migration, configuration change or deployment follows from this proposal.

## Next boundary

The [completed zero-combat v11 diagnosis](Tower-Loadout-Diversity-Diagnosis-Review.md) is complete. Assess one source-grounded question: whether recovery coverage should distinguish effects that require a death from recovery available without a death event. Inspect all eligible authored routes and runtime trigger semantics, including summon/death attribution, before proposing any change; the assessment may reject the idea. This selects an assessment topic only, not a provider filter, quota, deadline, new fitness term or implementation. Keep control recipes, IDs, counts, ancestry and held-out results outside generation. No new campaign is allocated. Preserve the observed 131/256 ceiling breach; any fresh confirmation needs its own separately frozen family, schedules and limits without pooling. Do not retune Kharad, strengthen gear, promote defaults/catalogs or expand floors.
