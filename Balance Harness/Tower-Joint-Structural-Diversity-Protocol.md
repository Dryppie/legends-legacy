# Joint structural search: candidate diversity implementation

15 September 2026. Target: offline `LL/tools/BalanceHarness`. Continue the verified saved-data diagnosis with a separate opt-in policy, `independent-joint-structural-diverse-v1` / `joint-structural-diverse`. Keep v1 outputs and all prior policy behavior unchanged. No gameplay, boss tuning, ability-order optimization, default promotion, preparation, fights, replay, fresh seeds or retry.

## Implementation decision

Keep the same authored-core construction and five-role-per-character, ten-distinct-loadout rules. Those are explicit search heuristics, not combat legality requirements. Relaxing them is a separate future change; this scope isolates between-candidate diversity. Do not hardcode any control Essence or use saved combat fitness to guide construction.

Add a bounded allocator traversal that restarts from the root after retaining a complete new party and favors recipes least exposed across retained parties. Within a party retain the existing repetition/novel-Essence/ordinal tie preferences. Select the smallest static slot pool first to avoid scanning every remaining slot's full pool at every restart. Backtrack when inventory prevents a completion. Global state/check caps span all restarts; never reset them. Complete-party identities suppress duplicates; an exhausted result requires traversing the remaining space, while reaching any cap is incomplete. Cancellation and shared copy limits remain mandatory. Legacy traversal remains the default and follows its existing code path.

The new registered policy uses one existing generation label, at most 16 proposals/evaluations, 64 cores, 256 states / 16 recipes per core, and 256 states / 250,000 checks / 16 retained parties across allocation. It keeps canonical ordinal ability order, normal checkpoint-before-evaluation charging and provenance validation. The trace's version identifies the policy and its fixed rules.

## Frozen verification

New output: `TestResults/balance/tower-joint-structural-diversity-20260915`. Capture the dirty checkout before editing and preserve unrelated changes. Freeze all source, scripts, fixtures, commands and input hashes before diagnostic execution. Use isolated builds against the sealed gameplay assemblies; no shared binaries are rebuilt.

Run exactly 34 backend tests once through `build/run-tests.ps1`: the existing ten party-allocation and twelve structural-search tests, plus twelve new diversity tests covering broad composition coverage, exhaustive small assignment sets, constrained-slot inventory, canonical determinism/immutability, global caps, single-solution exhaustion, invalid inputs/cancellation, policy registration/bounds, complete canonical generation, empty-pool charges, cancellation/failure checkpoint charging, and order-independent metadata/provenance. All callbacks are synthetic and a combat trace guard rejects engine entry.

Run the seven sealed prior-policy fixtures once (32 synthetic evaluations each) and require exact prior output equality. Run captured v1 once and require exact equality with its sealed 16-candidate synthetic result. Run the new policy twice on identical captured inputs, once with reversed metadata order: require exact result equality. Independently audit pool membership, family/role/repetition/copy legality, canonical order, identity, attempt counts, nominations, global limits and all 120 pairwise composition distances per captured policy. Use the same 680-recipe pool. The diversity gate is 16 complete distinct teams, at least 80 distinct loadouts, no fixed character slot and median multiset replacement distance at least five; it is not a strength claim. Record actual counts and timing even if the gate fails.

Phases: setup, freeze, build, test-build, tests, captured, audit, publication. At most **300 seconds total new workload**, conservatively including compilation, within the approved cumulative **2,400-second** cap. Carry **2,024.6949999999654 seconds**, leaving **375.3050000000346 seconds** before this scope. Per-phase ceilings: setup 15, freeze 20, build 90, test-build 60, tests 30, captured 30, audit 20, publication 15 seconds; reserve 15 seconds for closure. At most **192 MiB** new output, within the unchanged cumulative **4 GiB**, with a conservative additional 1 MiB allowance for the shared test receipt. Zero retries: stop dependent work at the first failure and retain evidence. Publication may close a failed scope without claiming success.

Verify all 52 predecessor packages before/after, including diagnosis seal `a1983501f7fded89ab66cade9d125b97aa01c8ca7a16d43db6787ebc8025ed9a`. Preserve all **482,686 reservations**, 253 v19 recipes and unused 512 confirmation values. Reliability Fail 1/3, deep recovery 0/3, v19 Unresolved and adoption Hold remain. No additional combat is authorized. Publish reproducible commands, measurements, source changes, tests, limitations and active Markdown handoffs.
