# Group/count comparison: ready for the seed exception

15 September 2026. **ReadyForSpecificSeedApproval.** The joined-mechanics versus group/count controller and exact protocol are implemented and verified. **56 backend tests and four Python metric fixtures passed**. The native preflight prepared two controls without combat; the independent audit verified identical gameplay inputs, all **482,416 reservations** and 66 interval fixtures. **Zero fights, zero fresh values and zero retries.** No bound study or execution package exists.

## The next combat comparison

| Phase | Allocation | Maximum fights |
| --- | --- | ---: |
| Joined baseline discovery | 44 teams × 4 shared seeds | 176 |
| Group/count discovery | 44 teams × the same 4 seeds | 176 |
| Screen | Top 2 per policy × 8 shared seeds | 32 |
| Confirmation | One winner per policy and two fixed controls × 32 shared seeds | 128 |
| Total | | **512** |

Baseline is `independent-joined-mechanics-v1`; candidate is `independent-group-count-v1`. Both use one compiled driver, captured gameplay/content and the same equipment, neutral identities, timestamp, scenario context and combat seeds. Each gets 44 distinct evaluated teams, at most 704 proposals, initial 11 fresh constructions and FreshEvery 4. Ten characters form two five-player parties with five Essence slots each. Ability order remains fixed. Controls never enter either search or screening. This preparation changes no harness search policy or gameplay source.

Each policy's top two are frozen before screening; its winner is selected by screen wins, discovery rank and ordinal identity. Both winners freeze before confirmation. Exact full-context duplicates share later measurements while retaining every origin; freed seats are never refilled. Discovery overlaps remain independently charged. Incomplete discovery stops the pipeline. These unchanged rules make the comparison reviewable before any outcomes exist.

Primary contrast is group/count finalist minus joined baseline finalist. Four secondary contrasts compare each finalist with each control. Wilson factors remain 8 for rates and 20 for paired discordances; all five contrasts remain in the report, including non-improvements. Separate policy streams and one shallow restart limit the conclusion. This study will not establish reliability, optimality or adoption.

The report also has prespecified **boss health remaining and duration**, paired finalist/control health differences, **all catalogue-group counts for every evaluated recipe and confirmed recipe**, and fresh-construction requested/placed/final counts, uniform routes and rejections. Missing optional telemetry remains separate from zero. These are descriptive diagnostics, with no extra significance claims or changes to selection. They can expose differences when wins alone show none. The 214-group catalogue is frozen from the verified content; this study samples only a short trajectory, not all 2,140 group/count combinations.

## Verification and measurements

| Check | Result |
| --- | --- |
| Existing composition / joined / group-count cases | 17 / 15 / 16 passed |
| Adapted comparison contract/controller cases | 8 passed |
| Required repository test wrapper | **56 passed, 0 failed, 0 skipped**; 4.047 s |
| Python metric fixtures | **4 passed**; 0.093 s |
| Native input/registry/control preflight | Passed; 57.766 s |
| Independent input/ledger/interval audit | Passed; 15.860 s |
| Interval fixtures | All 66 verified: 0..32 wins, family factors 8 and 20 |
| Control preparations / actual fights | 2 / 0 |
| Prior indexed artifacts preserved before and after | 4140 files across 10 packages |
| Diagnostic workload before publication | 79.423 / 300 s preparation; charged against 1,800 s total |
| Output before publication | 83.47 MiB / 1 GiB preparation + execution evidence |
| Compilation, recorded separately | 4.547 s |

The backend tests exercise exact policy/context/cost and schedule binding, separate nominations, screen tie-breaks, duplicate origin retention, paired uncertainty, interrupted durable attempt charging, previous-policy hashes and group/count construction. Engine-entry guards prevent combat in tests and preflight. Python fixtures check missing/zero telemetry, invalid numeric values, absent/overlapping groups and paired seed alignment. Preflight uses 45 existing reserved labels solely as non-runnable materialization fixtures; the complete real reservation ledger remains intact.

The isolated driver and test builds passed. One existing xUnit2031 style warning appears in the unchanged composition test; no errors. The shared prior TRX was copied before the repository wrapper replaced it. No required verification command failed or was blocked. The full dirty gameplay build, full backend suite and real combat/archive run have not been performed in this preparation. Native post-run archive reconstruction and the independent execution audit are ready, but cannot be verified against this study's combat output until the study exists.

## Reproducible sequence and files

The [protocol](Tower-Group-Count-Comparison-Protocol.md), controller/model/tests, metric readers, source snapshots, projects, dependencies and executables were pinned before their respective once-only diagnostics. The source comes from the sealed group/count implementation; gameplay dependencies come from its captured comparison scope. Existing app/search source was not edited. Active search handoffs and README are updated; the new evidence directory contains all runnable code and measurements. No configuration changes, migrations or deployment implications.

```powershell
$py = 'C:/Users/HrHoe/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe'
$w = 'TestResults/balance/tower-group-count-comparison-preparation-20260915'
& $py -B "$w/workflow.py" bootstrap
& $py -B "$w/workflow.py" build
& $py -B "$w/workflow.py" test-build
& $py -B "$w/workflow.py" tests
& $py -B "$w/workflow.py" metrics-tests
& $py -B "$w/workflow.py" check
& $py -B "$w/workflow.py" audit
& $py -B "$w/publish.py"
```

The test phase invokes:

```powershell
./build/run-tests.ps1 -NoBuild `
  -Filter 'FullyQualifiedName~BalanceHarnessCompositionSearchTests|FullyQualifiedName~BalanceHarnessJoinedMechanicsTests|FullyQualifiedName~BalanceHarnessGroupCountTests|FullyQualifiedName~BalanceHarnessGroupCountComparisonTests' `
  -ArtifactsPath 'TestResults/balance/tower-group-count-comparison-preparation-20260915/tests'
```

After the specific approval, record its exact text and protocol hash in a new execution `authorization.json`, with `freshValues:45` and `maximumFights:512`. Then run the frozen workflow's `bind`, `run`, `verify` and `audit-execution` modes once, in that order. They write separate study/execution directories, preserve all attempt charges and reject retries. **None of these modes has run.** Do not rerun preparation into the sealed package; reproduction requires a separate output path and frozen input copies.

Evidence: [test TRX](../TestResults/balance/tower-group-count-comparison-preparation-20260915/control/tests.trx), [native check](../TestResults/balance/tower-group-count-comparison-preparation-20260915/check/result.json), [independent check](../TestResults/balance/tower-group-count-comparison-preparation-20260915/control/independent-check.json), [completion and final resource counts](../TestResults/balance/tower-group-count-comparison-preparation-20260915/control/completion.json), [seal](../TestResults/balance/tower-group-count-comparison-preparation-20260915/preparation-files.json).

## Remaining authorization and limitations

Execution needs **45 fresh values**: 1 generation, 4 discovery, 8 screen and 32 confirmation. A successful allocation retains **482,461 total reservations**. The runner requires a matching protocol-hash authorization, rechecks the full registry, durably journals allocation attempts and preserves pending values on failure. Historical unused reservations, including v19's 512 unused confirmation values, remain excluded. The original zero-fresh-seed instruction is the reason for this specific exception; both earlier exceptions are exhausted.

Caps remain **512 charged fights, 30 minutes including preparation/verification, 4 GiB and zero retries/resumes/replays**. Native execution is bounded to 900 seconds within the remaining total budget. Existing incremental storage accounting and archive verification remain in use; there is no performance-speedup claim here. A bad seed trajectory, rejected constructions, retained mutation behavior or inadequate statistical precision can leave search improvement unresolved. Preserve any failure rather than extending the study.

No Kharad/content tuning, sealed experiment modification, 129,536-fight confirmation or old-cap increase. Historical reliability Fail 1/3, deep recovery 0/3, sealed v19 Unresolved and adoption **Hold** remain unchanged.
