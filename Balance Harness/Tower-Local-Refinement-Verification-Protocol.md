# Local refinement: first verification after concurrent checkout changes

16 September 2026. The [implementation scope](Tower-Local-Refinement-Protocol.md) stopped during its initial freeze because three unrelated frontend files changed concurrently. It ran **no compilation, backend fixtures, generation diagnostics or fights**. Its failure is retained and sealed in `TestResults/balance/tower-local-refinement-20260916`; that scope is closed and is not retried.

Continue the already-authorized implementation verification in `TestResults/balance/tower-local-refinement-verification-20260916`. Run the **same 69 facts for the first time**, with the same exact fixture cases, source implementation, pinned pre-change reference assembly, pinned gameplay/dependency assets, build entry point and phase deadlines specified in the implementation protocol. No change to test expectations or policy implementation is justified by the unrelated frontend edits. The continuation pins those harness/test inputs before executing anything.

Both scopes together retain the original **180-second / 18-MiB** allowance; subtract the failed freeze, inspection, closure, setup and retained bytes before starting this scope. All prior cumulative limits remain **4,080 seconds / 4,496,293,888 bytes**. Charge measured setup and closure; preserve all failures. Zero fresh seeds, production allocation, preparations, fights, replays or diagnostic reruns. First execution only: isolated build, isolated test build, `build/run-tests.ps1` for the frozen eight-class/69-fact filter, audit, publication. Stop on the first failure in those phases; do not rebuild or rerun a failed test in this scope.

The failed freeze exposed a concurrency assumption, not an input mismatch. Capture both the initial checkout hashes and current hashes of unrelated files; record any concurrent differences without overwriting those files. The task's write set is restricted to the eleven listed harness source files, two new test classes, its evidence and active Markdown. Assert byte-for-byte stability of every harness/test/fixture input and captured dependency used by verification. Concurrent UI edits outside that write/input set do not invalidate an isolated harness build. Audit and report their paths and hashes separately from task-owned changes.

No seed or combat permission is inferred. Preserve all **482,956 reservations**, sealed studies, V19's 253 recipes and 512 unused values, fixed ability order and adoption Hold. The implementation remains a combat-strength hypothesis until separately approved testing. No gameplay, migration, configuration or deployment changes.
