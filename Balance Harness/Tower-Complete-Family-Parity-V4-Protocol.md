# Complete retained-family parity v4 — frozen complete reference package

Frozen **14 September 2026**, before new setup or native execution. Target: offline `LL/tools/BalanceHarness`. Continue the [stopped v3 scope](Tower-Complete-Family-Parity-V3-Review.md) in **`TestResults/balance/tower-complete-family-parity-v4-20260914`**. Preserve v3 under seal `56704f04e0d82e7880295febb4de03de053729457e78ea54f6b12923c94cff0a`, including its partial reference. No old directory is rerun, repaired, resumed or overwritten.

## Exact inputs and execution

Copy v3's complete **37-file `corrected-reference-layout`**, candidate executable, probe source and producing evidence byte for byte. Use the unchanged `TowerParityReferenceHost` to call the general dispatcher in the captured midpoint harness (SHA-256 `16a2d316afcf227fae0904d5c4bd2144e29f4bf4e2798df381eb918c3f191a8b`). Reuse the exact reference definition and request, changing only `referenceRun` to the new output directory. Copy and verify every external binding and the complete v3 inventory. Reuse the **215 passing fixture tests from `build/run-tests.ps1`** only after verifying all **470** reviewed source hashes. No rebuild or new test invocation is scheduled for unchanged producing sources.

The two fixed cells remain non-anchor **`0000b8a0159100cfee696335c83a6b6908665d193492bd665a5df56cbdeaab68`**, then midpoint anchor **`02aba209053fea61609745b10bf1e7c367e9633cd7b0a264faa5ee78beb8d2a0`** (origin `team-eb63c3547f4c75de71105c01ecd9a989`). Keep the first **32** existing ceiling-screen shared values and all **256** existing midpoint shared values in their original order. They are 288 distinct, disjoint, already registered replay values. No selection by new outcomes, replacement case or fresh reservation is allowed. Content, settings, scenarios, participant identities, original character/Essence ordering and timestamps remain unchanged.

1. Capture the dirty baseline, copy and verify the inputs/binaries/evidence, then freeze every producing file and exact command in `engineering-freeze.json`.
2. Run **one actual native archive-only probe** using the copied `TowerParityArchiveProbe.dll` and a new `archive-probe-result` directory, with a **30-second** watchdog. It invokes the captured `TowerBossStudy.RetainExecutable` with a no-combat trace guard. Independently compare its complete retained inventory/hashes with the **25** static archive-required files saved by v3. Require zero preparations/fights and the full saved midpoint execution identity. This is an archive-copy fixture, not a campaign or replay.
3. Run **one host `--probe`** (30 seconds), verifying general dispatcher, original midpoint assembly entry point and full runtime/OS/architecture/assembly identity. Then run **one unchanged candidate typed `check`** (90 seconds), which validates both scenarios/schedules and the complete selected saved-anchor archive without preparing combat. All setup, copy/hash work, these three probes/checks and producing freezes share **120 seconds**; per-command timeouts are clipped to remaining setup allowance. Freeze successful receipts and the copied archive inventory before reference combat.
4. Run the reference host **once** with the copied definition, new `native/reference` and unchanged midpoint content. Its internal arguments fix **32 attempts**, chunk 32, retry reserve 0, `prepared-v1`, `owned-storage-v1`, **120 seconds / 256 MiB**. Accept scientific exit code 1 only with a complete verified 32-trial archive. The fixed feasibility gate is **at most 9 wins of 32**, equivalent to the one-cell first-stage upper bound at most 50%. Otherwise stop; do not select another case or run the candidate.
5. Run the unchanged diagnostic candidate **once**, with **288 attempts**, one second-stage place, **1,080 seconds / 2 GiB**. It verifies the complete source and prepares **all 43,879 rosters**, matching every saved participant hash before any candidate fight. The actual shared production adapters then run **32 first-stage fights**, compare every full report/gameplay digest with the new reference, durably publish stage selection, then run **256 anchor fights**, comparing them with the existing sealed midpoint reports. Only physical archive index/case labels may differ. Retain global/child durable attempt charges, owned-storage checks, exact archives, production reconstruction before publication and one completed reconstruction without combat. The full-study public run contract is not invoked.
6. Run the unchanged independent verifier once within **120 seconds**. It checks exact inventories, all 288 report comparisons/schedules, 43,879 preparation rows, stage decisions, native reconstructions, global/child charges and trace counts. Preserve prior packages, source hashes, all seed histories and unrelated checkout work within **120 seconds**, then seal within **120 seconds**. Freeze the closure script before closure. Charge **60 seconds** for static reads/metadata. Any failure or cap stops dependent execution with evidence retained; zero retries, resumes, substitutions or additional replays.

## Limits and interpretation

Maximum new combat: **32 reference + 32 first-stage candidate + 256 second-stage candidate = 320 fights**, below 512 including repetitions. Expected preparations: **43,879 full-family + 1 reference reusable + 2 candidate reusable = 43,882**. No preparation is permitted during any probe or verifier. Preserve all **481,603 reservations**, including the original **512 unused v19 confirmation values**; authoritative ledgers remain untouched.

The diagnostic-wide bound is **1,800 seconds / 4 GiB of new retained output**. Setup 120 + reference 120 + candidate 1,080 + independent 120 + preservation 120 + seal 120 + static 60 = **1,740 seconds**, leaving 60 seconds headroom. Native limits remain unchanged; external watchdogs persist timed failure receipts and enforce setup/command/output limits. Count copied evidence, archive fixture, native output, final metadata and whole changed Markdown files. Each start marker prevents repetition.

Persist the existing detailed performance trace, process CPU, allocations/peak memory, preparation hashes and journals. Do not add inclusive nested timings or claim a throughput speedup from this single parity run. Earlier discovery-accounting measurements remain unchanged; no scaling fixture is repeated here. Successful completion verifies full-roster preparation and the small actual two-stage production path, not full-family second-stage capacity, 256-cell outer batch-boundary combat or complete-study throughput.

No gameplay/Kharad/content edits, migrations, deployment, cap increases, allocator, confirmation launch or original-v19 modification. Preserve all 253 v19 recipes/nominations and earlier sealed study results. Reliability **Fail 1/3**, adoption **Hold**; the original sealed v19 remains Unresolved with no confirmation in that experiment.
