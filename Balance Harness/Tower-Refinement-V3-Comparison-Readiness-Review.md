# V3 refinement comparison readiness

16 September 2026. **ReadyAwaitingSpecificApproval**. V3 versus team coverage is ready: exact request, captured runtime, unchanged thin host, native preflight and independent checks passed. Zero new values or fights.

The [frozen protocol](Tower-Refinement-V3-Comparison-Protocol.md) and [exact request](../TestResults/balance/tower-refinement-v3-comparison-readiness-20260916/request.json) compare team coverage V1 with discovery refinement novel V3. Both use identical captured gameplay/content/settings/context and fixed ordinal ability order. Comparison-v2 is explicitly selected and bound to the request, driver receipt, authorization template, gate and archived definitions.

| Stage | Maximum teams | Fights each | Maximum fights |
| --- | ---: | ---: | ---: |
| Baseline discovery | 16 | 4 | 64 |
| V3 discovery | 16 | 4 | 64 |
| Selection | 4 | 8 | 32 |
| Finalists and two controls | 4 | 32 | 128 |

Maximum **288 charged attempts** and **45 fresh values**: one generation label, four discovery, eight selection and 32 confirmation. A finalist plays 44 fights across all three stages. Incomplete discovery stops the entire comparison; no refill. Duplicate recipes retain all origins and may reduce later fights. The primary endpoint is the paired confirmation win-rate difference; boss health is descriptive. Adoption remains Hold.

The **84 MiB study cap** exceeds the counted **73.00 MiB component minimum** by **11.00 MiB**. The minimum includes fourteen full compact historical arrays, one captured runtime and four content copies; it excludes reports and temporary writes. Completion is not guaranteed. The independent audit establishes that actual readiness output plus the full 88 MiB execution reservation fits only after the requested extension.

The study gets **360 seconds**, including a conservatively charged 150-second binding allowance, then 210 seconds for launch, its live refresh, combat and native reconstruction. The complete execution task gets **400 seconds / 4 MiB evidence**, including independent audit and publication. Three existing registry traversals remain inside binding/launch; no standalone full scan ran during readiness. The prior incomplete run's 144.54-second native duration is context, not a completion guarantee.

Await approval for exactly 45 fresh values, 240 additional cumulative diagnostic seconds and 64 MiB additional cumulative output. Then execute the sealed request once: at most 288 attempts, 400 task seconds, zero retries. No further standalone registry check is scheduled. Proposed cumulative ceilings are **3,840 seconds / (4 GiB + 128 MiB)**, replacing 3,600 seconds / (4 GiB + 64 MiB) only if approved. No old experiment cap changes. The template is unsigned; no execution authorization has been created.

The producing harness is the exact V3 DLL that passed 60 tests through `build/run-tests.ps1`. Those results are reused; no tests or harness/gameplay builds were repeated. The runtime directory contains that tested library plus unchanged captured dependency/configuration files, with exact provenance in `executable-provenance.json`. The library has no standalone entry point: reproduce using the sealed thin host and the study's retained executable directory. The unchanged host alone was compiled, with no restore/build-server reuse. Its inspect operation performed no seed derivation, live-registry scan, actor preparation or combat.

The independent readiness audit checks 482,866 reserved values, request/template hashes, V3 identity, runtime/content hashes, and the complete resource reservation. Its unchanged raw-record/interval helpers verified 128 saved confirmation records and four rates. This is evidence reading, not replay or new combat. Source and unrelated dirty files remain preserved.

| Completed phase before publication | Charged seconds |
| --- | ---: |
| audit | 0.3910 |
| build | 1.7810 |
| freeze | 1.0000 |
| inspect | 0.5160 |
| setup | 0.0000 |

First failure: none. Exact usage is in the [completion receipt](../TestResults/balance/tower-refinement-v3-comparison-readiness-20260916/completion.json); native metrics and per-phase commands/logs remain in the package. Preparation uses at most 40 seconds / 24 MiB within existing authority. Do not rerun this package or any sealed study.

```powershell
$python = 'C:/Users/HrHoe/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe'
$work = 'TestResults/balance/tower-refinement-v3-comparison-readiness-20260916'
# Completed readiness phases are recorded in control; do not repeat them.
# Only after exact seed/time/output approval and its bound receipts exist:
& $python -B "$work/execution.py"
```

Changed files: new protocol/review, isolated request/runtime/host/scripts and six active Markdown handoffs. No harness/search/gameplay source, configuration, migration or deployment changes. Preserve all 482,866 reservations, including the failed study's unused 40 values and V19's separate unused 512 values and 253 recipes. V19 Unresolved, later reliability Fail 1/3, deep recovery 0/3; adoption Hold.
