# Kharad +16% checked local application

Prepared 13 September 2026. This plan freezes with the application package's `protocol.json`, before diagnostic combat or the content edit. Target: the primary game's local `LL/src/API/API.LL/Data/world-tower/tower-floors.json`.

## Accepted setting and exact edit

The [completed precision resolution](Tower-Kharad-Precision-Resolution-Review.md) returns **Pass** for the complete 17,821-recipe family. Its fresh team won **339/1,000**, adjusted **30.27%–37.73%**; 18 teams have supported viability, and the maximum final upper is **49.31503%**. Its protocol hash is `8aa7fe9f10677414737f933f405a0e523fc8cde43eaa9dc792d1112c0184c4a1`, and its final manifest hash is `16dc135d2c7ce7de6a883cffbfdcd0aa365c1eafca0ad6709455d1773cddf403`.

Apply exactly two floor-5 values:

| Field | Before | Applied |
| --- | ---: | ---: |
| `guardianScaling.health` | 3.04881408 | **3.5366243328** |
| `guardianScaling.offense` (Power) | 3.85370128 | **4.4702934848** |

Both increase by the accepted linked factor **1.16**, preserving the Health/Power ratio. Preserve local file bytes and line endings except for these numeric tokens. Prove structural equality with the archived candidate for all 16 Tower content files; require byte equality for the other 15 files and LF-normalized byte equality for the floor file. No combat/search source, catalog, default policy, other floor or dependency changes.

## Frozen parity cases and limits

Choose four existing confirmation observations before any diagnostic fight:

1. The refreshed team's first Victory in the frozen 1,000-trial precision schedule.
2. That team's first Defeat in the same schedule.
3. The highest observed-rate retained second-stage team in the final composite report, breaking ties by ordinal ID, on its first original second-stage seed.
4. The prior candidate leader `team-a7e5de669c4a17287d84060e8ab6359b`, on its first original second-stage seed.

Preserve each exact source recipe, seed, case/trial ID, outcome and archive manifest hash. These outcome-aware choices cover deterministic parity branches; they are **not a new acceptance sample or search result**.

Reserve **four archived candidate replays plus four local replays = eight diagnostic starts and completions**, with **zero retries, resume or extensions**. The aggregate diagnostic-phase time cap is **600 seconds**; the application package storage cap is **512 MiB**. Preparation, builds, backend fixture tests, documentation and no-combat integrity reconstruction are outside diagnostic fight/time accounting. Cancellation and storage checks are cooperative. Every diagnostic start and completion is durably recorded; a failed phase cannot silently restart.

The two retained second-stage cases require their original full-family harness binary for archived replay. The refreshed cases and local comparisons use the captured precision binary. Require identical gameplay assembly hashes, runtime, OS, architecture and settings across both producing identities. Preserve both helper builds and their exact source. No producing harness rebuild is needed.

## Procedure and completion

1. Verify all four sealed complete-family/precision study and work manifests, the passing assessment and application decision, protected source/content/catalog hashes, and both captured execution identities. Save the original file, exact proposed bytes, selected cases, settings, helpers, this plan and limits; then freeze the protocol.
2. Replay the two retained cases with the original full-family executable, then the two precision cases with its original executable. Each detailed replay must match its compact source result. Preserve all four detailed reports and receipts before applying content.
3. Recheck frozen inputs and successful pre-application receipts. Replace exactly the two numeric values, verify all content equivalence checks, and write an application receipt.
4. Run all four local detailed reports and require complete canonical equality with their archived-candidate replays, including participants, outcomes, duration and events. Independently compare the saved report pairs and exact JSON differences.
5. Run relevant backend tests through `build/run-tests.ps1`. Reconstruct the complete source precision decision using its captured harness under a no-combat guard, with the intentional two-value workspace change explicitly recognized. Preserve the older study helpers' original workspace-integrity checks; do not edit historical artifacts to make those checks pass.
6. Write a separate application review and receipt, update active Markdown, and seal the new package. Preserve all prior results and the **476,054-reservation precision ledger**. This application allocates **zero new seeds** and provides no new win-rate evidence.

No service deployment, restart, shared database action or migration is part of this local application. A running service must reload the content before using the new values. Independent-search reliability remains **Fail 0/3** at the candidate; practical ownership, unsearched recipes, strongest-player readiness and floors 6–11 remain separate work.
