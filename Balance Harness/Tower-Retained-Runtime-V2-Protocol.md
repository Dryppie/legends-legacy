# Captured-v19 runtime v2 proof and UTC family binding — frozen zero-combat protocol

Frozen **14 September 2026** after the [stopped UTC diagnostic](Tower-Retained-Context-Review.md). Target: offline `LL/tools/BalanceHarness`. Proceed with the corrected comparator, a new bounded proof, and complete seed-free confirmation design binding only if every prerequisite passes. Preserve both old failed diagnostics and the complete retained-family audit.

New output: `TestResults/balance/tower-retained-runtime-v2-20260914`. Limits: **zero fights, zero fresh seeds, zero retries/resumes, 1,800 seconds diagnostic workload, 4 GiB total new output**. Native proof additionally allows **900 seconds / 1 GiB**. Compilation/correctness tests are timed separately; count builds, captured files and changed Markdown in output. Use the same captured-v19 gameplay DLLs, +10% midpoint inputs and one existing reserved value for preparation only. All **481,603 reservations**, including the original unused 512, remain excluded.

1. Use the implemented `tower-runtime-fields-v2`: retain all prepared combatant instance fields except exactly `PlayerEssence.AbsorbedAt`, `PlayerEssence.UpdatedAt`, `InstanceAttributeModifier.Id`, `ItemAttributeModifier.Id` and `ItemInstance.AcquiredAtUtc`. Keep those five fields' values in a separate bookkeeping map. Preserve all actor, equipped-Essence and equipment identities, mutable combat counters, health/barrier, attribute values, modifier amounts, abilities and every other field. Keep absence distinct from null. Never mutate a runtime to make the comparison pass.
2. Retain the justification for the separation against captured rehydration/executor IL already retained: those timestamps and modifier IDs are freshly generated bookkeeping; the explicit random seed and combat state drive engine execution. Test each exclusion and negative controls for meaningful changes. Run `build/run-tests.ps1` with the exact union of the new runtime-comparison tests, context tests and retained-audit tests using isolated artifacts.
3. Freeze script, source, producing executable, old seals and source input hashes before execution. The preceding stopped UTC package seal is `615888a49903a07f58de8c5c6e8f3e6d9d2f4256277cded8c4043efd67da72d7`; the complete audit seal remains `d346801a04691964966f3ac780dc52f39dac432a89f5d5c6693602ff187b5884`. Do not modify or resume either.
4. In one newly scoped native pass, map all **43,879 entries / 47,834 origins**, checking each legacy identity against the sealed native row. Prepare all **90 offset-group recipes** once in original +01:00 notation and once in UTC. For the first affected recipe, also prepare the identical original input a second time before the UTC case, making one explicit same-input control. For the first midpoint recipe, prepare **UTC twice**, then **+01:00, −07:00, +14:00, −12:00 and UTC plus one tick**. Total: **188 preparations / 97 comparisons**, comprising 90 offset pairs, two same-input controls and five further offset/instant comparisons. No engine entry is allowed.
5. Before asserting any comparison, durably save both state maps, separated metadata, original/normalized input and plan hashes, saved participant checks, every differing field path/value and expected same/different-instant behavior in a new compressed record. Verify the persisted record immediately. Expected equivalent cases must agree in every retained state field, normalized input/plan, participant digest and UTC context identity. The one-tick case must keep identical actors while distinguishing input, plan and context. Record CPU, allocations, memory, timings and partial evidence on failure as well as success. Stop dependent work on failure without retry.
6. Independently verify all 97 comparison records, state hashes, differences, counts, native inventory seal and every new context/cell identity. Verify that all 90 recipes remain distinct and that all 327 historical plus 253 midpoint anchor origins resolve to the fixed 560-cell union with complete context coverage. Preserve original scenario/legacy hashes and all origins; do not omit entries or pool outcomes.
7. If proof and verification succeed, bind the existing conditional two-stage design to the entire UTC family. Use 32 first-stage samples on every non-anchor, 256 separate second-stage samples on every fixed anchor plus every unresolved first-stage cell, maximum 4,096 second-stage cells, alpha .025 per stage and whole-stage multiplicity. Produce all 33 first-stage and 257 maximum-second-stage interval rows and independently check the arithmetic. No fresh values or executable combat controller are created. Existing 20,000-cell/500,000-fight caps stay unchanged.
8. Complete registered-history/checkout preservation, scoped whitespace/link checks and active documentation updates. Record reproducible producing commands, measured resource usage and limitations; seal the entire new evidence package. Do not claim a completed combat replay, live-gameplay equivalence, balance acceptance or completed confirmation.

Reliability **Fail 1/3**, adoption **Hold** and all preceding scoped balance conclusions remain unchanged. A verified seed-free binding would prepare the next implementation boundary; it would not authorize allocation, confirmation combat, content changes or deployment.

The preceding runtime proof remains sealed at `b94911fa953fbd48019779d5edab41003bce32903b37a8641db0f6bcceda4e5d`. The exact acquisition-field exclusion is supported by its saved regression and captured IL. Freeze `verify.py` and its lexical canonicalizer before this new proof. Independent verification is one pass, at most 600 seconds; final preservation and sealing each allow 120 seconds. Charge another 60 seconds for short static reads and metadata. A failure stops dependent binding; no retry or resume. The first-stage table uses 43,319 cells and the maximum second-stage table 4,096. Verify normal quantiles independently by erfc bisection and Wilson roots by Decimal arithmetic. These are approximate intervals. No controller or seed schedule is produced.
