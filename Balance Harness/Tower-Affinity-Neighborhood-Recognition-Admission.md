# Complete-neighborhood recognition admission

Current status: **RecognitionAdmittedNoReservation**. The exact frozen 44-team, shared 2,048-value, 90,112-fight design is admitted against the captured gameplay runtime. No OS entropy, new production values or battles were used. The preceding allied-action decision remains **Inconclusive**; this admission provides no efficacy result and changes no policy default.

## Frozen evidence and runtime

The [admission package](../TestResults/affinity-neighborhood-recognition-admission-20260924/admission.json) is sealed by manifest SHA-256 `2e6cce9842430ac3383df46f449d09a9a8140c1c2c551ae2c5b8c88b02eb6287`. Its request SHA-256 is `0519d5efa039459bcf5a99f06e61b8f707d9a48f649510483a3ddb91536a4878`. The [declaration](Tower-Affinity-Neighborhood-Recognition-Admission-Declaration.json) was written and validated before the one attempt; the frozen plan remains `a0b4a5dbc8fe7c65d4df80bfcb17b8f5623343cef7e0c1a1f2506648b3e2fc2b`.

The adapter reproduces the complete neighborhood from authenticated source exports and the sealed pure builder. The family contains three reference teams, 26 shared generated recipes and 15 control-only recipes. Every team uses the same future 2,048-value panel, partitioned into 1,000/1,000/48-value chunks: 132 transport bindings. All 46 endpoints and the three terminal decisions remain frozen. There is no population sampling, root replication, sample reduction or outcome-driven extension.

The producing allied-action runtime and content were authenticated in full. Only `BalanceHarness.dll`, its matching portable symbols, dependency manifest and runtime configuration were replaced with the four artifacts from the preceding verified build. All captured gameplay dependencies remain byte-identical. DLL SHA-256: `99fb2ec24db2a6de93b8317471a13ac53a04d3a36294708e3b405f7c4792b70f`; qualified execution hash: `e726fca8da8d04e17328cb9dd2b39c91e204d809dab1ed93c353f40e7ccc886d`.

Qualification authenticated 231 compiled source documents against matching portable symbols and resolved 632 native method bodies, including reservation, execution, both audits, publication and neighborhood assessment. It reconstructed the retained literal fixture and prepared both endpoint seeds of every chunk under an active combat-rejection guard: **264 encounter preparations** covering all 44 physical teams and 132 transports. The native request check prepared another 44 teams, totaling **308 preparations / zero battles**. Only previously stored literal entropy was read for fixture classification.

Complete production history was freshly scanned before and after the native check and once again by separate sealed-package verification: **782,796 values / 262 files**. Both nullable recovery fields are explicit and inherited intact. Admission and verification held exclusive registry/output leases. The prospective scientific output is absent.

## Resource admission

Every forecast uses a fixed factor of two. Actual completed source counts are 16,256 allied-action pilot fights and 37,120 preservation recognition fights. Report bytes were individually authenticated for all 53,376 source reports; maximum compressed report size is 20,966 bytes. Time uses the worse observed per-fight rate independently for native execution, audit and full postpublication verification, plus the current 33.750-second context/native-check overhead. Storage includes the worse non-report native overhead from either source or the full current literal fixture, fixed reserves, and current request/definition bytes for postpublication. The exact rule and retained measurements are in [resource-forecast.json](../TestResults/affinity-neighborhood-recognition-admission-20260924/resource-forecast.json).

| Phase | Forecast seconds | Cap seconds | Forecast GiB | Cap GiB |
| --- | ---: | ---: | ---: | ---: |
| Native execution | 10323.991 | 14,400 | 6.581 | 16.000 |
| Audits and publication | 2210.166 | 3,600 | 0.076 | 1.000 |
| Separate postpublication verification | 2208.780 | 3,600 | 0.057 | 0.250 |

All separate caps fit. These are conservative planning estimates, not guaranteed runtime bounds for unobserved recipes or machine load. The operational phase caps remain binding; no borrowing, resampling, refill, retry or partial scientific conclusion is permitted.

Admission consumed 214.953 seconds before sealing; the outer owned process recorded 215.219 seconds. The retained sealed package occupies 143,188,647 bytes. The **full 1,800-second / 536,870,912-byte allowance** was charged at start regardless of measured use. Recorded campaign totals are now **43062.098 seconds / 34,056,774,260 bytes**. Including the declared future 18,000-second / 17-GiB execution-and-audit allowance and separate 3,600-second / 256-MiB postpublication allowance, cumulative declared maxima are **139,980 seconds / 91,419,049,984 bytes**. Those future phases have not run or incurred recorded charges. No historical charge was removed or refunded.

The separate read-only engineering verification was predeclared at 600 seconds / 64 MiB and recorded 51.922 seconds. It is separate from scientific campaign charges and from the future scientific postpublication allowance. It authenticated the sealed admission, repeated complete live-history validation, and passed the retained launcher's admission/request checks without launching. Both owned process receipts report success and zero active descendants.

## Implementation and verification

- [prepare-affinity-neighborhood-recognition.py](analysis/prepare-affinity-neighborhood-recognition.py): provenance, exact definition/request, full history, captured-runtime substitution, conservative phase forecasts, failure retention and independent verification.
- [affinity-neighborhood-recognition-context.ps1](analysis/affinity-neighborhood-recognition-context.ps1): portable-symbol provenance, native method resolution, literal endpoint reconstruction and all-transport preparation.
- [test-affinity-neighborhood-recognition-admission.py](analysis/test-affinity-neighborhood-recognition-admission.py): **25 passing tests**, including changed evidence, bad source counts, nonfinite measurements, each nontransferable cap, explicit nullable fields, failure charge retention and no retries.
- `.gitattributes` adds LF rules for the three new helpers and declaration. The nine current-status documents change only line three; their remaining bytes match the before snapshots. Historical helpers, frozen plans and source archives are untouched.

The preceding 144 passing backend regression cases, final 26 native-profile cases, tested DLL/PDB identities and complete 90,112-report engineering fixture were authenticated, with the independent direct-report fixture audit run again during admission. Backend source was unchanged, so no rebuild or additional backend test execution was needed. PowerShell syntax validation passed. All 156 inherited historical pins and the preceding 96-member implementation package remain verified. No required command is blocked. No migrations, application configuration changes or deployment are required.

Principal commands (`python` is the bundled interpreter):

```powershell
python -B -X utf8 "Balance Harness/analysis/test-affinity-neighborhood-recognition-admission.py"
python -B -X utf8 "Balance Harness/analysis/prepare-affinity-neighborhood-recognition.py" prepare --declaration "Balance Harness/Tower-Affinity-Neighborhood-Recognition-Admission-Declaration.json"
python -B -X utf8 "TestResults/affinity-neighborhood-recognition-admission-20260924/prepare.py" verify --package "TestResults/affinity-neighborhood-recognition-admission-20260924" --manifest-sha256 2e6cce9842430ac3383df46f449d09a9a8140c1c2c551ae2c5b8c88b02eb6287 --live
```

Preparation ran once under the declared owned wrapper. Do not rerun `prepare` against this identity. The separate verification wrapper additionally exercised the retained launcher's validation without invoking its launch function.

## Next step

Execute the single admitted diagnostic using the retained owned launcher, retained runtime/request and external admission manifest pin, after its fresh history check. Draw one 65,536-byte OS batch, reserve every exposed fresh value (up to 16,384), use its first 2,048 eligible values for the common panel, and execute the fixed 90,112 cells. Complete both saved-report audits and publication inside their dedicated allowance, then run a separately bounded postpublication/history verification within 3,600 seconds / 256 MiB. Preserve failure evidence and charges if any phase fails; never resize or retry the experiment. The launcher and native command remain dormant in this admission.
