namespace BalanceHarness;

// Cumulative history is independent of the unchanged per-experiment combat caps.
public static class TowerStudyLimits
{
    public const int HistoricalSeeds = 1_000_000;
    public const long ImportBytes = 32 * 1024 * 1024;
}
