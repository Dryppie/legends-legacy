apiVersion: v1
kind: Service
metadata:
  name: {{ include "ll-liveops.fullname" . }}
  labels:
    {{- include "ll-liveops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "ll-liveops.selectorLabels" . | nindent 4 }}
