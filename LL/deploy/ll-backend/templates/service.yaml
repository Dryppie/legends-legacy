apiVersion: v1
kind: Service
metadata:
  name: {{ include "ll-backend.fullname" . }}
  labels: {{ - include "ll-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{ - include "ll-backend.selectorLabels" . | nindent 4 }}
