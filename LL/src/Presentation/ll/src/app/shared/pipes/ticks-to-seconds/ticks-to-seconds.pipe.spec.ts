import { TicksToSecondsPipe } from './ticks-to-seconds.pipe';

describe('TicksToSecondsPipe', () => {
  it('create an instance', () => {
    const pipe = new TicksToSecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
