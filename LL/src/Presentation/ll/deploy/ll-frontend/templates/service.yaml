apiVersion: v1
kind: Service
metadata:
  name: { { include "ll-frontend.fullname" . } }
  labels: { { - include "ll-frontend.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "ll-frontend.selectorLabels" . | nindent 4 } }
