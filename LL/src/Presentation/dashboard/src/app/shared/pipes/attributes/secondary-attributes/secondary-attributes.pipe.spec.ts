import { SecondaryAttributesPipe } from './secondary-attributes.pipe';

describe('SecondaryAttributesPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondaryAttributesPipe();
    expect(pipe).toBeTruthy();
  });
});
