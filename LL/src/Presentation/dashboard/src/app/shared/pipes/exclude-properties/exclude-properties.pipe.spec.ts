import { ExcludePropertiesPipe } from './exclude-properties.pipe';

describe('ExcludePropertiesPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcludePropertiesPipe();
    expect(pipe).toBeTruthy();
  });
});
