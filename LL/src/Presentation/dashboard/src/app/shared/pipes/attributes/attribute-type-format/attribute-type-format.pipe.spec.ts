import { AttributeTypeFormatPipe } from './attribute-type-format.pipe';

describe('AttributeTypeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AttributeTypeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
