import { AttributeInfoPipe } from './attribute-info.pipe';

describe('AttributeInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new AttributeInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
