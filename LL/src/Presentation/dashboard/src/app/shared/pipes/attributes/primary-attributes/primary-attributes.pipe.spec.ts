import { PrimaryAttributesPipe } from './primary-attributes.pipe';

describe('PrimaryAttributesPipe', () => {
  it('create an instance', () => {
    const pipe = new PrimaryAttributesPipe();
    expect(pipe).toBeTruthy();
  });
});
