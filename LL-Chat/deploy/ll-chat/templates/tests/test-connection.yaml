apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ll-chat.fullname" . }}-test-connection"
  labels:
    {{- include "ll-chat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ll-chat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
